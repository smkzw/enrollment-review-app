# Conference Output: medication-v4-scope-review-20260909 - evidence_single_object (v5 follow-up pass)

## Output

### 实际检查（全部完成，只读）

- 逐行审阅 `scripts/medication_component_experiment.py`（v5 合同）与 `tests/v2/scripts/test_medication_component_experiment.py`（47 个测试函数，实跑 **59/59 通过**，`-p no:cacheprovider`）。
- 读取 20260910 三组冻结工件（page3/page4/prescription 的 source、comparisons、指定 response、`pair-2-lane-0-failure.json`）。
- 代码溯源：三个 contract.json 记录的 `medication_component_experiment.py` 哈希 `abffed10…` 与 `run_medication_component_experiment.py` 哈希 `400f2616…` 均与当前工作树一致（我第一轮比较脚本写错对象，已复核更正）——冻结结果可由当前代码复现。
- 主持人四条陈述逐一对到原始工件，全部属实：page3 pair-0 的 ongoing="持续中"（引用里带着上一行疾病的句尾）；pair-2 GLM 剂量值 `90μg/喷，每次两喷` 跨"每日四次"拼接非连续原文被整条拒收；page4 两例未药物干预病史双道 `explicitly_no_medication` 一致、全 null、`times=[]`、无旗标；page4 跨页残段区间双道保留 unclear、不发明端点。

### v5 判定（先说结论）

v4 的核心缺陷已闭合：用途门 + 整条拒收（不静默抹除）+ 旗标按用途条件化 + 比较器用途优先降级，全部有实现、有测试、有活体证据。page3 pair-1（孟鲁司特钠）六项全 agree——剂型缺失药名双道 complete、分母保留、日期正确拆分——v4 的三类假冲突消失。处方三对 use=prescription 一致且规格括号未并入药名。**但出现两类新活体缺陷和一个未测空白**，按优先级如下。

### 优先级发现

**P1（最高，活体缺陷）——相邻记录语义渗漏通过绑定校验。** 同一份 GLM 响应里两处：pair-0 把摘录开头上一疾病句尾的“持续中”放进本药 ongoing（quote 原文可证：“持续中。2020.UK.UK-…”）；pair-2 把摘录尾部下一记录的"2025.4."片段作为本药 unclear 时间——冻结预期明文禁止“将其移给上一项”。根因在 page-review 摘录边界带入邻行文本，子串绑定按设计管不住。**回答主持人的问题：宽松精确子串绑定永远不能证明语义归属**——它只证明字符来自本观察摘录，是必要非充分条件；合同注释自己也这么写。继续改提示无法根治（模型看到的观察文本本身已被污染），只能降频。

**P2（高，活体回归）——可拆区间整段停泊 unclear。** GLM 对"2020.UK.UK-2020.UK.UK"（双端数字引导、v4 同模型曾正确拆分、且 v5 同响应里同形的 2024.9.27-2024.10.UK 又拆对了）输出单条 role=unclear，产生三条 missing 分歧。诱因是“无法定位的跨页残片可整体保留为unclear”这剂新许可以及校验器对 role=unclear 跳过拆分检查（`:340`）。**支持性修复（确定性、无字典）**：`_DATE_RANGE` 双端数字引导且 role=unclear 时同样拒收要求拆分；UK 引导/尾随单端区间（如"UK-2025. 6. UK"，端点确不可归属）维持 unclear 合法——这正好把“无法归属”与“懒整段”区分开，与所有者修正案完全兼容。测试空白对应补：`test_uk_endpoint_ranges...` 参数全是 role=use_start，未覆盖 unclear 停泊。

**P3（中，活体假冲突）——处方字段标签混入分项值。** dose"每次用量：200mg"vs"200mg"（2/3 对）、frequency“频次：3/日”vs"3/日”（1/3 对），而第三对同形记录 GLM 又写对了——随机性指令遵循。根因是规则冲突：“不带字段标题、冒号”与“不得删除『每次』”在标签本身含“每次”的字段化处方文本上相撞。**支持性修复（一句提示）**：“字段标题及其冒号（如‘每次用量：’‘频次：’）不属于任何分项值，即使标题含‘每次’等字样；分母仅指紧邻用量的‘/次’‘每次N’写法”。不要在比较器归一化剥离（那是抹文本差异）。

**P4（中，未测空白）——药名不详的真实使用与购药均无 v5 活体证据。** “银屑病应用HR治疗中”在 v5-prescription source 里存在但未配对（合同句与测试都有了，缺双道实跑）；purchase 语义同样只有测试覆盖（所有者“购药 times 置空”修正案无活体样本，D001 小票在 v4 期资料中现成可用）。另指出一个需保持成文的事实：购药记录的交易时间/效期在分项输出中无处安放、仅留原文——防止下游把“空”读成“无日期”。

**P5（低，残留）**——提示仍存“含剂型的名称”与“仅缺剂型不算fragment”的措辞张力，但活体行为已双双 complete，暂不动（下次版本升级再顺）。**P6（观察）**——单分项绑定失败导致整条拒收，pair-2 本来正确的药名/起止时间随之退出比较视图（原始响应仍可查）。整条拒收应保留；可选小改：failure.json 记录触发组件名，加快源核对。

### 定向视觉复核 vs 继续改提示（主持人征询）

分而治之，不二选一：**P2/P3 是确定性/一句话级修复，先做**，比任何模型轮便宜且稳定。**P1 属于提示不可修类**——归属争议的裁决证据只在页图上（“持续中”"2025.4."是否属于本记录，模型输入里看不到邻居边界），继续迭代文本提示只会扰动已稳定行为（P3 的随机翻转就是敏感度证据）。因此支持**仅对归属未决观察**启用产品定向原图复核（现有 high/high、最多两轮、只带该观察及邻行、不重读整页、不覆盖原读记录），触发条件建议：比较显示 ongoing/边界 unclear/未决药名等归属型分歧。这是**可选架构升级（升级路径）**，不是本轮必改；复核输出本身须带 quote/定位以保持可核。

### 最小下一实验（不采信、不接产品）

按项目规则改动即升版本，建议 v5.1，恰好两处改动 + 六对配对，全部用既有冻结来源：
1. 改动：(a) P2 校验器规则（双端数字引导区间停泊 unclear → 拒收须拆）+ 新测试；(b) P3 一句标签/分母辨析。其余提示一字不动。
2. 配对：obs-02/fact_2（HR，药名不详真实使用活体）；一对 D001 小票（purchase 活体）；obs-005/fact_4 与 obs-003/fact_2 复跑（分别度量拼接拒收是否复现、P1 频率、unclear 停泊在新规则下的行为）；obs-004/fact_3 作不动对照组监测两处改动的回归。
3. 通过线（结构层，非临床）：全部结构绑定；HR 对保 use=use 且方案/ongoing 不因药名缺失消失（药名 absent 与“HR”/uncertain 的分道 diverge 预测为 missing，接受不修）；小票对 use=purchase、times 空、无使用分项；无新标签假冲突；对照组不变。
4. 另行（零代码）：把 page3 两处 P1 观察交产品定向视觉复核裁决归属，作源核对证据保存。

### 支持性修复 vs 可选架构（明确分界）

- 支持性（本轮可做）：P2 校验器规则+测试；P3 一句提示；P6 failure.json 记组件名（runner 小改）；提示措辞 P5 留待下次升版。
- 可选架构（需另批，不阻塞 v5.1）：归属未决观察的定向视觉复核升级路径；比较器对“unclear 整段 vs 对侧已拆分端点”的可合并性提示（仍 unresolved、人工合并）；拒收重试一次策略。
- 禁止项维持：不引入药物/疾病/项目字典，不做归一化删分母或删标签，不改写临床答案供产品注入。

### 剩余不确定

我未目视任何原件图像（所有者目视核对已由 plan 记录）；P1 频率仅凭 12 份提取中的 2 例，无法量化；GLM 标签行为 1/3 干净，一句提示能否稳定待测；Gemini 是否也会停泊可拆区间无观测证据（其 page3 pair-0 拆分正确）。v4 保留集已降为校准集，当前不存在新的独立保留集——任何接入叙述在此之前都不成立；本轮一切 product_acceptance=false，所有者未批准正式接入，无任何事实入库。
