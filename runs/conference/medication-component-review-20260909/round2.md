复核完成，证据齐备。以下为第二轮报告。

# Conference Output: medication-component-review-20260909 - evidence_single_object

## Output

### 所有权决定落实核验（证据）

- **F1（区间守卫）** ✓ `scripts/medication_component_experiment.py:47-48,242`：采用完整日期 token 区间正则（`[-–—~～至到]` 连接两个日期 token，token 支持 年./- 分隔、uk 占位、短形式 `7.18`、ISO），仅对 `item.value` 做 fullmatch；`source_quote` 允许比 value 宽（`2025-03-02` 搭配 quote `2025-03-02至2025-03-15` 合法，tests:413-415）。执行验证：纯区间 `2025.7.18-2025.7.23`、短形式 `7.18-7.23`、en-dash ISO 区间均被拒；单条 ISO `2025-03-02` 不误伤。
- **F2（不可能日期）** ✓ `:315-329,342-351`：`_invalid_date` 探针全通过——`2025.4.31`/`2025.13.01`/`2025.4.31 10:00`（含时间戳形式）→ True；`2023.UK.UK`/`2024.08.uk`/`2026-03-02 16:35:11`/`适量` → False，无合法值误报。双侧一致的不可能日期 → `unresolved` + 行级 `qc_flags:["invalid_date"]`（tests:418-426）。
- **F3（漏提日期提示）** ✓ `:252-254`：仅当 `times` 为空且本观察 excerpt/raw_value 命中 `\d{4}[年./-](\d{1,2}|uk)` 时附 `possible_unextracted_time`；不补日期、不改 status；邻药日期场景有专测（tests:429-434，测试名即声明 "does not fill date or prove ownership"）。
- **F6（原始 context 保留）** ✓ `:159,251`：`original_context` 逐字入 bound（D001 两份 bound 均验证）；comparisons 只携带 `qc_flags`，context 不参与比较语义。
- **启发式拒绝** ✓：`compare_component_outputs`（`:358-388`）无冲突计数、无 `pairing_suspect`/同名药标记——事件同一性判定未引入启发式。
- **提示词修补** ✓ `:198`：新增“只有年或年月以及带UK未知部分的日期也须原样保留”。v3-targeted f11 复跑证实 lane-A 不再丢弃在文日期（v2 为 `times=[]`，v3 提取为 `2023.UK.UK` partial）——与我第一轮报告的修正一致：这是 v2 模型遗漏，v3 已修复；角色分歧（unclear vs use_start）以两行单侧 missing 保守呈现，未合并未借值。
- **测试**：`42 passed in 0.07s`（v2 为 33；`-p no:cacheprovider`）。新增测试与扩测标准逐条对应。
- **运行脚本不变量**：同模型拒斥、`open("x")`/`exist_ok=False` 不可变写、全程 `product_acceptance:false`、无 OMP/Hermes（`scripts/run_medication_component_experiment.py:23,63-68,74-134`）；两脚本哈希与 v3 两份 `contract.json` 完全一致。

### 新工件核验（证据）

- **v3-targeted（3 对，page4 问题对复跑）**：f07 药名仍 conflict（布地奈德混悬液 vs 吸入布地奈德混悬液，源核对事项）；区间正确拆分为 use_start/use_end 双 agree。f11/f13 角色错位均以两行 missing 呈现。全部 `qc_flags {a:[],b:[]}`（正确：两侧都提取了时间，提示不触发）。
- **D001 收据反例（1 对）**：源形状 `1.异烟肼片 0.1g*100片/盒 2028-08-29`（lane-0 context `location_text:"品名 规格 效期"`）。结果：**dose 双侧 null**（包装规格 `0.1g*100片/盒` 未被当作剂量）、**效期 `2028-08-29` 双侧 role=unclear → unresolved**（未被当作用药时间）。lane-1 context 的无关时间 `2026-03-02 16:35:11` 未被借用（不在 excerpt，绑定层本也会拒绝）。`product_acceptance:false` 全程保持。反例按所有权描述成立，且暴露的是合同层的真实剩余风险（见下）。

### 残留缺口与回归检查（按严重度）

- **R1（低）区间守卫 fullmatch 收窄**：带尾随/前缀文本的区间值（`2025.7.18-2025.7.23用药`、`用药2025.3.2至2025.3.15`）绕过守卫——其中“至+文本”在 v2 子串检查下本会被拒。实际可害性低：此类值在 `_time_fingerprint` 必与对侧干净日期不等 → 以 conflict 暴露，不会静默 agree；且纯区间形态（模型的现实输出形态）已被拦截。候选收口：fullmatch 改 search（我已验证 search 不会误伤单条 ISO `2025-03-02` 与无分隔符单日期），属边界细化而非重议所有权决定。
- **R2（低，提示层）**：`_DATE_HINT` 对 excerpt+raw_value 拼接串搜索，理论上边界拼接可合成假日期 token；仅提示级，无 status 影响。另提示只覆盖“完全无时间”情形——摘录含两个日期而模型只提一个时不触发；以及不含年份的短日期（`7月18日`）不触发。均为提示覆盖面残留，非安全问题。
- **R3（外观）**：测试文件 docstring 仍写 "(v2)"（tests:1），合同已是 v3。
- **非回归的观察**：f07 的 route 在 v2 复跑为 agree、v3 复跑为 conflict（`吸入` vs `吸入治疗`）——同输入复跑的模型非确定性，比较层如实暴露而非掩盖，符合合同语义。

**结论：未发现实质性回归。** 四项所有权修复落实正确，边界收紧方向全部保守（拒绝更严、agree 更难），v2 冻结工件不受影响（新增字段有默认值）。

### 未来自动接入的来源充分性评估（明确不构成批准）

当前来源范围**不足以支撑任何未来自动事实接入**，缺口按阻断性排序（推断，基于以上证据）：

1. **无位置锚定**：观察一律 `bbox:null`；子串绑定只证“出自本摘录”，不证“属于本药行”。D001 证明同页存在 名称/规格/效期 相邻列结构——未来某模型把 `0.1g*100片/盒` 引为 dose 可**通过**现行绑定（它确是摘录子串），本次双模型都守规矩是模型行为，不是合同强制。
2. **规格 vs 剂量、效期 vs 用药时间 无确定性防线**：两者目前依赖提示词+人工源核对；自动接入前必须有机械可判的守卫或提示。
3. **角色词表把所有非用药日期隔离进 unclear**：作为隔离区是安全的，但无 效期/生产日期 语义供分诊，自动接入无法区分“待核对的用药时间”与“确定不是用药时间”。
4. 页边残缺药名（如 page4 “鲁司特钠片”）永久 unresolved——按更正，这可能是原始页面边界而非上游错误；此类永不自动接入，是安全属性而非缺陷。

**最小下一步合同调整（非新框架，且仅为前置条件）**：
- **a（最小、最高价值）**：dose 值命中包装规格样式（如 `\d+(\.\d+)?\s*(mg|g|iu|μg)[*×x]\s*\d+\s*(片|粒|支|袋|瓶|盒)`）时拒绝或提示 `package_specification_not_dose`——D001 已证明该形态存在于真实冻结数据。
- **b**：当观察自身 context 的 `location_text`/邻文含 效期/失效/生产日期 且唯一时间值来自同摘录时，附 `possible_expiry_not_use_time` 提示（D001 lane-0 的 `location_text:"品名 规格 效期"` 即现成判据）。
- **c**：未来任何接入合同起草前，要求观察携带 `bbox` 或显式“无位置”理由，否则行归属永远无法机械核验。

产品接入维持未授权；以上仅定义“将来若要谈自动接入，来源侧还差什么”。

### 更正采纳与遗留

- 已更正首轮报告：三页合计 **15 对/30 响应**（“18”仅是 page4 响应数）；f11 丢日期为 **v2 模型遗漏**（v3 已修复并有复跑证据）；跨页残缺为**原始页面边界**，不表述为上游错误。原始临床材料未改动——本轮全程只读，无写入、无模型调用。
- 无待决问题；R1 的 fullmatch→search 细化是唯一开放的可选项，留待所有权人裁量。页面图像终审仍归 Codex。
