先核对会议上下文里的新增允许清单，再目视 D00142 原图并对照事后字段对应标注。接着读原图、两主读观察、实际配对结果和测试提示。# Conference Output: enrollment-pair-important-evidence-20260908 - evidence_single_object

## Output

本轮只做字段对应核对，不是数值正确、事件同一或产品采信。已重新目视原图 `f5159a54…7348ac`（T-SPOT 报告）。事后金标 `posthoc-source-correspondence.json` 自称模型输出后标注，`clinical_acceptance/product_acceptance/prompt_compliance_score` 均为 false。配对模型实际输出 12 对；`check_pairs` 全部 `accepted: false`。

### 原件上清楚的印刷槽位

页眉：性别男、年龄58岁、科室/病区东院皮肤、样本类型全血、样本性状外观正常、送检单位石家庄市中医院。  
结果表四格：检验结果阳性、阴性对照孔0、抗原检测孔6、阳性对照孔正常。  
页脚三时点：采集时间 2026-03-12 09:48、接收时间 2026-03-13 01:00、报告时间 2026-03-14 12:03。报告编码可见 `20260312X26031280025A`。主检实验室北京艾迪康。备注印刷「检测结果为灰区，请结合临床。」表内阳性与备注灰区并存。

### 1. 14 对是否指向同一印刷字段

对应成立（同一印刷槽，允许读值不同）：

| 对 | 槽位 | 说明 |
|---|---|---|
| F001–P001-F001 | 性别 | 清楚 |
| F002–P001-F002 | 年龄 | 清楚 |
| F004–P001-F003 | 样本类型 | 清楚 |
| F005–P001-F004 | 样本性状 | 清楚 |
| F006–P001-F005 | 送检单位 | 清楚 |
| F008–P001-F007 | 阴性对照孔 | 清楚 |
| F009–P001-F008 | 抗原检测孔 | 清楚 |
| F010–P001-F009 | 阳性对照孔 | 清楚 |
| F012–P001-F012 | 接收时间 | 清楚 |
| F013–P001-F013 | 报告时间 | 清楚 |

对应可成立、但不是事件核验：

- **F007–P001-F006**：都指向黄底「阳性」格。A 的 `target_text` 是试验标题，B 是「检验结果」；A 有 `time_text=采集时刻`，B 为 null。字段对应成立，对象/时间不齐。
- **F011–P001-F011**：都指向页脚采集时刻。原件印刷「采集时间」，A 写成「采样时间」。字段对应成立；`check_pairs` 记 `field_identity_unverified`。
- **F014–P001-F014**：都指向报告编码。A=`…8002SA`，B=`…80025A`，原件更像末位 `5A`。对应成立，内容不一致。
- **F016–P001-F010**：都摘自备注行。A 未看清，B 写出灰区。B 的 `target_text=检测结果`，有与「检验结果」阳性格混淆的风险；按 excerpt 仍是备注槽。

未配对且合理：

- **F003** 科室/病区：原件有「东院皮肤」，A 写成「皮肤皮肤」，B 无对应观察。
- **F015** 主检实验室：原件有，B 无对应观察。

### 2. 单页事后金标能否支撑「12 对、漏 2」

不能当作提示遵循或事实核实的通过计数。

仅在「与这 14 个事后 ID 对的集合交叠」这一层：模型 12 对全部落在 14 对内，未提出 F014–P001-F014、F016–P001-F010。这是描述，不是验收。

不能支撑的原因：金标在模型之后、非盲、仅一页、无困难负例；14 对里多数 `time_text` 为空，与系统提示「缺明确事件时间则不配」冲突；备注与报告编码正是内容分歧项，字段对应 ≠ 读对。`score_pairs` 对空金标也不会报完美召回；`check_pairs` 永不 `accepted=true`。

### 3. 三种验收必须分开（不取消时间约束）

1. **提示遵循**：双方已有明确对象和事件时间才允许提议配对；缺一侧不得用另一侧补齐。本页人口学、日期字段、B 侧结果格多为 `time_text=null`，按提示这 12 对本不应提出。日期字段的值就是时间，与「事件观察缺少 time_text」不是同一类缺口，测试夹具要分开写，而不是删掉事件时间约束。
2. **观察对应**：是否同一印刷槽。事后 14 对只覆盖这一层。
3. **事实核实**：值、单位、极性、源内冲突（阳性 vs 灰区）、科室错读。对应成立仍可内容错误。

### 4. 隔离测试最小正反例

正例（应配字段、且事件类需自带时间）：两侧都写抗原检测孔=6，且 `time_text` 同为采集 09:48。

反例（必须不配）：

- 同页多日期：采集 09:48 vs 接收 01:00 vs 报告 12:03。
- 同数字异项目：表内抗原孔 6 vs 解读区「≥6 为阳性」。
- 结果 vs 参考/规则：阳性格 vs 解读条文，不得并入。
- 备注 vs 结论：阳性格 vs 备注「灰区」。
- 源内冲突：同一报告同时印刷阳性与灰区，保留两槽，禁止合成一事实。

下一步只改隔离测试夹具与计分层，不宣布通过，不放宽产品时间/对象/单位核验。
