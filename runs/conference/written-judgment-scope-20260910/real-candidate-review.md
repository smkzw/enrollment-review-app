# Conference Output: written-judgment-scope-20260910 - evidence_single_object

## Output

只读源绑定复审完成。**视觉能力如实声明：本会话模型为 GLM-5.3（纯文本），对列入的冻结临床图像的 Read 尝试返回 "[Media omitted from provider request because the selected model does not support image input]"——我实际看不到这些页面**。因此本轮所有结论限于文本工件（response/status/input/receipt）、字节级元数据（PNG 头尺寸、sha256）与代码层；任何"摘录是否忠实于页面/页上是否还有其他相关陈述/bbox 落点是否正确"的判断**未做视觉核验**，需多模态节点（GLM-5.3-Flash 或声明的外部视觉路由）补做。测试复跑：`tests/v2/llm/test_judgment_search_reader.py + tests/v2/services/test_judgment_search_source.py + tests/v2/domain/test_judgment_search_coverage.py` **86 passed, 0 failed**（v3 代码）。

### Q1：v3 负排除与手写残片表示（文本级判断，保留我方独立判断）

- **页9 v3**：双读道干净 not_found/not_found；被排除的正是 v2 main-A 误收的“医师签名”与“复诊计划”两项——与调用前计划对这些内容“非本目标候选”的裁决一致；文本层面未发现 v3 丢失任一读道输出中的相关陈述（页7 的相关概括表述由 main-A 以 ambiguous 保留、句尾“且不符合”截断原样未补齐，main-B 为 not_found——分歧被保留，粒度差异≠一致性，正确）。**但"页上无其他相关内容"无法由我证实**（见视觉声明）。注意 v3 相关性规则是**提示级**（reader :67-70"先确认实质判断内容…单独签名/复诊安排不作为候选”），合同层仍接受任意 ambiguous——安全网是 ambiguous⇒coverage_incomplete 而非缺失，成立。
- **report1-sige 手写残片**：main-A 摘录 "与研究疾病相关" **静默丢弃了前导不确定字形**；main-B 转写为 "CS 与研究疾病相关”并带 bbox。前导字母的可读性分歧本身就是不确定性证据。**表示建议**：摘录只保留逐字可读跨度，不可读前缀以显式不清区标记呈现（如摘录内 `〔前缀字迹不清〕` 或候选级 `uncertain_spans` 字段，含不清字形计数）；**不得**补猜正确答案、不得把 B 道转写嫁接进 A 道摘录、不得硬编码患者补丁；双道暂定摘录按现合同多候选元组并列保留于 ambiguous。两例均不得据此晋升 found。

### Q2：BoundingBox 坐标系（数值证据，非猜测）

五页 PNG 均为 1240×1755。GLM（page9 v2）框 {570,360,740,400}/{200,215,1140,310}：x1=1140>1000 排除 normalized-1000、>1 排除 0..1——**像素量纲相符但落点未经视觉证实**；Gemini（report1）框 {427,646,646,684}：≤1000，像素与 normalized-1000 **两可**，且两 provider 可能异构。**最小改法**：bbox 原样保留（绝不按猜测缩放），合同加 `coordinate_convention: "unverified"`（或原样透传 + convention=None）；展示层在核验前不得渲染为精确定位证据。**核验路径**：每 (provider, model) 用合成标记图（已知像素坐标）经同一原生传输做一次校准探针，返回框与已知真值比对后才可标注量纲——一次校准、留回执，不碰临床页。

### Q3：目标/来源准备缺的具体确定性核对（已核实缺口，非重构）

1. **消费时目标重算绑定缺失**：receipt 带 `target_sha256`，但没有任何检查断言它等于当前 (authority, requirement) 重新推导的已发布目标。target_text 是 sort_keys 确定性 JSON（judgment_search_source.py:424-427），重算廉价——**修法**：组装/覆盖层重跑 `prepare_judgment_search_target` 并要求哈希相等，捕捉规则重发布/陈旧回执/调用方错配。
2. **跨读道同目标同范围核对缺失**：`JudgmentSearchPageResult` 不携带目标字段，同页两条回执可能应答不同目标而无人发现。**修法**：组装胶水要求同页双回执 scope_sha256 与 target_sha256 全等才可成页级双读结果，否则有界报错。
3. 正面核实（本轮证据）：五个探针 source.png 的 sha256 全部等于各自 scope 页身份哈希（字节绑定在工件层成立）；页7 main-B 的 v1 围栏失败诚实留痕（candidate_received=false / invalid_json、原始响应保留、无回执、未改写）；v3 围栏规则严格限定“整段单一完整围栏”（reader :153-155），围栏+前后杂文仍拒。

### Q4：有界分组目标设计（修正先前错误）

**先明确撤回**我上一轮"摘录挂错目标仅因候选阶段而无害”的说法：**错误**。错挂会污染逐要求覆盖簿记——某要求因他目标的摘录被记 found，会掩盖该要求的 not_found 信号，进而抑制其未来的缺失判定并误导适用性复核工作量。候选性只限制临床危害，不豁免簿记危害。

设计（不实施）：每（页×读道）一次调用携带紧凑目标组 `[{target_ref, target_text}]`，输出按 target_ref 各给双通道处置（复用现合同）；**响应必须给每个列出目标显式结果，缺任一目标即整次 schema 失败**（失败而非静默 not_found）；逐要求状态/相关性/歧义/失败各自保留于（页×读道×目标）粒度；确定性防串扰闸：同一响应中相同摘录文本出现于 >1 目标 ⇒ 标记跨目标污染、降级为待核对 ambiguous（不报错不采信）；**组大小不是被接受的固定阈值**——是按回执实测截断/失败率（stop 严格使超限安全失败）由运营方定的预算参数，合同不设魔数。逐要求仍各持自己的 scope；胶水按 Q3 两项检查重绑。无 time_text/作者/日期推断；供给域≠完整适格来源；found 永不采信；类别永不替代判断。

### 当前缺陷 / 修复 / 未实现边界

- **缺陷（表示约定）**：不确定字形被静默丢弃（report1 main-A 实例）→ 按上文不清区标记约定修，禁猜禁补。
- **缺陷（合同缺口）**：bbox 无量纲标注 → 加 `coordinate_convention:"unverified"`，展示降级；校准探针后才能标注。
- **缺陷（待随集成落地）**：Q3-1/2 两项绑定检查缺失——必须随下一体合入而非事后。
- **本轮未发现 reader/source v3 代码层新缺陷**（86 通过；fence/stop/重复键/原始保留均独立复核）。
- **未实现边界**：单次调用无重试/持久化/编排；页9 已成校准样本非留出；探针仅覆盖 24 页中的 4 页（EX-07）+1 页（IN-02），未搜页不是 not_found；D2 缺失生产者未接。

### 报告“确缺研究者书面判断”前还需要的证据

① 多模态节点对摘录忠实度与页面内容的视觉核验（本会话明确做不到）；② 该要求全部供给页×双读道×逐目标显式 not_found 的完整覆盖记录（含失败簿记闭合）；③ bbox 量纲校准（若需展示定位）；④ 回执持久链 + Q3 绑定检查接线；⑤ 供给域≠适格域的语义决定（恢复计划 §2 仍开放）。即便全部满足，结论也是“供给域内双模型一致未检出”，按用户规则报告为未决判据与所需补充资料（无法判定），不停工追问用户确认。同厂商同家族复审独立性受限；本报告非临床验收。
