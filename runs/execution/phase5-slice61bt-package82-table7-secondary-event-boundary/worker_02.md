Fallback state: I am the declared same-platform fallback `codebuddy-cli` / `deepseek-v4-flash` (route switched from `glm-5.3-flash` before a resumable session existed). The original prompt remains the task contract; I preserve the same bounded role, source list, and output schema. Starting with the initial read set.

I have the context and plan. Source of truth section says Codex must add authoritative sources before dispatch — but the task requires me to find the package-82 artifacts in the workspace. Let me explore the workspace structure to locate the relevant materials.

The context directory is huge. Let me find the package-82 related context and the prior sibling-package contexts (81, 80) to understand the pattern.

Let me look at the sibling package-81 context and plan to understand the established pattern for these package boundary tasks.

My fallback position is confirmed by the manifest (night route fallback #1 is `codebuddy-cli/deepseek-v4-flash`). Now let me study the established pattern from package 81's execution reports and the "执行合同" (execution contracts) referenced.

Package 81's worker reports exist — worker_02 there had the same role as mine. Let me read that report to learn the established pattern.

The package-81 pattern is now clear. My task is the same role for package 82 (`body.t12.r0-r4`, Table 7). Let me explore the research directory and the frozen plan.

There's an execution contract for package 82 already written. Let me read it, plus the package-81 config as template.

Now I have the execution contract and the package-81 config as template. Let me examine the frozen plan for package 82 ownership and the exact source text of `body.t12.r0-r4`.

The package ID wasn't found in this plan copy. The contract references the artifact path `artifacts/phase5-slice59i-d001-phase-table-caption-rebaseline-20260827/frozen_phase_plan.json`. Let me check that one.

Package 82 owns exactly `body.t12.r0-r4` (header + 4 rows). Let me extract the full owned unit excerpts and the key context excerpts from the frozen plan.

Owned excerpts confirmed. Now let me get the key context unit excerpts: p1030-p1033 and the t7 header (for t7/t12 anti-confusion).

Package 82's plan context is small (t7.r0 + p1032). The contract requires attaching p1030-p1032 (package 81) and p1033 (package 83). Let me get their excerpts from their owning packages in the frozen plan.

All source excerpts gathered. Now let me study the harness (`slice59n_representative_group_control_replay.py`) and the package-81 test as templates.

Now let me read the package-81 test file and parent checklist as templates.

Now the parent checklist template.

Let me check the reject-gates module (prepare closure gate) and the package-82 report directory status.

The reject gates are generic. Now let me check the t7 caption context and the structure blob entries for `t12` and `t7` (for the t7/t12 anti-conflation gate).

Let me check precedent for table-row ownership (package 32 probe) and package 80's config for disposition patterns, plus verify `body.p507` resolution.

All planned refs resolve. Let me extract the full excerpts for all my planned attached refs (from the frozen plan and coverage manifest) to lock verbatim assertions.

Let me check whether the t7 table has an official number/caption (to confirm "表7" uniquely refers to t12).

All evidence confirmed: "表 7" (p1032) unambiguously = t12; t7 is an unnumbered Phase III efficacy table. Now I'll create the package-82 artifacts. Setting up task tracking first.

I notice a typo in the p986 QC check (stray quote). Fixing it.

Now validate the config loads and run the dry-run prepare.

Prepare succeeded: 5 owned / 25 attached / 30 units, `claims_complete=false`. Let me verify the generated evidence files and prompt content.

Roles are correct. Now let me verify prompt content: all four rows verbatim, p1033 A/B note, t7 header present, and no t7 row content leakage.

Prompt verified clean. Config and prepare evidence complete. Now the parent checklist.

Now the deterministic regression test file.

Two failures to fix. Let me see the exact assertions.

Two fixes needed: test wording vs config wording, and checklist missing explicit 第90/第92.

All 36 dedicated tests pass. Now the adjacent regression suites (packages 75-81 + shared suites).

Adjacent suites pass (284). Now the full phase-closure directory.

Full suite: 324 passed (288 baseline + 36 new — delta matches exactly). Let me verify no shared code was modified and collect the final fingerprints.

The `app/` modifications are pre-existing worktree state (rearchitecture in progress), not mine — I never wrote to those paths. Let me confirm my artifacts are the only additions and are ignored by git tracking.

All artifacts confirmed. Final coherence check on the QC scaffold, then wrap up.
