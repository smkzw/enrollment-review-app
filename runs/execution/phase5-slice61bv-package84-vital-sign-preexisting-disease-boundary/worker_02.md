The write-boundary paths match the established pattern under `research/d001-ii-phase-closure/`. Let me study the accepted package 83 artifacts as the closest adjacent pattern.

Now let me extract package 84's owned unit excerpts from the frozen plan, and check the coverage manifest hash values and plan_id.

The catalog internal `catalog_sha256` matches package 83's expectation (file re-serialized, field stable). Now let me read the package 83 parent checklist and its prepare artifacts for exact patterns.

Now I have the full pattern. Let me extract excerpts for the candidate attached closure refs and check package 85's first owned unit (anti-absorption boundary).

Now let me check how similar recording-boundary packages handled dispositions, and look at the matrix file and package 85's span range.

I have the full picture of the pattern. Let me check the remaining reference files: the matrix file and how the previous worker's run was executed.

I now have all source data. Let me verify one more detail — the exact structure of a package 83 prepare source_rows (lookup values) and confirm the dry-run harness works with the venv.

All source data confirmed. Now I'll create the package 84 config following the accepted package 83 pattern.

All data confirmed. Now I'll create the package 84 config, following the accepted package 83 pattern.

Now run the dry-run prepare harness to generate the model-external prepare evidence.

Prepare passed. Let me verify prompt contents — owned sources verbatim, and no leakage of package 85+ content.

Prompt is correct: all 12 owned verbatim, no package 85+ leakage (海氏定律/ALT或AST/24小时/共同判断 all absent; 死亡 comes only from the intentional SAE definition context). Now I'll write the dedicated regression test.

37 passed, 4 failed. Let me see the failure details.

Minor test assertion mismatches with config text. Fixing the test to align with config wording.

Now create the parent checklist (the last missing artifact).

All 41 pass. Now run the adjacent regression suites — package 80-83 tests, phase closure/gate tests, and protocol/agent tests.

Now the protocol/agent and governance tool suites.

The full protocols suite is large. Let me run the targeted control-replay-related subsets.

All suites pass. Let me verify prepare determinism (rerun dry-run, confirm identical prompt hash) and check the final artifact state.

Prepare is deterministic. Final verification: full package 84 test suite plus adjacent package 75-79 boundary tests.

All tests pass. Final audit — verify frozen artifacts untouched and config hash reproducible.
