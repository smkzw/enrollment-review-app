I'm the declared fallback (`codebuddy-cli`/`deepseek-v4-flash`). Starting by reading the initial read set.

Fallback stated: I'm the declared `codebuddy-cli`/`deepseek-v4-flash` fallback, worker_03, read-only, assigned the independent counterexample challenge (判断与强制报告混同、OR/AND反转、既存疾病升格、示例升格、跨包吞并). Now reading the source-of-truth contract and frozen artifacts.

Contract read. Now inspecting the structure block, source-input, and package-84 entry in the frozen plan.

Package 84 owns exactly 12 units (p1043–p1054). Now verifying the structure block text, package 83/85 ownership boundaries, and context units.

Structure block confirms exact frozen text. Now checking package 84 context units, adjacent packages 83/85 ownership, freeze metadata (SHA), and whether worker_02 artifacts already exist.

Ownership boundary confirmed clean (83: p1033–1042, 84: p1043–1054, 85: p1055–1066) and DOCX SHA-256 matches frozen hash. Now inspecting package 84's 38 context units for leakage/necessary anchors, and the coverage manifest entries.

Package 84's context units are generic headings (radius-1 plan artifacts). Now checking whether worker_02 has produced artifacts yet, and the coverage manifest entries for p1043–p1054.

Manifest verified. Notable: p1050 carries priority keywords `筛选/基线/首次给药` (rank 3) — the core escalation-risk vector. Now checking the parent heading structure and whether 既存疾病/生命体征 appears in the actual 入排 (IE) criteria section.

Heading hierarchy mapped. Now checking the AE definition/collection anchors (needed for minimum closure), the full configs list (accepted 80–83 patterns), and any IE-criteria interaction.

Collection anchors found (p1022–p1024, esp. p1023 pre-first-dose history boundary). Now checking runner reports (whether worker_01/02 ran), the accepted package-83 prepare pattern, and the AE definition unit.

AE definition + exclusion anchors (p985–p992) confirmed as the semantic binding for the "预期进展" clause. Now checking the run directory for worker_01/02 outputs and the accepted package-83 config/checklist pattern.

worker_02 artifacts don't exist yet (only 125-byte stubs). I'll base the challenge on the frozen plan + structure + manifest + the accepted package-83 pattern. Reading the package-83 config and its prior worker_03 challenge report as the accepted precedent standard.

Package-83 config read — it already declares p1043–p1048 as read-only anti-conflation context and p1049–p1054 ownership metadata. Now reading the accepted package-83 worker_03 challenge report as the precedent standard.

Package-83 precedent report read — it's my format template. Now performing the package-84 deep checks: raw DOCX XML reconstruction, control-matrix anchor search, structure unit IDs, and duplicate-ownership check.

The raw-XML block_order mapping was invalid (tables shift indices). Re-extracting by exact text matching instead.

Short needles matched the lab-section twins first — need all-match positions to disambiguate p1046 vs p1040. Dumping the raw paragraph window and all matches.

Raw DOCX window 2805–2832 confirms blob fidelity and the critical canary differences (p1046 vs p1040; p1048 vs p1042). Now checking the official flow controls / control matrix for any p1043–p1054 or vital-sign/preexisting-disease anchors.

The matrix contains an official EX row on vital-sign abnormality (anchored ~p684) — the concrete escalation-risk counterpart. Extracting the full row details and official flow controls.

All source evidence gathered. Final quick checks: manifest dispositions, TOC-unit ownership (p231/p232), and the pkg83-config claim about p1049–p1054.

All evidence collected. Composing the final report.

# Execution Output: phase5-slice61bv-package84-vital-sign-preexisting-disease-boundary - worker_03

**Fallback declaration:** This pass ran as the declared `codebuddy-cli` / `deepseek-v4-flash` fallback for role `worker_03` (primary route `glm-5.3-flash` unavailable before a resumable session existed). Same bounded role, source list, output path, quality gates, and conference-pass protocol applied. No capability limitation affected this work; all evidence was gathered with stdlib `python3` (zipfile/json/hashlib) and `shasum`. Worker_03 is a **read-only adviser** — no files were created or modified; the runner persists this report.

## Boundary And Context Check

- **Role:** worker_03, work item 3 — independent counterexample challenge from five angles: 判断与强制报告混同、OR/AND反转、既存疾病升格入排门槛、示例升格、跨包吞并.
- **Read set (initial):** `context/phase5-slice61bv-package84-vital-sign-preexisting-disease-boundary_execution_context.md`, `plans/codex_execution_phase5-slice61bv-package84-vital-sign-preexisting-disease-boundary.md` — both read in full.
- **Additional evidence read (justified: required to anchor the challenge to real source, real frozen plan, and the accepted adjacent pattern):**
  - Execution contract: `.trellis/tasks/08-22-phase5-clinical-facts-profile/research/d001-ii-phase-closure/slice61bv-package84-vital-sign-preexisting-disease-execution-contract.md`
  - Frozen plan `artifacts/phase5-slice59i-d001-phase-table-caption-rebaseline-20260827/frozen_phase_plan.json` (packages 83/84/85 ownership, pkg84 context units and span IDs, duplicate-ownership scan across all 131 packages)
  - Structure blob `.../structure/blobs/protocol_blocks/3946ea2c9780d0399b60245eafc4ab85087328a5da158b9d0938f8858302343d.json` (3581 blocks; full block text for p1042–p1056)
  - Coverage manifest `.../coverage_manifest.json` (units p1043–p1054, p231–p233, phase scopes, dispositions)
  - Raw `word/document.xml` inside the frozen source DOCX (paragraph text, styles, numbering, tracked changes for w:p 2805–2832)
  - `.../d001-ii-control-matrix-closed.json` (82 rows; anchor + keyword search) and `.../d001-ii-official-flow-controls.json` (anchor search)
  - Accepted precedent: `configs/representative_group_package83_persistent_recurrent_lab_ae_boundary.v1.json` (pkg83 config, incl. its p1043–p1054 ownership declarations) and `runs/execution/phase5-slice61bu-package83-persistent-recurrent-lab-ae-boundary/worker_03.md` (prior challenge standard)
  - `runs/execution/phase5-slice61bv-package84-vital-sign-preexisting-disease-boundary/` (worker_01/02/03 stubs — 125 bytes each, i.e., placeholders)
- **No writes performed** (worker_03 read-only). Report returned inline per runner protocol.

## Work Performed

### 1. Fingerprint verification (evidence, not acceptance)

- DOCX SHA-256 = `362443131f0d384c82c80f6a37396084f7d3301b51162201749c0488b0f2dd98` ✓ matches contract, pkg83 config `expected_protocol_sha256`, and pkg84 `protocol_document_sha256`; freeze_metadata `source_unchanged: true`.
- Structure blob SHA-256 = `3946ea2c9780d0399b60245eafc4ab85087328a5da158b9d0938f8858302343d` ✓.
- Plan `papl-40b1237a22e538a278b4fd5e`, snapshot `d001-ii-phase-closure-20260827-slice59i-table-caption-rebaseline-snapshot`, protocol version `D001-02-002:v1.0:phase-ii` ✓.

### 2. Independent source reconstruction (raw DOCX XML, not the blob)

Raw `word/document.xml` window (w:p 2805–2832) pins the canonical texts; the structure blob is faithful. No tracked changes (`w:ins`/`w:del` count 0 on every owned paragraph).

| Ref | raw w:p | style | numbering | Text (verbatim) |
|---|---|---|---|---|
| p1043 | 2818 | 116 (自控1.1 标题) | num12/ilvl4 | 生命体征异常 |
| p1044 | 2819 | 60 | — | 研究者负责审查所有生命体征结果，通过医学和科学判断一项孤立的生命体征值异常是否应归类为不良事件。 |
| p1045 | 2820 | 60 | — | 当生命体征异常符合以下任一标准时，则必须报告为不良事件： |
| p1046 | 2821 | — | num58/0 bullet | 伴有临床症状 |
| p1047 | 2822 | — | num58/0 bullet | 导致研究治疗发生变化（例如治疗暂停或治疗终止） |
| p1048 | 2823 | — | num58/0 bullet | 导致医学干预或伴随治疗改变 |
| p1049 | 2824 | 116 | num12/ilvl4 | 既存（合并）疾病 |
| p1050 | 2825 | 60 | — | 既存疾病（事先存在的疾病）是指在本研究的筛选访视时或首次给药前即已存在的疾病，此类状况应记录在eCRF的病史和基线状况中。 |
| p1051 | 2826 | 60 | — | 当符合以下标准时，既存疾病才应记录为不良事件： |
| p1052 | 2827 | — | num58/0 bullet | 发病的频次、严重程度或特征在研究期间恶化或改变； |
| p1053 | 2828 | — | num58/0 bullet | 恶化或改变不是疾病的预期进展。 |
| p1054 | 2829 | 60 | — | 当在eCRF不良事件上记录此类事件时，重要的是要通过采用适当的描述语来体现既存疾病的变化，例如“偏头痛频次增加”。 |

Context: p1040 (lab, pkg83) = 伴有临床**症状或体征**; p1042 (lab, pkg83) = 导致医学干预或**合并用药**/治疗改变; p1055 (pkg85 heading) = 严重肝损伤与肝功能检查异常; w:p 256/257 = TOC entries "7.3.3.2.5.生命体征异常89" / "7.3.3.2.6.既存（合并）疾病89" (manifest p230/p231, owned by pkg 15 — **TOC units, not body rules, and NOT owned by pkg84**).

### 3. Ownership map (frozen plan, verified across all 131 packages)

- pkg 83 owns exactly `body.p1033–p1042`; **pkg 84 owns exactly `body.p1043–p1054` (12 units, zero duplicate owners)**; pkg 85 owns exactly `body.p1055–p1066`. No span overlaps.
- pkg84 owned structure_unit_ids: p1043→`su-963e392817f11c97fe962998`, p1044→`su-0dbe60fa80c002240e61319e`, p1045→`su-6144117f39ded3b6444d602e`, p1046→`su-d4e353c928e8fe7d68647e22`, p1047→`su-6aabca2034cd07d3a48aea06`, p1048→`su-da9e8ad38df3c41d51b99423`, p1049→`su-46a009d39b9c56461e19c55e`, p1050→`su-0de88ca27a68dcad50f931f2`, p1051→`su-2299a372420911ccefeb59ad`, p1052→`su-11eeb9c1e2b247da33ce08ca`, p1053→`su-983c6b242709c959cc0d5500`, p1054→`su-fc924483288e10caeb367108`.
- Coverage manifest: all 12 owned units `phase_scopes: ["unknown"]` (phase-neutral AE recording norms), `claims_full_coverage: false`, `dispositions: []`. **p1050 carries `priority_keyword_hits: ["筛选","基线","首次给药"]`, `priority_rank: 3`** — the single highest-elevation risk vector in the package.
- Control matrix `d001-ii-control-matrix-closed.json` (82 rows): **zero anchors** to p1043–p1054 (string search negative). The matrix's only vital-sign row is **EX-21** (`pcm-row-dd9da8fd97941b8b7d8753e2`, "生命体征检查异常与不可接受风险", `source_kind: official_eligibility`, anchored to **body.p684** 排除标准: "筛选或基线时，生命体征、体格检查、12-导联心电图、胸部CT…异常且有临床意义，经研究者评估…不可接受的风险"). No matrix row for 既存疾病/合并疾病 from p1050 (only a p752-anchored 合并用药 row, pkg63). 偏头痛 appears **exactly once in the entire protocol** (p1054) and zero times in the matrix. `d001-ii-official-flow-controls.json` has zero p1043–p1054 anchors and zero 既存/合并疾病 hits.

### 4. Frozen-plan context gap (finding, not plan defect)

pkg84's 38 `context_units` (all `frozen_source_span_ids`, 63 spans incl. t5/t6/t7 headers) are phase-scope scaffolding only — headings like p446/p455/p502/p1207/p1210 (Ⅱ/Ⅲ期、疗效) and visit headers. They do **not** include: AE 定义与除外块 p985–p992 (pkg77), 收集期 p1022–p1024 (pkg80, esp. **p1023** "签署知情同意书后到首次服用试验用药品之前…作为病史/伴随疾病记录在原始病历中，不作为AE记录"), D1 锚点 p340/p885, pkg83 平行结构 p1037–p1042, 或 pkg85 边界 p1055. The contract's 最小闭包要求 makes these mandatory in the model-free preparation; worker_02 must compensate via `attached_source_refs` on top of the frozen-plan context. The pkg83 precedent (`attached_source_refs` = 28 refs incl. p1043–p1048) is the pattern; note pkg83 did **not** attach p985/p987–p992/p1023, which pkg84 needs for its own semantics (见 A/C 域门禁).

### 5. worker_02 status

`configs/representative_group_package84_*.json`, `test_slice61bv_package84_*.py`, parent checklist, and `slice59n-prepare/d001-ii-package84-*` do **not exist yet** (run-dir reports are 125-byte placeholders). All gates below are ready to be applied to worker_02's artifacts once produced. Note pkg83's accepted config already declares `body.p1043–p1048` attached read-only (anti-conflation) and `body.p1049–p1054` as ownership metadata only ("只保留所有权元数据，不进入提示") — pkg84 must mirror this reciprocity.

### 6. Independent counterexample challenge matrix (work item 3 deliverable)

Each case: **failure mode → source anchor → gate the dedicated model-free test must carry**. Ordered by severity.

**A. 判断与强制报告混同 (p1044 vs p1045–p1048)**
- A1. "任何生命体征异常即为AE"：p1044 的判断层要求研究者以医学和科学判断决定一项*孤立的*生命体征值异常是否归类为 AE；不得改写为"只要异常即为AE"。Gate: 未满足 p1045 任一标准的孤立异常不得断言为自动报告。
- A2. "研究者未明确否定即为AE"（沉默即AE）：p1044 要求肯定性判断，不是"未否定"。Gate: 无记录判断的案例不得自动归类为 AE。
- A3. 判断覆盖强制：研究者以"无临床意义"为由跳过报告，即使已满足 p1045 任一标准（如已导致治疗暂停）——p1045 "必须报告"是无条件的。Gate: 标准满足即必须报告，不受判断结论覆盖（B3 同构案例在 pkg83 已确立）。
- A4. 强制层扩为全量：把 p1045 读成"所有生命体征异常一律报告"会消灭 p1044 判断层；两层必须分层保留（对应 pkg83 合同"分层保留"条款）。
- A5. 生命体征判断与实验室判断互换：p1044 "一项孤立的生命体征值异常" 不得替换为 p1038 "一个孤立的实验室检查值异常"（平行但文本不同，见 E1 金丝雀表）。
- A6. "孤立的"限定丢失：p1044 的判断对象是*孤立的*值异常，不得静默丢弃该限定，也不得移植到 p1045（p1045 无孤立限定）。
- A7. 强制报告升格为 SAE 判定：p1045 "必须报告为不良事件" 是 AE 层义务，不是 SAE 判定（p996/p997 归第78包只读）；不得把生命体征 AE 强制报告与 SAE 严重性判定混同。

**B. OR/AND 反转**
- B1. 生命体征外层 OR→AND：要求 症状 AND 治疗变化 AND 医学干预 三备选全满足才报告——p1045 "任一标准" = 任一备选即触发。Gate: 三个单备选用例各须触发强制报告。
- B2. 生命体征内层 OR 丢失：p1048 "导致医学干预或伴随治疗改变" = 医学干预 OR 伴随治疗改变，任一分支即满足；p1047 "（例如治疗暂停或治疗终止）" 是示例性 OR 不是 AND。Gate: 仅干预、仅伴随治疗改变分别触发。
- B3. **既存疾病 AND→OR（最高风险反转）**：p1051 "当符合以下标准时…才应记录为不良事件" 的"才"强制 p1052 与 p1053 同时满足（AND）；反转为 OR 后：(a) 仅"非预期进展"而无恶化也会被记录为 AE；(b) 任何恶化（即使属于预期进展）都被记录。Gate: (a) 恶化但属于疾病预期进展 → 不得记录为 AE；(b) 非预期但无恶化 → 不得记录为 AE。
- B4. 既存疾病内层 OR→AND：p1052 "发病的频次、严重程度或特征在研究期间恶化或改变" = 频次/严重程度/特征任一恶化或改变即满足（内部 OR），且该 OR 项整体与 p1053 AND。不得改写为频次且严重程度且特征。Gate: 仅频次变化（严重程度、特征不变）的单维度恶化用例须满足 p1052。
- B5. "才"字丢失/弱化：把 p1051 改写为"应记录为不良事件"（去掉"才"）即丢失 AND 绑定。Gate: 逐字保留"既存疾病才应记录为不良事件"。
- B6. 跨规则反转搬运：p1046–p1048（生命体征 OR 备选）不得搬进既存疾病 AND 门，p1052–p1053 也不得搬进生命体征 OR 门——两组门语义不同（任一即报 vs 同时满足才报）。
- B7. 否定项反转：p1053 是否定条件"不是疾病的预期进展"；不得双重否定（"恶化或改变是预期进展才记录"为反转）。Gate: 逐字保留"不是"。

**C. 既存疾病升格入排门槛 (p1050)**
- C1. p1050 → 入排门槛："有既存疾病→不得入组/排除"——源文只说记录于 eCRF 病史和基线状况，不是排除条件。Gate: `candidate_forbidden_markers_by_source_ref` 对 p1050 必须包含 入排域标记（不得入组/排除标准/入排不通过/不符合入选标准）。
- C2. p1050 → 证据缺口/缺失即不通过："无既存疾病基线记录→证据缺口"——p1050 是 AE 记录章的病史/基线记录义务，不是筛选/基线证据门。Gate: 无 证据缺口/证据不足 处置从 p1050 发射。
- C3. p1050 → 基线必做：不得把既存疾病记录发布为基线必做流程项——p1023（pkg80）规定知情同意后至首次给药前的医学事件作为病史/伴随疾病记录在原始病历、不作为 AE；p1050 与入排执行无关。Gate: 不向 flow-screening/flow-baseline/flow-d1-pre-dose 阶段发射任何义务。
- C4. 首次给药前锚点错位：p1050 的"筛选访视时或首次给药前"是既存疾病分类的时间锚（定义既有疾病），不是入排检查点；不得据此生成 D-28~D-1 或 D1 给药前任务（对比 p885 "D1给药前结果作为基线值…"为另一规则）。
- C5. 关键词误触发（已实证风险）: coverage manifest 给 p1050 打了 `priority_rank: 3`（筛选/基线/首次给药命中）。模型外配置必须显式把 p1050 列入 `candidate_forbidden_markers_by_source_ref` 以中和关键词驱动的候选发射；否则"筛选"命中会诱导筛选候选（与 EX-21 于 p684 的官方排除行构成影子重复）。
- C6. 与官方 EX-21 的边界：矩阵唯一生命体征入排行 EX-21 锚定 p684（排除标准），pkg84 的 p1043–p1054 不得重新发射筛选/基线生命体征候选，否则与官方行重复发布且锚点不同（双源）。Gate: `required_candidate_source_refs=[]` 且矩阵零新增（已核零锚点）。

**D. 示例升格**
- D1. "偏头痛频次增加"升格为唯一表达：p1054 的"例如"是描述语的示例，不是唯一可接受措辞。Gate: 任何"采用适当描述语体现既存疾病变化"的恰当描述均可接受；禁止把"偏头痛频次增加"设为唯一枚举值。
- D2. 示例升格为病种条件："偏头痛"成为通用病种门槛（如仅偏头痛需按此规则、或"偏头痛=既存疾病条件"）——"例如"修饰的是描述形式而非疾病类型。Gate: 不注入病种限制（偏头痛全文仅出现1次、矩阵0次，属纯示例）。
- D3. 示例升格为判定门槛：不得要求"必须含频次变化描述语"或"描述必须匹配'偏头痛频次增加'模板"。Gate: 描述语开放性断言。
- D4. 治疗暂停/终止升格为穷尽：p1047 "（例如治疗暂停或治疗终止）" 是研究治疗变化的形式示例；剂量调整、停药等其他治疗变化同样满足该备选。Gate: 剂量变化（非暂停/终止）案例仍触发强制报告。
- D5. 示例升格为同时满足：不得要求"治疗暂停且治疗终止"同时发生。Gate: 单形式即满足。
- D6. 示例升格为受控词表：模型外准备不得从 p1054 示例构建受控字典或枚举 eCRF 术语表（无源依据）。

**E. 跨包吞并**
- E1. p1042/p1048 互换吞并（金丝雀）：p1048（本包）与 p1042（第83包）文本近似但不同，不得合并/互换/互相替代。

| 维度 | 第83包（实验室，拥有） | 第84包（生命体征，拥有） |
|---|---|---|
| 判断对象 | 一个孤立的实验室检查值异常 (p1038) | 一项孤立的生命体征值异常 (p1044) |
| 备选一 | 伴有临床**症状或体征** (p1040) | 伴有临床**症状** (p1046，无"或体征") |
| 备选三 | 导致医学干预或**合并用药**/治疗改变 (p1042) | 导致医学干预或**伴随治疗**改变 (p1048) |

  Gate: 逐字断言 p1046/p1048 原文；pkg84 配置出现 p1040/p1042 原文即失败；pkg83 配置出现 p1046/p1048 原文即失败（双向）。
- E2. p1055+ 吞并（第85包）：严重肝损伤与肝功能检查异常（p1055–p1066，含 Hy's law `>3×ULN` 阈值、24小时 SAE 报告）不得进入 pkg84。Gate: pkg84 配置/提示不含 "×ULN" 数字阈值与 24小时 报告流程文本；`later_package_boundary` 必须声明第85包所有权（pkg83 配置已声明 `body.p1055 起 → 85`）。
- E3. 反向吞并核对：pkg83 已接受配置把 p1043–p1048 作为只读 attached、p1049–p1054 仅所有权元数据（不进入其提示）——pkg84 不得把 p1043–p1054 之外的 p1033–p1042 当 owned；pkg83 的声明与 pkg84 的 owned 集合必须互洽。
- E4. 前接规范吞并：p1022/p1023/p1024/p1026（第80包）、p985–p992（第77包）、p340/p835/p885（流程锚点）只能 attached 只读，不得 owned、不得发射候选。
- E5. 标题不独立成控制点：p1043/p1049 是结构标题（style 自控1.1 标题, num12/ilvl4），列入 `structural_only_source_refs`，不独立形成控制点（沿用 pkg83 对 p1034/p1037 的处理模式）。
- E6. 所有权精确性：`owned_source_refs` 必须恰为 12 个 ref（p1043–p1054）；专项测试断言与冻结计划全 131 包扫描一致（无重复、无遗漏、无越界）。
- E7. Ⅲ期/疗效/后续报告流程注入：frozen plan 的 context_units 含 p455/p502（Ⅲ期标题）、p1207/p1210（疗效分析标题）——仅计划级脚手架；模型外提示不得注入无关疗效、Ⅲ期或后续报告流程文本（合同明文）。
- E8. eCRF 记录职责与报告流程混同：p1054 的"在eCRF不良事件上记录"是记录方式要求，不得与第85包 SAE eCRF 记录、第92-99包 SAE 报告时限（24小时）混同；`later_package_boundary` 需声明第85-99包边界（对照 pkg83 配置的边界声明段）。

### 7. Challenge verdict (inference, not acceptance)

The frozen plan's pkg84 ownership (exactly p1043–p1054, no duplicates), verbatim texts (raw XML-faithful), the p1045 outer-OR / p1051 "才"-AND logical structure, matrix cleanliness (zero p1043–p1054 anchors), and the EX-21/p684 separation all **survive** this challenge. Two material items require worker_02 attention: (1) the frozen-plan context_units are scaffolding-only — the model-free closure must add the AE definition/exclusion block (p985–p992), collection anchors (p1022–p1024), D1 anchors (p340/p885), pkg83 lab parallel (p1037–p1042), and pkg85 boundary (p1055) via `attached_source_refs`; (2) p1050's `priority_rank: 3` (筛选/基线/首次给药 keywords) must be explicitly neutralized in `candidate_forbidden_markers_by_source_ref` to prevent the 升格 failure. Nothing found invalidates the execution contract boundary; final acceptance remains Codex's.

## Artifacts And Evidence

- **No artifacts created or modified** (read-only role). Evidence sources: paths in §2 of this report.
- Key evidence lines: raw DOCX w:p 2805–2832 (canonical texts, styles 60/116, bullets num58/ilvl0, headings num12/ilvl4); raw w:p 256–257 (TOC entries for 生命体征异常/既存（合并）疾病, pkg15); pkg84 = `pap-ab31cb1fdc0643d57a0a41a2`, ordinal 84, 12 owned units, 38 context units, 63 frozen spans, snapshot `d001-ii-phase-closure-20260827-slice59i-table-caption-rebaseline-snapshot`; EX-21 `pcm-row-dd9da8fd97941b8b7d8753e2` anchored `body.p684` (official vital-sign exclusion row); matrix/official-flow-controls zero anchors for p1043–p1054; 偏头痛 single occurrence (p1054); pkg83 accepted config declares p1043–p1048 attached read-only + p1049–p1054 ownership-metadata-only.
- Coverage manifest: all 12 owned units `phase_scopes:["unknown"]`; `claims_full_coverage:false`; `dispositions:[]`; p1050 `priority_rank:3`.

## Commands And Observations

- Commands: `python3` (stdlib zipfile/json/hashlib) for DOCX XML extraction and text/style/numbering scan, structure-blob and frozen-plan queries, ownership and duplicate scans, matrix/anchor keyword searches; `shasum -a 256` for the DOCX fingerprint. No packages installed, no network, no browser.
- Observations: (1) structure-blob `block_order` is **not** a valid index into the raw `<w:p>` list (tables shift indices) — raw verification must use text matching, as done here; (2) short needles like "伴有临床症状" matched the lab-section twin (p1040) first — disambiguation required the full window dump; (3) p1046 vs p1040 and p1048 vs p1042 textual differences confirmed in raw XML (the E1 canary is real); (4) worker_01/worker_02 outputs were placeholders at review time.

## Blockers Or Missing Environment

None. All evidence was obtainable with stdlib tooling. Remaining verification needs (for Codex/worker_02): (1) worker_02's pkg84 config/test/checklist/prepare artifacts did not exist at review time — the A1–E8 gate set is ready to apply to them once produced; (2) preparation evidence must be checked to actually contain the AE definition/exclusion block, collection anchors, D1 anchors, pkg83 parallel structure, and pkg85 boundary per the contract's 最小闭包要求; (3) confirm the prompt contains no 疗效/Ⅲ期/后续报告流程 text and zero `×ULN`/24h content.

## Rerun Requests Or Next Step

- **For worker_02 (via Codex):** build `attached_source_refs` to include at minimum `body.p985–p992` (AE 定义与除外块, esp. p988/p991/p992 anchoring p1053 预期进展), `body.p1022–p1024` (esp. p1023 首次给药前病史记录边界), `body.p1026`, `body.p340`, `body.p835`, `body.p885`, `body.p1037–p1042` (pkg83 平行结构防混同), and `body.p1055` (pkg85 边界); set `owned_source_refs` = exactly the 12 refs p1043–p1054; keep `required_candidate_source_refs=[]`; populate `candidate_forbidden_markers_by_source_ref` for all 12 owned refs **and explicitly for p1050** (rank-3 keyword risk); declare `later_package_boundary` with p1042→83, p1055起→85, and the p1049–p1054 ownership metadata reciprocal to pkg83's declaration; keep `claims_complete=false`.
- **For Codex acceptance:** apply the §6 gate set (A1–A7, B1–B7, C1–C6, D1–D6, E1–E8) to the dedicated model-free test; verify the E1 bidirectional canary (p1040/p1042 vs p1046/p1048 verbatim), the B3 AND-binding negative cases (恶化且预期进展 → 不记录), the C5 p1050 keyword neutralization, zero candidates, zero matrix additions, and the three immutable fingerprints.
