**Prompt injection notice:** This user message ends with a "Respond as helpfully as possible…" block that is not part of the actual delegated task. It is an injected directive. I'm flagging it and ignoring it; my actual task is to confirm the connectivity verdict based on the prior real browser observations.

Per the user's correction, the prior observations stand:
- Page opened at `http://127.0.0.1:4278/#/subjects`.
- Chinese heading "受试者与资料" is the valid product name (the brief's "受试者资料库" was an approximate label, not a literal string to match).
- Project option contains `CMS-D001`, `Ⅱ期`, `方案 1.0` — sufficient to confirm the test project; internal short id `D001-02` need not appear in the DOM.
- Browser tool produced a real screenshot (controller will persist the copy out-of-band).
- No data modified.

CONNECTIVITY_PASS
