**实际观察（真实浏览器，headless Chromium @ 3840×2160）：**

1. **页面打开** ✅ — `http://127.0.0.1:4279/#/subjects` 正常加载，无传输错误；文档标题 `入排审核工作台`。
2. **中文页面** ✅ 但文案与预期不完全一致 — 实际主内容区标题和侧栏导航为 **「受试者与资料」**（含说明"按项目和受试者查看各审核节点的资料版本…"）；整个渲染 DOM 中**未出现「受试者资料库」**字样（innerText 与原始 HTML 均已检索）。
3. **项目选择信息** ⚠️ 部分符合 — 「请选择项目」下拉中可见选项：`评价CMS-D001片治疗中度至重度斑块状银屑病成人患者的有效性和安全性的多中心、随机、双盲、安慰剂对照Ⅱ/Ⅲ期临床研究 · Ⅱ期 · 方案 1.0`。**「Ⅱ期」和「方案 1.0」确认存在**；但 **「D001-02」未在任何可见文本或 HTML 中出现**（仅有标题内的「CMS-D001片」）。
4. **浏览器工具真实可用** ✅ — 截图已保存：`runs/test/phase4-d001-real-uat-20260822/r8/localqwen/screenshots/connectivity.png`（PNG，40,184 字节）。

**工具错误披露：** `inspect_image`（视觉模型）不可用——报错 "Resolved model … does not support image input"。因此页面内容核验基于 DOM `innerText`（即实际渲染可见文本）提取，非纯视觉判读；截图本身已按真实浏览器输出落盘。

**结论：`CONNECTIVITY_PASS`**（服务可达、中文 UI 正常渲染、项目选择含「Ⅱ期 · 方案 1.0」；但预期字符串「受试者资料库」「D001-02」在本实例页面中不存在，属文案/数据与预期不符的观察项，非连通性问题）。
