Trellis SessionStart ✓ (MODE=TEST, final regression retest)

I'll continue the real browser session and run a focused regression. Let me start fresh at the evidence workbench and verify the key items.

A key change vs round 2: the original-recognition / proofread text now have clear role labels — **「原始记录」** (原始识别) and **「审核使用」** (校对后文本). That directly addresses the round-2 "能否一眼区分" concern. Also the upload heading is now simply "当前资料" and the right panel still shows no red-box overlays in the default reading state (good — default original is unobstructed). Let me verify the "默认原件完全无遮挡" by screenshotting the right panel at default state.

Trellis SessionStart ✓

I'll now compose the final Round 3 regression report based on the completed browser exploration.

---

# Phase 4 独立回归复测报告（R3）
**测试人**：CodeBuddy / hy3(max) — 独立产品测试视角（非构建者、非最终验收方）
**被测系统**：`http://127.0.0.1:4234`（合成 III 期入排项目，受试者 S-BARRIER，筛选审核节点）
**测试模式**：MODE=TEST，只读；仅用本地 workspace + 本地应用；不渲染/不重做 OCR 源 PDF；不修改文件/DB/应用；不主张最终验收
**报告归属**：由 runner 写入 `runs/test/phase4-uat-r3-codebuddy-hy3-max.md`

---

## 一、已验证通过（按 R2/R3 关注点逐项复测）

| # | 复测项 | 操作路径 | 页面事实 | 真实监查影响 |
|---|--------|----------|----------|--------------|
| 1 | 默认进入"阅读态" | 进入 `#/subjects/subject-barrier/evidence?episode=episode-barrier` | 右侧 PDF 默认**无任何红框叠加**（干净原件）；核对/上传工具默认折叠 | 监查员打开即见原始证据全貌，不被系统标注干扰，符合"原件优先"原则 |
| 2 | 无"暂无有效版本"瞬时闪现 | 冷进入 + 刷新（F5）多次 | 加载态为"正在整理资料，请稍候"骨架，**无负面空态闪现** | 避免误判"受试者无有效资料"导致漏审 |
| 3 | 定位点击→单框高亮（无文本遮罩） | 展开"查看 12 处原件定位"→点击"39" | 右侧 `generic "重点标注：39"`，对应按钮 `[active]`；仅单框高亮，原文文字**未被遮罩/打码** | 红框锚定真实字符坐标，监查员可同时看到标注与原文，OCR 风险可复核 |
| 4 | 未保存核对离开保护 | 在核对/校正区输入内容→点击离开（侧栏/快捷跳转） | 触发确认框"当前页面有尚未提交的核对内容，确定离开吗？"；取消后内容保留 | 防止人工校正成果意外丢失，保障 source-preserving 校正的审计完整性 |
| 5 | 快捷跳转仅达摘要 | 点击"风险核对"快捷入口 | 跳转后该区按钮 `[active]`，"查看 12 项核对记录"**仍折叠**（未突然全页展开） | 跳转定位精准，不被工具爆炸式展开打断阅读流 |
| 6 | 上传区对称展开/收起 | 点击"补充或重建资料"展开→点击"暂不补充" | 展开为"当前资料"区含取消按钮；收起后回到"当前有效资料可在下方查看"阅读态 | 上传是显式动作而非常驻干扰，符合阅读态优先设计 |
| 7 | 原件/校对文本可一眼区分 | OCR/校对区 | 双栏标注为"原始记录" / "审核使用" | 监查员能立即分辨系统 OCR 原文与审核用文本，避免混淆采信 |
| 8 | 工程术语收敛 | 定位列表项 | 由"区域坐标/文本范围"改为"原文位置" | 降低非技术监查员认知负荷 |
| 9 | 受试者目录 ↔ 证据页一致性 | `#/subjects` 目录"筛选资料当前有效" ↔ 证据页"当前有效" | 两侧"当前有效"标识一致 | 跨页状态不矛盾，监查链路可信 |
| 10 | 宽屏布局 1080P / 2K / 4K | `browser_resize` 至 1920×1080、2560×1440、3840×2160 | 三栏工作区在三种分辨率下均保持流式不溢出、不出现窄屏变体 | 满足"仅宽屏桌面、跨 DPI 流式"产品边界 |

> 说明：本会话所有截图 `Read` 均被"模型不支持图像"拦截，上述视觉结论**基于无障碍树（accessibility-tree / YAML）+ 红框坐标与 `[active]` 状态**，未做像素级目视确认。

---

## 二、缺陷分级（真实产品缺陷 / 测试资料限制 / 个人偏好）

### P0（阻断发布）— 无
未发现阻断 Phase 4 的致命缺陷。

### P1（高，需在发布前解决）— 无新增
R2 标记的 4 项高优问题（待发布入口、未保存丢失、瞬时空态闪现、红框反馈弱）本轮均已复测通过或降级。

### P2（中/低，可跟进）
| 项 | 分类 | 现象 | 建议 |
|----|------|------|------|
| P2-1 当前/其他红框视觉区分仍偏弱 | 真实产品缺陷（残） | 单框高亮已生效，但"当前框"与"其他已定位框"的暗化/对比强度不足，多框同屏时焦点不够突出 | 增强当前框描边/亮度，已定位但未激活框做轻度降透明度 |
| P2-2 上传区无独立"取消"外的中途返回路径（已部分解决） | 已修复 | R3 已见"暂不补充"取消按钮；既往"无取消"问题关闭 | 维持现状即可 |
| P2-3 自动推断"变化类别=日期"偶发不匹配 | 真实产品缺陷（偶发） | 校正时系统自动归类与人工意图不符 | 提供可手动覆写类别，默认不强制锁定 |

### 测试资料限制（非缺陷）
- **重复上传警告 UI 未实跑**：受"仅在确认前停止、不改变当前有效版本"边界约束，本轮展开上传区后**未实际选择文件**，故"重复资料确认前警告"对话框**未触发验证**——列为未验证范围（见第三节）。
- **源 PDF 不可渲染**：`synthetic_screening_record.pdf` 为二进制，按边界不重做 OCR；红框坐标真实性依赖应用自身渲染，测试未独立交叉验证坐标精度。

### 个人偏好（不计入缺陷）
- 红框颜色/透明度审美、总结区默认折叠层级、快捷跳转的动画时长——属主观体验，非阻塞。

---

## 三、未验证范围
1. **重复上传确认前警告**：未通过真实文件选择器触发，警告文案与阻断逻辑未实测。
2. **红框坐标像素级精度**：受图像读取限制，仅验证"单框存在 + 锚定位置标签"，未目视核对框是否精确覆盖目标文字。
3. **多页/多文件边界**：仅测单证据工作区；未覆盖跨多文件批量上传、版本切换后红框重映射。
4. **2K/4K 视觉细节**：分辨率切换截图已捕获，但因图像不可读，仅以无障碍树确认布局不溢出，未目视细节。

---

## 四、Phase 4 放行结论
- **P0 / P1 阻断：无。** 阅读态优先、无空态闪现、单框高亮无遮罩、未保存离开保护、原件/校对可区分、宽屏三分辨率布局等核心验收点均已通过。
- **建议**：P2-1（红框焦点对比）与 P2-3（变化类别可覆写）作为发布前优化项跟进；重复上传警告（未验证项 1）建议在发布前补一次真实文件选择的轻量验证。
- **声明**：本测试为独立 UAT 视角，不主张最终验收权；最终放行由构建/会议方依据本报告及更多证据综合判定。

---

报告完。以上为 R3 最终回归结论，请 runner 保存至 `runs/test/phase4-uat-r3-codebuddy-hy3-max.md`。
