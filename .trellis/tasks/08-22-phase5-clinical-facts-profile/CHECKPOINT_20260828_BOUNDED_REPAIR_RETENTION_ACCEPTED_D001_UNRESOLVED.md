# Phase 5.8d 修订状态确定性保留验收，D001 第 68 包仍未接受

## 目标

修复同一会话多轮修订中“修复 A 时回退 B”、无范围错误扩大为全批次可变、候选拆分/合并失去来源闭包，以及同一候选内一个义务错误导致兄弟义务被一并改写的系统性风险。

## 已完成

- 水合后错误缺少机器可读结构单元/候选范围时立即转“需要核对”。
- 每次有范围的失败输出成为下一轮滚动基线，不再永久保留第一个失败版。
- 候选普通修订以精确来源结构单元键一对一替换；未授权候选恢复。
- `ACTION_TARGET_SCOPE_MISMATCH` 允许显式候选重分区，但必须保持完整来源并集，交叉越界或来源丢失即拒绝。
- `RECORD_PRECISION_COMPRESSED` 现携带精确义务原文定位；候选、原子和分组对应唯一时，只替换授权原子，其他字段原样恢复。
- 处置链接校验改为比较候选来源闭包，容许系统内容寻址 ID 合法重算，仍拒绝链接语义变更。
- 真实 v9 事后核对确认：第 6 次响应已带有 `body.p780` 义务类型错误，第 7 次只修复体重分组并保留该错误。

## 验证证据

- 聚焦回归：`153 passed`。
- 方案/事实/存储组合回归：`1033 passed, 58 warnings in 140.00s`。
- `.venv/bin/python -m compileall -q ...` 通过。
- `git diff --check` 通过。
- `audit-execution --task-type finite_code_task` 通过。
- 评估工件：`artifacts/phase5-slice60zm-d001-height-weight-repair-retention-20260828/assessment.md`。

## 不得误报的边界

- D001 第 68 包仍未接受、未发布；当前 v9 仍为“需要核对”。
- 本切片没有调用真实模型，没有伪造第 8 次响应，没有增加修订预算。
- 跨运行 accepted snapshot 未实施：v7-v9 是不同冻结批次，v7 也未经父级临床接受，不能作为金标基线。
- D001 II 仍为 `1848/1245/131`，剩余 128 包，`claims_complete=false`。
- 受试者、OCR、病例审核、浏览器、视觉及独立试用均未启动。

## 下一安全动作

回到活动 131 包冻结计划，按稳定 `source_ref` 选择一个能增加新语义风险的极小代表组；先建立盲态来源闭包和父级检查清单，再决定是否执行唯一一次真实 Agent 运行。不直接重跑第 68 包，不全量扩大到剩余 128 包。
