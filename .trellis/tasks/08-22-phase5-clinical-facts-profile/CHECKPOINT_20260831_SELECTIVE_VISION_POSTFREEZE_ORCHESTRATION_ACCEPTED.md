# Phase 5 选择性视觉冻结后编排验收检查点

日期：2026-08-31  
状态：后端编排已验收；用户界面、人工重试和真实项目端到端验证待完成。

## 已完成

- 证据处理修订冻结后只幂等创建独立的“页面视觉核验”任务，不在 OCR 或修订冻结事务中等待远端模型。
- 视觉核验按“短事务读取准备 -> 无数据库事务的远端调用 -> 短事务持久化”执行，避免长时间占用数据库事务。
- 应用启动时注册独立执行器；任务可从租约过期、进程中断和重复提交中恢复，成功观察按不可变身份复用。
- 原生文字充分的页面跳过视觉模型；模型失败只形成无模型正文的关闭记录，不生成伪观察，也不覆盖 OCR 原文或哈希。
- 创建任务前验证处理修订真实存在；执行时拒绝不受支持的规划版本，防止旧任务在新合同下误跑。
- D001 控制任务 `3259ab5f070447c3938ff2de5f45c9cd` 保持第 19 个检查点后的暂停状态。

## 验收证据

- 聚焦编排与恢复测试：`45 passed`。
- 相邻证据执行器、任务接口、应用注册与冷导入：`117 passed`。
- v2 存储、证据、服务与接口回归：`1337 passed, 1 skipped`；跳过项为缺少真实 oMLX 探针工件，不影响本切片。
- 定向 `py_compile`、`git diff --check`、正式 review gate 与 `audit-execution` 均通过。
- 三名执行者耗时分别为 `143.796s`、`623.453s`、`852.141s`；未切换模型或路线。

## 已知执行瑕疵

- 首次父级并发控制脚本误用 zsh 保留变量 `status`，导致三个子进程收到 SIGHUP；确认无残留进程后，以相同提示、模型和路线重新执行一次，不计作模型 fallback。
- 执行者 02、03 的落盘报告只有不完整状态摘要；父级没有以其自报作为验收证据，而是重新检查实际代码、测试、编译和审计结果。
- 执行包的 Source Of Truth 在派发后才补齐；已作为过程缺陷记录，不能追溯宣称为派发前已具备。

## 尚未完成

- 面向医学监查员的中文状态、失败范围、人工重试和取消入口。
- 原始 PDF、图片或文档滚动查看及当前定位红框联动。
- 使用独立 GLM-5.3-Flash 对非敏感真实冻结修订进行完整业务端到端验证。
- 尚未宣称所有业务 harness 均已启用视觉模型；模型仍只用于扫描、复杂表格或版面、结构异常及 OCR 低置信风险页。

## 下一安全动作

先建立视觉后处理的用户可读查询模型与人工重试/取消接口，再接入证据工作台；随后选取非敏感真实冻结修订执行一次端到端验证。不得恢复 D001 串行回放，也不得整份方案逐页重复视觉识别。
