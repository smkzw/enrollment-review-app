# Phase 5.8d 访视合并时间边界已修复，基线值层级回放未接受

日期：2026-08-28  
分支：`codex/phase5-clinical-facts-profile`  
工作树：`.worktrees/phase5-clinical-facts-profile`

## 本轮目标

用 D001 Ⅱ期未经用户预处理的原始 DOCX 产品链，验证筛选与 D1 间隔、基线访视窗口、一般基线取值和 D1 给药前专用取值能否被系统准确区分。

## 已接受的系统修复

- `TimeConstraint` 现在显式保存上下界是否包含边界；`＞7天`、`≥7天`、`＜7天`、`≤7天/7天内` 不再共用同一闭区间语义。
- 日、周以及日历月/年的确定性求值均保留开闭边界；无对应边界时禁止声明开区间。
- 发布门禁逐原子核对原文比较符与结构化边界，改写大于/小于或是否含边界时以 `TIME_BOUND_COMPARATOR_MISMATCH` 拒绝。
- 时间锚点错误优先定位到具体原子编号和原句；只有没有可识别时间原子时才使用候选级兜底错误。
- Agent 提示明确一般规则与更严格精确项目的层级：精确项目不得继承一般时间窗，已有冻结流程目标覆盖时不重复建立义务。
- 前端结构合同和中文呈现同步显示“超过、至少、少于、不超过”，旧数据缺少开闭字段时按历史闭区间兼容。

## 真实回放与父级核对

- 工件：`artifacts/phase5-slice60f-d001-visit-merge-baseline-value-open-bound-replay-20260828/`。
- 路线：`MTPLX/mtplx-qwen38-27b-optimized-quality:medium`，严格结构输出，单次有界真实回放；3 次同会话调用，`214.199618s`，无 fallback。
- 第三次响应已正确表达：`≤7天` 为包含 7 天的上界，`＞7天` 为不包含 7 天的下界，`D1前7天内` 为包含 7 天的上界。
- 回放仍未形成可发布终稿。离线按当前门禁重放后，首个错误精确落在 `pca-ea2531bf1ddda28a347a2db0`，原句为“以给药前最近一次评估结果作为基线值”，缺少首次给药命名锚点。
- 父级临床复核继续拒绝：模型把 D1 给药前生命体征、PASI、PGA、BSA、DLQI 和皮损照片错误纳入较宽的基线/合并访视窗口，并重复了已由冻结精确节点覆盖的项目。
- 正确层级应保持：`≤7天` 可合并且相同事项可只做一次；`＞7天` 时基线须在 D1 前 7 天内；一般基线取给药前最近一次；明确列举项目必须取 D1 给药前结果。

## 验证

- 时间边界、控制 Agent、水合、发布门禁、传输、MTPLX 路线和合同工件组合回归：`213 passed in 4.06s`。
- 前端完整测试和生产构建在本轮前一验证点已分别为 `512 passed` 和构建通过；本轮最后一次前端修改后未再变更前端代码。
- `git diff --check` 和 Trellis 校验在本检查点收尾执行。

## 边界

- 只接受通用时间合同、诊断和提示修复，不接受本次 Agent 临床解构，不发布任何新控制点。
- D001 Ⅱ期仍为 `1848/1245/131`，剩余 `128` 包，`claims_complete=false`。
- 未运行受试者、OCR、病例审核、浏览器、视觉或独立测试者。

## 下一安全动作

先为“通用基线值规则与明确 D1 给药前特例”建立不依赖项目名称的确定性层级反例，证明精确项目不会继承较宽访视窗口且不会与冻结流程目标重复。通过后再决定是否对新的代表组做一次真实语义验证；不得原样重放本组或扩大到全部剩余包。
