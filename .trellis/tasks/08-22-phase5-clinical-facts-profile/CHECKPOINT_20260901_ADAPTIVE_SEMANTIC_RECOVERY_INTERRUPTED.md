# Phase 5 方案语义分批恢复机制中断检查点

日期：2026-09-01 06:16 CST  
分支：`codex/phase5-clinical-facts-profile`  
状态：立即无损暂停，Phase 5 继续进行中，`claims_complete=false`

## 本轮已完成

- 从 SAR III / 31001 失败作业 `1653540a54c747e4bc60d94ae65b0b18` 恢复冻结方案输入并查明超时机制：固定每批 3 条会把两个长父规则和相邻规则合并为过大输入/输出。
- 新增通用自适应分批：按实际 compact prompt 的估算 token 预算贪心分批，官方父规则不拆分，每批仍最多 3 条。
- 新增严格输出 Schema 范围：每批只允许当前官方编号、来源片段和由冻结输入规模推导的组件/原子/资料要求上限。
- 新增任务内语义批次断点：仅落盘已通过确定性校验的批次；缓存键包含完整提示、模型/思考强度/严格 Schema 合同和批次身份。损坏、提示变化或模型合同变化均失效关闭。
- 专项回归：`99 passed, 5 warnings`；目标模块编译通过。
- 使用失败作业的只读冻结输入离线计算新分批：由 8 批变为 9 批，`EX-06` 独立，`EX-07 + EX-08` 为一批，其余按来源顺序保持完整覆盖。未发起新模型请求。

## 独立 VLM / 语义模型路由纠正

- 本机 OMP 实际目录中的可用路由名是 `zhipu-coding-plan/glm-5.3-flash`，支持图像输入和 `low/high/max`；未发现 `zhipu-token-plan/glm-5.3-flash` 路由。
- 现有独立视觉适配器已对齐 OMP Coding Plan 端点 `https://open.bigmodel.cn/api/coding/paas/v4`，从项目 `.env` 独立取密钥，不复制 OMP 凭据。
- 本轮之前的持久设计记录把 GLM-5.3-Flash 限定为“独立页面视觉核验”，并保留 MTPLX 作为方案语义解构默认路由。这是本次 MTPLX 服务仍被保留/启动的直接原因。
- 用户本次已明确“默认是 GLM-5.3-Flash”，该更新优先于旧记录。恢复后不得自动启动 MTPLX；必须先将方案解构、独立视觉及其他 harness 的默认路由边界按 GLM-5.3-Flash 重新核定和测试。

## 暂停时运行状态

- oMLX PID `93819` 已正常关闭；`127.0.0.1:8001` 无监听。
- MTPLX PID `90504` 已终止；`127.0.0.1:8002` 无监听。
- 没有正在运行的 SAR 31001 方案解构、模型探针、OCR 或受试者任务。
- 失败作业 `1653540a54c747e4bc60d94ae65b0b18` 保持 `failed_final`，未修改、未伪造重试。
- D001 控制任务 `3259ab5f070447c3938ff2de5f45c9cd` 仍保持第 19 个发现包后的无损暂停。

## 尚未验收

- 批次文件缓存已接入生产执行器，但还缺独立的损坏文件、跨任务隔离和原子写直接测试。
- 尚未使用 GLM-5.3-Flash 完成复杂 SAR 父规则的真实探针。
- 尚未创建新的 SAR V2 运行目录，未运行 31001 Evidence Normalizer / Patient Profile，未进行宽屏浏览器验收。
- 未运行全量回归、独立视觉测试者或最终执行审计。

## 下一安全动作

1. 先修改并验证默认模型路由：禁止方案解构自动启动 MTPLX，按本机 OMP `zhipu-coding-plan/glm-5.3-flash` 合同对齐 GLM-5.3-Flash。
2. 补齐断点文件缓存的直接单元测试，再运行相邻回归。
3. 仅做一个复杂父规则的 GLM-5.3-Flash 真实探针，记录时延、完整性和来源闭包；通过后才能新建完整作业。
4. 不得恢复旧失败作业，不得恢复 D001 第 20 包。

恢复入口：本文件。
