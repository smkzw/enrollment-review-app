# Phase 5.8d D001 知情同意/人口学动作闭包验收

## 当前状态

- Phase 5 仍在进行，`claims_complete=false`。
- D001 Ⅱ期活动冻结计划为 `papl-40b1237a22e538a278b4fd5e`：覆盖单元 1,848、语义目标 1,245、包 131；剩余 128 包。
- 本次只接受活动计划第 67 包中 `body.p767` 至 `body.p770` 的知情同意/人口学代表组，不接受旧 217 包研究快照的包序号。
- 未运行受试者、OCR、病例审核、浏览器、视觉或独立测试者。

## 来源边界

- owned：`body.p768`、`body.p770`。
- attached：`body.p767`、`body.p769`、`body.t5.r5`、`body.t5.r6`、`body.p315`、`body.p317`、`body.p630`、`body.p631`。
- IN-01、IN-02、知情同意流程必做项、人口学采集流程必做项保持独立；采集完成不能等同于入选标准通过。

## 失败闭环与根因

1. v1：0 候选。模型把“筛选前解释研究程序”压缩成 ICF 签署，遗漏独立动作。
2. v2：仅加强提示后仍为 0 候选，证明问题不只是措辞不足。
3. v3：动作覆盖门禁能够发现遗漏，但既有义务类型无法表达“在命名节点前完成一般动作”，4 次修订仍未形成可接受结果。
4. v4：新增通用 `complete_before_anchor` 后，模型可将“解释所有研究程序”表达为 `screening_date/before`，同时不重复签署、IN-01 或 IN-02。

前三版工件是必要反例，必须保留，不能以最终成功覆盖或删除。

## 系统修改

- Agent 提示版本升至 `phase5/control-agent-prompt/v1.4`，加入动作谓词独立比较和节点前完成自检。
- 协议控制合同新增 `complete_before_anchor`，强制要求时间约束且方向为 `before`；中文显示为“节点前完成”。
- 流程必做目标可声明覆盖的动作类型；发布门禁逐项核对 owned 来源要求的动作是否被链接目标覆盖，缺失时返回通用动作未覆盖问题。
- 代表组配置只把动作期望交给确定性门禁，不进入 Agent 首轮输入，避免金标准泄露。
- 未写入 D001 专有条款或项目编号规则。

## 真实回放与父级验收

- 接受工件：`artifacts/phase5-slice60zb-d001-icf-demographics-replay-before-anchor-20260828/`。
- MTPLX medium：2 次调用，`79.382436s`；结果序列为 `schema_invalid`、`parsed`，无 fallback。
- 最终结果：1 个候选、1 条流程必做处置、发布门禁通过。
- `body.p768` 只新增“筛选前向参与者解释所有研究程序”；`body.p770` 由人口学筛选流程完整覆盖，不生成重复年龄候选。
- 父级验收：`parent-clinical-acceptance.md`。

## 验证

- 聚焦回归：`174 passed in 1.70s`。
- 方案层完整回归：`912 passed, 58 warnings in 131.97s`。
- `compileall`、`git diff --check`、Trellis 上下文校验均通过。
- `audit-execution` 通过：三路均为 `cursor-cli/auto`，无警告、无错误、无 fallback。

## 下一安全动作

从活动 131 包计划中选择下一组能暴露不同结构风险的极小异质来源，先建立来源闭包和父级检查清单，再决定是否运行一次有界语义回放。不得直接全跑剩余 128 包，也不得进入受试者、OCR、浏览器或视觉测试。
