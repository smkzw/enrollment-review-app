# Phase 5 Slice 5.8 无损暂停检查点

时间：2026-08-24  
分支：`codex/phase5-clinical-facts-profile`  
状态：`in_progress`，尚未验收 Slice 5.8

## 已完成

- 以最新实时执行路线 `codex-subagent/codex/gpt-5.6-luna:max` 在同一会话完成五轮方案结构化输出修订，无 fallback。
- 基于三次 D001 II 隔离真实失败，从共享 Schema/校验层依次修复长度截断、可空语义对象、谓词与逻辑节点混用、孤儿节点修复及比较符/值形态混用。
- 当前第五轮结构将谓词拆为存在性、标量、集合三类数组，逻辑节点独立；水合后在统一 ID 空间检查重复、环、孤儿、共享子节点、根、例外和领域语义。
- Codex 聚焦验证：`80 passed`；`compileall`、定向 `git diff --check` 通过。执行器报告完整协议测试：`425 passed, 58 warnings`。

## 失败证据

1. `artifacts/phase5-acceptance/20260823/data-wire-rerun-20260824`：16K 长输出截断，随后出现可空语义字段。
2. `artifacts/phase5-acceptance/20260823/data-wire-rerun2-20260824`：完整输出进入系统后，混合谓词/逻辑节点无法可靠区分。
3. `artifacts/phase5-acceptance/20260823/data-wire-rerun3-20260824`：存在性比较符 `exists` 携带值，暴露 Schema 未按比较符限定值形态。
4. 第四轮直接首批探针先因孤儿 `p2-sex` 拒绝，同会话定向修复后验证通过；这只证明局部修复路径，不是项目验收。

上述目录和 runner 报告是当前根因证据，不作为冗余缓存删除。

## 尚未完成

- 第五轮结构尚未调用真实 oMLX。
- D001 II、MG-K10-SAR III 新架构完整流程与病例级事实/历时线/证据定位核对。
- Cursor CLI `auto`、Pi `cms-router/minimax-m3(high)`、Pi `opencode-go/ox-alpha-free` 三路独立测试者连通性与端到端试用。
- 把数据库中的具体终败诊断转换为医学监查人员可理解、可恢复的中文界面提示。
- 评估并实现全任务级截止时间，避免单次调用虽有限但整项任务耗时无上界。

## 下一安全动作

1. 读取本文件、`implement.md` 和 `docs/PROJECT_CONTEXT.md`，确认代码仍为第五轮结构。
2. 用新的隔离数据目录启动后端，不复用三个失败目录。
3. 先运行 D001 第一批真实结构化输出探针，核对三类谓词是否一次被接收、规则编号及来源闭包是否精确、是否发生重复修复。
4. 探针通过后才启动 D001 完整浏览器流程；D001 病例核对通过后再进入 MG-K10-SAR III。
5. 确定性和真实项目门槛通过后，才按声明路线启动三路独立测试者；执行/会商角色不可替代测试者。

## 暂停运行态

- 本轮临时 V2 后端 `127.0.0.1:8912` 应在暂停前停止。
- 既有前端 `4174` 与 oMLX `8001` 不属于本轮临时进程，不在本检查点关闭。

## 2026-08-24 恢复后首批真实探针新增证据

- 使用第五轮代码、旧失败数据库中的冻结 D001 II 输入只读构造首批 `IN-01/IN-02/IN-03`；没有写回旧数据库。请求提示词 6038 字符，真实 oMLX 于 140.025 秒返回完整正文。
- 回包通过严格 provider Schema，但领域水合失败，证明当前 wire Schema 仍弱于领域合同：
  1. 一个原子条件同时返回单段与多段原文定位，违反二者互斥；
  2. 一个 `ALL/ANY` 逻辑节点仅有一个子表达式，领域合同要求至少两个。
- 证据保存在 `artifacts/phase5-acceptance/20260824/probe-wire-v5/summary.json` 与 `raw-response.json`。这次结果为失败诊断，不得视为验收，也未触发结构修复。
- 用户随即要求立即无损暂停。没有继续修改产品代码、没有启动完整 D001 流程、会商或独立测试者。

新的下一安全动作：先从共享 wire Schema/提示词/水合边界解释为何严格结构允许上述两种领域非法形态；加入不依赖项目内容的互斥定位和逻辑最小基数约束及回归，再用全新探针目录重跑同一首批。不要只对本次回包做后处理补丁。
