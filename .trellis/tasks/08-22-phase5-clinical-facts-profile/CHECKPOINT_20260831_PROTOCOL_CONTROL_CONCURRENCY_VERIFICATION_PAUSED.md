# Phase 5 方案控制并发切片无损暂停检查点

时间：2026-08-31

## 当前目标

在不改变临床语义、不恢复 D001 暂停重放的前提下，为方案控制发现批次增加任务内受控并发，并保持租约、重试、取消、恢复和检查点语义。

## 已完成

- 已完成受治理的三工作项执行；未使用 fallback。
- 并发仅作用于任务创建时冻结白名单中的 `discovery_*` 发现步骤。
- 深度分析、确定性汇总、正式目录写入和质量门仍保持串行。
- 生产路径每次调用环境工厂创建独立 transport；共享 transport 仅是测试注入兼容面。
- 已检查 `JobRunner`、`JobStore` 和 `protocol_control_execution` 的关键接口与失败收束路径。
- 聚焦状态机与方案控制回归：84 passed。
- 扩大方案解构、方案控制、阶段适用性和工作流回归：828 passed，5 条既有 SWIG 弃用警告。
- 当前没有运行中的测试、模型调用或子 Agent。

## 重要保留状态

- 不得恢复或清理 D001 暂停任务：`3259ab5f070447c3938ff2de5f45c9cd`。
- D001 已完成 19/40 个检查点；第 20 批中断；来源摘要和既有运行记录必须保留。
- `app/services/protocol_control_execution.py` 仍为未跟踪文件；执行工作中发生过截断后重建。当前 828 项相关回归通过，但尚未把它宣告为最终验收。
- 独立 GLM-5.3-Flash 视觉适配器仍处于离线合同通过、真实调用受供应商 429/1113 余额或资源包限制的状态。

## 尚未完成

- 对并发实现做 Codex 最终代码审阅，重点核对 ProcessDeath、租约丢失、同波多失败和 PreparedStepResult 的提交顺序。
- 增补或确认“每个并发发现步骤使用独立 transport 实例”的确定性测试。
- 运行并发波次的实际耗时对照，确认峰值受冻结上限约束且无上下文串扰。
- 填写本执行包 review/metrics，运行 `audit-execution`，形成正式接受或修订结论。
- 独立 VLM 真实业务调用与用户视角端到端测试须等待 API 资源恢复，不能用离线测试冒充。

## 下一步安全入口

1. 读取本检查点及 `runs/execution/phase5-protocol-control-concurrency-20260831/`。
2. 检查当前工作树和测试进程，确认没有外部变化。
3. 从 transport 实例隔离测试和并发提交语义审阅继续；不要重跑 D001。
4. 通过后再更新执行 review/metrics 并运行治理审计。

