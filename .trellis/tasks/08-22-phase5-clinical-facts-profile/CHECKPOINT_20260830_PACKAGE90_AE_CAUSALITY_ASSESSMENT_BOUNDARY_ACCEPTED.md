# Phase 5.8d 第 90 包不良事件因果关系判断边界验收检查点

## 当前结论

- 已接受 D001 II 当前冻结计划第 90 包 `body.p1087-p1097` 的模型外来源闭包。
- `p1087/p1097` 仅作结构；`p1088-p1096` 保持治疗后因果关系综合判断职责，不形成入排候选。
- 第 91 包表 9 `body.t14.r0-r7` 只读进入，所有权和候选发射权不转移，`claims_complete=false`。

## 父级关键判断

- 原始 DOCX、结构块和冻结计划三重确认：`p1095` 写“可参照表7”，紧接 `p1097` 与实际下表均为“表9”。系统保留原文并标记“需要核对”，不静默纠正，也不吸入第 82 包真正的表 7 继发事件规则。
- 五个评价要点是研究者综合评价输入，不是全部满足、固定计分、任一单项充分条件或机械决策树。
- 统计相关分组仅含“肯定有关、很可能有关、可能有关”，不能扩大到后两类，也不能反推个例报告规则。
- SAE 由研究者和申办者共同判断，但意见不一致时任意一方判断相关即进入报告范围；不得改成双方一致，也不得升级为最终因果确认。
- 攻击审阅发现短语探针可被同义改写绕过。父级确认这些探针仅用于规格审阅，不是运行时文本分类器；运行时对全部 19 个来源采用来源身份零候选门禁。新增无短语命中的同义改写回归，仍由 `CONTROL_DUPLICATE_RETAINED` 拒绝，未向共享规则硬编码项目特异短语。

## 验证证据

- 配置 SHA-256：`494405fba8a0d5f95897fb414d50a963a791eb7427f1c4d57509b318d2ec1c13`。
- 模型外准备：`11 owned / 8 attached / 19 total`；提示 `39306` 字符，SHA-256 `98b4804b6309a3bb31add74759ee200fe58b38780411f3df656eedc4a714d85f`。
- 专项 `46 passed`；第 89–90 包相邻回归 `91 passed`；Phase 闭包 `667 passed`。
- 方案与 Agent `1198 passed`；当前治理测试 `16 passed`。
- JSON、准备产物、差异与执行审计通过，审计无警告或错误。

## 正式状态与边界

- D001 II 保持 `1848` 个结构单元、`1245` 个语义目标、`131` 个包；剩余 `117` 包。
- 未调用临床语义模型、未发布控制点、未进入受试者、OCR、Patient Profile、浏览器或视觉阶段。

## 下一安全动作

建立第 91 包 `body.t14.r0-r7` 表 9 因果关系评价表本体的最小模型外来源闭包，重点核对合并单元格、五级分类列、`+/-/±/++/-?` 含义及与第 90 包总纲的只读闭合；不得把表格符号机械化为项目外计分规则。
