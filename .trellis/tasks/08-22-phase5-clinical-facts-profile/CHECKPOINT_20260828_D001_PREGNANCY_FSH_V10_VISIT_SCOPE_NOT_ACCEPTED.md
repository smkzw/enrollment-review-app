# Phase 5.8d 妊娠/FSH V10 已收敛，访视覆盖边界未接受

日期：2026-08-28  
分支：`codex/phase5-clinical-facts-profile`  
工作树：`.worktrees/phase5-clinical-facts-profile`

## 本轮目标

验证同轮汇总发布与临床问题后，妊娠/FSH 代表组能否在有界修订内形成临床可接受终稿。

## 已完成

- 代表组真实验收器在同一轮同时返回发布问题和临床遗漏，不再串行暴露缺陷。
- 确定性回归覆盖“筛选/首次给药阶段混合”和“初潮前豁免遗漏”同时出现，两个修订范围均被保留。
- V10 使用真实 `MTPLX/mtplx-qwen38-27b-optimized-quality:medium`，6 次调用、`414.810655s`，形成 3 个候选和 4 个处置，技术门禁通过。
- 筛选阳性与首次给药前阳性已拆分；治疗后可选复查未纳入；初潮前豁免保留在筛选节点。
- 父级仍拒绝 V10：跨访视原文被错误宣称由筛选、基线、D1 三个目标完整覆盖到 W12 和提前退出；首次给药前候选又错绑到基线节点而不是专门的 D1 首次给药前节点。

## 深层根因

- 冻结流程目标虽然有精确 `visit_instance`，但筛选、基线、D1 三个目标各自携带了同一整段跨访视 `source_excerpts`。来源摘录范围因此大于单个目标的实际访视范围。
- 模型以宽泛摘录推断“完整覆盖”，发布门禁只检查目标 ID 是否存在、是否完整列出，没有结构化核对处置声称覆盖的访视集合与目标 `visit_instance` 并集。
- `review_stage=baseline` 被错误当作精确节点身份，导致 `flow-d1-pre-dose` 被 `flow-baseline` 吞并。

## 验证

- 复合缺陷及相关聚焦回归：`167 passed in 1.55s`。
- 完整方案层回归：`872 passed, 58 warnings in 130.10s`。
- V10 父级评估：`artifacts/phase5-slice60d-d001-pregnancy-fsh-parent-assessment-20260828/assessment-summary.json`。

## 边界

- 本轮只接受同轮问题汇总机制和 V10 收敛证据，不接受妊娠/FSH 代表组临床解构。
- D001 II 仍为 `1848/1245/131`，剩余 `128` 包，`claims_complete=false`。
- 未运行受试者、OCR、病例审核、浏览器、视觉或独立测试者；未发起 V11。

## 下一安全动作

先修复流程目录的访视级来源范围合同：每个流程目标的来源语义必须与其 `visit_instance` 一致，并在发布门禁中比较“原文已声明覆盖的选定期访视集合”与“链接目标访视集合”。同时增加首次给药前节点不得退化为一般基线节点的确定性反例。修复通过前不得再次调用模型或扩大到剩余包。
