# Phase 5 临床质检与 R3 清理记录（2026-09-03）

## 重新锚定后的阶段状态

- 当前仍为 Phase 5 收口，Phase 5.5 尚未启动；`claims_complete=false`。
- 31001 的合法事实规范化作业 `0c477b671e3b4e798725c6cf294efea1` 已完成 25/25 步；已发布 223 条事实、19 个事件、10 条用药暴露、130 条资料期望和 1 个 Patient Profile。
- 运行层性能问题已修复并通过回归：收尾步骤现在会在长事务后续租；事实门禁、发布、索引、资料期望和 Profile 的重复全量读取已改为共享只读缓存。相关聚焦回归为 207 passed。
- 数量完成不等于临床验收。原始证据核对发现知情同意、心电图等已有事实因 `fact_type` 与 `supported_requirement_ids` 的双重精确绑定合同不一致而被误报为未观察；另有来源原文中的 `UK` 日期占位需要中文展示，药名推测及邮件讨论误投影为用药暴露需要纠正。因此不得把 `claims_complete` 改为 `true`。

## R3 合同复核

- main-A：`GLM-5.3-Flash:high`，智谱 Coding Plan 通道。
- main-B：`MiniMax-M3:high`，cms-smk 通道。
- handwriting-C：`Qwen3.8-Flash-Next`，MTPLX.app，effort low，只能写 `handwriting[]`。
- DeepSeek 不进入任何受试者读道；GLM-OCR 只作 OCR 侧车。
- 页级 Schema 只允许 `evidence_for / evidence_against / mentions / none`，不得包含 `exclusion_triggered`、`supports_not_met` 等判定词。
- ClausePack 必须携带 `determination_mode`；确定性条款方向由代码计算，研究者判断条款按书面判断记录是否存在处理。
- 手写字段强制结构化，三源任二一致；归一化先于对账；页级请求沿用厂商默认采样。
- 截断只允许翻倍预算重试一次；429 等待不计次；云端每模型并发 2–3；内容过滤和端点故障必须形成显式页覆盖记录。

## 本次清理范围与删除前证据

删除清单先在 `/tmp/phase5-cleanup-manifest.json` 生成，记录每个目标的文件数、字节数和按文件 SHA-256 汇总的目录摘要。清单本身的 SHA-256 为 `a60596d8b764470438b2e4ba662eb71f92afe51b685f2e28f455cc60b89b6249`。

| 类别 | 目标数 | 文件数 | 字节数 | 分组摘要 SHA-256 | 处置依据 |
|---|---:|---:|---:|---|---|
| 明确拒收的 D001 试跑 | 4 | 60 | 1,302,731 | `04efc34104a11acc67a8165f2d40b08803c9c9cfc505e07357d7cba63a5b876e` | `phase5-slice61bk-...attempt1` 至 `attempt4-rejected` 已有后继正式结果 |
| 被后继 v4 覆盖的冒烟试跑 | 7 | 58 | 17,770,324 | `0b9995d0eea9dd0c05bc429d8c5c1dae8ca4615953498246d56b24bf7b265dea` | 保留 `quality-positive-control-packed48-v4-20260831`，删除其余重复试跑 |
| Phase 4 冗余测试记录 | 23 | 113 | 35,197,403 | `530b66a51e174d0cc2a07a654ae4cb98fbcaeb4c4828cbb010c119c4b00c95c5` | Phase 4 已归档，删除 `runs/test/phase4-*` 的重复标准输出和临时测试目录 |
| 可再生缓存 | 30 | 335 | 10,481,724 | `f34ef88dfe24d2a8f0bd13511c9cd0a0e07adfaef368b36eef479cdbf33864da` | 删除 worktree 内 `.venv` 之外的 `__pycache__`、`.pytest_cache`、Playwright 临时结果及 `.DS_Store` |

总计 64 个目标、566 个文件、64,752,182 字节。执行时使用 macOS 系统废纸篓而非不可恢复删除；按同一目标清单复核，工作树内剩余目标为 0，`.venv` 之外的缓存目录为 0。清理后 `runs/` 从约 101 MB 降至 48 MB；`git diff --check` 通过。废纸篓保留恢复能力，其清空不属于本次任务。

## 明确保留

- 31001 当前运行数据库、原始资料、证据快照、已发布规则修订 16、事实和 Profile 历史。
- `~/tmp/ie-vlm-benchmark-20260902` 金标及横评基线。
- D001 方案解构的来源闭包、正式回放结果与当前 RuleModelRevision 的可追溯研究材料；它们不进入产品运行路由，也不作为项目特异硬编码。
- `runs/protocol_control_smoke/quality-positive-control-packed48-v4-20260831`，它是检查点引用的后继中立冒烟证据。
- R3 设计书、实施计划、当前检查点、guard 执行记录、前端视觉基线和所有无法明确归因的用户改动。

## 下一安全动作

## 2026-09-03 临床质控修订与真实探针

- 已以通用合同解除 `fact_type` 与 `supported_requirement_ids` 的错误同词约束；覆盖由同权威元组中的显式资料要求绑定决定，展示分类可使用临床原生词汇。相关后端回归 `111 passed`，相邻发布/Profile 回归此前为 `92 passed`。
- Evidence Normalizer 提示合同现明确禁止补全残缺药名、禁止把常识或推测写入药物字段，并要求只有受试者实际使用、给药或已生效医嘱才能形成用药暴露；邮件讨论、审核意见、建议、假设和药物清单不构成暴露。没有加入受试者、项目、疾病、药名或时间点特例。
- Profile 日期展示只把日期字段中的独立 `UK/uk` 占位转成“月不详/日不详”，原始字段与证据不修改。前端聚焦回归 `9 passed`。
- GLM-5.3-Flash 单页真实探针均使用智谱 Coding Plan 通道、厂商默认采样：
  - `high` 探针约 302 秒后正文为空，未进入解析，不能作为 Phase 5 当前低推理合同的性能结论；它证明 5.5 仍需实现 `finish_reason` 保留和截断翻倍预算重试。
  - `low` 知情同意页 72.638 秒完成，6 条事实、1 个事件、0 个暴露、4 个未解决项；“知情同意记录”成功显式绑定 `IN-07`，跨页残句未被伪造闭合。
  - `low` 邮件讨论页 39.463 秒完成，2 条事实、0 个事件、0 个暴露、2 个未解决项；没有再把邮件中的药物讨论投影为实际暴露。
  - `low` 用药密集页在约 5 分钟观察窗内没有形成输出文件，不计为通过；其性能和药物字段质量必须由全量不可变作业及原始证据复核确认。
- 新全量作业已由正式 API 创建：作业 `01d26ed612b34e8cadbf75fe80bab2e6`，运行 `e56f98a166924d929eeac568aaa2d8a8`。未复用任何取消作业；专用服务为 `127.0.0.1:8910`，显式配置为 `zhipu-coding-plan / glm-5.3-flash / low / 16384 / 每次 1 页 / 厂商默认采样`。

## 更新后的下一安全动作

1. 等待新全量作业进入明确终态；失败时从具体调用、`finish_reason` 或合同错误追根，不激活失败结果。
2. 成功后逐项核对 31001 的知情同意、心电图、药物原名与类别、讨论性材料、事件日期、资料期望及 Profile；不覆盖旧历史。
3. 只有原始证据临床 QC 通过后才可把 `claims_complete` 改为 `true`，此前继续保持 `false`。
4. Phase 5 收口后另建 Phase 5.5 Trellis 任务，先做 ClausePack 投影和页级合同，再接读道 harness。

## 2026-09-03 用药来源门禁与新一轮全量作业

- 作业 `01d26ed612b34e8cadbf75fe80bab2e6`（运行 `e56f98a166924d929eeac568aaa2d8a8`）在第 13/25 步进入 `failed_final`，没有激活任何结果。失败点为邮件资料第 4 页，模型把“司普奇拜单抗治疗”“肝功能检查”“糠酸莫米松使用”作为断言对象，但这些带后缀短语没有逐字出现在各自断言原句中；根因是修复提示没有明确要求从既有原句截取最短连续临床名词。
- Schema 修复合同现要求从既有 `assertion_text` 直接截取最短连续临床名词，不得添加“治疗/检查/使用”等原文没有的后缀，也不得删除候选规避修复。
- 新增确定性用药字段来源门禁：药名未逐字出现在所引原文时整条暴露关闭失败；类别、适应证、剂量、单位、频次、途径等可选字段若没有原文支持则清空，后续只能由带独立来源的术语层补充。聚焦回归 `48 passed`。
- 同一真实邮件页在生产等同的两次 Schema 修复预算下复跑成功：第二次输出形成 15 条事实、2 个事件、2 条用药暴露、4 个未解决项；“司普奇拜单抗”“糠酸莫米松”保留，模型补出的类别、适应证和原文未载剂量被清空，原文明示的“注射”“上午下午均有使用”保留。证据位于 `artifacts/phase5-acceptance/20260903/glm-single-page-quality-gate/email-page4-source-gate-low-r2.json`。
- 专用 `8910` 已按显式 env 契约重启并载入新代码。新全量作业 `0150285c67d142398679b11600918263`（运行 `92a00d3cfbad458f99b6c1597dcda120`）由正式 API 新建，未复用失败或取消作业；当前等待明确终态。
- `claims_complete` 继续保持 `false`。只有新运行完成并经过知情同意、心电图、用药、讨论性材料、事件日期、资料期望和 Profile 的逐项原始证据临床 QC 后，才可讨论改为 `true`。

## 2026-09-03 第二次全量失败与未知方向归一化

- 作业 `0150285c67d142398679b11600918263`（运行 `92a00d3cfbad458f99b6c1597dcda120`）在第 12/25 步后进入 `failed_final`，没有激活结果。失败页为邮件资料第 3 页，错误为“未知极性候选不能携带断言依据”；两轮模型修复仍未清除这一内部矛盾。
- 对 `polarity=unknown` 增加语义保持的确定性归一化：方向继续保持未知，清空与未知方向不相容的原始值、规范值、单位和断言依据；不得据此改成肯定或否定，也不删除未知候选。聚焦回归 `49 passed`。
- 同一真实失败页重放通过：58.824 秒，3 条事实、1 个事件、0 条用药暴露、4 个未解决项。证据位于 `artifacts/phase5-acceptance/20260903/glm-single-page-quality-gate/email-page3-unknown-normalization-low.json`。
- 下一安全动作仍是重启专用 `8910` 后从正式 API 新建不可变作业；不得重用上述失败运行。全量成功和临床 QC 前 `claims_complete=false`。

## 2026-09-03 第三次全量失败与规范值修复合同

- 作业 `90aabafebee04bf79f8a121ddd0f09d6`（运行 `fdb78962aa8546678e8a23943421097d`）越过前两处失败页后，在第 18/25 步进入 `failed_final`；没有激活结果。失败页为筛选病历第 4 页，错误为“肯定或否定候选必须携带规范值”。
- 没有把所有缺值候选机械改成布尔值。Schema 修复合同现区分：原句明确给出定量结果时必须回填原数值和原单位；原句仅表达定性存在或否认时，才按明确 polarity 分别填 `true` 或 `false`，单位为空。
- 同一真实失败页在三次输出内重放通过，用时 428.488 秒，形成 26 条事实、3 个事件、2 条用药暴露和 3 个未解决项；所有肯定/否定事实均有规范值。用药字段中原文明确的药名、适应证、剂量、单位、频次和途径保留，模型补充的药理类别被来源门禁清空。证据位于 `artifacts/phase5-acceptance/20260903/glm-single-page-quality-gate/screening-page4-value-repair-low.json`。
- 聚焦回归继续为 `49 passed`，相邻规范化、发布、资料期望与 Profile 回归为 `112 passed`。下一步按新提示版本创建第四个不可变全量运行；临床 QC 前 `claims_complete=false`。

## 2026-09-03 无损暂停点

- 第四次作业 `6b73614e5dc34998b4bf803a12e470fb`（运行 `d71c1c28e56542acb0d40aeed35d0db1`）使用规范值修复后的全新提示版本创建，服务端曾确认 `created=true`。
- 用户要求无损暂停后，已通过正式 `POST /api/v2/jobs/{job_id}/cancel` 请求停止。作业在当前页安全结束后进入终态 `cancelled`，保留 4/25 个已完成检查点；`cancel_requested=true`，无错误码，没有发布或激活本运行结果。
- 专用 `127.0.0.1:8910` 已正常关闭，端口无监听。没有事实规范化或单页探针进程继续运行。
- 本轮新增代码与验证均保留：用药字段原文来源门禁、未知方向语义保持归一化、断言对象及规范值精确修复合同；聚焦 `49 passed`，相邻 `112 passed`，`git diff --check` 通过。
- `claims_complete` 仍为 `false`；Phase 5 尚未收口，Phase 5.5 尚未启动。R3 三读道边界不变：GLM main-A、MiniMax main-B、Qwen3.8-Flash-Next 仅 handwriting-C，DeepSeek 不进入读道，GLM-OCR 仅为侧车。
- 恢复后的下一安全动作：先只读审计作业 `6b73614e…` 的终态、4 个检查点和页面租约；确认正式“重新处理失败部分”是否严格从未完成页恢复且不会激活取消态结果。合同满足则从该正式入口恢复；否则通过新的内容身份创建受控不可变运行。运行成功后再做 31001 原始证据临床 QC，之后才可讨论 `claims_complete=true` 和 Phase 5.5。
