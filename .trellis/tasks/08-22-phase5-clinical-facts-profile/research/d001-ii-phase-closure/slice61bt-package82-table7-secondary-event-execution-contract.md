# 第 82 包表 7 继发事件记录规则执行合同

## 目标

为冻结计划第 82 包 `pap-2979ce0b01f057b9f4f0de11` 建立模型外来源闭包、父级临床清单和确定性回归。只处理 `body.t12.r0-r4`，不调用临床语义模型、不发布控制点、不进入受试者或视觉阶段。

## 权威来源

1. `artifacts/phase5-slice59i-d001-phase-table-caption-rebaseline-20260827/frozen_phase_plan.json`
2. 同目录 `coverage_manifest.json`
3. 同目录 `structure/blobs/protocol_blocks/3946ea2c9780d0399b60245eafc4ab85087328a5da158b9d0938f8858302343d.json`
4. 同目录 `source-input/blobs/protocol_sources/362443131f0d384c82c80f6a37396084f7d3301b51162201749c0488b0f2dd98.docx`
5. 第 81 包已验收配置、测试、父级清单和准备证据仅作模式参照。

## 边界

- 拥有来源必须且只能是 `body.t12.r0-r4`；`r0` 是表头，仅作结构。
- 第 81 包 `body.p1030-p1032` 必须作为前接只读语境，尤其保留“根据主要原因确定是否独立”和表题。
- 第 83 包 `body.p1033` 必须作为表内 A/B 记号说明；`body.p1034-p1042` 只在确有防混同必要时作为只读语境，不得改变所有权。
- 四行规则是条件分支，不得压成“一律独立”或“一律合并”。
- `r1` 只有轻度/中度/非严重/无需干预治疗且示例无需额外治疗时仅报告事件 A；不得外推到所有继发事件。
- `r2` 的重度或严重继发性事件分别报告 A/B；示例不是新增的通用病种门槛。
- `r3` 保留“在时间上分离”与“具有重要医学意义”的合取限定；不得只凭先后发生即拆分。
- `r4` 仅在事件关系无法确定时全部单独记录；不得误写为证据不足或入排待补证。
- 表格规则属于治疗期 AE 记录规范，所有拥有来源均不得发射预筛、筛选或基线入排候选。
- `body.t7` 是Ⅲ期疗效伴发事件及处理策略表，不是本处表 7，不得混入。
- 不补写删除、历史版本、日期算法、严重程度推断或其它产品机制。

## 工作项

1. 独立核对 DOCX、结构块、覆盖清单、冻结计划及第 81-84 包所有权边界。
2. 建立第 82 包配置、模型外准备、父级清单和专项回归，复用现有 runner，不改共享实现。
3. 从反事实条件、表格行列关系、例外强度、t7/t12 混同和跨包吞并角度进行独立挑战。

## 完成标准

- 准备证据真实包含所有直接引用和必要的只读上下文，拥有/附加角色正确。
- 四行条件与结果逐项完整，表格列关系未被拆散或串行错配。
- 零候选、零正式矩阵新增、`claims_complete=false`。
- 专项、相邻、Phase 闭包、方案与 Agent、治理工具、JSON 与差异检查通过。
- Codex 独立审阅并完成执行审计、检查点与 Trellis 记录。
