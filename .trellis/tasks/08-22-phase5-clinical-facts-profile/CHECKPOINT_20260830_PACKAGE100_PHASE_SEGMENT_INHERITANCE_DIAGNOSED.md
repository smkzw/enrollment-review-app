# Phase 5.8d Package 100 阶段语义段继承异常诊断

## 当前边界

- Package 99 已验收；Phase 5.8d 仍未完成，`claims_complete=false`。
- 当前活动冻结基线仍是 `artifacts/phase5-slice59i-d001-phase-table-caption-rebaseline-20260827/frozen_phase_plan.json`，计划身份 `papl-40b1237a22e538a278b4fd5e`。
- Package 100 暂停生成控制矩阵，先修复上游阶段归属；不得针对 D001 的具体 `source_ref` 写死规则。

## 已确认现象

- `body.p1171`、`body.p1180`、`body.p1181` 已由原文明确标为 II 期。它们在期别语义包中只作只读上下文是既有确定性合同的正确行为，不应重新交给 Agent 推断。
- `body.p1172` 明确为 III 期，并在 `统计假设` 二级标题下引出“如下的假设检验”。紧随其后的 `body.p1173-p1176` 是该假设的引导句、H0、H1 和 alpha，`body.p1177` 是同一二级标题下的三级标题“多重性校正”。当前却被标为 `unknown` 并由 II 期 Package 100 拥有。
- 同级二级标题 `body.p1179`“样本量计算”是自然结构边界；它之后分别存在明确 II 期和 III 期分支。

## 根因假设与修复合同

- 当前 `phase_detection` 只允许结构标题、精确期别标题、期别流程表题或固定“适用/应执行以下要求”句式建立后继语境；未识别“明确期别段落在当前标题内引出后续定义、公式、假设、列表或说明”的结构关系。
- 通用修复应只允许“局部阶段明确 + 语法上明确引出后续内容”的普通段落建立有界语境，并以最近的上级标题为边界：更深层标题继承，同级或祖先标题关闭。
- 普通阶段叙述、历史比较、交叉引用和没有后继引导关系的句子仍不得扩散阶段归属。

## 重建验收前提

- 新增正例、结构边界反例、普通叙述不扩散反例和对侧期切换反例。
- 新基线写入新的版本化目录，保留旧基线不变。
- 比较全部结构单元、待处置目标、包所有权与上下文；重点证明已验收 Package 1-99 的源单元所有权和临床语义未被静默改写。
- 新 Package 100 必须不再拥有 III 期假设构件；II 期明确段落继续作为确定性上下文。
