# Phase 5.8d 生命体征操作模态：代码闭环，模型基础设施阻断

日期：2026-08-29

## 当前结论

- 接受：项目无关的动作主体、阶段范围、义务强度、最低证据、用户审核指引、列举项和跨来源关系保真门禁。
- 不接受：生命体征代表组的最终真实语义输出和父级临床闭环。
- 阻断原因：最新代码下的 v11 修复轮、v12 两次新会话及容量恢复后的 v13 两次新会话连续遭遇 MTPLX 服务端 `500 internal_error`，没有形成可验收终稿。
- 正式状态不变：D001 II 仍为 `1848` 个结构单元、`1245` 个语义目标、`131` 个包，剩余 `128` 包，`claims_complete=false`。

## 冻结来源

- 原始方案 SHA-256：`362443131f0d384c82c80f6a37396084f7d3301b51162201749c0488b0f2dd98`
- Owned：`body.p784-p786`
- Read-only：`body.t5.r10`、`body.p321`、`body.p684`、`body.p885`
- 结构单元、批次身份、来源范围与正式统计均未变化。
- 当前重放检查点 prompt SHA-256：`65b3021d1dcd8f03857d4ffc308679c6011fe003e0b26b53f504b9eff32bc9fc`
- 当前整包指纹：`21be5887366ce76efef352ad67f30e71c6b8b7afb73ea3033467b4adc40d98cb`

## v3-v12 递进证据

1. v3 把实际休息要求改写为“告知/建议参与者”，触发动作主体保真修正。
2. v4 只绑定筛选，遗漏 D1 给药前基线复核，触发阶段全集校验。
3. v5 把推荐性休息强化为必备证据，触发最低证据强度校验。
4. v6 修订错误携带了无关义务来源范围，触发并修复修订权限越界。
5. v7 正式义务遗漏生命体征列举项，触发候选义务动作覆盖。
6. v8 仍核对“是否建议”而非“是否实际休息”，触发准备动作落实保真。
7. v9 三轮均重复同一语义改写，提示词修订仍未收敛。
8. v10 技术门禁通过，但父级发现“四类检查/五个读数”口径错位，且新增要求关系被降为普通解释，继续拒绝。
9. v11 首轮正确触发“补充要求必须声明首次受影响节点”；修复轮 512.243 秒后返回 500，请求 `284e3d052bce`。
10. v12 两个新会话分别在 104.178 秒和 96.128 秒返回 500，请求 `06172f5bb8d0`、`6d7fb4f3fe1d`；没有模型输出。
11. v13 启动前会话缓存已从约 49.1 GB 自动回落至约 24.1 GB、9 个条目、0 个活动请求；两个新会话仍分别在 110.188 秒和 95.547 秒返回 500，请求 `e0c906e5d034`、`c283bba62027`。因此“缓存接近上限”不是充分原因。

## 代码与验证

- 新增/强化通用门禁：审核指引动作保真、最低证据模态保真、准备动作落实、用户可见列举项计数、动作增量关系强度。
- 新增通用动作识别：与参与者沟通、生命体征组成核对；不包含 D001、受试者编号或具体条款特例。
- 聚焦回归：`155 passed, 5 warnings`。
- 方案模块完整回归：`1145 passed, 58 warnings`。
- 治理执行三路均完成；独立 MiniMax 会商结论为 revise，不是通过。

## 不可丢失证据

- `artifacts/phase5-slice61be-d001-vital-sign-modality-closure-20260829`：v10 技术绿、父级拒绝反例。
- `artifacts/phase5-slice61bf-d001-vital-sign-modality-closure-20260829`：v11 schema 拒绝及请求 `284e3d052bce`。
- `artifacts/phase5-slice61bg-d001-vital-sign-modality-closure-20260829`：v12 两次传输失败。
- `artifacts/phase5-slice61bh-d001-vital-sign-modality-closure-20260829`：容量恢复后的 v13 两次传输失败。
- `reviews/codex_execution_phase5-slice61av-vital-sign-modality-contract_review.md`
- `reviews/codex_conference_phase5-slice61aw-vital-sign-parent-review_review.md`

## 下一安全动作

1. 先用不含临床答案的诊断夹具隔离“约 31k 字符提示 + 严格 response schema”服务端路径，区分结构化输出、长提示、MTP 深度或进程生命周期问题；不得继续用临床重放充当基础设施探针。
2. 保存诊断请求与 MTPLX 真实服务日志；若需重启，只在无活动请求且不影响其他本地任务时执行。
3. 基础设施修复后，基于 v13 后继配置只执行一次新的同源重放，不扩大来源、不更换临床问题、不泄露父级期望答案。
4. 仅在结构门禁、临床拒绝门禁和 Codex 父级逐字核对均通过后，才创建父级接受文件。
5. 在此之前不发布本组控制点、不并入正式 131 包、不进入受试者、OCR、Patient Profile、浏览器或视觉阶段。
