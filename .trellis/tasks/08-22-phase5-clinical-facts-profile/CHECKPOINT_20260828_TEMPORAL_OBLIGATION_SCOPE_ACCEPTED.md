# Phase 5.8d 时间义务作用域合同接受

日期：2026-08-28  
分支：`codex/phase5-clinical-facts-profile`  
工作树：`.worktrees/phase5-clinical-facts-profile`

## 本轮目标

把 D001 访视合并与基线值回放暴露的系统根因转成项目无关的结构化合同，防止通用访视时间窗扩散到精确检查项，并区分访视安排、结果有效期和基线值选取。

## 已接受的系统修复

- 新增三种时间相关义务：访视安排/核对、结果有效期核对、基线值选取。
- Agent 不得再用带时间约束的通用“完成/核对”表达上述语义。
- 访视安排必须关联本候选已绑定的冻结流程节点，不得关联单个流程必做项。
- 结果有效期必须以补充关系精确关联受影响的冻结流程必做项。
- 基线值选取必须明确选择通用流程节点或精确检查项；两种作用域同时出现时，必须拆成不同候选。
- Agent 水合和最终发布门禁均执行相同边界，领域对象不能绕过 wire 直接发布。
- 中文矩阵标签与修订提示同步使用临床语义，不向用户暴露新英文类型名。

## 真实反例重放

- 只读输入：`artifacts/phase5-slice60f-d001-visit-merge-baseline-value-open-bound-replay-20260828/execution/raw-responses.json` 的第三次响应及同目录冻结批次。
- 原响应在新合同下被拒：带时间约束的通用完成义务及错误的检查项关系不再可接受。
- 仅做结构化等价拆分后，两个候选可水合：
  - 访视合并与 D1 前 7 天窗口使用“访视安排/核对”，关联 `flow-d1-pre-dose`；
  - 一般基线值原则使用“基线值选取”，关联 `flow-d1-pre-dose`，不再重复精确 D1 项目。
- 该离线修正只证明合同表达能力和确定性门禁闭合，不是新模型输出，不构成临床解构接受，也不发布控制点。

## 验证

- 控制 Agent、发布门禁、传输、时间约束、MTPLX 路线和契约产物组合回归：`218 passed in 4.10s`。
- 完整协议层：`887 passed, 58 warnings in 124.64s`。
- `py_compile`、契约生成、`git diff --check` 通过。
- 完整协议层首次运行暴露一个旧测试仍断言历史允许集合；更新为包含冻结流程节点后，单测与全量均通过。

## 边界

- 只接受共享系统合同，不接受原 D001 Agent 临床解构。
- D001 II 保持 `1848/1245/131`，剩余 `128` 包，`claims_complete=false`。
- 未启动受试者、OCR、病例审核、浏览器、视觉或独立测试者。

## 下一安全动作

选择新的极小异质控制组，优先验证真实方案中“结果有效期”与“基线值特例”的分离表现。先做来源闭包和人工临床核对，再决定是否进行一次真实语义调用；不得把本轮人工结构修正当作模型成功，也不得直接扩展到全部剩余包。
