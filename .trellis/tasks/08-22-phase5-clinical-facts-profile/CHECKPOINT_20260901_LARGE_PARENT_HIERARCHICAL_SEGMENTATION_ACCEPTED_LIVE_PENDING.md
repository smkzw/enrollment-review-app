# Phase 5 超大父规则层级分段离线验收检查点

日期：2026-09-01

## 已完成

- 通用规划器按冻结顺序区分根限定语、嵌套限定语和正文闭合单元，不再以标题字数猜测层级。
- 逻辑圆括号、前导连接词和无法证明的跨块依赖继续失败关闭；引用/版式方括号不再把独立正文块误判成逻辑续接。
- 相同限定语栈的相邻正文按令牌软上限和每段最多 3 个闭合单元聚合；真实只读复杂父规则由 11 个单正文调用收敛为 4 段 `[3, 3, 3, 2]`。
- 每段保留限定语来源和自身正文来源；合并前后仍执行来源闭包、同父身份和完整父规则门禁，任一分段失败只走同模型整父回退。
- 聚焦测试 `41 passed`；完整协议回归 `1286 passed`，另有 1 个既存 D001 历史提示词哈希漂移；治理执行审计通过。

## 边界

- 本检查点只接受确定性规划、接线和离线回归，不接受真实性能或语义质量声明。
- 冻结 SAR 输入只读使用；失败任务未恢复，未创建新项目、未发布目录。
- D001 控制任务继续停在第 19 包后，不恢复第 20 包。
- 生产规划器不按研究编号、疾病、药物、量表、条款号或时间点决定切分。
- `claims_complete=false`。

## 下一安全动作

使用既有只读冻结复杂父规则输入，新建独立 GLM-5.3-Flash high 分段探针，记录 4 段的并发墙钟时间、每段调用、合并后完整门禁和与既有整父结果的来源/逻辑差异。失败时保留完整证据并停止，不恢复旧 SAR 或 D001 作业。
