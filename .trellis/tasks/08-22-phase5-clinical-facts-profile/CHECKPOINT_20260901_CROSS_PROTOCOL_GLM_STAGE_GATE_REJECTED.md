# 2026-09-01 跨方案 GLM 候选与复核阶段门禁检查点

## 当前结论

- GLM-5.3-Flash high 在 D001 只读冻结 EX-18 上生成了 1 条父规则、8 个组件的完整候选，保留了 8 个治疗史/洗脱期分支、时间长度和两项例外。
- 运行使用 `zhipu-coding-plan/glm-5.3-flash:high`，2 次同会话调用，模型耗时 585.89 秒；未分段、未跨提供方拼接。
- 冻结来源包前后 SHA-256 均为 `e4c6b5478f4de73216383219762126eb4277552f9ff4ce2f2b7019faba123c8d`，D001 第 20 包未恢复。
- 旧门禁存盘结果为可发布，但 Codex 复核发现 8 个组件全部只有筛选期资料要求，而原文判断锚点均为首次给药前。
- 通用门禁已补充“基线/随机/首次给药/研究药物给药前置锚点至少需要基线最终复核”的确定性合同；当前候选离线重放产生 8 个 `REVIEW_STAGE_REQUIREMENT_MISSING`，不得发布。
- 提示框架同步要求筛选期提前关注不能替代基线最终复核；未加入 D001、疾病、药物、评分或固定时间窗特异规则。

## 验证

- 聚焦时间锚点回归：`6 passed`。
- 协议门禁与适配器：`169 passed`。
- 协议与 Agent 扩展回归：`1376 passed, 1 failed`；唯一失败为只读 D001 历史检查点保存的旧提示词 SHA，历史锚点未修改。
- `py_compile`、`git diff --check` 通过；当前虚拟环境未安装 Ruff，因此未执行 Ruff。
- 受控执行路由审计通过；3 个执行角色均有合法提示、模型身份、终端输出和 stdout 日志。
- 确定性复核：`verification-results.json` 确认 9/9 来源闭包、0 项基线资料要求、8 项阶段缺口。

## 选样纠偏

本轮所谓“异构规则”仅按来源片段数、摘录数和字符数排序。EX-18 与先前 EX-06 都是多分支治疗史/洗脱期规则，因此只证明跨方案同类规则泛化，不能证明结构异质性。不得沿用该算法声称异构验收通过。

## 下一安全动作

1. 用项目无关结构特征描述父规则，例如逻辑深度、原子类型、数值/时间/例外/专业判断/流程资料结构和来源拓扑。
2. 以与已测 EX-06/EX-18 的结构距离选择真正不同类型的冻结父规则，不使用疾病、药物、项目编号或具体阈值。
3. 先离线验证选择稳定性、来源闭包和反过拟合，再决定是否发起一次 GLM 整候选调用。
4. 任何新候选继续执行完整门禁和 Codex 临床复核；不得恢复 D001 第 20 包、旧 SAR 失败任务或跨模型拼接候选。

Phase 5 仍为 `claims_complete=false`。
