# Phase 5.8d D001 病史/治疗史采集代表组验收

## 已完成

- 从未经用户预处理的 D001 Ⅱ期 DOCX 冻结结构中核对 `body.p772/p773/p858/p871`，并以 14 个跨章节单元作为只读上下文。
- 修复只读附件未进入 Agent 提示、回顾区间起点被误作执行访视、给药前事项被误判治疗后、访视目标过度绑定、改处分类型逃逸和并列资料家族漏链六个通用根因。
- 父级拒绝 v3-v8 中技术绿色但临床错误的结果；v9 使用结构化资料家族合同，不依赖中文关键词扫描或项目目录编号。
- v9 真实 MTPLX medium 运行 2 次调用后接受：0 新候选；筛选/基线全量收集和基线/D1 增量复核均绑定精确流程目标。
- 最终代码聚焦回归 `189 passed`；方案层完整回归 `911 passed, 58 warnings`。

## 不变边界

- D001 Ⅱ期仍为 `1848/1245/131`，剩余 128 包，`claims_complete=false`。
- 未运行受试者、OCR、病例审核、浏览器、视觉或三路真实用户测试。
- v1-v8 与 slice60o-v 产物均是不可变诊断证据；不得覆盖或改称临床接受。

## 下一安全动作

最终方案层回归与执行治理审计均已通过；执行包过程文件已归档。活动冻结计划是 `papl-40b1237a22e538a278b4fd5e`（131 包），其中第 67 包拥有 `body.p767` 至 `body.p770`；`research/.../frozen_phase_plan.json` 是 217 包旧快照，不得用于活动身份。随后核对该包的知情同意/人口学筛选前置与筛选期单节点；稳定身份继续使用 `source_ref`，不直接运行剩余 128 包。
