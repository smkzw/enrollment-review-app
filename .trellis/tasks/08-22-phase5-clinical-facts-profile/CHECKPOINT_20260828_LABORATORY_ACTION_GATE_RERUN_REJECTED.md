# Phase 5.8d 实验室动作保留重放拒绝检查点

## 当前结论

第 70 包在修复后的动作门控下完成一次真实 MTPLX medium 调用。模型输出结构合法，但仍将 `body.p799` 的“采集样品”和“按标准实验室程序执行”错误视为已被检查目录完整覆盖。确定性门控触发 `PROCEDURE_ACTION_UNCOVERED`，结果不接纳、不发布。

## 已完成

- 从未经用户预处理的 D001 II DOCX 重建第 70–71 包；来源和提示均不可变。
- `body.p799` 冻结 `collect_biospecimen`、`follow_specified_procedure`，仅供门控使用，未泄露给 Agent。
- 只调用第 70 包一次；第 71 包不拥有该单元且无新增提示变化，因此未重复调用。
- 父级核对确认失败属于“检查目录错误包含执行动作”的语义推断，不是 OCR 或来源缺失。
- GGT、尿糖、尿潜血和 EX-20 合取逻辑未出现新增错误。
- 60zt 原始反例、第 68 包、其余 128 包和现有控制点均未改动。
- 聚焦回归 `181 passed, 5 warnings`；完整方案模块 `944 passed, 58 warnings`；编译、JSON 和限定范围差异检查通过。
- Hermes 执行审计与评审门通过；过程目录已归档，可重建的 60zu 临时产品链已移入废纸篓。

## 未完成

- 第 70–71 包临床解构未接受，未发布新控制点。
- D001 II 仍为 `1848/1245/131`，剩余 128 包，`claims_complete=false`。
- 受试者、OCR、病例审核、浏览器、视觉和独立测试者流程尚未启动。

## 根因

当前模型会在“目标目录已覆盖检查项目和访视”时，错误推断同段采样和程序动作也被覆盖。门控已能阻断错误处置，但下一层合同还需验证：当模型改为候选时，候选义务本身必须逐项保留未覆盖动作，不能只改变 disposition 类型。

## 下一安全动作

先建立项目无关的“候选动作语义覆盖证明”合同和离线合成回归：

1. 对每个冻结动作定义可接受的义务表达映射，不依赖 D001 文本或项目编号。
2. `other_control_candidate` 只有在候选义务逐项覆盖未被目录覆盖的动作时才可通过。
3. 缺动作时返回机器可读范围，供有界修订；父级金标准不得进入首次提示。
4. 先用保存输出和合成反例离线验证，不能直接继续调用模型或扩大全量包。

恢复时先读取本文件、`artifacts/phase5-slice60zv-laboratory-action-gate-call-20260828/parent-clinical-review.md` 和 `batch-gate-report.json`。
