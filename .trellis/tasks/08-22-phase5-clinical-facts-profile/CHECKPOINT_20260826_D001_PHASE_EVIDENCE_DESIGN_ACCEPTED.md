# Phase 5.8d D001 单位级期别证据设计验收检查点

日期：2026-08-26

## 已完成

- 独立只读会商已通过正式验证；会商不修改应用、人工矩阵或源方案。
- 当前闭包矩阵共 82 行、155 个来源锚点、152 个唯一冻结结构单元。
- 152 个单元中 116 个结构期别为 `unknown`、35 个为 `phase_ii`、1 个为 `mixed`。
- 136 个期别问题实际影响 78 行；问题条数不能替代受影响行数或待处理控制数。
- 矩阵 `phase_disposition` 仅是待核对主张，不是冻结结构单元的期别真值。
- `blocked_structural` 与 `unresolved_semantic` 必须分开；混合成员边界未修复时不得交给单值期别语义 Agent。
- 跨期共用必须由同一义务家族的正向来源证明。不得把“Ⅱ/Ⅲ期评估和程序一致”或盲法共用语句广播到所有入排、治疗与合并用药控制。
- 多来源矩阵行只在全部来源单元均闭合且彼此兼容时才能汇总；任一来源未知、混合、对侧或与行处置冲突时继续阻断。

## 路由与验证说明

- `general_grok46` 实际使用 `cursor-cli/auto`，无 fallback；`grok-build/grok-4.6 medium` 仅为未触发的后备路线。
- 验证器同时要求历史兼容逻辑节点名 `cursor-grok-4.6-medium`；复核文件明确标注该名称不代表真实调度或模型替换。
- Qwen 初始会话未形成有效报告；后续有效报告仍为 `alibaba/qwen3.8-max:xhigh`。尝试后备时被夜间调度重新水合回 Qwen 的过程已保留为异常执行证据，不冒充干净 fallback。
- `validate-conference` 已通过。

## 下一安全动作

1. 新增一个确定性的单位级期别证据研究视图，不新增第二套临床语义引擎。
2. 视图绑定当前清单、矩阵、方案和语义计划身份，逐单元并列展示冻结来源、结构期别、包归属、矩阵引用与矩阵待核对处置。
3. 输出结构阻断、语义未决、来源冲突和行级未闭合原因，并保持 `claims_complete=false`。
4. 按明确Ⅱ期流程、共同章节未知叶节点、表5、结核/妊娠、结果有效期/复测、对侧期引用和治疗后污染风险选择异质代表包；不随机抽样，不直接全跑137包。
5. 收紧“共同章节结构”提示：只有共同结构本身明确约束同一义务家族，且不存在改变该义务的期别特异兄弟内容时，才能作为正向证据候选。

## 仍未完成

- 152 个矩阵引用单元的语义期别闭包。
- D001 II 全文待处置单元的最终处置与 `claims_complete=true`。
- MG-K10-SAR III 泛化校验、代表受试者、Patient Journey、浏览器与三路独立测试者验收。

以上未完成项不得因来源闭包或会商通过而提前宣称完成。
