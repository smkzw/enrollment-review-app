# Phase 5 选择性视觉观察侧车验收检查点

日期：2026-08-31  
状态：不可变持久化与显式后处理服务已接受；正式证据编排和前端待后续切片

## 已完成

- 新增选择性视觉观察合同、ORM、0019迁移、仓储和显式证据后处理服务。
- 每条观察绑定已落盘页产物、原始资料版本、页码、页图哈希及可选OCR页哈希。
- 成功观察按页图、规划、模型、提示和风险理由组成的内容身份幂等复用；同身份不同正文拒绝覆盖。
- 失败调用只追加`closed`审计行，不保存模型伪正文；鉴权、远端、缺图和来源不一致保持失败关闭。
- 原生文字充分时不调用视觉模型、不写观察；符合条件的页才延迟加载视觉传输。
- 写入和回读均验证来源闭包；服务前后复核OCR原文和哈希，未改写OCR缓存、租约或早期阶段结果。
- 当前服务是显式入口，尚未自动挂入OCR执行器或证据修订冻结流程。

## 验证

- 新迁移、仓储和服务聚焦测试：`15 passed`。
- v2存储、证据和服务层回归：`1052 passed, 1 skipped`；跳过项为未提供的真实oMLX探针产物。
- 相邻OCR并发断言在组合运行中一次出现峰值时序波动，隔离连续3次通过；未修改无关并发代码。
- `py_compile`、冷导入检查和本切片`git diff --check`通过。
- 正式`audit-execution`通过：3个执行角色均为`pi/cursor/default`、返回码0、无fallback；路线无独立manager，由Codex验收。
- 执行者02落盘仓储文件曾被分页标记截断；执行者03独立发现，父级恢复实现并以真实导入和测试重新验收，未采信自报成功。

## 未完成与限制

- 尚未把显式后处理服务接入“证据修订冻结后”的正式编排；不得宣称所有上传或OCR任务都会自动形成视觉观察。
- 尚未实现失败观察的用户触发重试、批次进度和前端查看入口。
- 尚未实现右侧原始PDF/图片/文档滚动、红框定位和观察到原文的交互下钻。
- D001控制任务`3259ab5f070447c3938ff2de5f45c9cd`继续保持19个完成检查点后的无损暂停；本切片未读取、恢复或修改该任务。
- 既存D001 v1.5提示哈希漂移仍需独立版本化迁移，不得修改冻结哈希掩盖变化。

## 下一安全动作

建立独立受控切片，把选择性视觉后处理挂在已冻结证据修订之后：只消费不可变页产物和质量元数据，按修订记录幂等进度与失败状态，不进入OCR核心事务、不延长OCR租约、不覆盖原始识别结果。先用合成页和非敏感单页验证崩溃恢复与重复调用，再进入只读真实项目验收。
