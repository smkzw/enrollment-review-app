# Phase 5.8d D001 心电图控制点接受：字形恢复与动作合同修复

## 当前结论

D001 II 心电图代表组已通过完整离线发布门禁与临床拒绝门禁。该结论只接受本代表组，不把它并入完整 131 包正式目录，也不改变剩余 128 包和 `claims_complete=false` 的边界。

## 真实运行记录

- 第一次真实运行暴露模型漏掉 QTcF 计算动作。
- 第二次真实运行经过两轮有界修订，第三份原始响应已同时保留检查前静息和 Fridericia 公式计算 QTcF。
- 第三份原始响应 SHA-256：`6e18be457055732ca68697a495e60db4e12fd47d5f2df6dc8c916e76a7e1cba0`。
- 原始响应使用直撇号 `Fridericia's`，方案原文使用弯撇号 `Fridericia’s`；除该字形外，文本连续、唯一且等长。

## 深层根因与共享修复

1. 原校验只接受逐字一致摘录，模型常见的 Unicode 引号归一化导致真实来源被误判为不存在。
2. 只允许“唯一、连续、等长、仅引号字形不同”的确定性恢复；词、数字、单位、比较符、空白、普通标点、多重匹配和跨来源变化仍拒绝。
3. 来源恢复在深拷贝后的 wire 上执行，调用方持有的原始响应对象不被修改。
4. 更深层阻断来自共享动作合同：冻结计划要求 `prepare_participant` 和 `calculate_qtcf`，但动作检测器从未定义这两个项目无关动作，导致模型即使完整表达也必然失败。
5. 已扩展通用动作语义目录，并加入设备名称不等于已执行测量等负向回归；未写入 D001、EX-21 或心电图条款编号特例。

## 验收证据

- 精确第三响应离线重放：1 个候选、1 个控制点。
- 发布门禁：通过，0 个问题。
- 临床拒绝门禁：通过，0 个问题。
- 聚焦回归：`283 passed`。
- 完整协议模块：`1106 passed, 58 warnings`，175.08 秒。
- 受控执行审计：通过；独立执行审阅结论为“接受，含 Codex 补充修正”。

持久证据：

- `artifacts/phase5-slice61at-d001-ecg-screening-repair-20260829/post-fix-offline-gate.json`
- `reviews/codex_execution_phase5-slice61au-typographic-source-restoration_review.md`
- `metrics/phase5-slice61au-typographic-source-restoration_execution_metrics.md`

## 保持不变的边界

- 不修改原始方案或历史真实响应。
- 不增加模型调用次数和修订预算。
- 不放宽来源闭包、父子逻辑、阶段绑定或临床拒绝门禁。
- 不进入受试者、OCR、Patient Profile、浏览器或视觉切片。
- D001 II 仍为 1848 个结构单元、1245 个语义目标、131 个包；剩余 128 包，`claims_complete=false`。

## 下一安全动作

从冻结 131 包计划中选择一个此前未处理、与心电图动作语义不同的最小异质来源组。先冻结来源闭包、流程目录覆盖和父级临床检查清单，再决定是否执行一次有界语义重放；不得直接扩大为全量运行。
