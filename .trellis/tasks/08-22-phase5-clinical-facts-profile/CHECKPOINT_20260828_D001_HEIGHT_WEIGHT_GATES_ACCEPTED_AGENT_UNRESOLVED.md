# Phase 5.8d D001 身高/体重门禁接受，Agent 终稿未收敛

## 当前状态

- Phase 5 仍在进行，`claims_complete=false`。
- D001 Ⅱ期活动冻结计划为 `papl-40b1237a22e538a278b4fd5e`：覆盖单元 1,848、语义目标 1,245、包 131；剩余 128 包。
- 本轮接受第 68 包身高/体重来源闭包及共享失效关闭门禁，不接受或发布该代表组的 Agent 临床解构结果。
- 未运行受试者、OCR、病例审核、浏览器、视觉或独立测试者。

## 来源闭包

- owned：`body.p774`–`body.p783`；其中 `p774`、`p776`、`p781` 是结构标题。
- attached：`body.p320`、`body.p845`、`body.t5.r9` 及对应冻结流程目标。
- 流程目录只覆盖测量执行、记录和筛选期原始记录；章节额外规定准备、设备、体位、操作步骤、单位和精度。
- 身高方法仅适用于筛选期身高测量；体重方法适用于筛选及基线体重测量。模糊短语“其余访视点”不能取代冻结目标集合。

## v4–v9 根因链

1. v4：两个测量项目被压成一个候选，身高方法随体重错误扩展到基线。
2. v5：目标范围门禁生效，但逐项动作遗漏逐轮返回，修订预算被局部问题耗尽。
3. v6：聚合动作遗漏后已拆分身高和体重，旧文本访视解析仍误拒 `p775` 的基线体重目标。
4. v7：冻结目标优先后形成临床语义正确的两个候选，但将 `p774/p776/p781` 标题提升为流程必做项，父级拒绝。
5. v8：结构标题门禁生效，模型把“kg 保留一位小数”压成一般核对义务，被记录精度门禁拒绝。
6. v9：增加一次有界修订及精度提示后，仍在第七次调用以“cm 取整数”压缩结束；无水合终稿，不再追加调用。

这些中间工件均为系统根因证据，不能删除或被技术绿色结果覆盖。

## 系统修改

- 批次可冻结每个来源单元拥有的流程目标集合；最终候选和流程处置必须保持同一目标范围，不能漏掉或扩展审核阶段。
- 批次可显式冻结结构标题；标题不得提升为正式入排规则、流程必做项或补充控制。
- 流程动作遗漏改为全批聚合反馈，避免一次只修一段来源。
- 冻结流程目标优先于对“其余访视点”等自然语言的近似解析。
- 带单位或记录精度的原文必须使用独立 `must_record` 义务；一般“完成/核对”不能吞并 cm 整数或 kg 一位小数。
- 上述结构期望只进入确定性门禁，不进入 Agent 首轮提示；未写入 D001 专有条款。

## 验证与治理

- 聚焦相关回归：`149 passed, 5 warnings`。
- 完整方案层回归：`919 passed, 58 warnings`。
- `compileall`、`git diff --check` 已通过；任务记录完成后再次执行。
- `audit-execution --task-type finite_code_task` 通过：三路均为 `cursor-cli/auto`，无警告、无错误、无 fallback；该 packet 按设计由 Codex 直接验收，无独立 manager。
- 执行提示、worker 输出和日志已归档到 `archives/execution/phase5-slice60zc-height-weight-source-closure-20260828/`。
- 未发现独立的身高/体重 prepare 缓存目录；v4–v9 真实回放产物全部保留。

## 下一安全动作

先检查代表组 runner 的修订状态保留方式，设计最小、通用、可测试的确定性保留/补丁机制：每轮仅替换被问题精确指向的候选或处置，保留上一轮已经通过的结构化部分，并在最终发布门禁重新做全批闭包。必须先用合成反例和保存响应离线验证，不能直接增加模型重试次数，也不能原样重跑第 68 包或扩大到剩余 128 包。

