# Phase 5 智谱 Coding Plan 独立视觉模型真实连通验收检查点

时间：2026-08-31

## 本轮结论

- 独立视觉模型已按本机 OMP `zhipu-coding-plan/glm-5.3-flash` 的接入合同迁移：`https://open.bigmodel.cn/api/coding/paas/v4`、OpenAI chat-completions 图像消息、`low|high|max` 推理强度及 ZAI 思考内容字段。
- 应用默认使用 `zhipu-coding-plan`，保留旧 `bigmodel` 名称兼容；默认推理强度仍按用户指定为 `high`，没有擅自改成 OMP 元数据默认的 `max`。
- 运行时适配器只读取应用自己的环境变量，不查询 OMP 凭据库。主启动目录 `.env` 已从现有 OMP Coding Plan 凭据安全同步，密钥未进入代码、测试输出或任务记录。
- 真实合成图片请求成功：严格 live 测试 `1 passed in 4.73s`。公共端点导致的 HTTP 429 / provider code 1113 根因已消除。
- 独立视觉、OCR、方案语义、方案控制和事实规范化仍是隔离路由；视觉输出不是入排结论，必须保留来源标识和页码锚点。

## 验证证据

- `py_compile`：`app/config.py`、`app/llm/independent_vlm.py`、`tests/v2/llm/test_independent_vlm.py` 通过。
- 合同、错误分类、来源保真和路由隔离：`44 passed, 1 skipped`。
- 显式真实连通：`INDEPENDENT_VLM_LIVE=1` 下 `1 passed in 4.73s`；远端鉴权、余额、限流和一般错误现在会使 live 验收失败，不再被当作成功。
- 无 D001、SAR、药物、疾病、评分或具体时间点硬编码；未恢复 D001 回放。

## 执行事故与约束

- 两个并发写入角色曾分别看到 `app/llm/independent_vlm.py` 被截断并重建。父级已冻结写入、重开完整文件、编译并重跑回归。
- 后续涉及同一共享文件时只能设置一个写入者；其余执行者和测试者必须只读复核，避免用回归测试掩盖并发覆盖风险。

## 保留状态

- D001 任务 `3259ab5f070447c3938ff2de5f45c9cd` 继续保持第 19 个检查点后的无损暂停；不得因新 VLM 可用而直接续跑旧串行任务。
- 独立 VLM 目前完成的是共享适配与真实连通，不代表已经接入所有业务 harness，更不代表已完成真实方案整份解构。
- 应用本地 `.env` 被 Git 忽略，密钥不得写入任何提交或报告。

## 下一安全动作

1. 完成当前执行包的正式治理审计并归档过程文件。
2. 选择一个不含临床敏感内容的原始 DOCX/PDF 页面，验证“原始页视觉核验 + 来源红框定位”最小业务调用；不得整份方案逐页重复 OCR。
3. 业务调用通过后，再将 VLM 显式接入方案结构异常页、表格/扫描页和 OCR 低置信度页；文本原生页继续使用结构化文本，不浪费视觉调用。
4. D001 恢复前先完成新任务的有限并发、时延与费用合同；旧任务仍按原检查点保留。
