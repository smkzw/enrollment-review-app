# Phase 5 选择性页面视觉核验验收检查点

日期：2026-08-31  
状态：已接受共享规划与真实单页调用；服务接线和持久化待后续切片

## 已完成

- 独立视觉模型沿用 OMP `zhipu-coding-plan/glm-5.3-flash` 的 Coding Plan 接入合同，应用默认推理强度保持 `high`。
- 原生 DOCX/PDF 文字是主路径；原生文字充分时跳过视觉模型，不对整份方案逐页重复识别。
- 只有扫描或纯图、复杂表格/版面、原生结构异常、OCR 低置信或阻断性质量风险页进入视觉核验。
- 页面规划只读取路线、结构和质量元数据，不读取研究号、疾病、药物、量表、访视或条款关键词，不包含 D001/SAR 特异规则。
- 核验结果保持 `source_ref` 与页码，只作为观察；不覆盖原 OCR，不伪造坐标，不输出入排结论。
- 鉴权、余额、限流、远端、来源保真、计划缺图均失败关闭，不静默回退到 OCR 或其他语义模型。
- VLM 传输延迟到真实调用时加载，确定性方案回放冷启动不再加载 OpenAI/httpx。

## 验证

- 聚焦合同：`56 passed, 1 skipped`。
- 证据、独立 VLM 与相关服务扩展回归：`326 passed, 2 skipped`。
- 严格真实视觉请求：`1 passed in 5.50s`。
- 全库：`3425 passed, 4 skipped`；冷启动回归已修复。
- `py_compile` 和本切片 `git diff --check` 通过。

## 未完成与限制

- 尚未把选择性视觉核验接入持久化证据处理服务；当前为共享规划器和显式调用适配，不得宣称所有 harness 已启用。
- 尚未建立观察侧车的不可变存储、重试状态和用户界面来源下钻。
- 全库仍有一个既存 D001 只读检查点失败：当前 `phase5/control-agent-prompt/v1.5` 生成哈希与 2026-08-30 冻结值不一致。旧检查点未修改；后续应单独做提示合同版本化和可重放迁移，不得更新旧哈希冒充确定性。
- D001 控制任务 `3259ab5f070447c3938ff2de5f45c9cd` 仍保持 19 个完成检查点后的无损暂停，本切片没有恢复或调用它。

## 下一安全动作

在新的受控切片中，为选择性核验观察建立独立持久化合同和证据服务后处理钩子：只消费已落盘的页产物/OCR 质量，不改变 OCR 缓存、租约、原文或方案语义流程；先用非敏感单页完成持久化往返，再进入真实项目只读测试。
