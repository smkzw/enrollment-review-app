# Phase 5.8d 候选义务动作覆盖证明已验收

## 当前结论

共享确定性门控现已闭合两条路径：流程必做处置必须由目录动作覆盖；其他控制候选必须由当前来源单元的义务陈述逐项保留目录未覆盖动作。保存的 60zv 错误结果仍被拒绝，没有调用模型或发布控制点。

## 已完成

- 新增 `CANDIDATE_OBLIGATION_ACTION_UNCOVERED`，阻断只改候选处置但不保留动作。
- 新增 `OBLIGATION_ACTION_SOURCE_ONLY`，阻断来源摘录冒充义务陈述。
- 流程目录已覆盖动作可合法减扣，候选只需表达剩余动作。
- 候选跨多个来源单元时，动作证明按当前单元自己的来源定位计算，不能跨单元借用。
- 允许义务陈述逐字采用真实方案原文，只要动作明确且来源闭包正确。
- 保存的 60zv 响应拒绝码保持 `PROCEDURE_ACTION_UNCOVERED`。
- 聚焦回归 `132 passed`；完整方案模块 `950 passed, 58 warnings`；编译和差异检查通过。
- Hermes 执行审计与评审门通过，过程文件已归档，同会话临时复核提示已清理。

## 未完成

- 第 70–71 包真实模型解构仍未接受或发布。
- D001 II 仍为 `1848/1245/131`，剩余 128 包，`claims_complete=false`。
- 受试者、OCR、病例审核、浏览器、视觉和独立测试者尚未启动。

## 下一安全动作

不要立即重跑第 70 包。按稳定 `source_ref` 从活动 131 包计划选择一个新的极小异质代表组，优先验证当前动作模式表尚未覆盖的动作类别及跨章节关系。先建立盲态来源闭包和父级医学检查清单，再决定是否需要扩展项目无关动作类型。

恢复时读取本文件及 `artifacts/phase5-slice60zw-candidate-action-semantic-coverage-20260828/assessment.md`。
