# Phase 5 单页选择性视觉真实端到端验收

## 已验收边界

- 从全新隔离 SQLite 与证据目录播种一页内容中立的风险页，形成冻结证据处理修订。
- 同一修订两次入队只形成一个选择性视觉任务；真实执行器调用与本机 OMP `zhipu-token-plan/glm-5.3-flash` 等价的 Coding Plan 端点 `https://open.bigmodel.cn/api/coding/paas/v4`。
- 成功结果以一条不可变视觉观察落库，正文非空，来源标识、页码、页图哈希、OCR 页与原文哈希、规划版本、模型身份及提示词/观察身份哈希均闭包一致。
- 远程调用前后 OCR 原文、OCR 哈希和页图字节保持不变；用户修订级查询投影与实际落库计数一致。
- 视觉调用仍只适用于扫描/纯图、复杂表格或版面、结构异常和 OCR 低置信页；不对整份方案逐页重复识别，不覆盖 OCR，不生成入排结论。

## 父级修正与风险裁决

- 真实测试的合成页图已从 Pillow 传递依赖改为 Python 标准库生成的有效 PNG，不新增依赖。
- 独立审阅者提出“桌面启动后无法获得独立 VLM 配置”；Codex 追踪实际调用者后确认 `app.config` 在导入时直接读取应用根目录 `.env`。运行状态文件只冻结启动器验证过的 MTPLX 路由，无需复制智谱密钥，因此未扩大配置面。
- 测试中的禁止词只用于反向证明合成输出没有污染旧项目，未进入任何生产规则或提示词。

## 验证证据

- 真实单页闭环：`2 passed in 21.31s`；模型 `glm-5.3-flash`，成功观察正文 859 字符，用量信息非空。
- 相邻选择性视觉回归：`109 passed, 2 skipped in 18.58s`；skip 为未显式启用的真实计费测试。
- 启动脚本 zsh 语法、目标 Python 编译与目标 `git diff --check` 通过。
- `review-gate` 和 `audit-execution` 均为 `ok=true`；三个执行者均使用 `zcode/GLM-5.3-Flash:max` 主路由完成，未 fallback；runner 过程文件已归档。

## 仍未完成

- Phase 5 整体仍为 `claims_complete=false`；合成单页验收不替代代表受试者逐事件医学核对。
- D001 控制任务 `3259ab5f070447c3938ff2de5f45c9cd` 仍在 19 个已完成发现批次后暂停；不恢复第 20 批，不将新视觉配置混入旧结果。

## 下一安全动作

回到 Phase 5.8 的真实代表病例验收前，先对“真实风险页视觉观察如何进入事实规范化的侯选输入，且不覆盖 OCR”建立最小合同与失败用例。观察只能作为带来源的补充候选或 OCR 风险提示，不得直接发布临床事实或入排结论。
