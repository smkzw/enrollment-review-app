# Phase 5 GLM 分段恢复机制验收、局部修订质量拒绝

日期：2026-09-01  
状态：`claims_complete=false`  
任务状态：`in_progress`

## 本轮边界

- D001 控制任务继续停在第 19 个已完成包之后，未运行第 20 包。
- 失败 SAR 作业、旧整父探针、冻结 EX-06 输入和既有调用记录均保持不可变。
- 本轮只验证通用的分段能力、内容寻址检查点、失败段恢复、合并候选和完整门禁；未写入 SAR、D001、疾病、药物、量表、条款编号或时间点特异规则。

## 已接受的工程结果

- 从执行者误截断事故中恢复 `protocol_deconstructor.py`、`deepseek_protocol_transport.py` 和 `protocol_deconstruction_executor.py`，只接受当前源码及确定性回归结果，不接受执行者自报。
- 分段能力已与 compact wire 能力解耦；支持分段的模型可显式进入父规则分段流程。
- 成功段按方案文件哈希、提取快照、提示模板、模型传输身份、父批次/分段身份和输出合同形成内容寻址检查点，并使用原子写入。
- 超时与 Schema 错误分开记录；失败段仅允许同模型、同分段有限恢复，不重放已验证成功段。
- 从检查点合并出的候选首次局部修订会新建真实远端会话，避免尝试续接并不存在的合成缓存会话；后续修订才续接该真实会话。
- 系统生成的分段合并候选身份保持权威。模型局部修订不得更换普通候选身份；仅对系统分段合并候选做编排层身份归一化，临床内容仍由完整门禁决定。
- 新增 14 项独立对抗测试，覆盖能力声明、缓存身份、超时恢复、正文来源闭包、合成会话恢复及候选身份边界。

## 真实 GLM 结果

- 冻结 EX-06 父规则仍按通用结构形成 4 段 `[3,3,3,2]`，并发上限为 2。
- 首次恢复运行中，3 段达到生产 Schema；缺失首段因正文来源引用不足失败关闭。
- 补强通用 Schema 修订提示后，仅重跑缺失首段：复用 3 个有效检查点，只发起 1 次 GLM 调用，耗时 `511.756` 秒，最终取得 4 个有效检查点。
- 直接确定性合并候选未通过完整门禁，共 6 个问题：分支未绑定来源、指标来源项类型错误、时间限定语丢失及共享时间限定语未绑定。
- 局部修订共发起 2 次同模型调用：首次新会话 `393.447` 秒，随后同会话 Schema 修订 `36.749` 秒。
- GLM 返回了自造候选身份；编排层身份归一化后离线重放其临床内容，完整门禁由 6 个问题扩大为 26 个问题，包括虚构筛选日期锚点、无效来源身份、来源覆盖丢失及阶段/日期混淆。
- 因此局部修订临床内容已拒绝，不发布、不回写检查点，也不再追加 GLM 调用。API 成功、JSON 可解析和 Schema 合法均不等于方案语义正确。

## 路由与隔离原则

- 复杂方案语义任务保持 `GLM-5.3-Flash high -> MTPLX medium -> DeepSeek V4 Flash high` 的完整作业级回退。
- 单规则、短提示和小工作量任务保持 `MTPLX medium -> DeepSeek V4 Flash high`。
- provider 切换必须新建完整候选、新会话和独立缓存；禁止把 GLM 分段、MTPLX 修订或 DeepSeek 片段拼接为一个正式候选。

## 验证证据

- 分段恢复对抗测试：`14 passed`。
- 相关运行器与会话恢复测试：`70 passed`。
- 路由与传输聚焦测试：`46 passed`。
- 最终协议测试：`1305 passed, 1 failed`；唯一失败为既存 D001 v1.5 只读检查点提示词哈希漂移，旧检查点未修改。
- `compileall`、`git diff --check` 通过。
- 受控执行审计 `phase5-segment-capability-checkpoint-recovery-20260901` 通过，`errors=[]`、`warnings=[]`。

## 证据位置

- 验收目录：`artifacts/phase5-acceptance/20260901/glm-segment-checkpoint-recovery-ex06-20260901/`
- 调用台账：`calls/`
- 分段检查点：`checkpoints/`
- 离线局部修订重放：`replayed-targeted-repair-result.json`

## 下一安全动作

在同一冻结 EX-06 输入上新建一个完全隔离的 MTPLX 整候选尝试，运行相同完整门禁并记录墙钟时间、调用次数和问题分类；不得修补或拼接 GLM 候选。若 MTPLX 整候选失败，再以相同原则新建 DeepSeek V4 Flash high 整候选。当前不得恢复 D001 第 20 包，也不得把本轮 GLM 局部修订标记为成功。
