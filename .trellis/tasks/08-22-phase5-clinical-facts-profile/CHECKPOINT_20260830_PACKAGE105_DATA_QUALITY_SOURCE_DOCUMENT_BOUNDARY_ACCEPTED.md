# Phase 5.8d 第105包数据质量与源数据/源文件边界验收检查点

## 当前结论

- 已接受 D001 II 不可变候选计划 `papl-e17d498106b6f71f440ff2be` 第105包 `pap-b8d5cdfbc6ac4c373c6576b3` 的模型外来源、数据治理和证据溯源边界。
- 第105包仅拥有 `body.p1238-p1247` 十个连续来源；不附加来源。冻结计划中的37个语境单元全部保持只读且不进入本包提示。
- 十个来源全部禁止生成受试者入排候选；要求候选、正式规则、要求执行项、受试者动作、访视、流程绑定和预筛选来源均为空；`claims_complete=false`。

## 父级临床与产品判断

- p1240/p1242 的申办方数据管理、EDC、质疑处理、培训、eCRF填写/审查/签名/日期属于研究执行与记录质量治理，不是受试者资格条件。
- p1244 定义源文件、源数据及核证副本等可接受来源词汇；它不构成每名受试者必须提供所有示例资料的清单，也不自动降低核证副本的证据等级。
- p1245 定义研究启动期监查计划中的源文件分类，以及直接录入eCRF且此前无记录的数据仍可视为源数据。它不生成 `collect_data` 受试者动作，也不能把“此前没有记录”倒置为资料缺失。
- p1246/p1247 的准确、清晰、同期、原始、可归因、电子记录/签名、核证副本、审核跟踪、保存和直接访问要求作用于来源可信度、记录质量和溯源；这些问题可由后续证据质量状态表达，但不得直接改写为入排失败。
- p1247 对第9.4节的引用不转移第106包 `p1248-p1250` 的记录保存期限和销毁规则所有权。

## 系统级纠错

- 攻击审阅复现共享回放解析器可将相邻包、冻结语境或覆盖清单来源伪装为当前包自有来源。新配置现显式声明 `expected_package_ordinal/id`，共享解析器对声明该合同的配置强制要求 `frozen_plan_owned` 且包身份一致。
- 旧版只读代表组曾有意把冻结语境提升为回放拥有来源。为避免无声改写历史证据，未声明包身份的旧配置保持兼容；第105包及后续新包使用严格身份合同。
- 自动动作检测器曾从 p1245 的“采集的数据”误推断 `collect_data`。共享回放现在对已明确处置为 `non_enrollment_execution` 或 `supporting_or_supplement` 的来源抑制自动受试者动作，同时仍允许真正控制项显式声明动作。
- 新回归覆盖第104/106包来源、无主语境、覆盖清单旁路，以及第105包完整37项只读语境隔离。

## 验证证据

- 模型外准备：`10 owned / 0 attached / 10 total`；十条 QC 均无候选，批次无规则、程序、动作、访视或阶段绑定。
- 专项最终测试 `16 passed, 5 warnings`；第103-105包和共享回放 `74 passed, 5 warnings`；此前扩展第102-105包和共享回放 `82 passed, 5 warnings`；`git diff --check` 通过。警告为既有 SWIG/PyMuPDF 弃用警告。
- 配置 SHA-256 `7f26269f907f04517140e329689a91fd6d26277ff8c9e27974251cc24541722b`；父级清单 `f5cd3ff8b01cbfeee8831e1677dd93deae789ef098295007e9b875c7068c6768`；专项测试 `b95df3e7266b44679385ba40c822745cb2cc30ad697c60515aaf915f66c1aa93`；共享回放帮助程序 `16e90e3fcecff0716f48528383d64289b278adbfaac738340e28ddbe274157b2`。
- 四路受控执行均使用主路由 `openai-codex/gpt-5.6-luna:max`，无 fallback。独立复核建议的 `collect_data` 文字残留已修正并复跑；review-gate 与正式执行审计均 `ok=true`。

## 正式状态与下一步

- 正式候选仍为1848个结构单元、1240个语义目标、131个包；按当前逐包闭合记录剩余102包。
- 未调用临床语义模型、未发布控制点、未进入受试者、OCR、Patient Profile、浏览器或视觉阶段。
- 下一安全动作：第106包。先核对 `body.p1248-p1250` 的记录保存责任、期限、销毁许可、适用主体及与 p1247 的跨包引用，再决定模型外处置；不得把记录保存治理升格为单例入排条件。
