# Phase 5 选择性页面视觉核验用户闭环验收

**日期：** 2026-09-01  
**分支：** `codex/phase5-clinical-facts-profile`  
**状态：** 工程合同已接受；Phase 5 未完成，`claims_complete=false`

## 本次完成

- 以证据处理修订为用户入口，提供页面视觉核验的中文状态、页面计数、失败范围、恢复说明、手动重试和取消。
- 修复完整处理修订与基础处理修订的任务身份错链：两者查询同一底层任务，不重复建任务，同时保留用户当前查看的修订号。
- 前端只显示医学监查人员可理解的业务字段，不显示模型、任务载荷、租约、日志或内部错误分类。
- 独立视觉模型仍按本机 OMP `zhipu-token-plan/glm-5.3-flash` 的 Coding Plan 接入合同使用，且仅在扫描、复杂表格/版面、结构异常或 OCR 低置信页面调用；不对整份方案逐页重复识别。

## 根因与边界

- 原错误是任务在基础修订上入队，界面却用完整修订查询；不是模型未返回或前端显示问题。
- 共享代码中没有 D001/SAR、疾病、药物、评分、访视日期或项目编号特异分支。测试中的合成资料只是界面夹具。
- 本切片没有修改 OCR 原文、临床事实、方案控制点、评估结论或冻结证据。

## 验收证据

- 后端聚焦：`41 passed, 5 warnings`。
- 后端 services + API：`610 passed, 5 warnings in 290.95s`。
- 前端聚焦：`13 passed`；全量：`525 passed`；生产构建通过。
- Playwright 1080P/2K/4K 新面板及相邻证据回归：`9 passed`，页面与面板均无横向溢出。
- Codex 直接复核 1080P 和 4K 截图；`review-gate` 与 `audit-execution` 通过。
- 执行包已经 workflow guard 归档；审阅、指标、截图和本检查点保留。

## 未完成与恢复

- 未完成真实风险页从冻结修订入队、远端视觉核验、观察落盘到界面展示的单页端到端验收。
- 未进入方案正式控制点目录、受试者审核或临床 Patient Profile 验收。
- D001 任务 `3259ab5f070447c3938ff2de5f45c9cd` 继续停在 19 个已完成发现批次的无损检查点，不恢复第 20 批，不把新模型配置混入旧结果。

**下一安全动作：** 使用新建隔离快照和一页非特异风险资料，运行冻结修订 → 选择性视觉任务 → 不可变观察 → 中文界面的端到端验收。任何预期外空观察、来源错链或状态不一致都必须查明根因，不得以“流程已通”代替验收。
