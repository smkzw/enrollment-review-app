# Phase 5 超大父规则语义分段离线验收检查点

日期：2026-09-01

## 已完成

- 建立项目无关的冻结来源块分段、有限并发、同父规则确定性合并和整父回退合同。
- 只切分结构上可独立闭合的多来源块父规则；单长段、开放连接词、例外或括号范围不明、跨段续接均保持整父规则。
- 分段使用同一模型身份的独立会话，默认并发 2、硬上限 3；任何分段失败只回退一次整父规则，禁止部分发布。
- 复杂与短任务模型路由保持既定分级策略，未因本切片启动 MTPLX 或恢复旧任务。
- 定向 `165 passed`；扩展协议 `1284 passed`，另有 1 个既存 D001 历史提示词哈希漂移；执行审计通过。

## 边界

- 本检查点只接受确定性合同和离线回归，不接受真实性能声明。
- D001 控制任务仍停在第 19 包后；SAR 失败任务保持不可变；未创建新项目、未发布目录。
- 新实现不得按研究编号、疾病、药物、量表、条款号或时间点决定是否分段。
- `claims_complete=false`。

## 下一安全动作

基于既有只读冻结复杂父规则输入运行独立真实 GLM 分段探针。若通用规划器判定不可安全分段，应接受整父回退事实并继续优化提示/输出预算，不得硬编码切点。
