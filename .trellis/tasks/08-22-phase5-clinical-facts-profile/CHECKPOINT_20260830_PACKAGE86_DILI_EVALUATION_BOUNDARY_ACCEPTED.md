# Phase 5.8d 第 86 包 DILI 评估、调查及潜在/确诊边界验收检查点

## 当前结论

- 已接受 D001 II 冻结计划第 86 包 `body.p1067-p1073` 的模型外来源闭包。
- 7 个拥有来源中 `p1067` 是标题性介绍段，其余 6 个语义单元是治疗后 DILI 评估、调查、潜在病例、确诊病例与报告规范。
- 本包不形成预筛、筛选或基线入排候选，不写入正式控制矩阵，`claims_complete=false`。

## 父级修订

- `p1072` 的“上述两个标准”在当前正文边界内没有唯一指向两个具体 `source_ref`。`p1060-p1066` 全量只读进入，仅用于保留两类基线人群及分支结构，不代表任意两条子规则已经被正式选中。
- 禁止把 `p1060-p1066` 扁平化为五个分支任选一项，也禁止依靠模型常识补出唯一绑定；无法唯一闭合时必须标记“需要核对”。
- `p1070` 的“尽快（如，获知异常结果后 48 小时内）”只限定返院评估，不是申办者报告期限；评估应同时包括实验室检查、详细病史和身体评估。
- `p1072` 保持完整合取链：尚未发现其他原因，且重复检查证实符合前接标准，才视为潜在 DILI、报告为 SAE，并在获知事件后不超过 24 小时报告申办者和提交说明。
- `p1071` 保留重复检测、“应包括”的实验室项目和“需要时/必要时”的条件性项目三层结构；GGT 仅为调查项目，不进入 ALT/AST 与 ALP 比值。
- `p1073` 只在收到所有合理调查结果并排除其他病因后由潜在转为确诊，不得用确诊条件阻断早期评估或潜在病例处置。

## 验证证据

- 配置 SHA-256：`4e05a909cdbbb0130bbaa9528ec41799ff800390b8cb32373d4f220474d5c41c`
- 模型外准备：`7 owned / 12 attached / 19 total`；提示 `36744` 字符，SHA-256 `94636c6432233091a5071f46d37a9504bffb79de3d43c9ad9e1aa09af8c22867`。
- 第 86 包专项：`42 passed, 5 warnings`；Phase 闭包：`491 passed, 5 warnings`。
- 方案与 Agent：`1198 passed, 58 warnings`；治理工具：`30 passed`。
- `jq empty`、准备产物 JSON 检查、`git diff --check` 与 `audit-execution` 通过；执行审计无警告或错误。

## 正式状态与边界

- D001 II 仍为 `1848` 个结构单元、`1245` 个语义目标、`131` 个包。
- 第 86 包闭包后剩余 `121` 包，`claims_complete=false`。
- 未调用临床语义模型、未发布控制点、未进入受试者、OCR、Patient Profile、浏览器或视觉阶段。

## 下一安全动作

按冻结计划建立第 87 包 `body.p1074-p1083` 的最小模型外来源闭包，聚焦死亡、药物过量与给药错误的记录边界；不得回吸第 86 包 DILI 评估与报告逻辑，不直接扩到后续因果关系、SAE 通用报告流程、受试者或视觉阶段。
