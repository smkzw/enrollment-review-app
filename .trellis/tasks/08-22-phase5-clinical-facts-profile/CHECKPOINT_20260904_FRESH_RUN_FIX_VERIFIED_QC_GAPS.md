# Phase 5 新鲜不可变运行完成与 QC 缺口记录（2026-09-04 CST 凌晨，执行包 phase5-closeout-r3-20260903 / worker_03）

## 状态不变量

- Phase 5 仍未收口；`claims_complete` 继续保持 `false`。本运行未通过完整原始证据临床 QC（心电图项与部分检验数值缺失），因此不得收口。
- 未复用任何失败/取消作业；本次运行为从正式 API 新建的完整不可变运行。

## 运行身份与机械结果（证据：主库 jobs/job_steps/job_events）

- 作业 `4800566f938f4da7b3d0264eadafe3fe`，运行 `f6fe423a90a54e0390686557438c5da1`，由 `POST /api/v2/subjects/0675cabc…/review-episodes/59b98368…/fact-normalization-jobs` 创建（HTTP 201，`created=true`）。
- 冻结权威：筛选期活动修订 `complete-529c2876…`，输入范围 `833ee61c…`（与此前全部近期运行逐字节一致，可横向对照）。
- 提示版本 `evidence-normalizer/prompt/08a9f311…`（模板 SHA `c2e9ad9b…`，即修复后 v5 合同；运行时自动登记）。模型配置冻结为 zhipu-coding-plan / glm-5.3-flash / low / 16384 / 每次 1 页 / 厂商默认采样。
- 2026-09-03 16:02:51 UTC 创建，16:57:09 UTC 终态：**25/25 步全部 attempt 1 一次通过，事件日志零失败记录**。
- 运行层终态 `partial`：含义是"存在被确定性门禁拒绝的候选"（此前被接受为基线的 `0c477b67…` 运行同为 partial），不代表页失败。
- 发布结果：151 事实、12 事件、3 用药暴露、130 资料期望、Profile 修订 3（succeeded，52 项待复核）；未解决项 77 条；被拒候选 219 个（3548 项门禁结果中 423 项拒绝）。

## 修复验证（work item 1/2 的生产级验收证据）

- 历史失败步骤 `normalize_018_f0be4759…`（筛选病历第 4 页）本次 attempt 1 直接通过；该页家族史候选落为 `asserted_object=遗传病病史`（逐字）、polarity=negated，与上次成功运行落库形态一致。
- 全运行无任何"逐字断言对象"或 schema 修复耗尽失败。worker_02 的机械逐字还原与提示 v5 在全量生产运行中成立。
- 剩余采样风险（worker_02 预判）未在本运行出现。

## 临床 QC 结果（逐项对照检查点清单；只读核查，明细见证据文件）

**通过项**

- 知情同意：事实 `知情同意与依从性评估=affirmed` 显式绑定 IN-07；事件"知情同意书签署"日精度 2025-08-08（原文"2025年8月8日"）。此前"知情同意误报未观察"问题在本运行不存在。
- 用药暴露逐字性：3 条暴露（吸入布地奈德混悬液/吸入、阿昔洛韦乳膏/适量、糠酸莫米松）药名均逐字出现在所引定位原文；类别/适应证为空（推测内容按合同清空）。
- 讨论性材料：邮件页 4 个候选因来源语义越界被正确拒绝并落未解决项（"邮件审核讨论不构成暴露"成立）；"口服孟"残缺药名等正确落入 `medication_name_incomplete` 未解决项。
- 事件日期：12 个事件均带精度与原文（如带状疱疹 2025-06-24/2025-07-25）；资料期望 130 与基线一致；Profile 高亮按"不兼容来源并列展示、不择优"的合同呈现。

**需 Codex 裁决项（flag-1）**

- 暴露 `exposure:34b141a3…`（糠酸莫米松）引自 `邮件.pdf` 第 5 页：原文"目前患者在上午下午均有使用"是邮件内转述的实际用药陈述。合同规定"邮件讨论不构成暴露"，但该句为实际使用状态陈述而非建议。是否接受邮件内"实际使用直述"作为暴露来源，属临床/产品裁决，worker 不代决。

**QC 未通过项（gap-1、gap-2）**

- gap-1（主要）：113 个事实候选因 `canonical_value` 为数字字符串 + 携带单位被"非数值事实不应携带单位"门禁拒绝。同型输入下相对上一运行（190 事实）出现回归：**整个心电图参数面板（心率 85bpm、PR 160ms、QRS 81ms、P 103ms、RV5+SVL 1.297mV 等 8 项中 5 项为纯数字字符串）、胆红素三项（总 30.5/直 8.7/间 21.8 μmol/L，属已发布的"肝功能异常"定性事实的数值支撑）、血肌酐 79、尿素 4.28、电解质、白蛋白、ALP、部分 IgE 全部未发布**。该类拒绝当前不生成未解决项，损失对复核者不可见。
- gap-2（次要）：61 个候选因来源语义标签与 Phase 4 文档类不匹配被拒，含本次运行丢失、上一次运行已发布的第 4 页家族史事实（模型把筛选病历上的家族史标为"既往原始资料"，正确应为"筛选病历转述"）。属采样依赖的模型标签漂移，fail-closed 本身正确。
- 证据基线：`心电图/胆红素/肌酐` 已发布数 d71c1c28=5 → 本运行=1；家族史 d71c1c28 已发布 → 本运行被拒。

## 最小修复建议（通用合同，无项目特异硬编码；供 Codex 裁决）

1. gap-1：在 `_normalize_evidence_json` 增加确定性数值字符串还原（与机械逐字还原同一先例类）：`canonical_value` 为字符串且存在单位且可解析为有限数 → 转 float；不可解析（">100.00"、"QT 346 ms；QTc 413 ms"）保持 fail-closed。
2. gap-2：提示合同向模型公布允许的来源语义标签词表（中文标签在 `_SOURCE_LABEL_TO_STRENGTH` 已有映射）；和/或为被门禁拒绝的候选生成未解决项，使损失对复核者可见（证据保存合同要求）。
3. flag-1 裁决后如需收紧，建议按"邮件文档内实际使用直述"建立通用来源规则，而非删除该暴露的临时硬编码。

## 专用服务与恢复入口

- 本会话启动时发现 8910 存留进程（21:54 CST 启动）为修复前旧代码（注册的仍是 v4 模板 `7e238da1…`，未见 v5 `08a9f311…`），已按 `ENROLLMENT_ENV_FILE` 显式 env 合同重启，并验证新进程登记 `08a9f311…`（模板 `c2e9ad9b…`）后才创建作业。
- 无损暂停补充记录：运行终态后专用 `8910` 已正常关闭，端口无监听；SQLite WAL/SHM 已随正常关闭落盘移除，`PRAGMA quick_check=ok`（2026-09-04 01:20 CST 主库 77,918,208 字节）。`8900` 上另一 uvicorn 属外部项目（见 2026-09-03 R3 对账），未触碰。
- 恢复后的下一安全动作：先落 gap-1/gap-2 的通用修复与聚焦回归，再从正式 API 新建下一个不可变运行重做 QC；本运行结果保持不激活复核，`claims_complete=false`。
- QC 证据汇总：`artifacts/phase5-acceptance/20260903/fresh-run-f6fe423a-qc/qc-summary.json`（SHA-256 `ecabd73127d644c9d9478fa100556c9af5dcc35e515398ea92367c574cc3535d`）。
