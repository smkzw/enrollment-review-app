# Phase 5.8d 妊娠/FSH V9 多访视闭包已实现，真实重放未接受

日期：2026-08-28  
分支：`codex/phase5-clinical-facts-profile`  
工作树：`.worktrees/phase5-clinical-facts-profile`

## 本轮目标

修复 V8 暴露的通用合同缺陷：一个未经用户预处理的方案原文单元可能同时由多个访视级必做项目完整覆盖，不能只保存一个流程目标。

## 已完成

- 当前 Agent wire、领域草稿和水合处置新增排序且不重复的 `linked_procedure_catalog_item_ids`；旧单值字段仅保留为历史只读兼容，新输出不得混用。
- Agent 输入校验、领域水合和发布边界均逐项核对多目标身份；流程必做处置没有目标、含未知目标、乱序、重复或新旧字段并用均被拒绝。
- 提示合同明确：跨访视原文必须列出全部共同覆盖目标，不得只保留最早或最后访视；治疗后或可选复查不得因此混入入排控制。
- 真实 D001 DOCX 产品目录确认妊娠/FSH 为三个独立访视级目标：筛选、基线、D1 首次给药前。
- V8 单候选本身已修正为筛选节点的初潮前豁免，但整批因旧单值目标丢失而父级拒绝。
- V9 真实 MTPLX medium 输出已能使用三目标字段并拆分筛选与首次给药前判定，但 11 次尝试后未形成可水合终稿，父级继续拒绝。
- V9 新根因不是模型容量，而是验收反馈串行暴露：发布问题和临床遗漏分轮出现，修复器将首个仍含潜在缺陷的水合结果冻结。代表组验收器已改为同轮汇总两类问题，未追加模型重跑。

## 验证

- 聚焦合同、发布、传输和规划回归：`143 passed in 0.88s`。
- 完整方案层回归：`872 passed, 58 warnings in 129.21s`。
- 合并验收反馈后的代表组及相关聚焦回归：`165 passed in 1.56s`。
- V9 真实调用：`11` 次，`732.838904s`，`hydrated=false`，`gate_accepted=false`，正确停在“需要核对”。
- V8 父级评估：`artifacts/phase5-slice59z-d001-pregnancy-fsh-parent-assessment-20260828/assessment-summary.json`。
- V9 父级评估：`artifacts/phase5-slice60b-d001-pregnancy-fsh-parent-assessment-20260828/assessment-summary.json`。

## 边界

- 本轮只接受多访视目标合同、发布校验和同轮问题汇总机制；不接受妊娠/FSH 代表组临床解构。
- D001 II 仍为 `1848/1245/131`，剩余 `128` 包，`claims_complete=false`。
- 未运行受试者、OCR、病例审核、浏览器、视觉或独立测试者。

## 下一安全动作

先以确定性夹具验证“同轮同时出现筛选/首次给药阶段错误与初潮前候选遗漏”时一次性给出完整修复范围；如再做真实重放，只允许一次 V10，并必须在父级逐单元核对后决定是否接受。不得直接扩大到剩余包。
