# 2026-09-01 异质规则 GLM 候选与局部例外安全门禁检查点

## 当前结论

- 使用项目无关结构特征、最低挑战度和最大最小 Jaccard 距离，从 SAR EX-06 与 D001 EX-18 之外选择只读 D001 EX-24；并列时按官方编号确定性决胜。
- 共享选择器不读取项目、疾病、药物、评分、条款具体值或时间点；真实方案只作为只读泛化样本，不形成共享临床硬编码。
- GLM-5.3-Flash high 整候选完成：两次同会话调用，模型耗时 393.22 秒，无分段、无跨提供方回退，冻结来源哈希前后一致。
- 当前门禁重放为可发布、零问题；Codex 临床复核另行追踪组件级例外执行语义，发现开放列举中的局部例外存在误豁免并存触发条件的通用风险。
- 共享门禁与提示框架新增项目无关保护：局部括号例外作为组件级例外时，必须明确表示该例外是唯一相关情况，否则拒绝或拆分为不会互相豁免的组件。
- EX-24 候选明确将阑尾切除术表示为唯一相关情况，保留开放列举、专业判断、筛选期资料要求和逐字来源，因此接受为异质语义泛化证据。

## 验证

- 结构选择器：`5 passed`。
- 门禁与适配器聚焦回归：`170 passed`。
- 协议与 Agent 扩展：`1377 passed, 1 failed`；唯一失败仍为不可变 D001 历史提示词 SHA，未修改旧检查点掩盖漂移。
- 可重复复核：`verification-results.json` 为 `accepted_as_heterogeneous_semantic_evidence=true`，当前门禁版本 `protocol-deconstruction-gate/2026-09-01.2`。
- 治理执行审计和 review gate 通过；三名执行者均保留合法路由、终态报告和 stdout 日志。

## 边界

- 接受范围仅为跨方案、异质结构的独立模型语义与证据闭包；不授权写入正式规则目录，不代表整个方案解构完成。
- D001 第 20 包未恢复；旧 SAR 失败任务未恢复；没有跨模型拼接候选。
- Phase 5 继续 `claims_complete=false`。

## 下一安全动作

回到 Phase 5 临床事实与 Patient Profile 主线，使用已准备的隔离代表受试者运行真实 Evidence Normalizer，先逐事件核对事实、日期、否认/沉默、冲突和来源定位；在正式全方案规则目录完成前，不把该事实运行伪装成完整入排判决。
