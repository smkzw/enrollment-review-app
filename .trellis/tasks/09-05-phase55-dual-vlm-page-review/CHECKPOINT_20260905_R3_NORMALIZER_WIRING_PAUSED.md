# Phase 5.5 R3 规范化接线无损暂停点

## 当前：扩大回归发现测试环境污染

- 上轮为覆盖共存修复；本轮 direct 运行 tests/v2 --maxfail=5，1365 passed / 5 failed / 2 skipped，205 秒，在约 36% 停止，不能称全量通过。
- 五项配置探针均因隐式加载工作树产品 .env 导致默认值/凭据复用断言污染；失败断言意外输出真实凭据，记录中不复制。产品凭据未修改；应视此次测试输出为含敏感信息，避免导出原始工具输出。
- 显式空 env 复验相关四模块 70 passed / 1 skipped；随后新增 tests/conftest.py 默认选择 tests/fixtures/isolated.env（显式用户 ENROLLMENT_ENV_FILE 仍优先），防止普通离线测试再次读取产品文件。加入 API 验证后 72 passed / 1 skipped，5 个 SWIG 警告。
- 全量尚待在隔离环境重跑，失败页受控重读/前端/临床 QC 仍未完成，claims_complete=false。下一动作隔离全量回归，并继续既定重读与产品接线。

## 当前：旧覆盖不阻塞当前覆盖

- 上轮为版本冻结进展；本轮 direct 增加新旧覆盖共存 HTTP 反例，先复现正常规范化被多候选拒绝。
- 选择器现先读取校验记录并按当前完整执行版本过滤，再判断唯一性。旧记录仍在库，不参与当前版本选择；同版本多个候选仍拒绝，不自动择优。API 回归 12 项通过（5 项 SWIG 警告），无模型/临床调用。
- 同版本受控重读的候选替代关系仍待实现，不能把版本过滤当成完整重读能力。下一动作设计并实现明确前驱关系的失败页重读，再连接前端。claims_complete=false。

## 当前：整份覆盖冻结执行版本

- 上轮为版本核验进展；本轮 direct 给 SubjectPageCoverage 增加 execution_versions，空字段不序列化以保留旧记录读取。新执行器把当前任务/提示/对账/页合同版本放入覆盖内容身份；任务合同 v3，旧任务不得混续。
- 正式选择在遍历页前比较版本，舍弃页同样不能绕过整份版本检查；旧覆盖仍存储/可读，不原地迁移。27 项 API/执行器/存储回归通过；再加 HTTP 执行版本漂移反例，2 项通过（5 个 SWIG 警告）。
- 仍未解决多覆盖候选及受控重读的最终选择，未做真实历史库迁移验证或临床验收。下一动作处理新旧覆盖共存，避免旧版本阻塞新结果；随后前端接线。claims_complete=false。

## 当前：采信页的提示与对账版本检查

- 上轮为 API 回归进展；本轮 direct 对正式 coverage 选择增加已采信页的页合同/主读与手写提示检查，并用当前代码按 ClausePack determination_mode 重算对账内容身份，不改写旧记录、不调用模型。
- 当前 API fixture 改为用真实当前对账函数生成（旧底层兼容 fixture 不变）；13 项回归通过。另加提示升级后原覆盖在 HTTP 被 409 拒绝的反例，HTTP 2 项通过，5 项 SWIG 警告。
- 仍有边界未闭合：无价值舍弃页没有 reconciliation_id，当前选择尚未追溯其作业版本；多覆盖选择/受控重试仍需设计实现。不能宣称所有历史覆盖版本问题已解决。来源语义/位置检查、前端/真实 QC 均待办，claims_complete=false。
- 下一动作：覆盖关联作业的冻结版本与所有页处置（含舍弃页）一起验证，再处理多候选与失败页受控重读，避免永久拒绝代替可用功能。

## 当前：旧规范化 API 回归前置条件升级

- 上轮为规范化接线进展；本轮 direct 扩大旧 API 测试，发现 6 failed / 4 passed，原因均为旧 fixture 仅有 OCR 资料而新正式入口要求 R3 覆盖。
- 测试增加真实持久化的当前规则包覆盖，不关闭 require_page_review；共享 fixture 支持显式 ClausePack，原接线测试保留默认历史 fixture。随后 API/接线 13 passed，5 个 SWIG 警告。
- 已验证幂等、登记配置绑定、错误与取消续跑仍通过；未做模型或临床验证。当前 fixture 仍使用历史 prompt_version，恰说明历史算法/提示过滤尚未实现，不能据此宣称旧覆盖可直接用于新流程。
- 下一动作：给正式覆盖选择绑定当前提示/页合同/对账算法并补历史拒绝及失败覆盖反例；多候选恢复策略、前端未完成。claims_complete=false。

## 当前：正式规范化入口选择 R3 覆盖

- 上轮为取消验证进展；本轮 direct 新增独立 coverage_selection。正式应用 require_page_review=True，按当前受试者/节点/快照/完整处理修订/ClausePack 精确选择，缺失/失败/多候选拒绝，不静默回 OCR。底层历史命令服务保留显式兼容默认。
- HTTP 测试证明页作业提交后尚未运行时规范化 409；注册执行器完成覆盖后规范化 201，任务 payload 冻结实际 coverage_id。模型为 fixture，非真实推理。启动/HTTP 13 项通过；新增完整接线断言后 HTTP 2 项通过（5 个 SWIG 警告）。
- 未闭合：多候选需要受控选择规则而非永久拒绝；历史算法/提示版本覆盖的正式选择仍需加强，失败覆盖 HTTP 反例及既有 normalization API 全回归需补；前端入口尚未接上。此次只证明提交链，不是事实/事件/Profile 完成，claims_complete=false。
- 下一动作优先扩大旧 API 回归并修订测试前置条件，验证失败/旧覆盖不可用；再接前端查询/操作。保留原始资料及所有未提交改动。

## 当前：取消边界与受控续跑实证

- 上轮为并发实现进展；本轮 direct 沿现有 Runner 增加真实隔离作业故障注入，无模型/生产数据调用。
- 模型 fixture 返回前写 request_cancel，Runner 在安全边界取消，保留至少一个已完成读检查点，不形成整份 coverage；resume_cancelled 后检查点原样且对应读道不重复调用，最终形成覆盖。执行器 11 项通过（5 个 SWIG 警告）。
- 不改写共享 Runner 的既有“当前步骤结束后取消”语义；此测试不证明在长网络/429 等待期间立即中止，也不覆盖进程死亡/失效租约，仍待后续验证。
- 下一动作继续正式规范化 coverage 选择与前端接线；运行预检失败分类和长等待取消仍须完成。claims_complete=false。

## 当前：应用内跨作业模型准入

- 上轮为正式入口进展；本轮 direct 新增独立 PageReviewAdmission，由唯一应用 runtime 持有，各作业线程/事件循环共享 provider+model 的名额，备用端点不另扩名额。预检配置控制 2–3 主读并发；手写沿路由限制。
- 非阻塞 acquire 配合可取消等待，finally 释放，不使用后台阻塞获取造成取消后名额遗留。4 项并发/异常/取消等待/HTTP 回归通过，5 个 SWIG 警告。
- 范围是单应用进程内共享（符合当前单用户本地运行）；不是跨进程分布式限流。async 取消测试不证明用户取消作业已接到模型等待，仍需任务取消/租约恢复实证。无真实模型/病例运行，claims_complete=false。
- 下一动作：将作业取消/恢复与运行预检错误分类闭合，接覆盖查询/规范化正式选择；不要把准入测试当端到端临床通过。

## 当前：正式页判读提交入口已注册

- 上轮为验证进展；本轮 direct 接既有应用生命周期，新增独立 page_review_runtime 与 API 模块，不依赖外部 harness。首次使用显式配置预检，成功后在进程内固定路由；预检失败不创建作业。
- 新 POST /api/v2/subjects/{subject_id}/review-episodes/{review_episode_id}/page-review-jobs 仅收空对象，服务端派生资料/规则，幂等复用。应用注册 r3_page_review 执行器。
- 23 项 HTTP/启动注册/执行器测试通过，5 项 SWIG 警告；真实 TestClient 创建作业后由注册执行器+JobRunner+隔离 SQLite 落覆盖，模型使用可控响应，不是临床模型实跑。无生产数据/端口修改。
- 未完成：全局模型并发、启动恢复预检失败分类、取消恢复、覆盖查询与前端接线、正式 Normalizer 自动选择 coverage、来源页一致性、临床 QC/金标。不要把此入口视为 D 收口，claims_complete=false。
- 下一动作：统一跨作业并发与恢复，再接 coverage 到规范化命令，补正式 HTTP 下游拒绝失败覆盖反例。

## 当前：失败分类回归与执行版本冻结

- 上轮为修复进展；本轮 direct 扩展同一确定性边界。任务合同升 v2，拒绝混续旧语义任务。
- 端点/length/content_filter/schema 四类故障覆盖通过，截断只在 harness 内加倍一次，不再由任务层重复；主读 A 均只调用一次。首次新增 schema fixture 错传 dict 给 text 字段，依据堆栈修正为 JSON 字符串，不为测试错误修改产品解析器。
- 46 项执行器/规划器/harness/存储/规范化接线回归通过，5 个 SWIG 弃用警告。Plan C 和目标正文的旧“执行器未测”描述已更新；未调用 goal 工具。
- 尚未接正式 API，无真实模型/临床调用。下一动作：正式 API 注册、跨批并发与取消/恢复验证；来源页一致性仍待补。claims_complete=false。

## 当前：失败读道保留页覆盖

- 上轮仅核文档，无实现进展；本轮 direct 处理确定性失败恢复，共享任务事务由当前线程单独修改，无外部会商或模型调用。
- PageReviewJobExecutor 对端点失败按任务预算重试，耗尽后保存 lane_failure；非端点模型错误不额外重复 harness 内部重试。对账步骤形成 failed_pending_reread 页处置，保留成功主读，不虚构失败读记录。
- 隔离 SQLite/真实 JobRunner 测试 7 passed（5 项 SWIG 弃用警告）。首次修复后测试仍失败，核查数据库显示 failed_retryable 尚未重排；测试加入既有 requeue_due_retries 后证明 A 一次、B 两次并有覆盖。运行步骤结束不代表临床采信。
- 尚未完成正式 API、全局并发、取消/重启验收及真实临床 QC；claims_complete=false。下一动作扩大失败分类及下游拒收回归，完善版本冻结后接正式任务链。未改真实病例或删除历史文件。

## 当前：Normalizer 候选绑定已采信观察

- direct 续跑；新增独立 page_review_sources 投影，模型输入给出稳定 source_observation_ref，事实 draft 与持久候选保存 source_observation_refs。R3 输出校验拒绝缺失/未知/待核对引用；历史非 R3 输入不强加新引用，空列表不改变候选序列化。
- 来源校验与 R3 接线 6 项通过，扩大事实规范化持久化 34 项通过，diff check 通过；未做真实模型/临床验收。
- 此项验证“引用属于已采信集合”，不证明候选语义与引用一致；事件/用药通过事实关联仍须整体审计，不能冒称所有来源语义错误已解决。下一动作：补来源页/候选定位一致性和完整集成反例，然后继续 C/D。
- claims_complete=false，所有未完成阶段保持原状。

## 当前：待核对原文传递

- direct 续跑；新增独立投影 page_review_pending.py，Normalizer 输入保留未采信观察的原文/context、读道记录、资料版本、页码及页 ID，不再只给冲突键。字段 use=unresolved_only 与提示明确限制用途；已采信观察不重复标为待核对。
- 7 项投影/存储/Normalizer 接线测试通过（可控测试，非真实模型）；未修改实际数据库或凭据。此改动不能替代下游候选来源验证，后续仍须测试模型试图将待核对项变为事实时会被确定性层拒绝。
- B 的真实位置/时间关联校准尚未验收；C/D、临床 QC 与金标仍未完成，claims_complete=false。

## 当前：缺关联不再进入采信结果

- direct 续跑 B；缺 context 两主读同文反例先失败后修复。事实/手写缺对象关联进入待核对，不凭字符串相同采信，原读记录不删除。对账算法 r3-v4，任务冻结版本随之生效。
- 正常 fixture 显式给出可见目标，执行器测试增加 accepted_fact_keys 非空断言，防止“页已处理但零采信”假通过。相关 76 passed / 5 Swig 警告；随后仅调整待核对说明文字，diff check 通过。
- 不等于实体关联完全验收：缺时间/位置但已给 target 的歧义、位置同义、金标标定仍待实证；不得关闭这些待办。下一动作扩展原件关联/缺失观察的下游可追踪性，随后 C/D 正式任务链。
- 无真实模型/病例运行；阶段完整性仍 false。

## 当前：观察对象/日期/位置关联字段已接入

- 本轮 direct 实施 B 关联合同：ObservationContext（target_text/time_text/location_text/polarity）进入事实和手写原始观察，代码计算上下文哈希进入键，日期格式统一。不引入方案特异规则或模型猜测目标。
- 产品 harness 校验并规范 context 后算键，主提示 v4 / 手写 v3 要求所指对象与日期/行列原文，不能确定时允许 null；Normalizer 现有完整观察序列化保留字段。
- 新测试证明：不同日期相同值不误配、双方均读出两日期则可分别采信、不同手写目标/极性不同键、等效日期一致。74 项回归通过；提示更新后相关 41 项通过，diff check 通过。v1/v2 字节兼容通过。
- 尚未闭合：当前 context 可空，缺 context 的旧形态仍沿旧对账路径；必须下一步让新提示产物缺关联时明确待核对而非静默采信，并保留旧读取。位置同义与真实金标校准尚未完成。不能把此初始字段能力称完整实体关联验收。
- 无临床页、无模型调用、无数据库迁移；C/D、金标和临床 QC 待办未变，claims_complete=false。

## 当前：来源一致性及重复位置误采信修复

- 本轮 direct 继续 B，4 反例先复现：同页 ID 掩盖不同图像哈希/文档版本/页码，重复事实键被直接合并采信。
- 对账现检查完整来源身份；普通事实相同键须两个读道各一条。手写同一读道同键多次出现不计该读道票，另两个无重复读道仍可形成两源，不删除原始观察。
- 对账算法 r3-v3 自动进入任务冻结检查；页合同仍 v3。聚焦 40 项通过，随后新增手写重复/第三源测试，领域 24 项通过。
- 这是防误采信边界，不是完整实体关联；没有显式时间/对象/位置关联时仍可能出现单条跨目标歧义。下一步必须落实观察关联字段及下游投影，不能以越来越严格的拒绝代替最终功能。失败覆盖/API 仍未接通，claims_complete=false。

## 当前：单位与日历语法纠偏，页合同 v3

- 本轮 direct 继续 B；10 个反例复现失败：普通词尾误剥单位（含 NCS→nc/s）、无效日历日期未拒绝、比较值未规范、x10^9/L 拆分错误。
- 新 scalar 只在完整数值表达式上剥离单位，保留比较运算符；日/月先校验有效日历；标准化 x/× 计数单位。复杂未识别表达式保留原文，不猜数值。
- 原 Decimal 逻辑冻结为 normalize_scalar_v2，v1/v2 只读校验仍调用冻结逻辑；新写 page-review/v3。新页 ID/任务已有共享合同版本校验，未迁移旧记录。
- 79 项聚焦通过，含 v1/v2 字节往返、v3 语法、对账、harness、存储、执行器及 Normalizer 接线；5 个 Swig 弃用警告，diff check 通过。无真实病例或外部模型调用。
- 下一动作仍为 B 实体/时间/手写目标关联，不能仅凭相同文字采信不同对象。C/D 失败覆盖和正式 API 未完成；claims_complete=false。

## 当前：页合同 v2 与旧精度记录读取

- 上轮为有证据的进展；本轮 direct 处理同一页合同的恢复兼容，不调用模型、不修改实际病例数据库。
- 先复现旧舍入键解码失败；增加 v1 事实只读兼容校验（已知浮点 g 与早期 Decimal），保留原值/原键及 JSON 字节/哈希，仍拒绝任意键与额外判定字段。
- 新页记录默认 page-review/v2，仍严格使用精确归一化；v1 记录不得直接进入新对账。页读 ID 及任务冻结/执行校验含页合同版本，避免升级混续。未对旧记录原地迁移。
- 验证 68 passed / 5 Swig 弃用警告；包含旧记录编码往返、旧对象不得绕过新校验、执行器版本漂移、对账、存储和 Normalizer 接线；diff check 通过。不是全库历史迁移验收。
- 下一动作：B 实体关联与单位/日期语法继续检查；未来调整 scalar 算法须保持 v1 兼容实现语义并增加版本，不能再次共享可变规范化规则而不测历史。C 失败覆盖/API/真实金标仍待完成，claims_complete=false。

## 对账身份续跑补充

- 上轮为实际进展；本轮继续直接检查版本与持久化共享合同，无模型调用或临床结论。
- 复现对账 ID 碰撞：同样的页读记录在不同 determination_modes / 可选失败回执下产生不同正文却得到相同 ID。新 ID 覆盖完整确定性结果、判定方式及算法版本 r3-v2；原存储不迁移、不改写。
- 任务同时冻结 reconciliation_version，执行器在读取原件前检查；防止升级对账算法后混续旧任务。相关聚焦 38 passed / 5 原生库弃用警告，diff check 通过。
- 历史数值键解码兼容仍未修复，实体关联仍未完成；当前修复只解决新对账内容寻址及任务算法版本。下一动作继续 B 的旧键版本读取与新数据严格校验，不能声称整个历史兼容已完成。

## 当前续跑：新目标已由用户提供，草稿验证进展

- 本轮按最新目标继续，选择 direct：先验证共享任务/对账合同，不涉及尚需独立会商的临床或主观验收；未派发外部 harness，未改产品路由。
- 重新读取全局 AGENTS；53 项聚焦回归通过，覆盖最近对账提取及页合同/存储/规划器，不覆盖全部执行器行为。
- 新故障注入先复现 3 失败：任务合同/main 提示/手写提示版本漂移仍进入图像读取。执行器现于读取原件和调用模型前拒绝，错误 `R3_EXECUTION_VERSION_CHANGED`，不可自动重试；共享任务合同常量避免重复字符串。
- 修复后 56 项聚焦通过；另加真实 JobRunner + 隔离 SQLite + ArtifactStore 正常执行测试，执行器 4 项通过，证明 A/B 独立检查点、读记录及有序页覆盖持久落库。模型响应为可控 fixture，非真实推理/临床验收。
- 执行器不再是完全未测草稿，但失败页覆盖、全局并发、租约重启、正式 API 仍未完成；历史规范化兼容与实体关联仍待实施。无运行中的测试会话，无真实病例作业启动，claims_complete=false。
- 下一动作：按 B 检查历史键版本与实体关联，随后完成 C 失败覆盖/恢复测试；不得从正常路径通过推断 C/D 已完成。

## 最新恢复入口：先完成文档，未设置新 goal

2026-09-05 用户最新要求：完整形成新设计、计划和 goal prompt 前不要设置 goal。已停止继续扩展实现，优先完成以下三份文档；没有创建、修改或关闭系统 goal。以下旧段落中的“待凭据”“没有真实调用”和旧提示版本均为较早状态，以本节和真实探针补充为准。

- 新设计：`docs/REARCHITECTURE_R3_ENGINEERING_DESIGN_20260905.md`。
- 新计划：`plans/REARCHITECTURE_RECOVERY_IMPLEMENTATION_PLAN_20260905.md`。
- 新目标正文：本任务 `GOAL_PROMPT_20260905_R3_TAKEOVER.md`，仅文档，不代表启动。
- main-A/main-B 显式产品凭据已验证，OMP 仅用户授权的一次性密钥迁移，无运行依赖。合成图像 probe-02 成功，非病例验收；提示 main v3 / handwriting v2。
- 代码草稿：`page_review_job_service.py` 3 项测试通过；`page_review_job_executor.py` 和最近提取的 `reconcile_completed_reads` 尚未回归验证，未接正式 API，未创建真实临床作业。失败步骤如何仍形成运行覆盖、全局并发、历史规范化键兼容、实体关联仍待完成。
- Phase 5/5.5 均未临床收口，claims_complete=false。完整工程审查仍有待深入组件，不冒称已全仓验收。
- 下一安全动作：依新计划先核草稿测试和 B 合同/兼容，不直接新开长时间临床批次；保留全部用户未提交修改、历史失败证据与恢复记录。

## 2026-09-05 接管审查（覆盖下方历史暂停状态）

- 用户已要求恢复：完整工程审查、必要测试、修订计划并连续实施；本轮不使用外部执行/会商机制。选择 direct，由当前 Codex 独立审查与集成，避免共享工作树并发写入。
- 当前目标：辨别离线模块完成与产品端到端完成，优先消除错误采信与正式入口断链；不修改原始临床资料、不删除既有未提交工作、不将测试通过等同临床验收。
- 已核对：R3 合同/投影/持久化/规范化接线聚焦回归 29 passed（本轮），历史 3714 passed 尚未复跑。
- 新发现：正式规范化 API 未传递 R3 coverage；批量页判读等待全部完成才统一持久化；可选手写预检错误会阻断主读；数值规范化存在精度碰撞；条款信号可能把同读道重复项当成双主读一致。均需修正或明确记录，凭据不是唯一阻塞项。
- 下一动作：先补可复现反例并修复确定性对账缺陷，继续检查任务执行、API、真实前端数据链，形成审查报告和修订计划。Phase 5/5.5 仍未通过临床验收，claims_complete=false。
- 本轮进展：上述重复信号、精度/零值、可选手写预检问题已修复；模型不再自行计算规范化键，提示版本 v2，未知条款拒绝；运行依赖补 multipart。Plan 与 PROJECT_CONTEXT 已更新，详细报告 `ENGINEERING_REVIEW_20260905_TAKEOVER.md`。
- 本轮验证：前端 526 + 正式构建；扩大后端 480/1 skipped（最终提示修订前）；最终 R3 聚焦 51 passed；diff check 通过。没有模型调用、没有临床验收。
- 当前下一安全动作：补 R3 逐页持久执行/API 整链，先读现有 jobstore/runner 和源适配合同；不要仅添加客户端 coverage 参数或给旧路径换名称。并行等待用户提供产品自有 main-B env 路径，严禁读取其他 harness 凭据。
- 用户随后明确授权从 OMP 读取密钥：已仅将 cms-smk 的具名环境凭据一次性迁移至本工作树 `.env`（0600、gitignore）；主检出产品 env 配置一起复制，未修改 OMP，未运行其 harness。纠正了初次把变量名当密钥导致的 401；Ruby 旧版本 filter_map 不支持的一次本地失败也已修正。最终产品自有 preflight `/models` 确认 main-A glm-5.3-flash 与 main-B MiniMax-M3，手写端点未进入可用集合。尚未发送推理请求或临床页面。之前“待 main-B 凭据”不再阻塞。

- 时间：2026-09-05 CST
- 工作树：`.worktrees/phase5-clinical-facts-profile`
- 分支：`codex/phase5-clinical-facts-profile`
- HEAD：`411832d`
- Trellis：`09-05-phase55-dual-vlm-page-review`
- 完整交接：`HANDOFF_20260905_GPT6_PHASE55_R3.md`

## 本轮完成

2026-09-05 接管续跑补充：真实合成图像已用产品自有 harness 双读。probe-01 MiniMax 布尔字段与事实矛盾被拒；补清提示后 probe-02 双读数值/日期一致，47.327s/39.432s。产物在 `artifacts/phase55-takeover/20260905/synthetic-probe-{01,02}`，非临床验收。标题同义产生假冲突待处理。当前 main 提示 v3、手写 v2。下一动作仍是 R3 持久任务及正式入口整链，复用 JobRunner 的 StepSpec.depends_on 与 PreparedStepResult 原子提交，不重建队列。

1. 将已持久化的 SubjectPageCoverage、PageReconciliation 和 PageReviewRecord 以内容寻址方式接入 Evidence Normalizer。
2. R3 输入只接受已采信且作用域闭合的页面；失败待复读页、记录缺失、条款包或页清单漂移均失败关闭。
3. R3 规范化输入使用采信页级事实、证据信号、手写结果和冲突；OCR 仅为侧车转录与定位锚点。
4. 保留历史 Phase 5 作业的旧输入结构，不改变既有作业哈希或回放语义。
5. 修正成功复读覆盖与失败覆盖的不可变身份碰撞，并收紧“声称有入排价值但无结构化内容”的页级记录门禁。

## 验证

- 新增/更新聚焦测试最高 `190 passed`。
- 完整回归：`3714 passed, 3 skipped, 139 warnings, 2 subtests passed`，耗时 846.28 秒，无失败。
- 3 个跳过项均为显式真实探针开关或缺少探针产物，不是回归失败。
- 未调用任何真实模型，未调用 Hermes、OMP、ZCode 或其他外部 harness。

## 现场

- `8001`、`8002`、`8910` 均关闭。
- `8900` 在监听，但属于 `/Users/smkzw/Vibe-Research/backend`，与本工作树无关，不得触碰。
- 工作树 `.env` 不存在；主检出目录 `.env` 只确认 GLM 侧变量存在，未发现 `CMS_SMK_API_KEY` 或 `PAGE_REVIEW_MAIN_B_API_KEY`。
- Phase 5 主库 `PRAGMA quick_check=ok`，当前 SHA-256 为 `3ba0604e18e86abe390f942d46d0c47f52bd6b17c2c1fa2e154b8c57398f8903`；没有活动租约。
- 最新事实规范化运行 `b7e401c614ab4c5089a740b2fba9357b` / 作业 `844036a5dccd40e8ae0c7892c4966ec3` 为失败终态，禁止复用或发布。
- Phase 5 与 Phase 5.5 均未完成，`claims_complete=false`。

## 下一安全动作

1. 从完整 handoff 重读权威边界，先审查本轮 R3 接线差异，不改历史运行。
2. 建立本 worktree 的显式 env 文件并补齐 main-B 凭据；只运行产品内三端点身份预检和最小合成页调用。
3. 预检通过后在隔离金标集复跑产品 harness，先报告阈值数据，不先进入 31001。
4. 达标并经用户批准后，再新建不可变 31001 R3 页级处理修订和规范化作业，完成事实、事件、用药暴露、Profile 与原始证据临床 QC。
5. 最后执行独立浏览器 UAT 和阶段回写；此前不得设置 `claims_complete=true` 或进入 Phase 6。
