# R3 工程接管新 Goal Prompt

日期：2026-09-05。状态：待后续使用的完整目标文本；本文件不创建、不修改、不激活系统 goal。

## 可直接使用的目标正文

接管入排审核系统 R3，在保留原件、历史与既有用户改动的前提下，完成全链工程审查、已证实缺陷修复、正式产品接线、临床证据 QC、金标评测及后续阶段实施。当前 Phase 5/5.5 未通过临床验收，不因已有模块或测试数量宣称完成。

工作目录必须核对为 `/Users/smkzw/Documents/康哲项目资料/AI/入排/enrollment-review-app/.worktrees/phase5-clinical-facts-profile`；主检出与活动树不得混淆。当前任务为 `.trellis/tasks/09-05-phase55-dual-vlm-page-review`，历史父任务为 `08-22-phase5-clinical-facts-profile`。恢复时核对最新 HEAD、用户消息、Trellis 检查点和现场，不依赖本文中的旧运行状态。

先阅读适用 AGENTS、最新用户指令、`CHECKPOINT_20260905_R3_NORMALIZER_WIRING_PAUSED.md`、`ENGINEERING_REVIEW_20260905_TAKEOVER.md`，再读：

1. `docs/REARCHITECTURE_FINAL_DESIGN_20260812.md`，尤其 R3 §3.3、§4.2、§5.4、§7.0/7.2/7.4、§11、§12。
2. `docs/REARCHITECTURE_R3_ENGINEERING_DESIGN_20260905.md`。
3. `plans/REARCHITECTURE_IMPLEMENTATION_PLAN_20260812.md` 与 `plans/REARCHITECTURE_RECOVERY_IMPLEMENTATION_PLAN_20260905.md`。
4. `docs/PROJECT_CONTEXT.md`、相关层 Trellis spec 与必要的历史 handoff。历史材料不能覆盖后来的明确用户变更。

当前 Codex 负责执行和集成，按2026-09-06用户更新遵循全局执行/会商机制，采用120分钟长等待，不人为创建暂停点。产品自身只能用项目 harness 直接调用配置 API，不依赖本机 Hermes、OMP、Pi 或 ZCode harness。2026-09-09新增授权允许在有实证收益时将成熟轻量开源harness作为产品自带依赖（如Pi核心包），这不等于调用用户个人CLI/配置；须验证许可、双读独立性、版本回执、失败恢复和真实质量，不能因换框架省略临床合同。用户授权的一次性 OMP 密钥迁移已完成，不能延伸成运行时配置发现、外部识别或密钥输出。

产品服务原生中文资深临床试验医学监查人员。让用户通过上传、选择审核节点、查看明确障碍/待办、点击原件、追踪研究者回应完成工作，不要求其理解模型、终端、日志或后端术语。不安排真人监查员试用作为推进前提；本轮没有外部角色测试就如实说明，以可控测试、产品模型实跑、Codex 原件与浏览器检查留证，不伪称独立专业签收。

当前构建仅使用 GLM-5.3-Flash:low（zhipu-coding-plan）与 Gemini-3.7-Flash:high（google-antigravity OAuth）两个独立主读，普通事实与手写内容均由两者各自读取，不设第三读。Gemini high 的实际请求标识为 gemini-3.7-flash-high，响应标识须核验。产品自有 HTTP 与 OAuth 刷新，不运行时读取个人 harness；主读采样不设置 temperature。系统构建完成前不开展详细模型横评，但必须完成真实产品流程及临床证据质量验证。

页 Schema 不输出最终判定，只允许规定的证据关系和原文。不同主读才构成双源；手写同一目标任二不同读道一致采信。代码先规范化再对账，保留原文与算法版本；不丢零、不用浮点近似碰撞、不合并不同时点/不同目标。deterministic 条款的方向由代码算，研究者判断不得由异常箭头代替。无方案阈值异常缺书面判断，输出 professional_judgment 缺口及研究者行动，不作满足/未触发。

方案语义以完整方案结构及引用上下文理解审核相关控制点，不重复全文 OCR 或硬编码研究名称、阈值、药物、疾病规则。当前方案上传格式按修订后的 DOCX 范围；受试者 PDF/图像保留原件。不能用 Codex 自己抽好的临床结论填进产品冒充独立模型输出。时间锚点先遵方案明确要求，未明确时遵用户筛选/基线各自回溯要求，晚期资料不改写早期结果。

按新计划 A–G 顺序执行：完成审查和文档 → 对账/实体关联/历史兼容 → 持久读道任务与失败覆盖 → 正式 API/前端任务整链 → 31001 Phase 5 原件 QC → Phase 5.5 同金标产品评测和大屏验收 → 按原计划 Phase 6–9 审核、行动、报告与交付。Phase 5 所需的 R3 基础修复可先做，但未验收不得跳到 Phase 6。每阶段开始和结束均复盘、更新实际状态与下一步。

当前页任务规划器、执行器正常与失败处置、对账提取、存储及 Normalizer 接线已有聚焦测试；任务合同 v2。正式 API、全局并发及取消/租约重启仍待验证，不能将局部通过当成整链收口，不直接开全病例作业。主读凭据已完成最小真实合成图像验证，不能将其当成真实临床页质量或全流程性能证据。

复用既有 runner/repository；每个成功读道独立持久，取消/重启不丢结果，失效租约不提交。失败页亦有运行覆盖，不因依赖失败而消失；采信覆盖另行校验。length 翻倍一次，429 等待不计内容重试，过滤/端点失败明确影响，不静默丢页。模型并发跨批次统一 2–3，OCR 共享 8 路；当前不启动本地主读服务。不要触碰其他项目端口服务。

正式 API 从服务端冻结身份派生 coverage 并接 Normalizer，不靠手工注入 ID，不静默回旧 OCR 语义链。事实、事件、用药、Profile、规则结果、行动及报告同源并有原文版本、页码、摘录与位置。原件右侧连续滚动、有坐标才画红框；不把 OCR 重排文本当原件。

验证层次分开：可控单测/故障注入、正式 HTTP 隔离流程、产品真实模型、原件临床 QC、1920/2560/3840 桌面浏览器与安装恢复。金标双模型并集事实召回 ≥0.95、负判定静默漏判 =0；另报采信召回、假冲突、手写/关联错误和性能。新增阈值与性能预算报用户批准，不自行定案。测试通过不等于临床授权。

使用 Ponytail 最小完整改动，新增双读代码独立模块，不扩巨型文件，不大范围重构。保留未提交代码与源资料，按清单清可再生缓存，不清会话数据库。工程审查尚未覆盖的组件继续补查，不能把局部检查称完整工程验收。

停止/交接规则：用户要求无损暂停时停止新增工作，记录已完成、未完成、运行状态、任务身份、产物、最后验证及下一安全动作；不删除恢复证据。需要真实缺失凭据、临床裁决或新增阈值批准时提出具体问题，继续不依赖该项的工作。任何未通过事项明确保留，不伪造通过、硬置 claims_complete=true 或把 goal 预算耗尽当完成。

最终交付必须包含修订后的设计/Plan、源码变更和测试证据、逐阶段完成矩阵、金标报告、原件/浏览器验证、实际运行入口与完整交接；正式临床/项目批准仍归用户。

## 使用限制

这不是自动启动指令。用户当前要求“完全形成新设计文档、计划规划、新 goal prompt 前不要设置 goal”；三文档准备与目标工具操作必须分开。若平台已有活动 goal，不能通过虚报完成/阻塞来替换它；应保留真实状态，并明确目标正文的更新能力限制。
