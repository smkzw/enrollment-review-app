# 当前低强度主读的原件抽查

这是构建者对已保存输出的质量核查，不是产品事实输入、金标补写或临床签收。

## 绑定范围

- 隔离作业：`3bd4e1a089064b2a8a3c1a295036ef1d`，运行尚未结束。
- 页图 SHA256：`c129ecdbea99e45572d7ecd447f70926b19ea842308ed6027c6e91d5e8de5df0`。
- main-A：`page-review:94f70b4b1c905138158395c7246890cb`。
- main-B：`page-review:fbba8e8cbd02c1c28081f96ff092874b`。
- 已直接查看原页图像，并逐项查看上述两条记录，不只查看 OCR。

## 初步发现

1. 两路均保留了本页三项用药及剂量，没有因为 GLM 改 low 而在本页漏掉这些关键内容；这不能外推全例召回。GLM 12项、MiniMax 18项只是不同拆分粒度，不能据此判定后者召回高50%。
2. GLM把出生日期及多项用药的 field_name 写成“现病史”；MiniMax使用“出生日期”“用药史”。两者字段身份及日期关联差异仍影响对账，不能简单把同值认作一致。不能加 SAR 或具体药物硬编码修正。
3. 对既往用药，GLM在其中一项保留 time_text=null，MiniMax关联到既往就诊日期。本页叙述没有提供全部实际给药起止边界；后续 Normalizer 不得把就诊日期直接变成实际暴露起止日期。保留原文与不确定性，不能由构建者填写起止日。
4. GLM科室读为“耳鼻喉科门诊”，MiniMax读为“耳鼻咽喉科门诊”；页首原件显示前者。即使医学意思相近，后者不属于忠实摘录，不能靠同义规范掩盖读字变化。
5. 两路都在日期字段自身重复填入 time_text，未完全遵从提示中的日期字段自身关联规则；这是语义合同遵从问题，不应宣称全部提示均被执行。

## 本轮另一页失败定位

read:2:main-B 保存响应 SHA256 `d0b941160b0853256658edc5a26471648d8dd0a08fa9e9719f86bca0cd7763f5`。
先执行与产品一致的派生归一化再验证：26条事实可以进入结构核对，但18条非空条款关系均无 region，触发“非空证据信号必须携带原文摘录”。未把第一次直接验证缺少派生字段的错误误归因给模型。

该页必须按正式失败覆盖处理；不能根据已看到的原件或事实列表替模型补造摘录。当前不修改正在执行作业的冻结提示或配置。等待完整覆盖后决定相同合同受控重读还是有证据支持的通用提示修订。
