# MiniMax 图像内容不符排查记录

## 范围

本记录不含病例图像、摘录、身份资料、端点凭据。尚未发送给供应方。
产品自身使用 OpenAI 兼容客户端直连配置端点；未借用本机第三方 Agent 处理图像。

## 已观察到的现象

- 同一原始PNG，完整页判读和短提示抬头抄录均出现与图像内容不同的报告内容。
- 短提示只要求读取图中机构、科室、项目和方向，未提供正确答案、临床结论或条款。
- 返回自称 MiniMax-M3，finish_reason=stop，但结构完成不能证明正确看到了图片。
- 对照路线能识别主要抬头信息；不能据此宣布对照模型全部文字正确。
- 当前证据不足以区分模型幻觉、图像预处理、网关缓存/会话隔离或供应商内部路由故障；上述均为排查方向，不是已确认根因。

## 供应方可核对的请求

| 项目 | 完整页请求 | 短提示请求 |
| --- | --- | --- |
| 响应ID | 06ec9528ae170260264c5b8bfa761a84 | 06ecc1a1464a605d0c6ff681a9cb58cd |
| 返回模型 | MiniMax-M3 | MiniMax-M3 |
| 思考强度 | high | high |
| 输出额度 | 24000 | 4000 |
| 结束原因 | stop | stop |
| 客户端耗时 | 281.904秒 | 19.824秒 |

短提示运行发生于2026-09-07中国标准时间约00:03。以供应方响应ID定位为准。
两次读取同一原始图片，不以文件名称、目录或已有识别文字代替图像。

## 产品侧已核对

- 完整页请求内容寻址回执与原页SHA一致；图像为data:image/png;base64内嵌，不走外链图像下载。
- direct_openai_completion将messages直接传入客户端，trust_env=False，SDK max_retries=0；未设置temperature。
- 增加真实SDK序列化+模拟HTTP传输测试，逐字节核对出站图片与输入、messages不替换；相关59项测试通过。
- 该测试证明被测产品侧序列化正确，不证明上游服务接收后的行为。
- 用量回执的reasoning_tokens=0不可解释为没有思考，已另存正文/思考字符计数；不据零计数推断模型能力。

## 希望供应方确认

1. 按上述响应ID核查实际目标模型、原始图像附件是否抵达目标服务。
2. OpenAI image_url内嵌PNG经兼容层转换后是否被保留，以及图像顺序是否保持。
3. 缓存键、批处理和请求隔离是否包含图片内容；是否存在错误复用。
4. 当前具体模型/通道是否支持所使用的图像输入格式，是否需要不同的官方参数。

不要求供应方从本文恢复任何病例，不对外提供病例原图。优先由其按响应ID排查。

## 当前处置与恢复条件

暂停对该路线追加病例诊断请求；不修改既有记录、不改用另一模型假装双源、不启用自动配对。
冻结原图和请求/响应证据，正式临床验收仍不通过，claims_complete=false。
待现端点修复或用户授权可用端点后，先以非敏感视觉探针验证，再以明确绑定的隔离原页复验；正确性与模型身份分别验证。
恢复旧任务之前还须核验冻结配置、版本与租约，必要时走合法新任务入口，不覆盖失败历史。
