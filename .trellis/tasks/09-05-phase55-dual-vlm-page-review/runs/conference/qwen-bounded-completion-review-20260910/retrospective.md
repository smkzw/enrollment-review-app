证据齐备。以下为阶段复盘与续审终稿。

# Conference Output: qwen-bounded-completion-review-20260910 - evidence_single_object

## Output

续审第十遍（阶段复盘 + P0 修复复核）。边界遵守：只读、无调用、无派发、未查询在途进程（1876 不作评价）。

### 一、P0 修复复核（证据：合格）

`protocol_deconstructor.py:1447` 条件改为 `(compact or include_frozen_context)`；`:4688-4690` 仅 `supports_bounded_batch_context` 为真时传入；slice3:194-213 钉住“形式修复携带目标规则+冻结原文、无 compact 字样、默认关闭”三断言。修复最小且方向正确。1418 全量回归在此修复之前，不能混同为覆盖。残余小缺口（推断）：调用侧“远端/无能力不传参”无直接测试，低风险。

### 二、撤回我的上一轮未证风险（证据）

`formal-wire-compile.json`：同分词器 CPU GrammarCompiler 编译成功 0.607s、未加载权重。pass-8/9 的“递归 Schema 疑编译失败”在此运行时**被证伪，撤回**。但须分清三级：编译成功（已证）≠ 解码期强制生效（仅“11 回执无 Warning”间接）≠ 语义正确（未证，response-7 仍保留“至少4周”于说明而 time_constraint 缺下界）。

### 三、阶段困难分析

**为何结构修复未带来临床完整性（证据→推断）**：证据链——nonblank 单字化→转义洞→投影删 pattern 后格式通过但 EX-04 频次仍漏；DNF 合取错误→提示修正→“加频次又删频次”振荡；正式表达第 7/17 批缺 EX-11/12 终态失败。推断：**格式/编号/数量类问题可被约束收敛；频次结构化、开放列举保留、时间窗下界、来源连续性属语义层，是表示法与模型能力的交叉缺陷**——两种表示下同型出错，证明非单纯表示法问题。

**档位不可直接排名（证据）**：oMLX 三档均 34/34 数值，但 513/1477/991 秒、facts 41/47/52、手写均未裁定、low 缺采样/报告时分；单样本+未裁定维度+内存/缓存状态差异。

**平台干扰与表示难度分辨（证据）**：MTPLX 同 SHA 两次断流+allocator_fraction 1.431/1.657、exit137/139、SIGSEGV 报告、AR 拒图 400；oMLX 需 PLE 卸载才可加载；提示 35K 输入（条款包 117433 字符）。判别法已部分建立（同请求 SHA 单因素、Warning 回执、重复检测），但**归因仍靠多轮对照，成本高**。

**原地打转原因（推断）**：把语义缺陷当格式缺陷修；全流程验证太贵（formal 一轮 ~40 分钟只失败于第 7 批），反馈慢；多变量同改（提示+合同+预算+缓存）难归因；反复把结构成功误读为语义成功。

### 四、下一轮分步方案（建议）

1. 等 1876 终态后只读核对：编号/数量类错误是否消除；频次/时间窗下界是否仍错。
2. 若编号修复而语义仍错：**停止全流程重测**，转单父规则/单批次单因素实验台，一次只测一个语义维度（频次、下界、连续性、开放列举）。
3. 停止准则（无临床硬编码）：同一错误类经两轮单因素修复仍复现→记为当前表示/模型能力边界，转产品既有的“未解决项-责任人-证据”人工流程，不再迭代提示。
4. 四类确定性 gate（频次结构、时间窗、来源闭包/连续性、开放列举）作回归判据；两轮不复现才冻结版本。
5. 冻结后跑标准三平台×三档矩阵，届时才允许排名。

**应暂缓**：提示有损删减（无损空间已基本用尽）、exclude_defaults、表示法整体迁移、标准矩阵在版本未冻结时重跑、任何临床答案注入。

### 五、限度

同会话工程顾问视角；不宣称任何模型“能修好”；在途试验与未裁定维度（手写作者/范围、bbox、免疫学之外字段）均不构成临床验收；本复盘亦非验收。
