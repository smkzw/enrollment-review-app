All assigned work is complete. Here is the report:

# Execution Output: r3-two-main-readers-only-20260908 - worker_01

## Boundary And Context Check
- Runner-given initial read set paths (`context/...`, `plans/...`) did not exist at the worktree root; located and read both under `.trellis/tasks/09-05-phase55-dual-vlm-page-review/` (packet context matches the prompt; its "Source Of Truth" section was still a TODO, so I located the third-read implementation by code search and recorded each file read here).
- Edits confined to `app/**` and `tests/v2/**` page-review sources/tests. Not touched: docs, plans, `.env.example`, `.env` (never read), clinical artifacts, home configs, migrations, DB/data files, `scripts/`, root `tests/` files, network/browser. No packages installed. No recursive dispatch. Page-review files on this branch are untracked working-tree files; commits remain Codex's.

## Work Performed
**Product is now exactly two main readers: GLM-5.3-Flash low (main-A) + MTPLX `mtplx-flash-next-optimized-speed` high (main-B).**

1. **Harness (`app/llm/page_review_harness.py`)** — deleted the HANDWRITING_C route construction, the `main_b_provider == "mtplx"` pop (temporary provider-conditional C retention), `PAGE_REVIEW_HANDWRITING_MODEL_MATCH`/`BASE_URL` env reads and Flash-Next match validation, the C branch in `resolve_route_model` (GUI model resolution), the optional-C tolerate branch in `preflight_page_reader_routes` (now resolves exactly MAIN_A/MAIN_B, any main failure raises), the handwriting-only system prompt/schema (`HandwritingPayload`) in `build_page_review_messages`, and the C payload branch in `read_page`. Removed the `model_match` field from `PageReaderRoute` and `HANDWRITING_PROMPT_VERSION`. Normal handwriting extraction by A/B (including mandatory `handwriting=[]` on empty pages) is fully preserved.
2. **Config (`app/config.py`)** — removed `PAGE_REVIEW_HANDWRITING_BASE_URL` / `PAGE_REVIEW_HANDWRITING_MODEL_MATCH`.
3. **Reconciliation (`app/domain/page_reconciliation.py`)** — removed the `handwriting_third_read_expected` parameter and the trigger/missing-lane computation entirely; `RECONCILIATION_VERSION` → `page-reconciliation/r3-v13`. Added the forbidden-third-vote gate: any `handwriting-C` record entering reconciliation raises `页级对账只接受 main-A 与 main-B 两票，不接受第三读记录`. Dual-source handwriting adoption (≥2 agreeing main lanes; disagreement preserved as conflicts) unchanged.
4. **Contract (`app/domain/contracts/page_review.py`)** — deleted `HandwritingPayload` and the `validate_lane_scope` handwriting-C branch. `PageReconciliation` bumped to **`page-reconciliation/v4`** (new default): v4 rejects and never serializes `handwriting_reader_triggered` / `missing_optional_lanes` / `optional_lane_failures` (before-validator + wrap `model_serializer`), while the declared fields remain solely so persisted v1–v3 receipts decode byte-stable (verified: v3 payload with third-read fields round-trips byte-identical). `PageReviewLane.HANDWRITING_C` member kept only because removing it would make existing persisted third-read receipts undecodable and 500 the normalization intake on subjects with old history (violates 禁止删除历史数据/不改数据库) — no live path can produce or accept it.
5. **Execution (`app/services/page_review_execution.py`)** — deleted `_needs_handwriting_reader` and the optional C read/failure block; removed the now-dead `routes` and `completion` parameters from `reconcile_completed_reads` (it no longer calls any model); reconciles with dual-main-only semantics. Cancellation via `run_cancellable` and the formal resume path untouched and verified.
6. **Job service (`app/services/page_review_job_service.py`)** — `PAGE_REVIEW_JOB_CONTRACT` v8→v9; removed `handwriting_prompt_version` from payload and `page_review_execution_versions()`; `route_identity` no longer freezes `model_match`. Old jobs/coverages therefore fail version checks and are excluded from selection/replay by design.
7. **Coverage selection replay (`app/services/page_review_coverage_selection.py`) — the named regression is fixed**: replay previously called `reconcile_page_reviews` with the `handwriting_third_read_expected=True` default while mtplx runs wrote `False`, so normal mtplx coverage 409'd instead of feeding 整理. The parameter no longer exists, so write-path and replay semantics cannot diverge; prompt check simplified to `PAGE_REVIEW_PROMPT_VERSION` for all lanes.
8. **Storage (`app/storage/page_review_repository.py`)** — `get_reconciliation` column-mirror check for `handwriting_reader_triggered` is now conditional on the payload carrying it (legacy rows only); the NOT NULL column still receives `False` for new v4 rows. Migration 0020 untouched.
9. **Batch experiment (`app/llm/page_review_batch_experiment.py`)** — batch prompt version is now unconditionally the main prompt version; C lane references removed.

**Tests rewritten to dual-main / forbidden-third-vote counterexamples (no correctness assertions deleted):**
- `tests/v2/domain/test_page_review_contracts.py`: C-lane "may only write handwriting" test → retired-third-lane receipt decodes but **cannot vote** (reconciliation raises); third-vote tie-break in ambiguous-handwriting test → now forbidden; trigger/missing assertions replaced with v4 version pin; identity test reduced to determination-mode variation.
- `tests/v2/domain/test_page_review_legacy_read.py`: new tests — v3 receipt with third-read fields stays byte-stable, v4 payload carrying third-read fields is rejected, new v4 dumps drop the three fields.
- `tests/v2/services/test_page_review_execution.py`: two-reader-only test (exactly 2 records, both lanes, v4 reconciliation), new disagreement-preserved test (A/B handwriting mismatch → conflict, not accepted), mtplx main-B test (no third call, dual-source handwriting accepted); optional-C failure/missing-route tests removed with the feature.
- `tests/v2/services/test_page_review_job_executor.py`: C receipt-retention test rewritten as main-B raw-response-retained-even-when-schema-rejected; `handwriting_prompt_version` dropped from version-drift params.
- `tests/v2/api/test_page_review_resume.py`: "missing optional lane" restart test → restart resolves the same two main readers; C model-drift refusal test deleted (no C identity exists; main-reader drift refusal remains covered).
- `tests/v2/llm/test_page_review_harness.py` / `test_page_review_local_route.py` / `test_page_review_admission.py`: C preflight/route/prompt tests rewritten to two-reader assertions; mtplx preflight now asserts exactly two lanes and only `Bearer local-product`.
- **New formal HTTP test** `tests/v2/api/test_fact_normalization.py::test_mtplx_dual_main_coverage_is_selectable_for_normalization`: seeds real dual-main results (GLM low A + MTPLX high B via `reconcile_page_reviews`, current execution versions + reader identity), POSTs the formal normalization intake endpoint, asserts **201** — proving mtplx coverage is usable for 整理 end-to-end (this exact scenario failed before the replay fix).

## Artifacts And Evidence
App: `app/config.py`, `app/llm/page_review_harness.py`, `app/llm/page_review_batch_experiment.py`, `app/domain/page_reconciliation.py`, `app/domain/contracts/page_review.py`, `app/services/page_review_execution.py`, `app/services/page_review_job_service.py`, `app/services/page_review_job_executor.py`, `app/services/page_review_coverage_selection.py`, `app/storage/page_review_repository.py`. Tests: `tests/v2/domain/{test_page_review_contracts,test_page_review_legacy_read,test_page_source_association}.py`, `tests/v2/services/{test_page_review_execution,test_page_review_job_executor,test_r3_page_review_normalizer_wiring}.py`, `tests/v2/api/{test_fact_normalization,test_page_review_resume}.py`, `tests/v2/llm/{test_page_review_harness,test_page_review_local_route,test_page_review_admission}.py`.

## Commands And Observations
- `.venv/bin/python -m pytest tests/v2 -q` (full suite, 22m41s): **4030 passed, 3 skipped, 4 failed**. Failures: (a) two `tests/v2/llm/test_page_review_harness.py::test_diagnostic_*` — `ImportError` from out-of-boundary `scripts/r3_page_diagnostic.py`; (b) `test_page_source_association` version pin v3→v4 — fixed after the run (file now 17 passed); (c) `tests/v2/protocols/test_deconstruction_transport_config.py::test_omlx_transport_...` — passes in isolation, as a file (14 passed), and with neighbor runs; module asserts only `REVIEW_*/DECONSTRUCT_*/MTPLX_PROTOCOL_BATCH_MAX_TOKENS` config via subprocess, nothing I touched — assessed as a pre-existing order/isolation flake (observed once).
- Post-fix confirmation batch: contract/legacy/association/candidate/pending + execution/executor/job-service/cancellation/wiring + storage repository + harness/local-route/admission (2 script-import tests deselected) = **188 passed**; formal API suites (fact-normalization incl. new mtplx HTTP test, page-review, resume, length-recovery) = **35 passed**. New HTTP test alone: 1 passed.
- `python -c` import sweep of every edited module: clean; grep sweep: no live third-read symbols remain in `app/` or `tests/v2/` outside intentional legacy-decode/counterexample references.

## Blockers Or Missing Environment
1. **Out of write boundary, needs one-line-class fixes by Codex (or a granted extension):** `scripts/r3_page_diagnostic.py` — remove `HANDWRITING_PROMPT_VERSION` from the import (line 15) and from the `prompt_versions` dict (line 68); this un-breaks the two deselected `tests/v2/llm/test_page_review_harness.py::test_diagnostic_*` tests (their assertions are intact, currently failing only on that import). `scripts/replay_reader_pair.py` still runs (legacy field remains readable; prints `missing_optional_lanes: []`) but reports a retired concept.
2. **Root `tests/test_page_review_batch_experiment.py` (outside `tests/v2`) fails at collection** (`ImportError: HANDWRITING_PROMPT_VERSION`, plus a C-lane batch test at lines 567–584). I could not modify it per boundary; its C-lane batch test needs the same dual-main treatment.
3. `ruff`/linters are not installed in `.venv`; not installed per no-silent-install rule — lint verification of touched files remains open.
4. Kept deliberately (inference, flagged for Codex acceptance): `PageReviewLane.HANDWRITING_C` enum member and the three legacy `PageReconciliation` fields exist only for historical receipt decode (v4 forbids/never serializes them). Deleting them outright would break reads of existing history; a future additive migration could archive them if ever desired.

## Rerun Requests Or Next Step
No rerun of this worker needed. Suggested Codex acceptance: review the contract-v4 legacy shim (byte-stability tests) and the new HTTP test; apply the two one-line `scripts/r3_page_diagnostic.py` fixes and re-enable the two deselected diagnostic tests; update or delegate `tests/test_page_review_batch_experiment.py`; optionally re-run the full `tests/v2` suite to confirm only the known protocols flake (unrelated module) can appear.
