# Execution Output: r3-reader-format-repair-20260909 - worker_01

## Boundary And Context Check

- **Runner-referenced context files do not exist.** Both `context/r3-reader-format-repair-20260909_execution_context.md` and `plans/codex_execution_r3-reader-format-repair-20260909.md` were absent from the workspace (searched workspace root, `context/`, `plans/`, `runs/`, and the parent repo — no file matching `*r3-reader-format*` exists anywhere under `入排/`). I proceeded from the self-contained inline task description and recorded every decision here for Codex review.
- Read set used beyond the prompt (all inside the authorized workspace, read-only): `app/llm/page_review_harness.py`, `app/llm/gemini_transport.py`, `app/llm/page_review_admission.py`, `app/llm/page_review_context_layout.py`, `app/services/page_review_job_executor.py`, `app/services/page_request_receipt.py`, `app/services/page_review_job_service.py`, `app/services/page_review_coverage_selection.py`, `app/domain/contracts/page_review.py`, `app/domain/contracts/common.py`, `app/domain/page_normalization.py`, `app/projections/page_review_prompt_pack.py`, and the existing tests under `tests/v2/llm/`, `tests/v2/services/`, `tests/test_page_review_batch_experiment.py`.
- Key architectural fact verified: the job executor wraps every `read_page` completion call in `recorded_completion` (`store_page_request` + `raw_response` artifact per call). Therefore a repair call issued *inside* `read_page` through the same `completion` callable is automatically persisted and auditable with zero executor changes — no real model involved in tests.
- Files edited (complete list): `app/llm/page_review_format_repair.py` (new), `app/llm/page_review_harness.py`, `tests/v2/llm/test_page_review_format_repair.py` (new), `tests/v2/llm/test_page_review_harness.py`, `tests/v2/services/test_page_review_job_executor.py`, `tests/v2/services/test_targeted_page_review_jobs.py`. `gemini_oauth.py` / `gemini_transport.py` were only read, never modified. No `.env`/docs/plans/scripts/DB/artifacts touched; no network; no recursive dispatch.

## Work Performed

**1. New independent module `app/llm/page_review_format_repair.py`** — extracts format validation/repair out of `read_page` (which had been growing into a giant function):
- `evaluate_page_review_response(text, *, clause_pack, review_focus)` — the full validation pipeline moved verbatim from `read_page` (JSON extraction, context validation, deterministic normalization keys, `PageReviewPayload` validation, targeted-review empty-signals check, unknown-clause check). Returns `PageResponseEvaluation(raw, payload, response_sha256)`.
- Raises `PageResponseFormatError` with `failure_kind`, `repairable`, and `errors` (pydantic loc:msg list, capped at 10 entries). Repairable: invalid JSON, extra fields (e.g., the incident's `region.location`), `has_eligibility_value=false` with observations, template copying, unknown `clause_id`, targeted `clause_signals` violation. **Explicitly not repairable:** the date/numeric normalization `ValueError` (“资料包含无效日期或数值”) — date/value ambiguity must never be rewritten via format repair, so it fails immediately on the first read.
- `build_format_repair_messages(messages, *, previous_response_text, errors)` — returns a new list with one appended user turn (original system/page messages byte-identical, i.e., same model, same original image, same ClausePack, same complete original prompt). The turn carries versioned JSON: `repair_version: "page-review-format-repair/v1"`, explicit `validation_errors`, the `previous_response` as **untrusted reference**, and requirements: re-read the original image, return the complete contract, no field deletion, no date/value rewriting, no clause guessing, no forwarding to any other model. Serialization through the native Gemini transport was verified (string-content user turn → `user` role with text part).

**2. One-shot wiring in `read_page`** (`app/llm/page_review_harness.py`): on `PageResponseFormatError` — if repairable and not yet used, append the repair turn and continue; if non-repairable or the repair also fails, raise `PageReviewHarnessError` explicitly (message prefixed “格式纠正一次后仍失败：” after a used repair, underlying `failure_kind` preserved). Existing boundaries unchanged: 429 waits do not consume the repair, `length` doubling still applies to the repair turn, content-filter/endpoint fallback untouched, non-stop `finish_reason` still fails directly without repair. `PAGE_REVIEW_PROMPT_VERSION` bumped `v11 → v12` so the versioned repair semantics propagate through `page_review_execution_versions()` (in-flight jobs fail closed with `R3_EXECUTION_VERSION_CHANGED`) and coverage selection (old v11 records become “旧处理方式”， forcing re-read) — no consumer code changes needed, both import the constant.

**3. Synthetic tests** (`tests/v2/llm/test_page_review_format_repair.py`, 18 tests, no real model): success reads exactly once with no repair turn; the four incident causes each fail once then are corrected with call-count=2, byte-identical system+image parts, and repair-turn content assertions; second failure cap (no third call, explicit error, `failure_kind` preserved for both `schema` and `invalid_json`); unknown clause reported in the repair errors then corrected; date ambiguity non-repairable (1 call only); 429 during repair waits 60s without consuming the single repair; length doubling still applies to the repair turn; cancellation propagates from both the first and the repair call; failed repair does not leak into the next read; credentials never appear in error strings or any message payload; targeted-review repair stays inside focus scope; repair-message builder does not mutate the original list.

**4. Version-consumer test updates (necessary minimum):** harness test literal `page-review-r3/v11 → v12`; executor `schema` failure now expects 2 recorded calls/attempts, and the malformed-response persistence test now verifies the repair round's `raw_request` receipt (3 messages, third contains `format_repair`) plus both raw responses retained; targeted-jobs call-count for the schema-failing reread lane is 2→3 (one repair consumed).

## Artifacts And Evidence

- `app/llm/page_review_format_repair.py` — new module (canonical home of format validation + repair; ~160 lines).
- `app/llm/page_review_harness.py` — version bump, imports, `format_repair_used` flag, ~55-line tail extracted; read_page is now shorter than before despite the new behavior.
- `tests/v2/llm/test_page_review_format_repair.py` — 18 synthetic tests (coverage matrix listed above).
- Three consumer test files updated as listed in Work Performed.
- Evidence chain for auditability: every repair model call flows through the executor's `recorded_completion` → `raw_request` (full messages, `repair_version` + previous answer inside) and `raw_response` artifacts, surfaced in `response_attempts`; proven by the updated executor test.

## Commands And Observations

- `.venv/bin/python -m pytest tests/v2/llm/test_page_review_format_repair.py -q` → **18 passed**.
- `.venv/bin/python -m pytest tests/v2/llm -q` → **148 passed, 1 skipped** (the skipped test requires `INDEPENDENT_VLM_LIVE=1` — correctly not run; no real model calls anywhere).
- Executor + execution + repository suites → **38 passed**; targeted-jobs + context-layout → **12 passed**; page-review services → **22 passed**; combined in-scope regression → **220 passed, 1 skipped**; `tests/v2/test_architecture_boundaries.py` → **8 passed**.
- Direct serialization check: `build_gemini_request` accepts repaired messages; roles `['user','user']`, inline image preserved, repair JSON intact → OK (read-only use of the untouched transport).
- Observation during testing: `fact_normalization_key('检查值','2026-13-45')` raises `ValueError` (month 1..12) — used as the deterministic non-repairable trigger. `handwriting_normalization_key` cannot raise, so the historically uncaught `ValueError` path there is unreachable; behavior preserved as-is.
- One test-harness bug of mine found and fixed during the run (the parametrized fail-then-correct fake initially never served the invalid first response).

## Blockers Or Missing Environment

1. **Pre-existing failures outside my boundary (not caused by this change):** `tests/test_page_review_batch_experiment.py` — 16 failures. Its `_routes()` env omits `PAGE_REVIEW_MAIN_B_PROVIDER`/`MODEL`, so it now inherits the uncommitted Gemini-mainline defaults (`google-antigravity` / `gemini-3.7-flash`) and fails inside `require_page_reader_routes` with credential/model-identity errors — code paths and files I did not modify (the failing statements execute before any of my changed lines). Note these files (including `page_review_harness.py` itself) are **untracked**, so there is no committed baseline; the batch-experiment test is stale WIP from the batch-read stream. Smallest fix for the owning stream: add `"PAGE_REVIEW_MAIN_B_PROVIDER": "cms-smk", "PAGE_REVIEW_MAIN_B_MODEL": "MiniMax-M3"` to that file's `_routes()` env dict (one-line fixture change, mirrors the harness test's env).
2. `ruff` is not installed in `.venv`; lint could not run. Compile/import and full pytest passed; residual lint risk is minimal (unused imports were grepped and removed).
3. Missing runner context/plan files (see Boundary Check) — no other Codex-side inputs were available to consult.

## Rerun Requests Or Next Step

- No rerun of this worker needed; the assigned item is implemented and verified within boundaries. **Not verified here (explicitly out of scope):** any real-model or real-24-page product run, and any clinical/regulatory/rendered acceptance — Codex remains the final authority. Suggested next steps for Codex: (a) decide whether to assign the stale batch-experiment fixture fix to the batch/Gemini stream owner; (b) optionally authorize a real product replay of the previously failing 24-page document to observe the repair rate of the one-shot reread on GLM-low / Gemini-3.7-high, since synthetic tests cannot measure real-world repair success.
