# 历史横评评分口径核查

2026-09-06；只读核查，不修改外部横评、金标或历史成绩。

## 来源与范围

- `~/tmp/ie-vlm-benchmark-20260902/harness/score.py` 的 `num_norm`、`fact_hit`。
- 同目录 `reports/FINAL_REPORT_20260902.md` 与 `gt/IE_VLM_benchmark_金标_双审.xlsx`。
- 原件71页，但事实金标仅SAR44页；D001仅条款级。表内关键事实1019行（包含删除项），不能直接作保留事实分母。历史报告称保留832项，须由实际裁决重算确认。
- 历史报告承认多模型预填带来的选择偏差以及9条仅单审，不是用户专业签收。

## 已复现问题

使用产品隔离测试环境导入原评分模块，仅传入合成字典，不执行其main：

| 反例 | 原函数结果 | 含义 |
|---|---|---|
| `num_norm(0)` | `None` | 数值零经 `v or ""` 丢失 |
| 金标required_field、2025日期、5 mg；输出unrelated_field、2020日期、5 mg | structured/anywhere均True | 未核字段、对象、日期，只凭值和单位即命中 |
| 金标5 mg；输出仅正文unrelated number 5 | structured=False、anywhere=True | 无该事实也可被任意位置数字算命中 |

因此历史0.969并集召回并非字段级临床事实准确召回的充分证据。此结论不推翻原始模型输出，也不事后重写旧报告；它限制可据旧分数主张的范围。

## 后续评测约束

06001/06002归属追查：清单没有把一个原本06002命名的文件改错；原始目录实际为`06 北京大学第三医院/06002/4.筛选-基线检验报告单/06001-D1血常规.pdf`，原PDF SHA256为`ddda846a9594d5a36efd8e139b5f13c3a2c05de447d43f695d03278fdb08ef53`，横评页图为`e12d0a42185249d54eeac5b7ad839dba980b0afebaedf114b3beda35decb69bb`。因此矛盾已存在于源目录/文件名，不是本轮配对脚本造成；目录不能单独证明归属，页图身份遮挡也不足。保留该页用于不绑定受试者的版式识读诊断，但受试者级金标归属须来源方确认，不自动改名、移动或删除分母。

16条新增项的第一轮原图核查已完成，详见 `artifacts/phase55-takeover/20260906/fact-gold-source-qc.json`；图像字节均与清单哈希相符。这不是全部832项金标重审，也不是独立签收。确认新增意图并不保证新增内容正确：1005行“下周随诊”与图上读到的“不适随诊”不符；1010行只有“时间”列，不能由页面标题断言“查询时间”；1013、1015行将非该页原文的阈值当参考范围；1014行的2025-08-13是报告日期，采样是2025-08-12，且小于检测限不可改写为不存在。1008行原文件名06001与清单06002不一致，图中身份遮挡，归属尚须核实。摘要、推导和逐字摘录须分别计量。差异保留，不静默删除分母或替换旧表。

进一步追因更正：`harness/apply_gt_review.py:107` 追加16项时把notes放到裁决列、把“新增”放到一致模型数/状态列；subject取fact而非外层review，因而为空。已逐条精确匹配原review JSON，16项均唯一匹配、verdict=新增且外层subject存在。因此并非缺乏原始新增裁决，也不应将分母改成816；但原review同样未提供excerpt，来源摘录仍需核对。追溯记录及review文件哈希见 `fact-gold-provenance.json`；不修改外部生成脚本/金标，不把来源恢复等同临床重新验收。工具保留多候选而非按最后文件覆盖，反例已验证。

本轮只读清点已保存 `artifacts/phase55-takeover/20260906/fact-gold-inventory.json`：源XLSX SHA256 `03d6f99860b1df9084eb2459c7cac04059b5b0befa73d50446ff37dda2c149f1`。1019行=804保留+12修正+187删除+16自由说明。严格明确裁决为816行，历史非删除分母832包含16行自由说明；这16行同时缺subject与excerpt。不能自行删除这16行来提高分数，应按page_id追回原页、核对身份和摘录后形成独立裁决修订；现阶段分列待核范围，不声明816已全部有效或832错误。只读清点脚本的零值/自由裁决/原文件不变测试通过。

1. 保留原口径作为“历史宽松匹配对照”，与正式字段级口径分列，禁止把宽松分数当临床通过。
2. 正式口径绑定页、字段/对象、极性、值、单位及金标已明确的时间，精确数值规范保留零；不得引入模糊数字命中。
3. 金标同义字段映射和日期角色须先核查，不能因为自由标签不同一律当漏读，也不能以人工推断填模型输出。歧义另列并保留分母说明。
4. 先统计有效金标、可确定比较与待复核范围；校准集与保留集按受试者隔离。SAR31001已多轮用于校准，不能作为未见过的保留集。
5. 现有批准阈值仍为并集召回≥0.95、负判定静默漏判=0；若正式口径需变更阈值，单独报用户批准。未完成产品评测，不声明达到阈值。
6. R3页级不含最终判定，负判定必须经正式下游规则/缺口链评价，不把证据关系机械改成最终判定。

隔离评分原语 `scripts/score_exact_fact_keys.py` 只接受已明确投影的项目、受试者、页图哈希、字段、对象/时间角色上下文、极性、值及单位；箭头单独保留。它不从自由文字推断同义字段、日期角色或身份，因此尚不能用于宣称全量临床召回。13项反例验证异字段/标本/日期/时间角色/单位不误命中、零保留、比较符和箭头不消失；重复预测不增加召回且降低精确率，重复金标拒绝，空分母不报100%。没有读取旧成绩调阈值，也没有改变产品对账。
