Continue the approved same-session read-only engineering review. No edits, no model/service calls, no credentials, no raw clinical files outside the workspace, no delegation. Repository is three parents above task workdir. Inspect only the relevant current code and tests: app/agents/protocol_semantic_transport.py (new compact_wire optional constructor argument, property and schema generation), app/agents/protocol_deconstructor.py (existing formal vs DNF builders/parsers, batching and output scope), scripts/qwen_protocol_measurement.py (--formal-wire), tests/v2/protocols/test_deepseek_protocol_transport_slice3.py, tests/v2/agents/test_protocol_benchmark_defaults.py. Focused offline tests are allowed with repository .venv, no installs.

Purpose: isolate whether requiring local models to distribute nested source logic into DNF adds avoidable failure. This is an opt-in experiment using the ALREADY EXISTING formal product schema/prompt/parser; defaults remain unchanged. No claim that formal representation fixes semantics, no new clinical fields, no expected answers injected. The source-bound failures from prior review remain unaccepted. Do not demand a broad domain migration for this experiment; identify any real downstream incompatibility instead.

Review consistency of prompts/parsing/cache identity/segmentation/repair when compact_wire=False. In particular identify whether local scopes or budget assumptions require a guard before the real diagnostic, whether the CLI record adequately distinguishes this experiment, and whether remote/default behavior is preserved. Report prioritized actionable findings with file/line and smallest repair; no code changes. If no issue, state limitations (no live/clinical validation). Do not equate HTTP stop or absent Warning with usable or constrained output.
