Continue the same bounded read-only engineering review. No clinical files, network, model calls, .env, writes, tests or delegation. Only app/ and tests/ Python plus scripts/run_isolated_page_revision.py may be read. Return report to runner.

Verify current changes, do not accept the owner's description as proof: SubjectPageCoverage now optionally carries main_reader_identity_sha256, old empty field omitted on serialization; page job contract v8, executor derives identity from main-A/B provider/base_url/model/effort. Formal normalization obtains that identity from runtime without model initialization and filters coverage after lineage resolution. Matching duplicate candidates still fail. Reread scope requires same identity. New local key is PAGE_REVIEW_LOCAL_API_KEY, not inherited generic main-B or CMS key. Inspect production call sites and relevant tests for hidden bypasses or breakage.

Owner disposition of prior recommendations: resetting auxiliary two-round budget by adding route to its idempotency key is rejected, since the user explicitly limited rounds per original case; failure to resume with changed routes is retained instead of silently resetting. Same-model explicitly configured alternate endpoint is not automatic model substitution, so the blanket removal recommendation is not accepted. No mixed-page normalization shortening is implemented yet; it needs source-completeness evidence.

Report confirmed remaining bugs with file/line evidence; identify which previous coverage-selection concern is fixed or remains. Do not ask for a third business review round or autoaccept facts. No clinical acceptance claims. No fallback in this invocation.
