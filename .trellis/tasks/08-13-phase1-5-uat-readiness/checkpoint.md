# Phase 1.5 修复后验收检查点

记录时间：2026-08-13

## 当前状态

本任务状态保持 `in_progress`，但 Phase 1.5 构建者预试与返工已完成；同一独立复核会话最终 `ACCEPT`，P0/P1/P2 均为 0。当前门禁是等待正式用户 UAT，不是 Phase 2 开始。

稳定实现提交：`85670be feat(v2): complete phase 1.5 UAT readiness`。

没有启动 Phase 2，也没有触碰真实资料整理、文字识别、模型审核或数据库实现。未经用户明确批准，不得把构建者预试或独立复核代替正式用户 UAT。

## 已实现且已保留的功能

1. 从方案新建项目的三步确认：核对方案信息、确认独立研究期别、选择独立审核节点。
2. 当前方案与新草稿并列比较；新增、删除、逻辑或时间变化分组；草稿保存不覆盖当前版本。
3. 人工处理理由必填、范围二次确认、关闭与重开追加记录，行动状态与规则状态分离。
4. 批量操作执行前后显示对象清单，排序与筛选不扩大已勾选范围。
5. 任务恢复、个例“缺少记录不等于明确否认”、帮助页、中文用户试用任务卡与原始记录表。
6. 14 项 UAT 的组件、浏览器和视觉测试骨架。

## 独立复核结论

独立复核代理：`019ff872-5a7f-7b91-8998-f2350769a033`。结论为否决，主要问题如下：

1. **方案溯源不真实**：原实现把同一组全局页码显示到每条变更，导致新增 EX-05 错误指向当前 V1 第 10 页。
2. **任务总进度与逐文件状态矛盾**：总数可变化，但页面内三个文件仍是静态状态，恢复证明不成立。
3. **缩放验收名不副实**：自动化只改变视口，不能等同浏览器真实 100%、150%、200% 缩放。
4. **人工处理仍是前端会话演示**：尚未形成真实不可变审核批次；属于 Phase 1 已知原型边界，不能冒充后端合同完成。
5. **会话状态缺少运行时校验**：旧格式或损坏数据可被直接断言为正确类型。
6. 主路径缺少浏览器控制台错误捕获断言。

## 否决后已完成并复验的修复

1. 协议合同新增 `source_refs_by_rule_code`，要求每个新增、删除或变更规则都有独立且合法的方案页码映射；EX-05 只指向新草稿第 12 页，REQ-02 只指向当前版本第 10 页，EX-01 同时指向两版。
2. 任务进度改为逐文件状态驱动，总进度由已完成文件数派生；失败、未处理和已完成状态可恢复且不会互相矛盾。
3. `useSessionState` 改为必须传入运行时解析器；项目创建、方案、人工处理和任务页均增加旧数据或损坏数据降级处理。
4. Playwright 主路径增加 `console.error` 与页面运行错误捕获。
5. 自动化中的“缩放”文字已改为“响应式布局压力”，不再把视口变化宣称为真实浏览器缩放。
6. 契约、映射、组件和浏览器断言已随上述机制调整，并完成整轮重验。

## 最终独立复核收口

1. 原复核者接受逐规则方案溯源、逐文件任务恢复、真实浏览器缩放、运行时会话结构校验和控制台错误门禁。
2. 复核者继续构造出 `completed + 0/3`、`partial + 3/3` 等跨字段矛盾，推动任务会话从枚举校验升级为 8 种状态的完整语义不变量表。
3. 复核者再从真实界面复现 `0/3 + partial` 的中文说明矛盾；根因是把 `partial` 误写成“已有部分完成”。文案已改为不预设完成数，并增加完整界面路径测试。
4. 最终聚焦复核结果：`ACCEPT`；P0/P1/P2 均为 0。复核者未修改工作区。

## 修复后验证证据

1. Python 契约：31 项通过；逐规则方案页码为必填、覆盖完整且只能引用已登记来源。
2. 前端全量 21 个文件、156 项通过；任务状态专项最终 17 项通过。
3. 生产构建通过。约 1.5 MB 的主入口仍来自内置合成示例数据，是 Phase 1 已知边界，Phase 2 真实仓储接入后再拆分。
4. Playwright 全量：232 项中 192 项按适用视口通过、40 项按合同条件跳过、0 失败、0 重试；一级页面同时检查控制台错误、页面脚本错误和页面级横向溢出。
5. axe 未发现严重或关键级问题；1280、1440、1920、390 的关键页面和确认弹窗已人工查看。
6. 真实 Chrome 151 隔离实例完成 100%、150%、200% 缩放：CSS 可视宽度分别为 1440、960、720，DPR 分别为 2、3、4；三档均完成“个例风险 → 规则子项 → 原始证据 → 返回”，无横向溢出或运行错误。
7. 三档真实窗口截图已人工查看；弹窗标题、关闭按钮、文件、页码、定位精度、摘录和定位说明均可见，无裁切或重叠。详见 `real-browser-zoom-evidence.md`。

## 预期外结果与根因

1. P1-12 全量浏览器首次复测失败：两项资料都正确变为“已完成”，旧断言却要求全页只有一个同名状态。改为逐文件核对筛选病历、实验室检查和既往用药记录；三个桌面宽度专项及全量复测通过。
2. Edge 隔离实例可调试，但 macOS 辅助功能层把同一应用的两个实例合并，缩放快捷键落到用户原有窗口。没有把未变化的截图当成功，改用当时未运行的 Chrome 独立实例，并用 CSS 宽度和 DPR 证明缩放真实生效。
3. Playwright 的 CDP 页面截图在真实缩放且页面有纵向滚动时出现偏移或空白；DOM 几何和系统级窗口截图证明页面与弹窗真实可见。删除误导性的 CDP 图片，只保留 macOS 系统级窗口截图；后续不得用 CDP 图片单独证明真实缩放视觉结果。

## 当前下一步与保护边界

1. 恢复时先读取本检查点、`task.json`、`uat-readiness-matrix.md`、`real-browser-zoom-evidence.md` 和当前 `git status --short`。
2. 当前状态为“等待用户正式 UAT”；正式 UAT 仍需不少于 10 名目标用户、无辅助完成率不低于 90%、错误结论为 0。
3. 仅在用户明确批准后才可创建或启动 Phase 2 子任务。
4. 不修改 legacy 临床审核结果，不宣称真实 OCR、模型或临床判定已实现。

## 正式界面试用任务包补强（2026-08-13）

在原预试通过后继续从真实参与者可执行性反查，发现原任务卡仍有“指定受试者/指定规则/指定行动”等依赖记录人员临场解释的空位，且四类界面试用操作分别保存在不同页面，缺少统一恢复起点。若不修订，参与者无法独立完成，正式统计中的“无辅助完成率”也会失真。

本轮已完成：

1. 建立中央界面试用状态清单和“界面试用版 1.5.1”；四个页面统一引用，不再各自硬编码。
2. 系统帮助页增加“开始新的界面试用”：首次点击只显示影响说明；取消不改变状态；确认只恢复四类界面试用操作记录并回到今日工作，不影响其他网页或本机资料。
3. 14 项参与者任务全部写明起始页、受试者、审核阶段及必要规则/行动/证据对象；参与者卡不提供正确判定，记录人员核对答案单独保存。
4. 新增记录人员指南和正式批次汇总模板，覆盖不少于 10 人、140 次任务运行、无辅助完成率、E1-E4、关键证据操作数和用户明确批准门。
5. 发现参与者卡使用内部编号 `REQ-02`、界面显示“必做-02”。根因是任务文档绕过界面显示码转换；已统一为参与者可见的“必做-02”，并从用户文档清除存储机制等工程话术。
6. 发现项目期别同时出现 `III期` 与 `Ⅲ期`；已统一为 `Ⅰ/Ⅱ/Ⅲ/Ⅳ期`。
7. 独立测试者新增的页面放大脚本把 CDP 页面放大误当真实浏览器缩放，并产出一张左侧裁切的矛盾截图。该脚本和截图已删除；保留真实 Chrome 重排缩放证据，并给验收脚本增加弹窗左右边界断言。
8. 证据弹窗宽度和字段栏从固定像素改为相对字体单位与可视区上限；重新检查 720 与 390 CSS 像素，弹窗左右边界分别为 20/700 和 16/374，无页面级横向溢出，标题与字段完整。

最终验证：

- Vitest：22 个文件，166 项全部通过。
- 生产构建：通过；约 1.5 MB 合成示例资料入口警告仍为 Phase 1 已知边界。
- Playwright：263 项通过、61 项按视口适用性跳过、0 失败。
- 真实 Chrome 100%/150%/200% 系统截图复核：弹窗完整；误导性 CDP 证据已清理。
- 独立执行管理者最终结论：`ACCEPT`，P0=0、P1=0、P2=2。两项 P2 均为试用组织要求：记录人员不得向参与者宣读核对答案；首位参与者前需实际走一次窄屏复位流程。

门状态仍是 `awaiting_user_uat`。上述结论只表示正式界面试用任务包可开跑，不表示正式用户试用已经通过；未经用户明确批准，不得进入 Phase 2。

## 正式界面试用启动入口与窄屏复位验收（2026-08-13 第二轮）

在任务包补强基础上，补齐独立一键启动入口与 390 像素窄屏复位的真实走查。

### 一键启动入口（不依赖旧版 FastAPI 服务）

- 参与者入口为桌面「启动入排审核系统」，双击即打开浏览器进入首页；参与者无需输入网址、无需登录，也无需执行任何命令。具体端口只作为记录人员故障核对信息。
- 启动器只服务已构建的 `frontend/dist`（macOS 自带 `/usr/bin/python3 -m http.server`），启动路径不运行 npm/Node；缺少构建产物或版本标记时给出中文说明并退出。
- 健康判断以 `/uat-status.json` 的 `service == enrollment-review-v2` 为准，不能只用“端口能访问”判断；与旧版 FastAPI 服务明确区分。
- 端口占用一律顺延：非本启动器四重确认（pidfile + 进程存活 + 监听 PID 一致 + V2 标记）的服务视为占用并顺延端口，包括“返回相同标记但无本启动器有效记录的未登记服务”；绝不结束无法确认归属的进程。
- 停止入口为桌面「停止入排审核界面试用」，只结束可确认归属的试用服务。
- 仓库内脚本：`scripts/start_v2_uat.sh`（核心逻辑）、`scripts/stop_v2_uat.sh`、`scripts/start_v2_uat.command` / `stop_v2_uat.command`（Finder 双击壳）、`scripts/test_start_v2_uat.sh`（行为验收）。桌面 `.command` 文件由 Codex 放置并最终验收。

### 本轮已执行验证

- 启动器行为验收 9/9 通过：语法；首次启动版本标记验证；重复启动不新建服务；端口被其他服务占用时顺延且不结束占用方；未登记同标记服务不得复用；候选端口全部占用时中文报错退出；缺少构建产物报错退出；缺少版本标记报错退出；重复停止按“已经停止”成功处理。无遗留进程或临时目录。
- `frontend/src/test/uat-status.test.ts` 4/4 通过（标记 JSON、服务标识、页面版本与 `UAT_PAGE_VERSION` 一致、构建产物与源一致）；前端全量 23 个文件 170 项通过。
- 390 像素窄屏复位专项 3 项通过：取消（对话框关闭、登记键与无关键均不变、焦点回到触发按钮、无横向溢出）、确认与状态边界（只清除登记键、回到今日工作、无横向溢出）；narrow-390 项目全量 52 通过 / 32 按视口跳过 / 0 失败 / 0 重试。
- 新截图 `narrow-390-help-reset-confirm.png`（复位确认弹窗）与 `narrow-390-today-after-reset.png`（复位后今日工作）已由 Codex 完成视觉复核：弹窗在 390px 视口内完整显示、左右无裁切，复位后页面无横向溢出。

### 本轮清理

- 删除过期的启动器运行状态 `output/v2_uat_runtime/server.pid` 与 `server.log`（记录进程已不存在、端口无监听，属临时运行产物，启动器会在下次启动时重建）。
- 未删除任何截图：本轮新截图与既有验收锚点不冲突，没有被新证据明确替代的旧截图。
- 环境观察：复核时确认 4173 端口上的开发服务来自本工作区既有 Vite 会话；全量生产构建验收前已正常停止，正式试用入口不再依赖该开发服务。

### P2 关闭

- 「记录人员不得向参与者宣读核对答案」：记录人员指南第三节已明确禁止提供路径、答案或代操作，并提供 E8 标记规则。
- 「首位参与者前需实际走一次窄屏复位流程」：已在本轮完成（见上）。

### 最终独立复核与桌面实点

- 独立执行管理者复跑启动器 8/8、版本标记 4/4、窄屏复位 3/3，结论为接受，无需执行者返工；Codex 随后补充重复停止场景，启动器最终专项为 9/9。
- Codex 使用生产构建完成全量复验：Vitest 23 个文件、170 项通过；Playwright 266 项通过、70 项按视口合同跳过、0 失败；生产构建通过，合成示例资料入口约 1.5 MB 的已知提示未变化。
- Codex 通过 macOS `open` 按 Finder 双击方式实点桌面「启动入排审核系统」：服务在 4173 启动、版本标记正确、Microsoft Edge 成为前台浏览器；随后实点「停止入排审核界面试用」，启动记录清除且 4173 端口释放。
- 原桌面入口已保留为「启动旧版入排审核系统」，避免与 V2 正式界面试用混淆；参与者只使用未带“旧版”的标准入口。

门状态保持 `awaiting_user_uat`，未经用户明确批准不得进入 Phase 2。
