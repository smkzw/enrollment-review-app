# Phase 1.5 无损暂停检查点

记录时间：2026-08-13

## 当前状态

本任务按用户要求无损暂停，状态保持 `in_progress`。工作区内的实现、测试和截图改动全部保留为未提交内容；没有启动 Phase 2，也没有触碰真实资料整理、文字识别、模型审核或数据库实现。

当前不能表述为“构建者预试已通过”或“已具备正式用户试用条件”。上一轮自动化与视觉证据产生于独立复核之前；复核发现合同级缺口后已进行代码修复，但修复后的契约测试、前端测试、生产构建、浏览器全量和真实缩放验收尚未重新完成。

## 已实现且已保留的功能

1. 从方案新建项目的三步确认：核对方案信息、确认独立研究期别、选择独立审核节点。
2. 当前方案与新草稿并列比较；新增、删除、逻辑或时间变化分组；草稿保存不覆盖当前版本。
3. 人工处理理由必填、范围二次确认、关闭与重开追加记录，行动状态与规则状态分离。
4. 批量操作执行前后显示对象清单，排序与筛选不扩大已勾选范围。
5. 任务恢复、个例“缺少记录不等于明确否认”、帮助页、中文用户试用任务卡与原始记录表。
6. 14 项 UAT 的组件、浏览器和视觉测试骨架。

## 独立复核结论

独立复核代理：`019ff872-5a7f-7b91-8998-f2350769a033`。结论为否决，主要问题如下：

1. **方案溯源不真实**：原实现把同一组全局页码显示到每条变更，导致新增 EX-05 错误指向当前 V1 第 10 页。
2. **任务总进度与逐文件状态矛盾**：总数可变化，但页面内三个文件仍是静态状态，恢复证明不成立。
3. **缩放验收名不副实**：自动化只改变视口，不能等同浏览器真实 100%、150%、200% 缩放。
4. **人工处理仍是前端会话演示**：尚未形成真实不可变审核批次；属于 Phase 1 已知原型边界，不能冒充后端合同完成。
5. **会话状态缺少运行时校验**：旧格式或损坏数据可被直接断言为正确类型。
6. 主路径缺少浏览器控制台错误捕获断言。

## 否决后已完成但尚未复验的修复

1. 协议合同新增 `source_refs_by_rule_code`，要求每个新增、删除或变更规则都有独立且合法的方案页码映射；EX-05 只指向新草稿第 12 页，REQ-02 只指向当前版本第 10 页，EX-01 同时指向两版。
2. 任务进度改为逐文件状态驱动，总进度由已完成文件数派生；失败、未处理和已完成状态可恢复且不会互相矛盾。
3. `useSessionState` 改为必须传入运行时解析器；项目创建、方案、人工处理和任务页均增加旧数据或损坏数据降级处理。
4. Playwright 主路径增加 `console.error` 与页面运行错误捕获。
5. 自动化中的“缩放”文字已改为“响应式布局压力”，不再把视口变化宣称为真实浏览器缩放。
6. 契约、映射、组件和浏览器断言已随上述机制调整，但尚未完成整轮重验。

## 暂停时未完成事项

1. 上一条组合验证命令从 `frontend` 目录错误调用仓库根目录的 `.venv`，且输出被截断；结果无效，不得引用。
2. 尚未从仓库根目录重跑 Python 契约测试。
3. 尚未从 `frontend` 重跑新增会话校验、方案、任务专项测试及全部 Vitest。
4. 尚未重跑生产构建、Playwright 全量、视觉截图、横向溢出、无障碍与控制台检查。
5. 尚未使用真实 Edge/Chrome 浏览器完成 100%、150%、200% 缩放验收；必须记录真实浏览器缩放，不得再用视口替代。
6. 尚未把修复结果交回独立复核代理进行第二轮验收。
7. `uat-readiness-matrix.md` 中大部分“构建者预试通过”是修复前记录；P1-03、P1-12、P1-14 和总体验收结论必须在重验后更新。
8. 未提交当前工作区改动；最近稳定提交仍为 `5dc1b4d`，实现基线提交为 `72059e0`。

## 恢复时的准确顺序

1. 读取本检查点、`task.json`、`uat-readiness-matrix.md` 和 `git status --short`，确认用户没有改变范围。
2. 从仓库根目录执行：`PYTHONPATH=. .venv/bin/python -m pytest tests/v2/test_contract_artifacts.py -q`。
3. 从 `frontend` 执行新增专项测试，再执行全部 `npm test -- --run` 和 `npm run build`；先修根因，不放宽断言掩盖失败。
4. 重跑 Playwright 全量和截图；逐页检查中文、证据页码、任务恢复、控制台错误、横向溢出与视觉裁切。
5. 用隔离的真实 Edge/Chrome 窗口完成 100%、150%、200% 浏览器缩放，并记录 CSS 可视宽度、截图及是否出现整页横向滚动。
6. 更新矩阵和检查点后，把修复证据提交给同一独立复核代理做第二轮验收；只有其接受且 Codex 复核真实界面后，才能标记“等待用户正式 UAT”。
7. 用户正式 UAT 仍需不少于 10 名目标用户、无辅助完成率不低于 90%、错误结论为 0。仅在用户明确批准后才可创建 Phase 2 子任务。

## 暂停保护边界

- 不清理当前未提交截图、测试或代码，因为它们是本轮恢复所需现场。
- 不把上一轮 144/144、192 项浏览器通过等旧结果当作修复后证据。
- 不新增 Phase 2 工作，不修改 legacy 临床审核结果，不宣称真实 OCR、模型或临床判定已实现。
