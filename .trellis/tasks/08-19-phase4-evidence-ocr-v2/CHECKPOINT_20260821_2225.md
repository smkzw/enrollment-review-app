# Phase 4 Slice 4.5 终局与 Slice 4.6 中间检查点

**时间：** 2026-08-21 22:25 CST
**分支：** `codex/phase4-evidence-ocr-v2`
**工作树：** `.worktrees/phase4-evidence-ocr-v2`
**任务状态：** `in_progress`。Slice 4.5 已完成；P4-AC13 未完成；不得进入 Phase 5。

## 已完成

1. 完成连续原始资料、文件/页/文字/原图联动、真实区域定位、无坐标诚实降级、风险核对、校对、失败恢复、任务详情、受试者入口和中文帮助。
2. 三路指定测试模型完成 R3 复测，路线均未替换：
   - CodeBuddy CLI `hy3(max)`：核心流程通过，但模型不能读取截图，仅可作为无障碍树与交互复核证据。
   - Pi `cms-router/minimax-m3(high)`：核心流程通过；1366×768 阻断意见超出本产品 1080P 以上范围，不采纳。
   - Grok Build `grok-4.6(medium)`：核心流程通过；1080P 短文本红框压字被实际截图证实并修复。
3. 共享层终局修复：红框改为框外描边；纯标点不进入通用定位清单；空白资料追踪登记表默认折叠；中心信息增加分隔；用户状态改为“原件可查看”。
4. 清理本轮可再生根目录截图、Playwright 临时结果、构建前截图；保留最终截图、报告、提示词、金标准与决策记录。

## 终局验证

- 后端全量：`1557 passed, 130 warnings, 2 subtests passed`。本轮终局只改前端，后端结果沿用同一代码基线。
- 前端聚焦：`55 passed`。
- 前端全量：`48 files / 382 tests passed`。
- 生产构建：通过；仅保留既有主包体积提示。
- stub 证据流程 Playwright：`15 passed`，覆盖 1080P、2K、4K 和 100%/125%/150%/200% 布局压力组合。
- 真实后端 Playwright：`3 passed`，覆盖 1920×1080、2560×1440、3840×2160。
- `git diff --check`：通过。
- Codex 目视：1080P 和 4K 最终截图无页面横滚；默认原件无遮挡；选中后仅一个真实框，短文本不再被边框覆盖。

## 独立试用裁决

- R3 报告均未发现 P0。CodeBuddy 的 P2“多框区分”与当前只显示单框的实现不适用；其“变化类别自动推断”已在 R3 前取消。
- Pi 的 1366×768 P0 超出冻结产品边界；风险记录需要滚动是长记录的自然行为，不等同资料丢失或不可达。
- Grok 的红框压字、纯标点定位、空白登记表和中心粘连均已按共同原因修复；原始识别上的加工提示已移除。

## 未完成与下一安全动作

P4-AC13 仍缺少每一名测试者在各自隔离清洁库中的完整量化无辅助任务：创建/删除项目与受试者、两种上传方式、重复/冲突/不支持文件、OCR、页核对、校对、失败恢复、历史版本和全宽屏视觉。当前 R3 只覆盖已准备的合成活动项目，不能代替该验收。

恢复后：

1. 为三名指定测试者分别准备独立清洁数据目录和可删除的合成项目；先再次确认原会话/路线可用，不随意 fallback。
2. 让测试者通过产品 UI 和产品配置的独立 Agent 完成量化无辅助全流程，记录完成率、用时、误操作、遗漏和关键证据交互数。
3. Codex 复核真实浏览器、数据库不变量和全部异常共同原因；必要时修复并重复同路线复测。
4. 只有 P4-AC01 至 P4-AC13 全部有独立证据后，才执行最终清理、提交、Trellis 归档并确认 Phase 5 入口。
