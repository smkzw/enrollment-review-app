# Phase 4 Slice 4.5/4.6 无损暂停检查点

**时间：** 2026-08-21 15:51 CST
**分支：** `codex/phase4-evidence-ocr-v2`
**工作树：** `.worktrees/phase4-evidence-ocr-v2`
**状态：** `in_progress`，用户要求无损暂停；Phase 4 未放行，不得进入 Phase 5。

## 本轮新增要求

- 会商与测试不是同一角色。会商参与者只读质疑架构、设计和实施方案；独立试用者在隔离清洁环境中以资深医学监查员身份真实操作产品，可创建/删除测试数据，但不修改产品源代码。会商意见不能替代真实试用证据。

## 本轮已完成

1. 修复证据处理的系统级问题：
   - 资料集合身份不再包含上传方式；完整上传与补充上传形成相同成员集合时复用现有资料版本，不生成冗余版本。
   - 补充资料形成新基础处理版本时，沿用未变化页面的风险核对和校对记录；新增确定性沿用记录并保留原说明。
   - 基础处理完成后立即扫描识别风险并建立定位，不再等待用户点击生成可启用版本。
   - 原生文本与真实坐标同源时建立经过真实性核验的区域定位；普通 OCR 只建立文本范围定位，不绘制伪红框。
   - 完整处理版本自动纳入风险定位，构建器与仓储闭包校验使用同一规则。
   - 任务总体进度改用全资料页事件，原生 PDF/TXT 不再出现任务完成但 `0/0`。
2. 收口证据工作台：
   - 将“文字已识别”和“风险已核对”拆分，显示待核对数量。
   - 核对说明为空时明确提示填写后才能保存。
   - “启用这个资料版本”移动到中栏顶部并保持可见；启用后明确说明规则匹配属于下一阶段。
   - 长文件名使用省略显示并保留完整悬停文字。
3. 修复脱敏合成 PDF：
   - 根因为 Chromium PDF 字体映射产生康熙部首，不是产品 OCR 错误。
   - 用 LibreOffice 重建并把基本信息改为稳定表格；日期使用 `2026-08-21`，避免 Writer/Web 中文日期断行改变文本层顺序。
   - 最终 PDF 为 1 页，康熙部首字符 0 个，六个关键语句全部匹配；150 dpi 视觉检查标题、字段、正文和声明完整。
4. 更新测试：
   - 增加识别完成/风险待核对分离、必填说明、普通 OCR 无坐标无红框、增量沿用与跨上传方式去重的回归断言。
   - 修正三处旧测试数据：旧用例把“补充资料”构造成与当前完整版本相同的集合，却期待创建新版本；现分别改为正确的 no-op 断言或真实新增 C/D/E 的增长链。

## 已取得的验证证据

- 后端相关回归：`31 passed`。
- 增量沿用与普通 OCR 定位：`1 passed`；三项仓储契约组合：`3 passed`。
- 前端证据页聚焦：`35 passed`；新增风险状态测试：`6 passed`。
- 前端全量：`47 files / 372 tests passed`。
- 前端生产构建通过；仅有现存主包体积提示。
- Python `compileall` 通过。
- 合成 PDF：1 页、0 个 U+2F00–U+2FD5 字符、关键句 6/6，视觉图见 `runs/verification/fixture-regeneration-20260821/final-page-150.png`。

## 未完成或不能宣称通过

- 后端全量 `tests/v2` 尚无本轮终局通过结果：前三次试跑分别发现三处旧测试假设并已修复；第四次按用户暂停指令在约 9% 主动中断。中断不是失败，也不是通过。
- 尚未基于最新实现完成 1080P、2K、4K 浏览器视觉矩阵。
- 尚未发起修复后的 R5 三路独立真实试用。
- 尚未执行最终独立代码复核、阶段清理、提交和 Trellis 归档。
- 虚拟环境没有 Ruff 可执行文件；本轮不能声称 Ruff 已通过。

## 精确恢复顺序

1. 读取本文件、`implement.md` 尾部、`docs/PROJECT_CONTEXT.md` 尾部和工作树状态；确认没有后台测试或本任务临时服务。
2. 先运行 `.venv/bin/python -m pytest tests/v2 -q --disable-warnings --maxfail=1`，再运行 `cd frontend && npm test -- --run && npm run build`。
3. 若全量通过，启动一套新的隔离后端/前端；不得复用 R3/R4 数据库。
4. 在真实浏览器完成 1920×1080、2560×1440、3840×2160 的证据页和任务详情验收，重点核对：风险在首次打开时即出现、任务完成进度非 0/0、原生坐标红框真实、普通 OCR 不画框、启用入口可见、增量沿用和跨方式重复不建新版本。
5. 重新派发 R5 独立试用：CodeBuddy CLI `hy3(max)`、Pi `cms-router/minimax-m3`、Grok Build `grok-4.6(medium)`；首次仍先做连通性测试，保持长等待。提示词必须写明其角色是独立真实试用者，不是会商顾问。
6. Codex 独立裁决后再做阶段清理、最终记录与归档；Phase 4 放行前不得进入 Phase 5。

## 暂停清理状态

- 已停止本轮后端全量测试。
- 已关闭 R3、R4、final 的 Phase 4 临时前后端服务。
- 未删除 R4 报告、测试数据库、截图或合成资料，供恢复时追溯。
- 未修改源方案、真实受试者资料或旧临床报告。
