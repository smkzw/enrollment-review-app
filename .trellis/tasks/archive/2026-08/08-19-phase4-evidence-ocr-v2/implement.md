# Phase 4 实施计划

## 实施顺序

### Slice 4.0 能力试验与验收基线

- [x] 建立脱敏/合成页级金标准：原生 PDF、扫描 PDF、照片、DOCX/DOC、TXT、多页 TIFF、否定/肯定、数值/单位、完整/部分日期、重复文本定位和失败页。
- [x] 验证 `pdfplumber` 原生字符/词坐标与页图映射。
- [x] 通过共享 oMLX 门禁对隔离运行时发起选择 `GLM-OCR-bf16` 的真实原始返回探针；2/2 合成视觉页逐字一致且无显式机器坐标，冻结 text-only 路线与无坐标不画红框。记录明确：响应模型名为 provider 自报，未保留独立服务清单/启动日志，不宣称实际加载身份或一般准确率。
- [x] 独立评估完整 PaddleOCR-VL 布局流水线的真实坐标能力；因本阶段未达到可采用证据门槛，不接入 V2 真相层，统一使用 text-only 与诚实降级路线。
- [x] 冻结 PageArtifact、OCRProfile、OCRPage、原始请求/响应工件、定位精度和风险输出合同。
- [x] 冻结验收算法：原生 PDF 文本回读 100%/目标定位≥95%，扫描布局候选目标定位≥90%且区域均覆盖正确文本；关键风险漏检 0、页面误报≤10%，并明确阻断/提示矩阵。
- [x] 记录采用/拒绝路径、许可证、已实测耗时和诚实降级结论（GLM 文字能力仅在本合成样本范围冻结；PaddleOCR-VL/坐标能力未采用，不以合成布局演示冒充采用）。

**停止点：** 坐标、页图和风险合同未由金标准证明前，不进入 OCR 正式持久化。

**检查者恢复复核（2026-08-19）：** 原生 PDF、确定性金标准、风险覆盖和合同回归已通过；门禁选择 GLM 的探针 2/2 文字回读通过且无机器坐标，故冻结“采用 text-only 识别、无坐标不画红框”。模型实际加载身份未由独立服务清单/启动日志证明。本次只更新 Slice 4.0 证据和决策，不实施 Slice 4.1、迁移或正式 OCR 持久化。能力停止条件通过，但当前共享树的 Slice 4.1 ORM 已注册 6 张 `*_v2` 证据表而迁移 head 仍为 0007，标准 V2 迁移后校验报告 6 张表缺失；本检查不创建 4.1 迁移，因此不对当前 Slice 4.1 代码放行。

### Slice 4.1 领域合同与证据存储

- [x] 冻结受试者、审核节点、SourceBlob、逻辑资料/版本关系、SourceDocumentMetadataRevision、EvidenceSnapshot、EvidenceProcessingRevision/清单、ActivationEvent 和快照成员合同。
- [x] 实现本 Slice 已有表的仓储：作用域、前序链无环、显式替代、重复集合 no-op 和集合哈希；处理修订与激活仅冻结接口，不提前实现无表仓储。
- [x] 实施并验证 `0008_evidence_ingestion` 迁移、备份和回滚。
- [x] 补齐受试者/审核节点基础 API 和合同测试。

**停止点：** 重复提交、跨作用域、旧修订和非法快照链必须由当前 Slice 的确定性测试拒绝；处理修订漂移、激活和回滚在 4.4 建表后验收。

**独立检查者复核（2026-08-19）：** Slice 4.1 停止点通过。`0008_evidence_ingestion` 与 ORM 的 6 张 Phase 4 证据表、外键/索引、SQLite 外键/WAL/备份及升降级均有真实回归证据；含证据数据时降级会拒绝，避免静默丢失不可变历史。SourceBlob、逻辑资料版本、元数据修订、快照成员、集合哈希和状态事件均完成作用域、前序链、镜像字段、成员身份和不可变状态检查；补充/完整快照的继承、新增、显式替代、遗漏和重复集合 no-op 有确定性及并发反例测试。受试者/审核节点 API 已覆盖跨项目、重复、时间和中文错误信封，并避免逐节点 N+1 查询。

Slice 4.0 的真实门禁探针仍只证明合成样本下的 text-only 文字回读；响应中的模型名没有独立服务清单/启动日志证明，不能宣称独立模型身份；没有真实坐标时不绘制红框。EvidenceProcessingRevision、ActivationEvent 的正式持久化、处理修订漂移/激活/回滚仍按计划留给后续切片。本次没有实现上传、OCR 正式持久化或 Slice 4.2。结论：满足 Slice 4.1 停止点，可放行进入 Slice 4.2，但不等于 Slice 4.2 已实现。

### Slice 4.2 上传预览、确认与前端骨架

- [x] 以追加迁移 `0008a_evidence_upload_previews` 建立上传预览、逐文件条目和确认记录；不回改已验收的 `0008`，并保持后续 `0009_ocr_artifacts` 职责不变。
- [x] 实现暂存、格式探测、SHA-256、预览差异、取消清理和确认幂等。
- [x] 实现补充资料与完整资料快照两种语义及比较基线。
- [x] 实现同名异内容的“作为新版本/并列保留”、完整资料遗漏清单和同集合 no-op。
- [x] 创建候选快照和持久任务，不在 HTTP 请求内同步完成 OCR。
- [x] 建立 `/subjects/:subjectId/evidence?episode=...` 路由、入口和三栏工作台骨架。
- [x] 以中文业务语言展示新增、重复、冲突、不支持、遗漏和预计处理范围；固定上下文带与每项下一步动作持续可见。
- [x] 用无辅助脚本验证用户可在不阅读帮助的情况下辨认两种方式、发现遗漏、处理同名异内容、取消预览和确认正确对象。

**Slice 4.2 服务层检查点（2026-08-19）：** `0008a`、上传预览/逐文件差异/确认合同、内容格式探测、补充与完整资料集合、显式冲突处置、取消待清理状态、同事务候选快照/Job/幂等记录已实现。独立复核纠正了候选误作有效前序和旧预览借重复 no-op 绕过过期校验；Codex 聚焦回归 `111 passed`、V2 全量 `988 passed, 2 subtests passed`，Ruff/Pyright/diff check 通过。API、前端和无辅助宽屏任务尚未完成，因此 Slice 4.2 不放行。Slice 4.4 引入活动指针后必须把有效基准选择切换到 `ReviewEpisode.active_evidence_snapshot_id`。

**Slice 4.2 API 检查点（2026-08-19）：** 已实现设计书冻结的 6 个上传预览/确认/快照接口，未保留自创兼容路径；确认结果明确区分“首次创建”“同一请求回放”“不同上传命中同一资料集合”，取消清理失败保持“正在取消”并可重试，接口不暴露暂存路径。Codex 独立验收：聚焦 `89 passed`，V2 全量 `1034 passed, 2 subtests passed`，Ruff/Pyright/diff check 通过。前端与无辅助宽屏任务尚未完成，因此 Slice 4.2 仍不放行。

**Slice 4.2 最终检查点（2026-08-19）：** 前端已接入冻结接口并完成受试者×审核节点深链、两种上传方式、确认前逐项复核和三栏宽屏骨架。独立源码审查进一步纠正了合法的“回放且原确认重复”组合、重试换幂等键、切换/移除时遗留预览、审核节点修订号冒充资料版本和吸顶遮挡。Codex 验证前端 `310 passed`、证据工作台 Playwright `9 passed`、桌面路由 `40 passed, 2 skipped`、构建通过；后端 V2 `1034 passed, 2 subtests passed`。1920×1080、2560×1440、3840×2160 最终截图无页面横滚、无伪红框，模式、遗漏、冲突和恢复动作可辨认。Slice 4.2 停止点通过，放行 Slice 4.3；真实页产物、坐标和红框仍严格留在 4.3–4.5。

**停止点：** 无辅助脚本的关键步骤完成率必须为 100%，不允许错受试者/错节点/漏看全量遗漏；上传不得造成页面跳顶或跨节点串资料。

### Slice 4.3 页产物、OCR 缓存与持久流程

- [x] 实施 `0009_ocr_artifacts` 迁移及仓储，包含 EvidenceProcessingRevision 主表和基础页清单。
- [x] 实现逐格式页清单、渲染/解码元数据、页图、原生 PDF 文本/坐标和扫描页路由。
- [x] 封装现有 OCR 适配器，统一 OCRProfile 指纹、不可变原始请求/响应保存和页级缓存。
- [x] 数据库租约仅防重复页处理；共享 oMLX 门禁作为真实外部推理请求的唯一 8 路额度，并实现租约代次、晚到结果拒绝和独立页面渲染背压。
- [x] 实现文件/页检查点、限定范围重试、取消、安全恢复和 SSE 只读进度。
- [x] 实现不可变基础处理修订回放，冻结页产物、OCR 和资料元数据修订；该修订明确标记为不可激活，定位/校对/被引用资料关联由 4.4 新建完整处理修订时引用，不回写基础修订。
- [x] 此 Slice 只允许形成候选快照和处理修订；在 Slice 4.4 的定位/风险/校对门禁通过前，活动指针与激活 API 保持禁用。

**Slice 4.3 底座检查点（2026-08-19）：** `0009`、追加写页/OCR/运行/尝试/租约仓储、
不可激活基础处理修订、逐格式分页、内容寻址页工件、原生 PDF 文本坐标、扫描页纯文字识别、
TXT 原文保存和分阶段请求/结果适配器已完成。独立复核纠正了失败页伪几何、同快照修订唯一、
失败 OCR 原地更新、晚到结果非原子、页图输入哈希错配、DOCX 转换身份漂移、重复渲染、
路线重复判断、技术异常泄漏和处理版本无法共存。Codex 最终回归：聚焦 `141 passed`，
全量 V2 `1187 passed, 2 subtests passed`，Ruff、Pyright、`git diff --check` 通过。
页级执行、共享 8 路门禁、恢复/取消/重试和只读进度尚未完成，因此 Slice 4.3 尚不放行。

**停止点：** 故障注入无永久处理中、无重复 OCR、无部分失败伪完整；多进程、外部门禁占槽、布局多请求、崩溃和晚到结果测试中真实推理峰值不超过 8；活动指针仍未改变。

**最终质量检查（2026-08-19，独立 fresh-context 复核）：** 页工作租约 -> 共享 oMLX 租约 -> 外部推理顺序、心跳/代次/晚到原子提交、取消/重试/崩溃恢复、持久进度与 SSE、中文用户投影和 Slice 4.4 边界均已补反例并通过。独立检查者聚焦回归 `150 passed`，Codex 扩展聚焦回归 `172 passed`；V2 全量 `1233 passed, 2 subtests passed`。Ruff、生产代码 Pyright 和 `git diff --check` 通过。现场 12 路合成门禁探针返回 12/12，峰值 8，剩余租约 0；真实服务响应模型与非空文本校验通过。已收到但被拒的 provider 原始响应也保存为内容寻址工件。启动脚本仍按项目约束保持 legacy 默认入口，V2 通过独立 factory 启动。

### Slice 4.4 定位、风险与校对

- [x] **WP-44A 领域与持久化闭包：** 实施 `0010_evidence_locator_corrections`；收敛唯一 `ReviewEpisode` 合同并加入成对活动指针；新增 base/complete 修订辨别、处理候选、定位、风险扫描/核对、校对、被引用资料及 ActivationEvent 的追加仓储。升级不回写 4.3 原 OCR、基础修订 payload/hash 或页清单；正式历史存在时拒绝有损降级。
- [x] **WP-44A 迁移反例：** 证明 SQLite 重建/扩展后全部既有子表外键、行数、payload/hash、WAL、FK/integrity check 不漂移；不得按时间、ID 或历史 `ACTIVE` 状态回填活动指针，legacy `evidence_snapshot_id` 不改义。
- [x] **WP-44B 诚实定位：** 实现 occurrence-aware 四级定位、定位真实性门禁、重复文本消歧和中文降级原因。bbox 必须有同源字符/词坐标、页/文本/sidecar/变换哈希闭包并覆盖目标；当前 text-only 扫描/照片路线保持无 bbox。
- [x] **WP-44B 风险与校对：** 风险扫描作为原 OCR 外的追加旁路工件，覆盖否定/肯定、数值、小数点、单位、日期且不得写临床事实、规则表达式或判断；实现风险核对、原始 OCR 与有效校对层并列、关键语义变化二次确认、重叠/双标签页冲突和限定影响范围。
- [x] **WP-44B 完整修订与激活：** 通过持久处理候选从基础页产物冻结 complete 修订，精确回放页图/OCR/定位/风险核对/校对/资料元数据/引用关系；完整闭包及 expected episode revision 通过后，ActivationEvent、首次 snapshot 激活事件和成对指针在同一事务内更新。回滚只追加新事件；失败、取消、待核对和冲突均不触碰现行版本。
- [x] **WP-44C 后端接口：** 页接口分别返回原始识别、校对后文本、风险核对和真实定位精度；提供校对、完整修订、激活/回滚、被提及资料修订/满足关系 API；所有 current 读取只消费 episode pointer，不使用创建时间、列表顺序、状态或 legacy fallback。
- [x] **WP-44C-R 持久构建契约修复：** 校对、风险核对和显式构建命令在同一事务中冻结尝试清单并创建 `staged` 候选与持久任务；HTTP 不同步构建，后台任务取得租约后才进入 `processing`。排队期间新增的旁路记录不得被旧候选吸收；失败、取消、重试和服务重启不得改变现行成对指针或旧审核结果。
- [x] **WP-44D 中文核对闭环：** 中栏并列原始识别/校对后文本、识别核对与关键确认；展示真实定位精度及降级原因；支持“资料中提及但未提供”的确认、修改、解除和关联已上传文件；409 保留用户输入与系统当前记录的差异。连续原件滚动和真实红框完整体验仍留到 4.5。
- [x] **全矩阵与边界：** 状态机非法边、幂等、竞态、故障注入、旧修订回放、孤立候选为 0；复合句 `ALT > 3×ULN 且 AST > 3×ULN，或总胆红素 > 2×ULN` 扫描前后逐字一致且不产生临床规则；被提及资料全流程不创建 ClinicalFact、规则判断、ActionRequest 或 ReviewRun。

**Slice 4.4 详细合同（2026-08-19）：** 以 `research/slice44-detailed-contract-review.md` 为本切片细化依据。四个工作包严格按 A -> B -> C -> D 串行执行、写入面互斥；每包经独立检查后再进入下一包。种子风险门槛只对冻结金标准负责，不宣称一般临床准确率；连续页图、缩放映射和红框视觉验收仍属 Slice 4.5。

**WP-44A 最终验收（2026-08-20）：** `0010`、唯一审核节点合同、基础/完整修订辨别、追加式定位/风险/校对/被提及资料/候选/激活事件仓储及完整闭包已通过独立复核。复核期间先后封闭半成品完整修订、根记录自证页闭包、定位来源替换、陈旧链头破坏历史回放、当前校对/被提及资料遗漏和同一原文范围多根校对等系统级缺口。新完整修订必须完整冻结当前有效集合；旧完整修订按已冻结 ID 回放，不追随以后新增后继。最终聚焦回归 `225 passed`，全量 V2 `1361 passed, 2 subtests passed`，Ruff、限定 Pyright 与 `git diff --check` 通过；独立 `gpt-5.6-sol:high` 复核终局 `ACCEPT`。现可按串行顺序进入 WP-44B，不代表 Slice 4.4 整体完成。

**WP-44B 最终验收（2026-08-20）：** 独立复核先后拒绝重叠 occurrence 被漏计、被提及资料链头/人工确认生命周期不闭合、默认激活不幂等、孤立激活事件、陈旧 READY 候选可直切权威指针及候选状态不同步。修复后，确定性候选来源与用户复核分离；确认/修改/解除保留非空原因；激活仓储入口在同一保存点校验候选冻结修订并同步写事件、成对指针和候选 ACTIVE 状态。风险种子分母固定为 26 类风险、13 个干净页、10 个风险页和 23/23 覆盖。最终聚焦 `209 passed`，全量 V2 `1455 passed, 130 warnings, 2 subtests passed`，限定 Ruff/Pyright 与 `git diff --check` 通过；同一 `gpt-5.6-sol:high` 独立会话经两轮修复后终局 `ACCEPT`，无未闭合 P0/P1/P2。WP-44C 后端接口现已解锁，WP-44D 仍按串行顺序阻塞。

**WP-44C 最终验收（2026-08-21）：** 页、校对、风险核对、完整修订、激活/回滚、被提及资料及 current 指针接口已完成。独立复核首轮拒绝三个系统级缺口：重复启用当前版本的新幂等键返回 500、被提及资料可绑定另一受试者资料版本、API 错误映射器通过上传服务再导出识别存储异常。修复后，重复启用返回结构化 `409 CURRENT_VERSION_UNCHANGED` 且不新增事件/幂等历史；provided 满足关系在服务与仓储双层要求同作用域且属于当前活动快照；API 只识别应用错误。Codex 全量 V2 `1537 passed, 130 warnings, 2 subtests passed`，Ruff、项目虚拟环境 Pyright、`git diff --check` 通过；同一 fresh-context `gpt-5.6-sol:high` 复核终局 `ACCEPT`，无未闭合 P0/P1/P2。WP-44D 中文核对闭环现已解锁；Slice 4.5 连续原件滚动与真实红框完整体验仍不提前实现。

**WP-44C-R 复核重开（2026-08-21）：** WP-44D 全量前端验证后进行的 fresh-context 合同审查发现，现有校对/风险核对只追加旁路记录，未原子建立可恢复的候选与持久任务；显式构建仍在 HTTP 请求内同步运行，且候选输入未冻结全部旁路记录 ID，排队期间存在吸收后续记录的时序漂移。该问题与冻结设计 §6 直接冲突，定为阻断 Slice 4.4 的 P1。暂停 WP-44D 最终验收，先完成 WP-44C-R；此前 WP-44A/B/C 的领域、激活和 API 结果仍作为回归锚点，不视为整体作废。

**WP-44C-R/WP-44D 最终验收（2026-08-21）：** 构建命令已改为原子建立持久候选、冻结尝试清单和后台任务；HTTP 不再同步构建。后台执行支持租约、进程死亡恢复、同一候选幂等续跑和不改变现行成对指针。前端持久恢复候选、持续轮询并保留 409 输入；旧候选延迟恢复或 404 均须同时核对当前候选编号与本地恢复锚点，不能覆盖或删除用户刚创建的新候选。独立 fresh-context 检查先后拒绝同步构建、候选输入漂移、READY 重放、轮询停止、候选恢复竞态和用户可见技术术语；全部按共享状态机与恢复合同修复后，同一 `gpt-5.6-sol:high` 会话终局 `ACCEPT`。Codex 最终锚点：后端全量 `1543 passed, 130 warnings, 2 subtests passed`；前端 `336 passed`；构建通过；证据工作台 Playwright 在 1920×1080、2560×1440、3840×2160 共 `6 passed`；Ruff、项目虚拟环境 Pyright 与 `git diff --check` 通过。Slice 4.4 至此完成；右栏连续原件滚动、真实坐标缩放和红框仍严格属于 Slice 4.5，当前诚实占位不视为完成。

**停止点：** 达到 Slice 4.0 的定位和风险量化门槛；关键语义变化不得静默通过；校对不得修改原始 OCR；旧处理修订可精确回放；此后才启用激活 API。

### Slice 4.5 完整体验与恢复闭环

- [x] 完成文件/页/文本/原图联动、定位精度、风险核对、失败页和重试体验；右栏按实际页序连续滚动原始资料，并以统一选择状态联动左栏页清单和中栏证据。
- [x] 实现真实区域坐标到页图覆盖层的缩放映射和风险红重点框；为文本范围、页内摘录、仅页码三种降级定位提供不带伪红框的中文提示。
- [x] 完成任务中心的中文处理详情和受试者/个例工作台入口。
- [x] 更新系统帮助：两种上传方式、重复文件、失败恢复、校对、定位精度和常见问题。
- [x] 运行真实浏览器宽屏组合：1920×1080（100%/125%）、2560×1440（100%/150%）、3840×2160（100%/150%/200%）。
- [x] 清理本轮可再生缓存、旧截图和临时试验文件，保留金标准、最终截图和决策记录。

**停止点：** P4-AC01 至 P4-AC13 全部有独立可复现证据；任何错对象、伪定位、红框错页/错位、静默关键风险、不可恢复任务、页面横滚、裁切或内部术语均阻止进入外部试用。

### Slice 4.6 独立试用与阶段验收

- [x] 先分别对 Cursor CLI `cursor-grok-4.6(medium)`、Pi `cms-router/minimax-m3(high)`、Pi/oMLX `Qwen3.8-27B-oQ8e-fp16-mtp(medium)` 做一次真实连通性测试；本轮模型变更后旧连通性记录不得沿用，失败时保留证据，不随意替换模型。旧 `ox-alpha` 运行结果仅保留为历史失败证据，不参与当前 P4-AC13 验收。
- [x] Cursor CLI `cursor-grok-4.6(medium)`、Pi `cms-router/minimax-m3(high)`、Pi/oMLX `Qwen3.8-27B-oQ8e-fp16-mtp(medium)` 三名测试者在互不污染的新架构 D001 II 期项目副本中，使用各自不同的真实受试者资料，通过产品界面和产品配置的独立 Agent 完成上传预览、快照、OCR、页核对、校对、失败恢复和宽屏视觉试用；不得以测试模型自身的医学推理替代产品能力。本地 oMLX 测试与产品 OCR 串行安排，完成后卸载测试模型。
- [x] 测试提示给足业务背景和探索空间，不把测试限制为页面显示或流程跑通。
- [x] 三路测试均执行量化无辅助任务脚本并记录完成率、用时、误操作、遗漏和证据交互数；明确标注这不是一次真人可用性研究。
- [x] Codex 独立复核测试输出、真实浏览器、数据库不变量和临床边界；所有异常追到共同原因并补回归测试。
- [x] 完成全量测试、任务记录、阶段清理、提交和 Trellis 归档；确认 Phase 5 的唯一安全入口。

**Slice 4.5 终局与 Slice 4.6 中间检查点（2026-08-21 22:25）：** 连续原件、真实区域定位、诚实降级、上传/校对/风险/恢复入口和中文帮助已完成。三路 R3 使用指定测试模型与既有真实浏览器会话复测，均确认默认阅读态、单框定位、上传收拢、重复文件不诱导确认及当前版本语义；CodeBuddy 因模型图像能力限制只形成无障碍树核对，Pi 把明确范围外的 1366×768 误列阻断，Grok 在 1080P 发现短文本红框压字。Codex 以实际截图复现后从共享层改为框外描边、过滤通用定位清单中的纯标点、收拢空白资料追踪表单，并修正中心分隔和用户状态措辞。终局前端 `382 passed`、生产构建通过、stub 证据流程 `15 passed`、真实后端 1080P/2K/4K `3 passed`；后端未再改动，沿用本阶段同一代码基线的全量 `1557 passed, 130 warnings, 2 subtests passed`。P4-AC13 仍未完成：三名测试者尚未分别在清洁库执行创建/删除项目与受试者及量化无辅助全流程，因此任务保持 `in_progress`，不得进入 Phase 5。

**Phase 4 终局（2026-08-22）：** 指定三路测试路线已完成连通性和隔离真实项目试用，旧 `ox-alpha` 仅保留历史失败证据。系统级终局修复覆盖处理中快照任务深链、关键校对最小原文范围、半开区间边界插入、修订隔离、风险批量聚合和严重整页重复风险不得由普通阅览关闭。最终后端 `1633 passed, 139 warnings, 2 subtests passed`，前端 `49 files / 416 tests passed`，生产构建通过，1080P/2K/4K Playwright `9 passed`；真实 D001 隔离库可从证据页进入对应资料处理详情。fresh-context 独立检查结论为 `ACCEPT`。三名模型测试者不等同真人可用性研究，密集风险页仍可能带来较高人工核对工作量；这些作为残余边界记录，不阻断 Phase 4。

## 验证矩阵

| 层级 | 验证 |
|---|---|
| 合同 | Pydantic/JSON Schema/OpenAPI 快照、枚举和错误结构 |
| 数据库 | Alembic 升降级、SQLite backup、WAL/外键/索引、集合哈希和镜像字段一致性 |
| 快照 | 有效版本继承、显式替代、完整资料、重复集合 no-op、链无环、处理修订回放、ActivationEvent、阶段隔离和修订冲突 |
| 文件 | 逐格式分页路线、渲染器/解码器版本、内容去重、同名异内容、损坏文件、多页完整性和不支持格式 |
| OCR | 页级缓存、原始请求/响应、配置变化、多进程真实 8 路峰值、租约代次、晚到结果、否定/肯定、数值/单位/日期 |
| 定位 | 文本回读 100%、原生定位≥95%、扫描候选≥90%、坐标覆盖正确文本、四级降级和重复文本消歧 |
| 恢复 | 各步骤中断、浏览器关闭、服务重启、租约过期、部分页失败、重试和取消 |
| 前端 | 路由深链、键盘操作、三栏同步、原始资料连续滚动、证据跳转、真实红框缩放映射、降级定位不画框、无页面级横滚、文字裁切/重叠、中文标签扫描 |
| 回归 | 现有 V2 单元/集成/E2E、Phase 3 方案发布锚点、旧项目只读验证 |

## 计划验证命令

具体命令在每个 Slice 开始时按仓库当前脚本确认，至少包括：

```bash
python -m pytest tests/v2 -q
python -m pytest tests/v2 -m "contract or storage or workflow or ocr" -q
cd frontend && npm run typecheck
cd frontend && npm run test
cd frontend && npm run build
cd frontend && npm run test:e2e
```

OCR 能力试验和正式调用统一使用：

```bash
/Users/smkzw/.codex/tools/omlx_workload_gate.py run \
  --kind ocr --owner phase4-evidence-ocr-v2 -- <ocr-command>
```

## 高风险文件与回滚点

- Alembic 迁移和仓储：每个迁移单独提交，先备份、再升级、验证、降级、恢复。
- EvidenceSnapshot/ReviewEpisode 活动版本：候选发布前不改变现行快照/处理修订；激活和回滚均追加 ActivationEvent，不直接回写历史。
- OCR 适配器：旧适配器不直接成为 V2 真相层；新接口失败可退回纯文本识别与诚实降级。
- 前端路由：保留现有受试者和任务页入口；新证据页失败不影响 Phase 3 方案工作台。
- 旧系统资料：全程只读，不执行迁移、删除、重置 OCR 或重新审核。

## 实施前检查

- [x] `prd.md`、`design.md`、`implement.md` 已完成收敛审阅。
- [x] `implement.jsonl` 与 `check.jsonl` 均包含真实规范和研究上下文。
- [x] 用户在看到本版最终规划摘要后的后续消息中明确批准实施。
- [x] 运行 `task.py start`，状态变为 `in_progress` 后才修改产品代码。
- [x] 每个 Slice 开始前加载相应 Trellis 规范，结束后由独立检查者验收。

## 2026-08-21 15:51 R4 后无损暂停

- 会商顾问与独立试用者分为两条证据链：前者只读挑战架构与设计，后者在隔离清洁环境真实操作产品；会商结论不得替代端到端试用。
- 已完成跨上传方式集合去重、增量未变化页面核对/校对沿用、基础处理后立即风险扫描与真实定位、全资料页进度，以及风险状态/启用入口的前端收口。
- 前端全量 `372 passed` 且生产构建通过；后端聚焦回归通过。后端全量在修正三处旧“空补充版本”测试假设后，最终一轮按用户暂停指令在约 9% 中断，尚无本轮终局通过结论。
- 恢复锚点：`CHECKPOINT_20260821_1551_PAUSED.md`。恢复后先完成全量回归，再做新隔离浏览器矩阵和 R5 三路独立真实试用；Phase 4 放行前不得进入 Phase 5。
