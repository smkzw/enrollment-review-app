# 0924V1 入排审核系统无损交接

记录日：2026-09-24。状态：用户要求完成当前收尾后暂停；**未完成产品交付**，`claims_complete=false`。本文件记录本轮可复核结果，不把结构通过、模型响应或旧测试通过写成临床验收。

## 1. 接手入口与保护范围

- 唯一工作树：`/Users/smkzw/Documents/康哲项目资料/AI/入排/enrollment-review-app/.worktrees/phase5-clinical-facts-profile`，分支 `codex/phase5-clinical-facts-profile`。本轮起点 HEAD `35459c6da26fc17ddbf0cf5fbe87d631ef274c25`；接手时以 `git rev-parse HEAD`、`git status --short` 与 GitHub 远端现状重新核对。不要 reset/clean，也不要在主 checkout 覆盖这些变更。
- 当前任务：`.trellis/tasks/09-11-e2e-eligibility-review/`。先读 `delivery_20260922/00_START_HERE.md`、`02_EXECUTION_RULES.md`、`03_PLAN.md`，再读当前 `prd.md`、`design.md` 与 `implement.md` 文头。0924V1 专家包在 `/Users/smkzw/Downloads/enrollment_review_0924V1_35459c6.zip`；只按涉及的修改读取对应章节。权威架构及阶段安排分别为 `docs/REARCHITECTURE_FINAL_DESIGN_20260812.md`、`plans/REARCHITECTURE_IMPLEMENTATION_PLAN_20260812.md`，实际代码、作业回执和原件优于历史 handoff 的状态判断。
- 原始方案、受试者资料、正式临床库、旧报告和本轮隔离 SQLite/模型回执均不得清理或改写。`artifacts/0924v1-w6-*/result.json` 含临床摘录，只在本机核查，不提交到 GitHub。`.env`/密钥不提交、不打印、不转给未知服务。其他任务拥有的 8001/8002/8900/8910 等端口不可因本任务关停。
- 产品模型调用必须由本系统内置方案 Agent/资料 harness 直连当前合法产品端点；外部个人 CLI、Hermes、OMP 只能承担授权的工程执行/会商，不能代产品识别病例。当前诊断用 `cms-router/deepseek-latest-cloud`，不是替用户重新决定所有产品模型。仍需以现场显式配置和真实回执确认模型身份。

## 2. 目标及当前计划位置

目标是从原 DOCX/PDF 方案经内置 Agent 建立可追溯的官方入排规则与跨章节控制点，同源核对并正式发布；再把真实病例原件经可靠文字主读/必要局部视觉核实送入事实、事件、用药暴露、Patient Profile，按预筛/筛选/基线当前节点形成可追溯工作稿、报告和一次更正/补证后的新结果，保留旧结果；最后完成第二结构留出、宽屏真实浏览器交互、启动/停止/备份恢复和 Q3 验收。不能把方案/疾病/药物/单一模型写死，也不能恢复全页强制双 VLM 或编造两份读取记录。研究者书面判断缺失应进入明确的待补资料说明，不凭空判符合或排除。

现行 0922/0923/0924 连续计划中，W0–W2/Q1、W3–W5/Q2 有此前聚焦通过记录；它们不替代本轮真实临床验收。**W6 尚未正式发布跨章目录，W7/Q3 尚未完成。**已有官方草稿修订 21 的 `publishable=true` 只是草稿门禁，不是完整正式规则集发布。五份24页病例尚未在本轮已发布新规则下形成整例、补证和报告。不要以“全未决报告”结束产品验收。

## 3. 本轮工程结果与证据

1. 从旧失败作业 `fba86f5c8e65480a8009802ccd949220` 的确定性收口检查点**只读**取得同一冻结首批；未续跑失败作业、未新建正式临床作业。v77 产品诊断约338秒失败，原因之一是非禁令原子挂上持续义务，系统又把它送入冻结该字段的单原子修订，属于不可完成的修复范围。
2. 修正 `app/agents/protocol_control_deconstructor.py` 的单原子修订选择：仅求值/时间字段进入原子级修订；其他包括持续义务归属错误进入候选级有界修订，不自动删改临床含义。同步更新 `app/agents/protocol_control_source_interpretation.py`：时间短语必须属于该条逐字陈述或标题，不能借同段另一动作的时间；版本身份升至执行 v79。新增定向反例。诊断首批 v79 经产品 DeepSeek 两轮完成：208.70秒，第一轮缺一个日期属性，第二轮69字符定向补答后通过当前结构与批次门禁。原始请求/响应/hash在本机 `artifacts/0924v1-w6-v79-first-batch/result.json`，标记 `diagnostic_only=true`、`clinical_acceptance=false`。
3. v79 的22条来源陈述中，逐字覆盖账：完全表达2、同单元候选关联但未完整表达5、仅链接目标1、未定位14。许多未定位来自阶段排除/给药后内容，不能简单算漏项；但同一候选段落中的知情同意与导入后再核资格仍只是“同单元关联”。冻结目录有 IN-07 及访视流程目标，却没有逐陈述证明动作、时间、例外被已有目标覆盖。**结构通过不能作为发布依据。**这是真实的混合来源单元合同缺口，而非通过重复提示或放宽门禁可消除。
4. `scripts/start_enrollment_review.command` 的端口检查由不存在的函数改为直接检查监听状态；未显式配置时的默认模型值与 `.env.example`、现行产品配置对齐。没有启动/停止正式服务，更没有验证启动、备份恢复。
5. 本轮集中定向验证：157 passed，5项第三方 SWIG 弃用提醒；脚本 `/bin/zsh -n` 与 `git diff --check` 均通过。此前包含旧默认模型断言的全套 `test_mtplx_semantic_route_regressions.py` 曾 11 passed/1 failed，该失败夹具仍期待历史智谱默认，不可通过改变当前产品路由来追绿。未跑全库、正式病例、前端浏览器或 Q3；上述157项只证明相关合同未明显回退。

## 4. 停滞原因与避免重复劳动

长期耗时不是单一模型“慢”或一个提示词不足。完整方案跨章发现会产生大量来源单元，首批常混合入排控制、治疗前条件、治疗后义务与流程目标；模型一次需要输出深嵌套结构，局部格式错误可能触发昂贵的整候选重读。此前缩小批次、调整提示与多轮重试使失败移到另一批次，没有解决原文陈述与既有目标/新增候选之间的逐项闭包。v79 解决了**一个确定性错误修订路径**并改善首批结构产出，未证明临床语义完整。若再对同一冻结批无新假设地反复调用，只会耗时和增添歧义。

不应直接把所有 `candidate_linked` 判遗漏，也不能把标题相似判已覆盖。`SourceStatementCoverage` 目前只是 `app/agents/protocol_control_deconstructor.py` 的字面诊断；`ProtocolControlAgentWireDisposition` 一整个来源单元只有一个处置类别，且官方/流程链接只能附在相应处置类别，难表达同段“已有流程目标 + 另有增量限制”的混合情况。下一修订需要以**同一有源产物的来源陈述**为核对单位，逐项核动作、时间锚点、条件/例外、已有目标及增量候选；没有证据的保持未核实。避免另建第二真相表、泛化平台或针对 SAR/知情同意/药物写死匹配规则。先看现有消费者和发布边界，再作最小纵向修改。

## 5. 安全接续顺序

1. 核现场 HEAD/dirty、隔离 SQLite 作业终态、服务归属、产品实际 provider/model、v79 回执 hash。旧失败/取消作业不能越版续跑；有活动租约时不要抢占。不要把本地 artifacts 缺少 Git 追踪误作证据不存在。
2. 阅读 `SourceStatement`/`SourceStatementCoverage`、`ProtocolControlAgentWireDisposition`、`ProtocolControlAgentRunner.run`、`_validate_deep_batch_output` 与正式 hydrate/gate/共同发布消费者。为混合来源单元提供来源化逐陈述核对的最小合同：允许证明“已由既有官方/流程目标覆盖”或“由增量候选表达”，不强迫同段只取其一；核不清就保持阻断/待核，不凭字面相似自动通过。区分应在当前节点审核的禁止/要求与未来义务。
3. 对冻结首批做一次小范围反证，确认逐项有源闭包及旧反例；相连功能集中验证，不每改一处重跑。只有合同可说明缺口后，才由合法入口新建当前版本隔离 W6 作业，完成所有批次、来源核对、官方和跨章共同发布。`publishable=true` 草稿不能替代发布。
4. 在已发布新规则下导入本轮真实病例原件，形成可追溯事实/事件/用药/Profile、当前节点完整工作稿和报告；处理真实更正/补证，证明相关结果失效、新结果生成、旧报告及原件 hash 不变。最后按 W7/Q3 完成第二结构留出、Ego Lite 1080P/2K/4K、原件滚动与有据标注、启动/备份恢复。正式签发授权单独核对，不能自动批准。

暂停状态：本轮没有运行中的临床 Job 或测试 session 需要等待；未激活失败快照、未发布规则、未改原件。接手者恢复前先重核现场，不依赖历史 PID。Goal 由用户明确要求暂停，只有下一轮用户恢复后再执行上述步骤。
