目标：在现有入排审核系统中，以最小完整改动建成“内置前置方案Agent→完整规则共同发布→一名受试者完整当前节点审核→可回原件报告→更正/补证后新审核”的多Agent业务闭环。面向多个临床试验方案，不写病例或项目特例。

工作树：/Users/smkzw/Documents/康哲项目资料/AI/入排/enrollment-review-app/.worktrees/phase5-clinical-facts-profile
现有任务：.trellis/tasks/09-11-e2e-eligibility-review
现行交接：ENROLLMENT_REVIEW_AGENT_RECOVERY_V3_20260917.md；取代V1/V2现行建议。比较附件SAR_PROTOCOL_THREE_MINUTE_COMPARATIVE_REVIEW_20260917.md按需读取，不将其中病例/方案值写进通用代码。

最新决定：
1. 前置方案解构/审核Agent必须集成产品内，相对独立运行。用户只需上传正式DOCX和现行修订，不将手工调用外部AI生成JSON作为主路径。不为此前提案另建公共导入Schema或外部运行平台。
2. 产品多Agent按职责和版本化产物协作；不等于每一步都需要不同模型，也不等于开发Agent可越过本地Trellis inline派发规则。
3. 正式取消所有受试者事实必须经过两个完整VLM一致读取的首版硬要求。主OCR/可靠原生文字常规读取，一个本地VLM按需核对区域，人工有据确认可成为真实可用来源。不可伪造A/B或旧方法批准。
4. 保留FastAPI、React、SQLite、JobRunner、原件/快照、规则和事实/Profile、确定性求值、行动与冻结报告。不另建Agent框架、任务队列、规则引擎或第二套业务真相。
5. 研究者CS/NCS/QTc便签是真实资料；姓名和人口学遮盖是常态。不恢复遮盖内容；临床所需但缺失属性单列。读取已有报告/评分，不独立诊断波形、重新判图或重新评分。
6. M5 Max 128GB，质量优先。已发布规则下每例尽量≤30分钟，非硬截断；方案首次/修订和首次全流程总耗时分别记录，不隐去成本。

方案Agent实施：
先追正式上传/工作台→两类方案执行器→源文/分包→提示/Schema→语义草稿/水合→核对/局部修订→共同发布的真实调用链。已有自适应分包、父条款分段、检查点复用不得再从零建设。

复用DOCX结构来源和全文处置；全局研究/节点/定义语境有原文支持。每包包含完整语义及必要引导语、表头、脚注、例外和跨章定义，模型可以有界请求补取。拥有写入范围与可读取来源范围分开。关键词只用于找候选，不决定删除来源。

合法的有源语义候选一经保存即通过现有工作台显示，之后在同一对象上核对/装配/发布。不新增一轮自由Markdown摘要或另一套草稿权威。条件/例外只生成一次，判断点、通过/不通过、速查从同一内容投影。首个预览不是整份规则发布；三分钟只作学习方向，不是硬时限或当前实测。

原编号检查必须核研究部分、原父子范围及内容映射，不只核总条数。时间逐条件保存实际锚点，不全局改成随机日。核对者检查否定/例外/未知、研究者归属、测量与聚合、机构/有效期和附录效力；候选新增细节先回源脚注，不预设错误。源文冲突/编辑残留单独澄清，不以常识代修。优先当前真实依赖，不扩展未来所有逻辑组合。

官方解构和跨章控制共享已冻结语境，各保留现有不同来源族合同。模型生成作者语义，不填哈希、数据库ID或正式发布证明。短来源引用由程序映射。首次/紧凑/普通/修复输出与领域必填保持一致，不靠反复模型调用修宿主Schema错误。

增加或复用一个方案核对职责：原文→要求查遗漏，要求→原文查误解；包括被标为非相关的来源、定义和脚注。只返回实际核查范围及有出处的问题，不再重生成另一份全规则求交。不同角色同模型不冒充异构独立证明。

内容修订只替换受影响完整小对象，保留合法兄弟。共享定义变化传播到实际依赖。最多两轮且同问题无新增信息提前停止；服务失败、缺上下文、临床歧义、执行能力不足分开。模型不按多数投票改临床要求，不伪造缺失时间或来源。

保留全部官方/跨章/流程要求；现有RuleSet/RuleModelRevision/控制目录同一事务发布。原文不明仍保留未决，不用人工标签绕过来源或完整性。含义明确但不能自动执行的要求在同一体系明确人工处理，不填恒真条件或删项。

资料与审核实施：
保留original-page-images/v1原图准备，在其后接新读取。复用现有OCR、来源/定位、模型传输、装卸和恢复，不只改当前不生效的旧pipeline。

用一个有来源的读取清单连接OCR、局部视觉、人工确认与既有事实发布；同步来源合同、提示/修复、locator、门禁及消费者。不能仅关闭require_page_review，不能构造假双源。

视觉转录不携完整ClausePack。处理同页多报告、检验行列、手写/评分、跨页否定和药名连续来源。区分原始读数、可用字段和未决；临床来源数量不等于模型读取次数。

按对象/属性/时点/来源显式绑定谓词；类型只找候选。数值/单位/时间/逻辑由现有代码计算；合并同一原文重复的语义核实，复杂真实未决保留具体原因。累计Profile不能只取最后run。

执行与交付：
P0核调用链/配置/完整规则，并完成P0.P内置方案最小完整增量；已有合格规则时P1/P2资料链可交错推进，但仅旧规则结果不构成本轮完成。
P1一份真实报告→可回源事实；P2整例全部当前资料；P3必须接内置方案实际发布的规则→逐条审核/行动/报告；P4更正或补证→新审核且历史不变；P5集中核查另一资料/另一真实方案、质量、时间和可操作界面。

按本地Trellis Codex inline更新现有prd/design/implement和现行设计/Plan。JSONL占位不是inline阻塞。Ponytail用于复用和最小完整改动，不能省掉用户已要求的前置Agent。一个所有者集成共享接口，必要独立审阅按现有批准机制，不每个小函数开会商。

保留必要编译和最小关键行为检查，尽快在隔离副本通过正式入口试跑，不等所有遗留测试全绿或未来复杂表达式实现后才运行。完整路径后集中验收；不新建庞大测试工程或模型档位矩阵。

完成必须有：内置方案真实产出并共同发布；全部当前资料/要求有处置；有依据的有效结果能产生，非全部UNKNOWN；问题可回正确原件；人工更正/补证后只重算受影响范围，累计事实和历史报告保持。未做新方案验证不声称泛化已证明。

不把源码/Schema/哈希/编译/模型一致/文件存在当作医学正确或产品完成。保留未提交工作、原件、旧结果和失败回执。普通技术问题直接解决；只在用户暂停、真实需解释决定、无法自行解决的阻塞或实际完成时停止。不重复询问已明确的产品方向。
