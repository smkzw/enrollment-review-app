# Q2 无损暂停点

更新时间：2026-09-22（Asia/Shanghai）

## 当前结论

W0、W1、W2、W3、W4、W5及Q1、Q2已经完成。当前没有正在运行的应用任务、临床任务、模型调用或测试进程。原始方案、病例资料、临床数据库和环境凭据均未改动。`claims_complete`仍为`false`。

本暂停点不是全产品完成。尚未开始W6真实全链、W7留出/宽屏/部署及Q3最终验收。

## 本轮完成

1. 工作台改为“待处理/全部条款”单列表，保留筛选、显示数量和条款深链；右侧原件区明确区分引用原文与浏览原件。
2. 新资料默认使用可靠原生文字或OCR，只有低置信、复杂版面、提取异常或确实缺字的页进入局部视觉核实；所有风险页分批处理，不因单批上限被丢弃。
3. 更正事实后，旧审核准备、旧审核续跑和旧工作稿均失效；新审核必须基于当前病史重新准备，历史结果保留。
4. 模型可用性已纠偏：OpenCode旧400源于缺少稳定会话头；产品直连补齐后MiMo/DeepSeek最小调用可用，Muse当次为上游503。当前Ollama主路由缺专用凭据，产品按供应商隔离拒绝借用其他密钥。上述只证明最小通路，不等于真实病例质量验收。

## 集中验证

- 前端生产构建：通过。
- 工作台与原件组件：21项通过。
- 选择性视觉、事实规范化、更正、任务恢复、审核投影等后端跨层检查：224项通过。
- 病史更正后旧审核失效反例：2项通过。
- `git diff --check`：通过。
- 未运行真实模型、真实临床Job或浏览器Q3验收。

## 踩坑与反思

1. 旧测试把“单次最多处理几页”误写成“总共只处理几页”。这会静默漏掉后续风险页，已改为全部保留、按批处理。
2. 旧视觉任务夹具虽然声称是纯图片，却带有足够长的OCR正文。新规则正确跳过了模型，导致测试误报。夹具现改为只有不足以采信的一字符结果，同时继续验证OCR原文与哈希不被视觉处理改写。
3. UI测试仍假设旧的双列表和默认全量展示。已按当前“先看待处理事项”的懒惰用户路径更新，避免为了测试恢复信息冗余界面。
4. 最小端点成功不能推导真实长提示、图片理解或临床质量可用；这些必须留到W6真实全链，不再用端口或短问答替代验收。

## 下一安全动作

从W6开始：先读`delivery_20260922/00_START_HERE.md`、`03_PLAN.md`、`04_WORK_PACKAGES.md`中W6和`06_MATERIALS.md`，核对当前HEAD、dirty状态、资料与服务归属。随后从内置DOCX入口新建方案并发布规则，使用一例新病例完成当前节点审核、工作稿、报告、一次更正/补证和受影响范围重算。不得使用旧规则代替最终方案发布，不得把最小模型探针当临床验收。

W6完成后才进入W7/Q3：第二结构/第二病例留出、1080P/2K/4K Ego Lite实操、性能、启动关闭、备份恢复和交付清理。
