# 可直接发送给 fork 的持续实施指令

你接手本入排审核系统的0922V2后续完整交付，开发会话使用用户指定的gpt-5.6-sol、reasoning effort=medium。先核宿主实际模型；若不符如实提示，不能靠文字宣称切换，也不因此修改产品模型。

唯一工作树：/Users/smkzw/Documents/康哲项目资料/AI/入排/enrollment-review-app/.worktrees/phase5-clinical-facts-profile。
现有任务：.trellis/tasks/09-11-e2e-eligibility-review。
首先读取该任务delivery_20260922/00_START_HERE.md、02_EXECUTION_RULES.md、03_PLAN.md，以及当前prd.md、design.md、implement.md。仅在涉及相应改动时读04工作包、专家条目及V3章节，不重读全部历史。核HEAD/dirty、原件/库/服务归属；本包基线为e7f34d0508c05481164cf13569f66ddf64e2e74f，后续变更应保留并先对照。

你的目标是完成W0–W7和Q3完整验收，不是给计划、修一个文件或跑出一个全未决报告。正常顺序W0→W1→W2→Q1→W3→W4→W5→Q2→W6→W7/Q3；外部模型故障不阻止其余离线工作。每包完成自动继续，不用“继续推进”收尾代替执行。

重点：
1. 保留内置前置方案Agent和同源草稿预览/核对/共同发布；格式错误不是研究者医学判断，不静默改临床含义。
2. 资料默认主OCR/可靠原生文字+局部核实，真实来源贯通现有事实体系；不恢复全页强制双VLM，不复制A/B记录，不仅关闭旧gate，不凭policy字符串获得采信。
3. 复用整条件资格/选择/求值；工作稿和正式报告同证据语义，签发授权独立。拒绝/未核/陈旧不能回退类型猜测。官方与跨章要求完整。
4. 更正以真实观察与派生链为依据，补证失效范围性检索，保留历史与累计Profile；同页不是同观察。
5. 改正式中文宽屏界面，原件滚动+有依据标注、稳定问题排序、正确版本和返回上下文；用Ego Lite做最终真实交互。
6. 新方案、新例完整当前节点、报告、更正补证与第二结构留出验证完成，启动/续跑/备份恢复/交付清理不遗漏。工作稿可用不等于已签发，不伪造金标或批准。
7. 先读10_MODEL_AVAILABILITY.md：当前Ollama路由缺独立凭据而会取GLM key；OpenCode产品请求漏会话头，隔离补头后MiMo/DeepSeek文字与合成图可用，Muse本次503。不要继续说所有模型不可用，也不向错误服务发密钥；正式业务可用性仍需产品单页与使用范围核实。

遵循最新全局AGENTS和Ponytail，复用已有Job/服务/合同；禁止另建通用框架/队列/资料库/展示壳，禁止针对SAR或药物阈值硬编码。产品调用由产品harness直连独立端点，个人CLI只可工程执行/会商，不替产品读病例。

验证采用完整功能包批量实现和Q1/Q2/Q3集中窗口，不每改一处跑测试、不每包全真实重跑、不以全库追绿推迟真实交付。允许必要的导入/危险写入前校验与一次最小provider诊断；同失败两次无新证据改假设。输出预算按角色/实际能力给足，不全角色统一65K、不截断当完成。

持续工作直至完整交付，只有用户暂停、真正必须用户决定、所有可推进路径均被不可自行解决的外部条件阻塞时才停止；额度或宿主强制结束时如实保存可恢复记录，绝不宣称完成。进度仅合并到implement.md，实质里程碑记录来源/产物/调用/消费/入口及未决。交回按08_RETURN_TEMPLATE.md。

不自动commit/push、不清理他人变更、不启动未知归属服务、不使用泄露凭据。必要执行/会商按当前批准机制；120分钟完成等待，不无故重派。全部本地模型由单一所有者串行装卸，卸载完成后才加载下一模型。

本文件是Goal候选正文，不等于已创建/恢复运行时Goal。已有Goal只能更状态时，不用complete/blocked冒充修改目标；若已有目标指向文件可在本轮范围内同步并核实，按工具真实能力记录。无需用户重新批准已明确产品方向。
