# 当前实施记录

更新2026-09-24；唯一当前进度。旧记录完整保留history/20260922-pre-review/implement.md，不在历史日志继续堆“最新”。

## 0924V1 当前实施（进行中，非验收）

- 2026-09-24 本轮收尾：v79 原始回执再核为 208.70 秒、两轮输出、22 条来源陈述；覆盖账为完整表达 2、仅同单元候选关联 5、仅链接目标 1、未定位 14。逐字匹配只是诊断，不足以宣称已有目标在动作、时间及例外方面覆盖同段混合要求；正式 W6 不据此发布。正式双击启动脚本的端口探测已修正，未显式配置时的模型默认值与 `.env.example`/现行产品配置对齐，未触碰本地密钥。集中定向回归 157 passed，脚本语法、`git diff --check` 通过；未做全库、真实病例或浏览器验收。原始诊断留本地 `artifacts/0924v1-w6-v79-first-batch/result.json`，不提交临床摘录。当前要求无损暂停；接续时先核同单元多要求的逐项来源闭包，再新建合法隔离作业，不续用失败/取消的旧任务。`claims_complete=false`。
- 2026-09-24 v79 冻结首批对照：只读校验 v67 失败作业 `fba86f5c8e65480a8009802ccd949220` 的确定性收口快照，直接通过产品控制 Agent 与 `cms-router/deepseek-latest-cloud:high` 身份预检，未启动旧/新临床 Job，也未发布。首轮仅一项日期属性缺失，第二轮69字符的定向补答后通过**当前**结构及批次候选门禁，总耗时约208.7秒；回执含所有原始响应及哈希 `artifacts/0924v1-w6-v79-first-batch/result.json`。来源清单有22条、12个 owned 单元；逐条字面账只有2条在候选义务中完整表达、5条同单元关联但未完整表达、1条仅链接已知目标、14条未定位（多数为本期排除或给药后单元）。特别是同一候选来源中的知情同意和导入后再核资格两项仍仅同单元关联；冻结目录虽有 IN-07、签署知情同意和两节点审核入排的流程项，当前候选未为这两项建立逐要求覆盖关系。结构通过不能证明该关系，也不能擅自另造重复要求。**不能据 v79 诊断发布。**下一步以来源陈述为单位核已有目标的动作/时间/例外覆盖和剩余增量，不凭标题相似或候选存在标绿；然后再从合法入口运行全批。`claims_complete=false`。
- 2026-09-24 v73/v74/v76/v77 同一冻结首批的隔离诊断：v73 约425秒，初答结构错误，后续分别为未来禁止提前判定、给药后事项错放当前阶段、无源审核动作；v74 有源陈述单独调用约66秒，但把两个不连续时间短语拼成一个假引文；改为逐段连续原文数组后，v76 约48秒得到29条有源陈述，仍把“每日一次”等用法误当跨期时间范围，因此撤销粗粒度阶段归属字段。v77 完整调用约338秒，首答把后续持续义务挂到非禁止类给药原子，还用“不可调整背景治疗”的摘录支持继续给药；第一轮被送入冻结后续义务字段的单原子修订，必然失败；第二轮整候选修订仍未纠正。原始回执分别在 `artifacts/0924v1-w6-v73-first-batch/`、`...v74-source-only/`、`...v76-source-only/`、`...v77-first-batch/`；均未发布。现有源陈述只保留逐字段落原文，不充当阶段判定，时间措辞必须出现在该陈述逐字摘录或所属标题，防同段另一动作的期限被借用；单原子修订仅处理其准许变化的求值/时间结构，后续义务错误转至候选级并明确要求原文支持，不自动删改临床含义。提示 v2.59、来源提示 v6、执行 v79；深审/传输/执行/作业与启动脚本聚焦回归157项通过。把 v77 的25条历史来源陈述仅替换版本字段后，在只读冻结批上用新校验复核通过；这**不是**v79模型响应或临床正确性证明。此修订只排除一条必败重试路径，**未证明**模型能正确解构复杂时间义务；W6、W7、Q3仍未完成，`claims_complete=false`。下一步先做有源语义到现有结构的有限装配与双向核对，再进行合法新作业；不得以同源诊断通过代替发布。
- W7 启动准备发现并修复正式双击入口的端口探测使用不存在函数，现直接检查监听占用；脚本语法和相邻启动入口定向测试通过。尚未真正启动、停止或备份恢复正式服务，不能算W7/Q3验收。旧路由断言大套件11通过/1失败，其失败为测试仍要求早期智谱默认，而现场为CMS线路；未因本修订改生产配置或追绿。
- 2026-09-24 冻结首批 v72 失败的两项义务缺独立核对日期属性。现仅对“同候选所有结构错误都恰为缺该属性”的结果启用短修复：独立产品请求只见需修原子及其直接摘录，返回每个位置的 `date_range`、`record_time` 或 `unresolved`；程序按授权位置回填，重验整个原输出，不允许改其他字段。原文不足则终止并保留未核实，不转全候选猜答。提示 v2.56、执行身份 v73，相关深审/传输/执行/作业集中回归 155 项通过；尚未进行 v73 真实模型调用。v72 后续七天被误借给另一条禁令的语义缺陷不由此修复，W6/W7/Q3 和 `claims_complete=false` 不变。下一步按专家包把逐条来源语义解释、确定性装配与双向核对接成可验证纵切片，同冻结原文对照，勿重复同类整批补答。
- 2026-09-24 v71 冻结首批受控诊断及失败证据保全：预检产品直连为 `cms-router/deepseek-latest-cloud:high`、预算 65536，身份探测回报同一模型；用 v67 失败作业中不可变 `deep_plan.batches[0]` 经产品 `ProtocolControlAgentRunner`、现有完整输出校验做一次隔离小样，未启动/续用临床作业。约 189.8 秒后仍为“需要核对”：初答三个义务原子缺观察采用说明；局部补答后暴露首原子时间窗同时填两套上界；整候选补答又缺观察采用说明。回执位于 `artifacts/0924v1-w6-v71-first-batch/result.json`，标记非临床验收、不发布。该次诊断脚本退出前只存已有 attempts/hash，不能还原原始无效响应，因此不能断言最初时间字段何时产生。为修这个真实取证缺口，现有 JobRunner 失败提交可附非成功诊断检查点；W6 深审失败只保存该批每轮原始模型输出、hash、错误类、模型/提示身份，不携密钥；成功回执格式不变，失败态/依赖阻断不变。执行身份 v72，工作流/深审聚焦回归 175 项通过（其中一条旧测试曾把未注册任务误期望为可领取失败，已按现行注册范围修正）；未再调用模型验证 v72。W6 仍未发布，`claims_complete=false`。下一步使用保全的原始输出，在同一冻结复杂小样上核清时间与观察语义的具体缺口，设计有源语义解释→有限确定性装配，而非继续追加全候选随机补答。
- v72 冻结首批第二次隔离诊断约260.6秒，仍未通过，原始三轮响应及 SHA 留在 `artifacts/0924v1-w6-v72-first-batch/result.json`（不是正式 Job 检查点、不是临床验收）。初答两项有时间约束的义务缺独立日期属性；第一次整候选补答将后续持续义务与当前原子直接来源拆散；第二次补答虽补结构，却给“筛选/导入期不允许调整背景治疗”的义务填7天上界，而该原子自身摘录不含7天时长，触发 `TIME_CALENDAR_BOUND_UNSUPPORTED`。三轮原文哈希已复核。这里不能由程序从相邻义务偷取7天，也不能把未来双盲期禁调提前判为当前满足。需要在有源语义解释中分别说明当前审核截止范围、命名期间与跨陈述时长依赖，程序只装配已明确且有对应出处的时间语义；相同冻结批不再无据重复整候选试跑。
- 2026-09-24 资料来源字段局部修订与覆盖诊断纠偏：首轮完整输出若仅同一候选的一项 `minimum_evidence.source_policy` 结构不合规，产品在原会话身份下发独立短请求，只允许返回该字段；程序验证每对来源位置/逐字摘录属于该候选授权原文，再合并回原输出并执行完整结构、来源、发布门禁。其他证据、义务、节点及候选均不可由该短请求改写。缺来源仍失败，不猜测。另将有源陈述“已表达”限定为完整陈述出现在相应义务/持续义务摘录，局部摘录重合只记候选有关联；空白陈述拒绝。诊断另列已知官方/流程目标是否完整包含该陈述的逐字摘录，但不以此自动补关系或宣告语义覆盖。冻结首批实证中签署同意与导入后复核只有备注声称由流程目标覆盖，流程目标摘录仅为项目名称，仍需逐陈述核对。提示身份 v2.55、执行身份 v71；聚焦深审/传输/执行 140 项通过。此为合同与诊断纠偏，**未证明** v67 的节点错配已解决，也未进行 v71 真实完整 W6 作业；既有失败批次和 `claims_complete=false` 不变。下一步仍以冻结来源核对多义务、时间及目标关系，形成有源语义解释到结构成形的最小闭环，再做一次受控真实对照。
- 2026-09-24 来源双向核对纵向切片：复核独立会商所指三项合同死结，当前代码已将基线通用/特例混合、访视安排/结果有效期混合列入有界候选重分区，并对原文未来期间却被当前确定性判定的情况拒绝；不重复补丁。现将有源陈述与最终候选的直接原子摘录/同单元来源片段作字面对应，成功回执逐条保存“原子已表达/候选有关联但原子未定位/仅链接已知目标/尚未定位”的诊断账，并保留处置类别；它不推断医学等价、不补临床事实、不替代既有来源/发布门禁。因成功回执结构新增该账，执行身份升为 v64；已运行的 v63 隔离服务须重启才会生效，旧失败结果不能越版续用。合成反例及深审模块集中检查 `90 passed`。只读回看同一冻结首批历史 v54：17 条有源陈述中 8 条标为必做，4 条所属单元有候选；其中 2 条虽属候选单元，却未在候选原子摘录中逐字对应。它们不能凭“候选存在”宣称已覆盖，也不能仅凭字面未命中断定医学遗漏。下一步对这两条及同段时序、例外作来源级临床核查，再决定发布阻断条件；不能把只链接官方/流程目标视为已完整表达额外约束。
- 2026-09-24 局部补答纠偏：观察采用说明的提示现在只提供需修原子及其成对直接来源，不再同时附上整段可误引的周边文字；直接来源不足时仍用未核实而不造选择策略。提示身份 v2.52、执行身份 v65；v63 隔离服务已在审计库无活动作业后正常关闭。深审模块 `90 passed`，相关局部补答 4 passed；传输广套件有 `9 failed`，多数是旧MTPLX装卸环境/默认路由断言，另有旧GLM凭据预期，不计为本轮通过，也未为了追绿切换产品模型。真实受控批次结果见下一条，不能宣称 v63 的三个错误或 W6 已经全部解决。
- 2026-09-24 v65 隔离真实反证：由产品正式 API 新建 `6313960d59ce4d7e9152ccb072a9eab1`，同一冻结来源、同一隔离库、无旧结果混接；约3分半后 `deep_0001=failed_final`，未发布目录。末轮诊断不再是观察摘录错配，而是 `RECOMMENDED_MODALITY_DROPPED`、`FUTURE_PROHIBITION_DECIDED_EARLY` 与 `PROCEDURE_AFFECTED_STAGE_MISMATCH`。这证明局部来源提示不足以稳定解决完整语义/节点装配；继续同法整批重跑无新信息。产品门禁正确拒绝，`claims_complete=false`。下一步应以此原始回执定位三个错误对应的候选/原文/节点，设计仅修该候选语义且不改非目标来源的局部成形合同；旧作业保留失败态。
- 随后尝试为上述三类错误增加短指引，集中检查立即发现与现有更精确的指引重复，且遮蔽了同阶段/跨阶段差异；这部分新增已完整撤回，提示/执行身份仍为 v2.52/v65，深审模块 `90 passed`、`git diff --check` 通过。问题不在“缺一句提示”，应把有源陈述、候选义务与目标节点分开核对后再定修订范围。
- 对已通过完整结构/来源解析、仅一个候选被发布门禁指出的问题，产品传输新增短上下文候选修订：只发送该候选原稿、授权原文、冻结目标及具体错误，返回仍限单候选 Schema；原会话历史保留供其他受限回退，系统回填后照常验证全批与发布门禁。初次结构无效、来源新增、多候选重分区不启用此捷径。提示 v2.53、执行 v66；深审模块 `91 passed`、相关传输 `3 passed`，尚未证明真实模型节时或质量改善。下一步同冻结首批只做一次 v66 受控对照。
- v66 隔离作业 `7fcc3e583e334d21ac5b696be0cfa364` 从合法入口完成前6个深审批次，在 `deep_0007` 终止（92/148 步，未发布）：当前阶段禁止要求被遗漏，来源限定补入虽启动，但新增原子的 time_constraint 与 evaluation.time_purpose 不一致，故 `SOURCE_INSERT_INVALID`，没有采信该候选。不能仅凭前6批通过归因于短上下文，亦不能用同法重复整批生成。现将已授权来源补入也改为独立短请求，保留原批历史与失败范围，明确时间约束和核对用途一致；提示 v2.54、执行 v67。深审模块 `91 passed`、相关传输 `3 passed`、差异检查通过；待一次受控真实对照。
- v67 真实对照 `fba86f5c8e65480a8009802ccd949220` 在同一隔离库从合法入口新建，约5分半后首批失败：`PROCEDURE_AFFECTED_STAGE_MISMATCH`、单候选补答新增非法 `source_excerpts` 字段，以及来源补入缺少原文定位。没有目录发布。短请求两种变体均未在真实作业证明稳定或节时，已撤回本轮新加的独立单候选传输、原稿拼接及其专属测试；保留已验证的有源陈述诊断和观察摘录局部修订，当前提示/执行身份回到 v2.52/v65。深审模块与传输合跑 `114 passed / 10 failed`，10 项为旧默认路由、旧凭据预期和未启动 MTPLX 造成的传输测试前提失败，不计通过；`git diff --check` 通过。下一步以已冻结原文和失败回执核对“每条有源陈述→候选义务→节点/时间/例外”的对应，不再重复整批随机试跑；未取得完整来源闭包前保持 W6 未发布、`claims_complete=false`。
- 来源覆盖诊断进一步区分“原文只出现在适用/触发/例外”与“作为义务或后续持续要求表达”：必做、禁止、建议陈述只有直接义务/持续义务摘录对应才标为已表达，同时保存命中层位；单元有关联但未表达仍待核，不因候选标题或触发文字出现而假装覆盖。它仍是诊断而非医学等价裁决/自动发布门禁。回执字段变化将执行身份升为 v68；深审模块 `90 passed`，没有重跑完整真实方案。下一步在同一冻结小样核查这类待核陈述是否已由官方原文直接覆盖，建立可验证的有源局部补入/结构装配后才尝试 W6 新作业。
- 对首批冻结来源只读交叉核查：旧成功小样中同一混合来源单元的“签署ICF”和“导入后重新核查”没有直接义务原子；当前冻结目录确有 IN-07 及签署知情同意流程项，也有筛选/导入期和基线期各自的“审核入选/排除标准”流程项。旧处置仅在 notes 中宣称这两条已覆盖，结构化关系为空，因此不能以候选存在或目录同名证明来源要求的动作、时点均已表达；也不能把它们未经医学核对直接判为新候选遗漏。该问题作为逐陈述“来源→现有目标/增量候选→覆盖维度”的下一项合同工作；原件、数据库和旧回执均未改写。
- 传输回归维护与实质分类修复：原测试仍把旧智谱直连当默认、要求假 MTPLX 客户端先启动本地模型，并使用早于现行求值/证据合同的夹具；这些前提已按当前独立路由和纯假客户端边界修正，未改生产模型选择。产品来源解读在**未收到响应**时现记传输失败，收到不合规响应才记结构失败，避免将服务故障误导为方案资料问题。深审+传输聚焦回归 `124 passed`；这不是 W6 真实发布或 Q3 临床验收。
- 工作台来源缺失显式呈现：正式条款或跨章要求没有原文摘录时，详情写“方案原文定位待补齐”，不把系统解构文字伪装为方案原文，也不在前端改医学判定。工作台组件 `16 passed`、正式前端构建通过；仅界面合同回归，尚未做 Q3 真实浏览器验收。
- 2026-09-24 W6 v62/v63 隔离反证：`e986aaa2367e4280b42234df2bbff547` 和 `67bacf43d4504855ad88b977e1e3664e` 均从合法入口新建、复用同一冻结发现来源，均在 `deep_0001` 终止，没有发布目录。v62 末两轮为无来源审核动作与同候选四个义务均缺观察采用说明；v63 末两轮转为求值来源错配、后续禁令提前判定及观察政策摘录不属于原子。二者分别约5分钟，不证明模型全部不可用，也不支持再用相同完整请求重试。隔离库、来源、旧失败作业均不变，`claims_complete=false`。
- 本轮工程修订：仅在单一受损义务原子时从 Pydantic 结构化错误定位，不解析中文报错分隔符；单原子补答冻结其原句、强度、时间与来源，独立短上下文发送，原会话保留供有界回退。多原子仅缺观察采用说明时允许只返回各原子的观察政策，代码只替换这些字段并重验完整模型；无授权位置或来源不符仍失败。当前提示 v2.51、执行 v63、门禁 v26。另修复“已有其他候选却未覆盖禁止句”被跳过的问题；官方/流程同文覆盖必须有明确链接及同源片段，不能只碰巧文字相同。相关深审测试 89 项通过，传输/深审定向集合 113 通过、10 个旧环境断言排除；门禁新反例 8 通过。广门禁套件另有27个旧夹具缺现行求值规格的失败，尚未整体清偿。格式与通过数均不代表医学或全文验收。
- 下一步停止同法新建整方案作业。以首批冻结来源和 v54 曾通过的历史结果作只读对照，不把旧结果跨版本发布；确定“来源解释→结构装配→双向来源核对”最小可行产物及消费点，先核同段多义务、时间分期和观察政策，再从合法新作业运行。随后仍需同源官方+跨章目录、5份24页病例、工作稿/报告、更正补证及 Q3。
- 2026-09-24 新一轮反证：v58 `c8640ed233a3444b907aaf633b6f6415` 第8批访视表失败；v59 `bb11cde1c7a84567ac4731f0cc94cdb9` 首批遇502/流中断。OmniRouter本次断流回执为499、`request_signal_aborted`、无最终用量；同段有另一条超长DeepSeek会话，拥堵只是可能因素，非已证实根因。空闲时从合法入口创建同版作业 `f38b5d026d3d495cae6d7384774a5102`，前7批通过，第8批仍失败，具体是把“在基线访视完成随机分组”误作持续期间和回溯时间锚点。现区分无相对时窗的访视操作与真正前后/持续/洗脱约束，门禁v25、执行v60；冻结原句和“基线访视前4天内”反例的聚焦检查通过。新合法作业 `b5f0c4d1b4094fc485bb6d6354068026` 又在首批失败：无来源动作、流程受影响节点错配、时间约束用途矛盾。两次不同失败证明访视修正不足以稳定整方案，已停止同法重复新建作业。所有失败终态和成功批次保持原样，无目录发布，`claims_complete=false`。
- 下一实质步骤按0924V1专家包，在现有内置方案Agent中试一个有源语义解释→确定性装配→双向来源核对→原门禁的纵向切片；先用首批冻结输入与同结构对照证明语义/时间/节点不丢，再决定是否替换既有深审输出成形。禁止硬编本方案阈值、药物或访视，禁止把格式错误转成研究者裁量。此工作不改变W0–W5/Q1/Q2既有结果，也不代表W6、W7/Q3完成。
- 2026-09-24 受控 W6 续试：v54 `b101652ca2804c519902421866d78873` 首批失败；v55 `dfe0313f268e4b758dc139c36bc66422` 完成前9批，在访视表第10批失败。冻结表格项目行仅有压缩的勾选标记，旧只读上下文缺同表前置访视行；程序现把同表前五行作为只读语境，不把它们变成候选。v56 同一批直读约270秒，首轮输出完整但某候选访视关系错，随后的整批修复丢11项/1项处置。现将访视关系错误定位到具体候选，仅允许候选级修订且系统保留其他条目；聚焦深审回归85项通过。
- v57 `3eba2fe444a44f219595dbe1facc321f` 在调用模型前被深审上下文合同拒绝：旧合同不接受同表前置行。已限定合同只接收完整来源清单内、同一表格、行序0–4的只读语境，其余额外上下文仍拒绝；新 v58 `c8640ed233a3444b907aaf633b6f6415` 从合法入口创建，在隔离库复用已核发现，开始深审，观察时前4批完成。不能以此宣布全文、医学或正式发布合格；旧失败作业原样保留。相关聚焦测试101项通过、1项既有提示长度<30000断言失败（实际约50773字符）；前端生产构建通过；`claims_complete=false`。
- 接下来观察 v58 全部深审批次与水合/发布门槛；失败以具体来源和回执重新定因，不通过随机重试、删候选、手工补临床答案或放松门禁求绿。通过后仍须同源官方+跨章正式发布、5份24页病例事实/Profile/工作稿/报告、更正补证及Q3真实界面验收。
- 2026-09-24 新证据：v52 作业 `8fc45d1c9bb9476e9e21eeea4cd6da7d`、v53 作业 `b22a19f1dde04343ad818241989a3449` 均在 `deep_0001` 失败，已停止专用服务；前者同时出现未来禁令提前判定、节点错位及时间字段冲突，后者出现建议语气无来源、求值字段冲突与观察选择缺失。不能继续声称 v52 正在运行，也不能用规范化一处字段解释全部失败。
- 同一冻结首批（12 单元）的 v54 隔离小样：产品方案 Agent 先生成有源陈述清单（单次约 57 秒、27 条、逐条摘录定位通过），再按现有完整结构与批次门槛执行，约 245 秒、17 条陈述、12 条处置、1 个候选，首轮 `已解析`，无结构/批次门槛错误；原始产品回执在 `/Users/smkzw/tmp/enrollment-review-w6-20260923-v35-deep4-01/v54-first-batch-diagnostic.json`。两次陈述数量不同，显示模型输出有随机性；首批通过不代表临床完整性或整方案可发布。清单不产生独立规则身份，原文和最终发布门槛仍是权威。执行身份 v54、提示 v2.47；合同聚焦回归 145 passed。
- v54 小样之后的正式运行及 v55-v58 现况见上方；不得跨版本复用旧失败深审，不得把单批成功或 `publishable=true` 宣称为正式发布。完整方案、病例、更正补证、Q3 均未验收，`claims_complete=false`。

- 工作方式：本轮由任务所有者内联完成相互依赖的 W6 修复与来源展示，避免在共享工作树、同一隔离库并发写；实质临床解释仍以原件和产品回执为准，若最终主观验收存在重大歧义再按批准机制独立会商。0924V1 专家包是审阅证据，不是新的临床事实或发布批准。
- W6 合同：无候选但原文有当前禁令时，可按冻结单元和来源范围新增候选；支持的产品传输只补写单候选，旧候选与范围外处置保持原样，补入后仍跑完整水合和发布门禁。标题/表头承载阶段、正文承载禁止，以及同段已有逐字绑定的一条候选却漏另一条，增加保守拦截；正则只作明确漏项提示，不代表全部语义覆盖。身份升为执行 v52、提示 v2.46、门禁 v23。新修复不从代码制造医学结论。
- 路由与界面：产品逐页双读道若角色显式地址/凭据与 CMS 路由冲突，现在拒绝启动而不暗改实际去向；审核工作区的控制要求显示为“系统解构的审核要求”，真实原文摘录经正式控制原子→审核投影→API→页面独立展示，无真实摘录时不显示伪原文。现有列表标题、分页与问题队列保留。
- 一次合并检查：W6/执行/候选分区及不含旧失配夹具的门禁测试 245 passed、30 deselected；新增来源哨兵 7 passed；审核投影/API 26 passed；工作区 16 passed；前端正式构建通过。完整旧门禁套件仍有 29 项因夹具缺求值规格失败；旧页级套件仍有 4 项流式模拟或过时默认路由断言失败。上述通过不代表临床发布或 Q3。
- 隔离真实作业：专用 V2 服务 `127.0.0.1:8911` 指向 `/Users/smkzw/tmp/enrollment-review-w6-20260923-v35-deep4-01/data`；此条形成时曾停止，现按上方 v58 状态运行。原三个失败作业只读核对均为 `failed_final`；v52/v53 也已失败。`claims_complete=false`。

## 2026-09-24 W6 深审合同与隔离试跑

- 暂停前核实：三份隔离深审作业 `b55a6c4b11ba4daeada43c5352eefb29`、`7914ce8306304f39b69837e6fe854754`、`1dad32380f22478ca6d657d4331c19b0` 在原隔离库均为 `failed_final`，没有新的正式目录。后续单单元产品诊断也经过两次定向修订仍未得到可发布输出；不能继续把缩批次、增加一般提示或多重试当作已证实的解决办法。需在既有内置方案 Agent 中降低一次输出同时承载的医学解释与结构装配负担，并保留来源闭包和失败状态，不建立平行规则真相表。
- 当前病例读取路线已用产品 `direct_completion` 对**非临床合成红色图片**做最小能力预检：A 请求 `cms-router/glm-5.3-flash:high`，B 请求 `cms-router/deepseek-latest-cloud:high`，两者均返回 `stop` 且回答颜色正确。B 服务回报模型名为 `deepseek-flash`，与请求别名不同；这只证明当前路由能处理图片，不证明上游权重版本或临床质量。未运行真实病例双读、事实规范化或 Profile 临床核对。本机 `.env` 的非密钥角色配置已对齐 CMS 路由；该文件不入库，接手时须从现行显式配置核实，不得把示例配置当生产设置。
- 本次集中定向检查：路由/凭据优先级 6 passed、深审与执行 86 passed；旧页级套件仍含与当前流式读取不符的模拟客户端，停止于 25 passed/5 failed，旧门禁宽套件停止于 91 passed/8 failed（旧夹具缺当前必填求值规格）。这些旧套件不能记为全绿，也不能据此否定上述最小能力预检。`git diff --check` 通过；Q3 和真实临床验收仍未做，`claims_complete=false`。

- 本轮核对两份合法新作业：v48 `7914ce8306304f39b69837e6fe854754` 复用已核发现后完成23批深审，在第24批访视表单元 `failed_final`；v49 `1dad32380f22478ca6d657d4331c19b0` 仅完成首批，在第2批 `failed_final`。后者四段来源混合了给药后剂量安排与“筛选/导入期不允许调整背景用药”的当前限制；失败回执依次为未来义务过早判定、流程访视关系、观察选择和持续义务来源错误。二者都没有共同发布正式目录。用产品 `cms-router/glm-5.3-flash:high` 对同一冻结第2批只做一次隔离直读，约460秒、结构通过但零候选；这会漏掉原文明确的限制，不可用作正式结果或模型替换依据。
- 对明确写出入排阶段与禁止行为、却既无候选也无逐字同源既有目标的结构单元，新增保守的来源缺漏拦截；只报原文待核，不自动生成临床规则，也不把给药后安排算入当前审核。支持“阶段在禁止词前/后”两种句式，空候选和已由同源目标覆盖有反例；门禁/提示/执行身份现为 v22/v2.44/v50。6项定向反例通过，深审器和作业执行 86 项通过；旧发布门禁宽套件仍因多项夹具缺现行必填求值规格而先行失败（本轮止于 8 failed / 91 passed），不得计作本门禁全套通过。`claims_complete=false`。
- 下一步不盲目从头重复随机全方案：先在合法冻结来源上证明复杂批次能同时保留当前禁止行为、后续持续义务与来源/时间/访视闭包，再以新版本作业验证完整发布。旧 v48/v49 失败作业和成功回执保留，不跨身份续跑；正式病例、补证和 Q3 均未启动验收。
- 同一冻结第2批仅取有明确背景用药限制的一个来源单元，由产品 `cms-router/deepseek-latest-cloud:high` 及原门禁隔离判读，约145秒、首轮加两次修订仍失败：先缺数值单位/观察选择，后两次分别把未来期间混入当前判断、再将建议语气误写为确定要求。故“四段混读是唯一原因”的假设不成立；不能继续靠缩批次或增加修订轮数宣称可交付。单段诊断未写回作业、未发布。
- 后续病例路线配置复核发现 `.env` 中第二逐页读取和事实整理仍指向旧 OpenCode 模型，而方案深审已切到 CMS。现将这两角色的非密钥配置改为 `cms-router/deepseek-latest-cloud:high`，并让 CMS 自身凭据/地址优先于遗留角色配置；显式其它供应商路线不改。假凭据路由及缺漏拦截合并检查 8 passed，深审/执行 86 passed；真实病例读取与医学验收尚未运行。`claims_complete=false`。
- 后续同一工作树的集中修订将深审提示升至 v2.42、执行身份升至 v48；旧 v41 检查点不可跨版本继续。修复了局部候选提示同时要求整批输出的矛盾，候选局部修订仍保留其他条目；多候选结构错误可按冻结位置局部修订，身份/来源不一致立即拒绝。产品传输在非严格 Schema 的截断续读不再因索引不存在的 Schema 包装而报错；任务异常恢复路径补齐 `ProtocolControlGateError` 导入。无配置默认模型与现行产品路由统一为 `cms-router/deepseek-latest-cloud`，显式 `.env` 仍优先。基线取值范围及时间约束/求值一致性的定向提示增加了通用约束，未写入本研究评分或药物常量。
- 对冻结第31批（基线评分取值、人口学、病史和治疗史四单元）用本系统产品传输与原发布校验器做一次只读隔离诊断：实际路由 `cms-router/deepseek-latest-cloud:high`，约269秒、三次输出，依次出现基线值关联缺失、条件分支/时间锚点/日历窗问题、时间约束与求值用途冲突，最终 `需要核对`；未写回旧任务，也未发布。相同来源、相同随机路线的重复试跑不再视为进展。冻结目录中确有相应筛选/基线评分检查项，失败应继续从最小来源摘录和候选结构查因，而非代填关系或放松来源校验。
- 聚焦合同/执行套件曾为 `134 passed, 5 warnings`；其后新增提示断言首轮暴露了旧的宽泛基线修订分支抢先匹配，已合并到既有分支，不另增重复分支。包含旧传输夹具的扩展套件为 `158 passed, 9 failed`，9项涉及旧默认 GLM 断言、未配置 MTPLX 装卸所有权或旧凭据报错文案，不能计作当前产品路由通过；新改动仍需一轮最终集中复核。`claims_complete=false`。

- 当前工作树的深审执行身份升至 v44，原因仅为修订/诊断路径变化，旧 v41 作业不可跨版本当成成功任务续跑。相同真实冻结第31批以产品自带 Agent 直接调用 `cms-router/deepseek-latest-cloud:high` 两次隔离诊断，均未写回旧任务、未生成正式目录：v43 路径约175秒、3次回答后报基线关联、条件分支及日历窗问题；v44 路径约196秒、3次回答后仍报基线关联，首次还有多个候选的求值结构错误。两个不同随机回答不能相互当成同一轮修订或速度排名；两次均为“需要核对”，不作为医学结论。`claims_complete=false`。
- 已在既有校验器中一次返回各独立候选的首个问题，结构性候选拆分仍先单独处理；单个候选的初始结构错误只要求模型重写该候选，原处置和其余候选由系统保留。产品传输的 `json_object`/文本通道输出截断时不再误取仅严格结构通道才有的 Schema 字段。新反例在合并的定向检查中 7 项通过；旧全库测试、正式整方案及临床核对尚未进行。下一步不能盲目加修复轮数：要针对同批多候选结构错误降低重写范围，保持原件、候选来源与最终门禁完整，再从合法版本化作业取得正式结果。
- 本轮对冻结来源做只读合同复核，并完成一轮独立只读会商 `enrollment-w6-schema-rescue-20260924`（报告在 `runs/conference/.../evidence_single_object.md`；仅工程建议，未读隔离库原件、未作临床批准）。门禁原先对混合“访视安排+结果有效期”候选无任何合法关系形状，且要求拆开的 `BASELINE_VALUE_SCOPE_MIXED` 不在候选重分区授权中。现以 `MIXED_OBLIGATION_KIND_SCOPE` 明确要求分别表达，两码均授权来源全集守恒的候选拆分；修复提示仅指示保留各自来源，不代写医学事实。门禁 v20、深审提示 v2.38、执行 v41。会商还建议研究期间持有者、字段路径与Schema简化；这些尚未采纳或证明，不能写成已解决。
- 新隔离作业 `b55a6c4b11ba4daeada43c5352eefb29` 从同一 DOCX 冻结来源和已核85批发现合法新建，保留原 v39/v40 作业；首轮至深审第24批失败，原始错误包括日期属性、时间锚点和求值结构；一次合法人工范围重试让第24批通过并继续至第30批。第31批首次因模型给出与冻结单元编号不一致的修订范围而抛技术异常；已改为明确的“需要核对”并保留诊断，不猜测编号、不改写原件，成功检查点保持可重放。该错误处理不改变任何成功输出合同，因此保持 v41 身份。聚焦的修订范围/执行/新门禁回归 62 passed。
- 第31批在修订范围保护后再次合法试跑仍失败，最近诊断为基线值关系缺失、观察选择原文不属于该原子、多个观察选择规则缺失等，任务 `failed_final`；这与旧 v39 同类复杂批次的重复失败形成反证，不再无依据重试。广泛旧门禁套件有29项既有夹具缺当前求值规格而失败（本轮定向 62 项通过，不能等同全库通过）。当前仅30批深审已通过，后续水合/共同发布、病例/Profile/工作稿/报告、更正补证及 Q3均未验收；`claims_complete=false`。下一步须改变深审输出成形与局部恢复方法，先确保来源和审核节点不丢，再从合法新作业或可验证的同版本失败范围继续；不得把失败目录发布。
- 从原始 DOCX 哈希 `075c93b4…` 由正式入口重新登记、确认 III 期并冻结新来源 `03f900e8e8e04cc3b17023a2e2ac218d`；仅在隔离库运行，未生成或发布草稿。此前官方目录冻结未传结构块，EX-06 的部分摘录在页内断开；目录构建现优先使用同一来源的完整 DOCX 段落。新目录 EX-06 对应段落由 62 字恢复到 97 字，原件段落逐字一致；旧目录和失败作业不改。冻结来源任务在 `generate_draft` 按受控准备入口停止，不能称作官方规则已发布。
- 新来源的 v39 发现/单段作业 `6cd88bddb6f74ad8a574f4d6585fa0e0`：85 批发现全部通过，1,927 单元为 398 candidate、7 uncertain、111 context_only、1,411 non_control；首条深审 `failed_final`。同批只读产品模型诊断一次返回治疗期执行、无候选，证明答案有波动，不得把诊断结果补进作业。
- 同来源 v39 四段作业 `08ad1a140f95453cb7540efdaf55c20e` 合法采用以上85批发现；首次深审 19 批完成，第20批因持续期、日期属性和输出形状失败。按持久任务的失败范围人工重试一次后，第20批一次通过，继续完成至第30批；第31批在基线评分取值和病史采集的组合上因条件分支映射、基线值范围及输出结构 `failed_final`。85批发现和30批已完成深审仍保留；水合/发布门禁依赖失败，没有目录发布。
- 曾隔离试行 v40 粗筛提示，作业 `49fe2b5deeb4481b9285080e5deda64a` 完成85批发现、首批深审，第二批失败。候选由398降到280，但逐单元对照发现它错误排除了筛选病史收集、背景用药剂量、给药前检查操作等真正所需的资料；提示和版本已撤回，v40 结果不能用于发布或速度/质量排名。这个反例说明不能以压低候选数量代替临床完整性。
- 此处原记录形成时代码为 v39；候选定向修复及完整来源段落修订当时已在工作树，目标回归曾有113项通过，但全套旧夹具/真实全链尚未通过。第31批在后续 v41 中仍重复失败，现以本节新增记录为准，不再按旧建议继续同法重试；`claims_complete=false`。
- 本日追加核对：旧单段第4批的六份真实回执，按当前 v38 逐份只读重放，最后两份通过完整批次门禁；不是重新生成临床结论。v38 单段新作业 `6a2fc4c94b534685a4170f6643ba6e72` 从合法入口复用85批已核发现，前6批通过，第7批因流程目标、过早最终判定及时间字段错误 `failed_final`。v38 四段作业 `1f2f41612ab14745b9974b29d6432ee5` 同样复用发现，前6批通过，第7批条件分支及结构错误 `failed_final`。两个作业均未水合、发布或进入个例审核。
- 校验器已去掉把“所有受试者均……”误识为条件分支的过宽模式；候选来源 ID 只做系统确定性排序去重，不改临床原文。聚焦解构/执行回归75项通过；旧门禁宽套件中缺 evaluation 的夹具仍失败，未算通过。一次针对条件许可的提示增补曾以早期 v39 隔离作业 `9d2e16c1a7ba42188274c9cdee80b86d` 实跑，首批通过、第二批因流程和字段结构失败，已撤回该未奏效提示，保留失败回执。该段记录形成时的代码为 v38/v2.36/v19；现行版本以本节首段为准，未将失败作业伪装为可续成功。
- 结论：缩批次与追加一般性提示均未稳定消除复杂条款错误；下一步在既有深审步骤内减轻单次结构化成形负担，并保持来源闭包、条件/例外与节点时间门禁。不能只因某次模型回答通过结构检查就宣布医学正确；目前正式目录、病例工作稿、更正补证与 Q3 未完成，`claims_complete=false`。
- 继续使用同一隔离库 `/Users/smkzw/tmp/enrollment-review-w6-20260923-v35-deep4-01/data`、冻结来源任务 `94ea6ac2d1454db0b0d73af91532d26b` 和已完成85批发现的作业 `59bdec6c80ef4cfab58a2a78b44c78dd`。新增任务均逐批核验发现来源后写入自己的检查点，旧失败、取消和正式库未改。
- 深审批次减到单段的 v35 作业 `069ea1ea2d414da88020e31e937be81a`：前三段通过，第四段（筛选起始的具体治疗及持续期）仍失败，说明原因不只是批次大小。原合同把带时间的操作限制为访视安排或结果有效期，不能合法表示具体治疗持续期。现增补通用的 `complete_or_verify + semantic + interval_condition + 命名锚点` 路径；单次药名值比较不能证明全疗程完成。提示、修复指引和执行版本升为 v36，保留来源和时间门禁，不写项目专属药物规则。
- v36 四段作业 `80548606d4664a33beb2cf350b25f6dd`：首批经一次定向修复通过，保留具体用药、稳定剂量、筛选起点及7天持续要求；这只证明该批结构/来源校验，并非临床全方案通过。继续运行至第7批，前6批通过；第7批四段不同用药限制的条件分支、时间用途及具体操作模式在有限修复中未收敛，任务 `failed_final`，后续 hydrate/gate 未完成。
- v36 单段作业 `0803d67f714c42d78375b56116bb1f3f`：前三段通过，第四段又在条件分支映射、来源 ID 顺序及观察摘录归属上失败。与四段第7批构成不同冻结输入的失败证据；不得再凭缩批次或增加修复轮次反复试跑。下一改动应在既有深审步骤内降低模型每次需同时完成的独立判断，并保留原批次闭包、逐字来源、合法修复和发布门禁，不手写临床 JSON。
- 定向执行/解构检查 `74 passed`；整套模型身份测试仍有13项因当前 MTPLX 装卸夹具未就绪，未算通过。正式跨章控制目录未生成、官方草稿尚未与其共同发布、病例完整审核及 Q3 均未完成；`claims_complete=false`。本轮没有可合法激活的正式规则快照。

## 2026-09-23 W6 本轮收尾与暂停

- 用户已授权隔离真实方案试跑，并将发现、深审模型改为 OmniRouter `cms-router/deepseek-latest-cloud`；仅授权为现有产品密钥新增该模型权限。该密钥其他权限未撤销，未将密钥写入仓库；本机 OmniRouter SQLite 一致备份在 `~/.omniroute/backups/storage.sqlite.bak.enrollment-key-scope-20260923`。受限密钥的模型清单为空，产品现在以不含临床资料的小请求核对返回模型身份，身份不符仍拒绝发送方案。进程继承的同名密钥可能覆盖工作树 `.env`，隔离试跑已显式装载目标配置。
- 原方案来源保持不变。v26 隔离作业 `44501a44e0df4f39b7872f9a18dc2632` 在深审第8批失败；原始回执依次显示计算语义、时间锚点和访视流程关系错误。修订通用提示后，同一冻结批次的产品读取与原校验器一次通过，约80秒，零候选；这只是结构核查，不是临床验收。
- v27 隔离作业 `293815d0aa0249b99f2e3157de368d60` 在发现第52批 `failed_final`：同一批候选引用的上下文被写作 `non_control`。复用已有跨批确定性规则，把被明确引用的同批单元仅提升为 `context_only`，不产生新控制；冻结原输出的离线回放通过。
- v28 隔离作业 `f6ad8965200c4089bb28f5e20a4cfb01` 在发现第53批 `failed_final`（55/86）：模型先回显整份 `$defs` 输出结构再给实际分类，修订请求重复发送结构后仍失败。现仅在发现修订提示中不再重发结构，首次提示和传输 Schema 仍保留；发现提示 v3、执行合同 v27。对同一冻结批次经产品传输与原校验器单批真实核查：24个单元一次返回，约37秒，状态“已解析”。不把该诊断写回失败作业、不改旧回执、不发布目录。
- 聚焦提示/解构反例合并 77 passed；身份测试仅 cms-router 三项通过，整套旧 MTPLX 夹具中13项因缺产品装卸清单失败，属另项环境/夹具缺口，不能写成全套通过。完整 W6、官方+跨章同源发布、五份病例事实/Profile/审核/报告、更正补证及 Q3 均未完成；`claims_complete=false`。下一安全动作见 `HANDOFF_20260923_W6_PAUSED.md`。

## W6 授权隔离试跑与当前观察（2026-09-23）

- 用户已单独授权**隔离真实试跑**，不等于授权修改原件、恢复旧取消作业、正式发布、提交或推送。旧 `0da5ef099b3e4972a012d14a3d5d75fe` 保持取消。试跑均用同一冻结方案来源 `94ea6ac2d1454db0b0d73af91532d26b` 的独立 SQLite 一致副本与 blobs；正式工作库未改。
- 第一份隔离副本 `~/tmp/enrollment-review-w6-20260923-v15/data` 的新任务 `5355193a66134ed08a7f0d73333fdaa8`：发现结果完整，1,927个结构单元中 423 candidate、3 uncertain、100 context_only、1,401 non_control；426个单元进入66个深审批次。首批因来源片段闭包、时间用途和未来窗结构错误而 `failed_final`，依赖步骤随之失败；没有生成发布目录。曾对发现批次63作有界真实调用并按合法任务重试，保留成功批次，不修改旧任务状态。当前代码版本已从 v15 演进，不得拿该失败任务混版本续跑。
- 产品传输的替代供应商路径曾暴露跨供应商端点/凭据继承问题：代码已修正并以定向回归验证。一次早期模型清单探测可能把 CMS 凭据送向 OpenCode 地址，未发送临床原件；应轮换该凭据。之后分别核实 `opencode-go/deepseek-v4.1-flash` 与 `cms-router/cms-glm-flash` 的模型标识、地址和响应模式。后者针对冻结首批在产品深审及原校验器下取得**结构通过**，耗时约763秒；这既不是临床内容QC，也不足以证明全方案可行或模型优劣。较短的初始 Schema 提示实验未获速度/质量收益，已撤回。
- 只读重分包模拟：当前 12 单元上限为66批、总提示约579万字符；上限4单元为130批、约1112万字符。固定合同的重复成本远大于本轮缩小批次节约的单批原文，故不以小批次直接替代。冻结期别**仅属于另一研究期**的28个单元仍被发现模型列为候选；新通用路由在深审前将它们保留为可追溯的非深审处置，若被本期单元引用则仅作只读上下文。混合、共用及未知期别不跳过。v17只读重算为398个深审单元、63批、总提示约553万字符；首批仍约9.4万字符，不能宣称根治耗时。
- 新隔离副本 `~/tmp/enrollment-review-w6-20260923-v17/data` 已经原库一致备份，新任务 `41f5ed409172484bbffcb0d7183d6482` 从合法入口创建。发现读道为产品直连 OpenCode DeepSeek，深审读道为产品直连 OmniRouter CMS GLM，两者通过模型清单预检。新发现闭包为1,927单元：381 candidate、7 uncertain、94 context_only、1,445 non_control；388个单元进入64个深审批次。与上一试跑的数量差异属于真实模型输出变化，不能当作期别路由的确定性收益。首批和第二批深审结构通过；首批需要2次输出修复。首批候选将“筛选/导入期及双盲治疗期禁止调整背景药物”合为一条，且在基线节点的命题要求证明整个未来双盲期未调整；虽然原结构门禁放行，但临床时间语义不可接受。系统通过合法取消入口请求停止，本任务终态 `cancelled`、88/152，未发布或激活任何控制目录，其他成功批次原样留存。不能把它说成方案深审完成。当前持久任务尚未冻结两读道的模型身份到 payload，不能在换模型配置下续跑。
- 针对此例只追加通用“当前节点前可核事项与节点后持续义务分离”提示，版本 `control-agent-prompt/v2.23`、执行版本 v18。用同一冻结首批、产品深审 runner 与原发布批次校验器做隔离诊断，`~/tmp/enrollment-review-w6-20260923-v18-targeted/first-batch-result.json`：约675秒，3次尝试后仍为“需要核对”，顺次为不合法 JSON、时间用途/额外字段、官方目标缺失；无合格候选。说明仅加一段提示不足以解决复杂输出的稳定性，也没有证明未来义务错误已经消失。未把失败改写为研究者医学判断，下一步须从请求/结构修复与候选时间语义两个不同问题分别查因。
- 新增通用禁止类时间越界门禁 `FUTURE_PROHIBITION_DECIDED_EARLY`，覆盖来源和求值命题均包含研究/治疗期间持续义务且绑定当前决定节点的情况；“治疗期”无需必须写作“治疗期间”。门禁版本 `control-publication-gate/v17`。对 v17 隔离库中已通过旧结构校验的原始首批候选作只读回放，现确实被该错误码阻断；未改原回执或数据库，未发布。定向反例 4 passed。整份旧 `test_slice58c_protocol_control_gate.py` 运行是 93 passed / 29 failed，失败集中于旧夹具缺少现行必填求值规格，不将其说成当前门禁回归；须按受影响消费者更新夹具并再跑。此门禁不证明所有未来时间表达都已覆盖，v18 隔离诊断的 JSON/合同失败也尚未解决。
- 同一 v17 冻结来源的首批，产品深审运行器+正式校验器隔离诊断：12单元约258秒、6单元约216秒和756秒，均未产出合格结果；6单元先遇输出结构/访视关系错误，后又出现未来持续义务误判。产品定向修复已保留失败回执，不能用诊断结果替换作业步骤。将首批拆为单单元后，描述研究对象的一条约21秒通过且无新增候选，不足以代表真实控制；真实背景治疗限制单元在提示 v2.24 下约2分半、三次输出仍因未来持续义务误判被拒。由此不能把减少批量当作根治。隔离原始回执在 `~/tmp/enrollment-review-w6-20260923-v19-targeted/`，无正式库写入。
- 正式任务建立时现冻结发现和深审两条模型线路的非密钥摘要；执行模型调用前重新核对，不同则以 `PROTOCOL_CONTROL_ROUTE_CHANGED` 停止，禁止换配置混跑。产品 API 已接入，旧执行版本不能混算。提示只移除 JSON Schema 中自动生成的 `title` 字段，保留全部临床 `description`、约束和正式响应 Schema；每次深审提示结构段由34921减至29419字符。提示版本 `control-agent-prompt/v2.25`、执行版本 `protocol-control-execution/v22`。相同限制单元在 v2.25 下约5分多、三次输出仍未通过（先为结构/未来窗错误，最后仍为未来持续义务错误）；单次随机试跑不证明质量退化或改善。接口及服务/提示聚焦回归 `67 passed`，涵盖发现/深审两读道在模型变更时均于调用前拒绝；旧大套件29项夹具失败尚未处理。
- 独立会商 `w6-temporal-scope-review-20260923` 只读完成，指出同一原子上的 `prospective_period` 会令节点前计算也变为未知。主线程核实后采用同来源的 `continuing_obligation`：固定“本次未到期”，不进入当前节点真假；不新增治疗期审核节点。正式/草稿合同、模型输出专用结构、来源闭包、时间门禁、候选水合及方案预览已接通；当前提示/门禁/执行分别为 v2.28/v18/v25。模型输出结构不再带系统内部 `schema_version`，且要求显式给出未到期状态；失败回执新增输出字符数，便于区分短残缺响应与长结构错误。聚焦合同及执行检查 67 passed；时间门禁 3 passed；前端正式构建通过。旧门禁大套件仍有28项失败，集中为夹具缺现行求值规格，不能当作已修复或新合同通过。
- 授权隔离诊断仍使用已取消 v17 作业的**只读冻结输入**，没有恢复/发布旧作业：v2.26 的一单元真实限制三次输出依次为 `PROVIDER_ID_FORBIDDEN`、`WIRE_SCHEMA_INVALID`、`WIRE_SCHEMA_INVALID`；修正模型输出结构后，v2.27 依次为时间用途 `WIRE_SCHEMA_INVALID`、`INVALID_JSON`、访视关联 `PROCEDURE_AFFECTED_STAGE_MISMATCH`。两次均为“需要核对”、无合格候选，不代表合同成功或模型质量排名。回执分别在 `~/tmp/enrollment-review-w6-20260923-v19-targeted/deepseek-batch-5-1-product-validator-v2.26.json` 和 `...v2.27.json`；试跑调用本产品 runner、OpenCode DeepSeek 独立端点，未调用外部 harness 执行临床识别。
- 下一安全动作：先用现行 wire 对同一来源做有限的模型输出结构诊断，区分输出格式失败、求值原文丢失与访视关联错误；不得扩大自动修复预算或用人工重写临床答案。只有真实候选同时通过来源、时间和访视门禁，才从**合法新作业**进入 W6 整体深审，旧取消作业不混跑。正式目录、受试者事实与报告仍未产出，`claims_complete=false`。
- 另一条已配置的 CMS GLM 读道用相同隔离单元试探，身份预检即拒绝：本机 `20128/v1/models` 返回 HTTP 200 但 `data=[]`；没有发送临床原文给 GLM，更不能把端口监听当作模型可用。隔离回执 `~/tmp/enrollment-review-w6-20260923-v19-targeted/cms-glm-batch-5-1-product-validator-v2.27.json`。未改共享服务或切换密钥。DeepSeek 的连续真实输出失败与 CMS 模型清单空属于不同问题，不能合成“所有模型不可用”。
- 受试者只读投影、API 合同、前端解码和宽屏审核详情新增独立的后续持续要求说明；当前控制义务的状态、事实选择和授权规则不变。定向后台说明测试 1 passed，前端工作台 16 passed，正式构建通过；未做真实浏览器/Q3 验收。待方案目录有合格发布实例后，仍须核对真实当前节点结果与后续说明同屏、报告/打印一致性。
- 当前修订的针对性测试：期别路由及执行合同 `9 passed`；此前提示/传输相关聚焦回归 `63 passed`。另一次宽测试的22项失败涉及旧默认模型与当前环境不一致、本地模型所有权未就绪，未作为本次通过证据。代码与诊断只解决工程边界；规则发布、真实病例事实/工作稿、补证及Q3验收均未完成，`claims_complete=false`。

## 0923V1 审阅纠偏（当前）

- 本轮获授权修订代码与定向验证，但未获授权恢复临床作业、收费调用、提交推送或改写原件。当前工作树保持脏状态；基线 HEAD `bc5d1cb815bf512c964784cda92914689ef1ce11`。
- RV1-01–05 已在选择性视觉规划、持久任务、事实整理入口及正式工作台作针对性修订：8字不再单独证明扫描页可靠，已知表格/标记和未知质量进入核对范围；失败不再提交为完成；旧v1覆盖不被v2计数借用；可读页先保存，缺图页单列，换模型不复用旧观察；跨章要求与官方条款共用完整列表、计数、筛选和分页，保留不同来源身份。任务身份现绑定不含密钥的模型路由指纹；事实整理只接收当前核对回执列明的观察ID，旧观察不混入新病史。此为合同修复，非真实病例验收。
- RV1-06/07 只读现场复核：跨章作业 `0da5ef099b3e4972a012d14a3d5d75fe` 仍为 `cancelled`、92/172，`deep_0007=failed_final` 且 `hydrate` 仍依赖它。它不是可忽略的历史失败；不得盲目 `resume_cancelled`。冻结收口有 1,927 单元：candidate 488、uncertain 50、context_only 281、non_control 1,108；538 个深审拥有单元分成 84 批，16 批仅有 1 个单元，59 种标题路径；上下文出现 182 次。实际 token 计数未知。修订史中的候选控制和仅盲法标题也进入深审，应核实分类/分包是否过宽；不能直接删候选或按特定方案硬编码。
- W6 补充只读测量：用生产 `build_protocol_control_agent_prompt` 对冻结的 84 个批次重建实际提示，共 7,381,055 字符；单批最小 83,015、中位 87,739.5、最大 101,301 字符；16 个单单元批次合计 1,359,918 字符。批次序列化中重复携带已知官方目标约 1,020,684 字符、流程目标约 1,435,980 字符；这些是字符量而非供应商实际 token/费用。538 个深审拥有单元中，目录指路性条目 20 个（7 candidate、13 uncertain），修订史 8 个 candidate。冻结的前6个已完成深审批次中，5批无候选、1批有1个候选；只说明值得继续核查粗筛，不能外推余下批次或直接删选。抽样显示部分仅为页码标题或变更说明，另有导入、给药前等真实控制；不能按章节一刀切。下一设计应区分“导航/历史文字无独立规范内容”与“当前有效的受试者要求”，并在保持每单元来源/上下文身份的条件下评估跨标题有界合批。发现提示或分包合同变更必须建立新版本作业，旧回执只能逐项证明可复用，不能混接。
- RV1-08 仍未验收真实更正/补证后新结果。正式规则尚未发布，5份24页资料尚未走事实/Profile/报告链；`claims_complete=false`。
- 聚焦回归：后端相关套件本轮扩大到 `137 passed, 2 skipped`（两项需显式启用真实端点），工作台组件 `16 passed`；前端正式 build 曾在本轮通过。当前任务回执的观察 ID 到病史输入的隔离已覆盖定向反例；尚未做产品真实连接/病例验证。Q1/Q2旧通过结果未重跑；Q3及真实病例、宽屏浏览器、第二结构留出均未执行。详细处置及剩余风险见 `HANDOFF_0923V1_RETURN.md`。
- W6 新发现提示已按上述反例修订为 `control-discovery-prompt/v2`，执行版本升至 `protocol-control-execution/v15`；旧取消作业不与新提示混算。隔离 W6 数据库只读核对：v14 跨章作业仅 1 个，状态 `cancelled`，无该版本活动作业；不能据此推断其他数据目录无活动作业。新提示的模型质量尚未试跑，不能把旧发现结果改写成新版本已通过。只读模拟按原顺序、每批最多12单元跨标题装箱，并为各单元保留自身来源/标题/必需上下文：45批、提示合计4,184,393字符，最长106,200字符，其中3批超过当前实测最长101,301字符。它只证明重复输入有缩减空间，不证明输出质量或服务上下文足够；正式分包尚未改变，需先以实际模型容量、输出预算及跨标题质量验证设定界限。
- W6 合同集中回归 `41 passed, 1 failed`：新增旧版本检查点拒绝重放与通用提示断言通过；唯一失败是既有 `test_protocol_control_generalization.py::test_discovery_gate_closes_results_and_routes_only_candidate_uncertain_deep` 的深审提示 `<30,000` 字符预算断言，当前实测 53,906 字符。此测试所测深审提示未被本轮修改；固定内容中系统合同 17,259 字符、完整输出 Schema 34,921 字符，是实质效率债务，不以放宽断言掩盖。须在不损失结构接受和临床完整性的前提下验证可否瘦身，不能直接删结构或宣称模型质量不受影响。
- 下一安全动作：先以来源保真的方式完成 W6 有界分包及版本身份，再从合法入口新建受控作业；原作业继续保持取消态，`deep_0007` 不能伪装为历史成功。不得把草稿 `publishable=true` 当正式发布。真实服务仍需验证模型别名、配置切换和逐页观察到事实/Profile的完整来源闭包。无需反复重跑已通过的 Q1/Q2。

## 本轮合同
用户已授权在唯一工作树按W0→W7和Q1→Q3连续实施，不再等待另开fork。当前不启动临床Job、不改env；按相连功能集中验证，避免逐处改动逐次测试。
方式：当前所有者负责单写者集成；共享生产路径的连续修改以内联实施为主，实质解释风险和最终临床验收按现行执行/会商机制独立挑战。

## 当前依据与环境
- 入口delivery_20260922/00_START_HERE.md；prd/design/03_PLAN/04_WORK_PACKAGES/05_ACCEPTANCE当前，旧Phase范围保留。
- 唯一worktree：/Users/smkzw/Documents/康哲项目资料/AI/入排/enrollment-review-app/.worktrees/phase5-clinical-facts-profile。
- 起点本地/GitHub HEAD一致e7f34d0508c05481164cf13569f66ddf64e2e74f；起点clean、无open PR。后续里程碑提交以当前`git log`为准。
- 现有方案、原图/OCR、持久任务、事实/Profile、求值/工作台/报告/更正骨架，不从零重写。
- C01–C10问题和专家12条裁决见01_REVIEW；模型诊断补充见10_MODEL_AVAILABILITY（生成后）。claims_complete=false。
- AST扫描592文件无语法失败；前端真实build失败6项；一批相关pytest为14失败/20通过/2xfailed。未逐项划定产品/fixture原因。
- 无应用/临床库写入、无浏览器或完整病例验收。模型诊断不等于质量验收。
- 运行时get_goal为paused，正文V3；本轮未修改/恢复/新建Goal。07_FORK_PROMPT为下一会话目标候选，执行者依据工具实际能力更新，不伪报已改。

## 状态
| 包 | 状态 | 下一动作 |
|---|---|---|
| Review与资料包 | 已完成且文件检查通过 | 09为证据/裁决，10为模型诊断；可交给fork从W0执行 |
| W0 | 已实施并通过Q1 | 前端正式构建通过；资料页数按正式上传+真实解码核算，未知阻断；唯一源码树外目录+完整hash；旧watch拒绝执行；跨供应商密钥回退已关闭 |
| W1 | 已实施并通过Q1合同回归 | 当前范围最新作业优先；未完成核对不直接用已发布事实判定；回执核实的条件、原文含义、复查关系、频次和书面判断进入工作稿；正式签发仍要求独立授权；官方条款与跨章要求同修订展示 |
| W2 | 已实施并通过Q1合同回归 | 坏结构保留为技术失败并进入既有限次修复；不得改写期别、裁剪来源或把解析失败降级为医学待确认；工作台发布强制携带全文控制点覆盖结果及完整谱系 |
| Q1 | 已通过 | 后端W0–W2集中套件127项通过；正式方案发布19项通过；前端`npm run build`通过；仅有既有大包体提醒和SWIG弃用警告 |
| W3 | 已实施并通过Q2 | 新资料采用文字主读和风险页局部视觉核实；风险页逐页分批、不漏页，未核完不进入正式病史；旧双读仅作历史兼容 |
| W4 | 已实施并通过Q2 | 左栏待处理/全部条款单视图；筛选、显示数和条款深链可恢复；原件明确引用/浏览状态并扩大桌面占比 |
| W5 | 已实施并通过Q2 | 追加式更正沿用既有闭环；当前事实变化后旧准备任务、旧工作稿和旧审核入口均拒绝继续使用 |
| Q2 | 已通过 | 前端正式构建通过；工作台/原件21项、后端跨层224项、更正失效2项通过；无真实模型或临床资料写入 |
| W6 | 隔离真实试跑中，尚未发布 | 新DOCX官方规则草稿修订21仍未正式发布；隔离全文发现已完成，首轮深审失败；通用合同/提示修正后首批真实深审已通过，仍须从合法入口完成全部批次及来源核对 |
| W7/Q3 | 待执行 | 完成真实整例、更正补证、第二结构留出、大屏、部署与最终验收 |

## 下一动作和纪律
用户已授权隔离真实试跑；原取消作业、正式临床资料及已发布规则保持不动。不要重跑Q1/Q2；按当前冻结结果与产品实际身份，从合法入口新建版本化作业。`HANDOFF_0922V2_RETURN.md`中“84/85个单元”“failed_final只是历史步骤”两句已被现场反证，不得作为恢复依据。
实质变化时合并更新本表和证据、实际运行身份、下一动作；不每改一行写日志。原始回执在artifacts；无密钥；不得覆盖本轮未提交文档。
交接08_RETURN_TEMPLATE。仅在用户明确授权时提交/推送；不清原件/历史/别人的dirty，不把部分完成伪报全交付。

## W6 隔离试跑与通用合同纠偏（2026-09-23）

- 从未改动的 v17 来源库备份到 `/Users/smkzw/tmp/enrollment-review-w6-20260923-v29-cms-deepseek-01/data`，由产品 API 创建作业 `caba64aac19a474d8c4386d6b986ffa2`；产品经 OmniRouter `cms-router/deepseek-latest-cloud` 直接推理。85个发现批次完成，深审计划56批；首批经两轮修复仍失败，`deep_0001=failed_final`，后续依赖未运行，未发布目录。旧库与原始方案未修改。
- 根因不是单一模型故障：首批12个单元请求约6.7万输入 token，提示里重复了已通过 `response_format=json_schema` 随请求送出的完整结构；原有同源不同摘录在控制原子可表达、在求值/观察/资料政策中却被按来源位置禁止，核验字典还会丢掉前一摘录；“给药方案”的普通用语被误判为研究方案权威引用。对应合同改为逐对来源与摘录核验，完全相同的对仍拒绝，真正附录引用仍严格要求原文支持。当前临床动作混合、后续义务提前判定仍由原门禁拒绝，不自动拆分或补造。
- 通用提示区分当前节点事实与未到期持续义务；`json_schema` 通道不再在文本提示重复整份结构，非严格结构通道仍保留。请求身份版本已更新。相同冻结两单元复杂小样用 DeepSeek：旧路径3轮失败；新路径提示约8.7万降至5.7万字符，2轮在产品结构/发布门禁通过，耗时约185秒。完整首批新路径提示约6.4万字符，1轮通过、约166秒、1条候选。一次隔离GLM同小样约949秒仍失败，不能据此宣布其整体劣于DeepSeek或切换正式配置。
- 定向合同/提示/门禁检查31项通过；先前宽套件29项失败是既有缺失 evaluation 的旧夹具，未把其计作通过，也未改宽门禁追绿。以上仅是深审首批诊断，不是官方+跨章完整发布，更不是个例临床验收；`claims_complete=false`。下一步在新隔离库由合法入口跑完整版本化W6，核对每批最终状态、来源覆盖和审阅结果后才允许同源发布。
- 随后从同一v17来源另建 `/Users/smkzw/tmp/enrollment-review-w6-20260923-v34-compact-schema-01/data`，产品作业 `062afa810d0b4c53adc22a92e6e4e57b` 约21分钟后 `deep_0001=failed_final`；发现阶段重新抽出60批深审，首批相对前次多出给药后内容，因此先前通过的诊断批并非同一冻结输入。未发布。
- 从真实v34冻结批中隔离取四单元用同一产品 harness 诊断，约158秒、3轮完成并通过门禁。由此再从v17新建 `/Users/smkzw/tmp/enrollment-review-w6-20260923-v35-deep4-01/data`，经现有服务批量参数冻结每批4单元，作业 `59bdec6c80ef4cfab58a2a78b44c78dd` 约20分钟后仍在 `deep_0001=failed_final`；新发现阶段本次生成104个深审批，首批包含“自筛选开始稳定背景治疗7天”，模型未能给时间原子保留命名锚点。两次均在来源/结构校验处停止，成功发现批次保留在各自隔离库，旧库与原件不动。
- 结论：缩短提示和单批量改善了某些同源小样，尚未解决发现阶段分类波动、复杂时间/后续义务一次性成形与失败后重复全部发现的恢复成本。不得用单批绿灯或再次随机重跑宣布W6完成；先核合法的分段恢复/发现复用和深审合同拆负担，同时推进不依赖临床发布的W7准备。`claims_complete=false`。

## W6 实跑与 2026-09-23 无损暂停

- 真实方案解构作业`94ea6ac2d1454db0b0d73af91532d26b`保留在`waiting_user` 8/10；同源草稿`draft:candidate-94ea6ac2d145-in01-all`已到修订21，23条官方规则（7条入选、16条排除）通过完整性检查，`publishable=true`。
- IN03、EX01、EX12的来源、分支和逻辑结构已逐项修正；“可发布”仅是草稿门禁结果。跨章节控制点作业未完成，因此未正式发布规则。
- OpenCode Go DeepSeek端点不接受`response_format=json_schema`。现按供应商能力选择`json_schema/json_object/text`，DeepSeek走`json_object`，内部严格Schema校验不变；完整Markdown JSON围栏可剥离，不猜测或改写临床内容。
- 跨批发现把被其他候选引用的`non_control`单元同时当成不可用上下文。现确定性提升为`context_only`，只供上下文，不生成独立控制点；修复后收口成功。
- 跨章节作业`0da5ef099b3e4972a012d14a3d5d75fe`已按用户要求取消，终态`cancelled`，92/172；85个发现批次完成，深审扩展为84批，仅完成少量。84/85的比例可能提示发现合同、切分或不确定路由过宽，恢复后必须先抽查，不得盲目续跑。
- 专用`57060`服务已关闭，无活动租约；本轮未关闭不归本任务所有的共享服务。5份受试者PDF共24页尚未进入事实、事件、用药暴露、Profile和报告流程，`claims_complete=false`。
- 完整恢复状态、文件路径、验证边界与下一安全动作见`HANDOFF_0922V2_RETURN.md`。

## W1 实施证据（2026-09-22）
- `qualified_binding_selection.py` 增加与正式授权类型隔离的回执核实工作稿入口；复用既有来源拒绝、观察选择、命题、复查和频次消费者，不生成授权对象，不允许发布边界接收工作稿。
- 已完成的书面判断、原文含义、复查关系和频次作业须从原始回执重建，并与同一候选作业、审核上下文、冻结输入、比较指纹和来源配对逐项相符后才进入预览计算。
- `frozen_review_calculation.py` 对工作稿和正式选择使用同一命题、频次、复查与控制计算；二者差异保留在授权和发布边界。缺研究者书面判断仍是正常的“无法判定”缺口，不触发额外确认。
- `eligibility_review_projection.py` 只读取当前完整authority下最新作业；若最新作业尚未完成、失败或取消，显示当前未核状态，不借更旧完成结果冒充当前。官方条款和跨章节要求来自同一已发布修订。
- 活接口与前端解码已增加独立`controls`结构；方案确有补充要求时不塞入官方条款，官方与补充要求总数为零则拒绝生成审核结果。
- 本包只执行了约定允许的静态校验：相关Python文件编译、模块导入及`git diff --check`通过；功能/反例/前端构建留到W2后Q1一次集中执行。

## W2 实施证据（2026-09-22）
- 删除`protocol_control_deconstructor.py`中把缺失或不合格`evaluation`改写为`investigator_judgment`的预清洗。合同错误现在保留为`WIRE_SCHEMA_INVALID`，由既有同任务、有限次数结构修复处理；只有方案原文明确要求研究者裁量时才允许形成专业判断条款。
- 恢复`protocol_control_catalog_publication.py`中曾被临时`pass`绕过的正式发布绑定：当前方案输入、完整原文定位、草稿/规则版本/期别、全文覆盖清单、发布门禁版本、处置计划、批次来源范围和候选集合必须逐项一致。
- 正式工作台的首次发布与重新发布均要求已完成的全文控制点任务及其最终保存记录；即使没有额外控制点，也必须有来源化的全方案覆盖结果，不能用空参数绕过跨章节核对。
- 官方规则、流程节点与跨章控制仍在同一发布事务内写入；控制目录保存失败会回滚整次发布。移除了临时标准错误打印，持久化内容损坏也统一转为可理解的发布谱系拒绝。
- 本包按约定仅执行Python编译、关键模块导入及`git diff --check`，均通过；反例、服务路径和前端构建纳入Q1一次集中验证。

## Q1 集中验证证据（2026-09-22）
- 修复集中检查发现的历史临时旁路：未知/混合期别不再自动改成当前期别；跨章只读上下文不再因发现阶段标记为非控制而被裁剪；流程目录多出的访视不再静默删除；对侧期别必须有明确不适用处置。
- 深析模型在有限修复后仍无法产生合规结构时，任务以技术失败结束并保留诊断，不再伪装成`PENDING_CONFIRMATION`医学待确认项。
- 当前节点没有完整核对作业时，审核投影明确显示资料尚未核实，不采用未经过本次资格选择的已发布事实；缺少节点映射不会再令整个页面异常退出。
- 后端集中命令覆盖W1/W2、API、页级读取与资料处理，共`127 passed`；方案正式发布服务另`19 passed`；只有第三方SWIG弃用警告。
- `frontend/npm run build`正式构建通过；仅报告既有入口包体超过500kB的性能提醒，不属于本轮合同失败。`git diff --check`通过。

## W3 实施证据（2026-09-22）
- 复核“模型不可用”原始回执并作当日最小调用：OpenCode旧400由缺少稳定会话标识造成；补齐产品自有会话身份后MiMo与DeepSeek可用，Muse仍由上游返回503。当前`.env`的Ollama main-A缺专用凭据，产品按供应商隔离规则在本机拒绝，不再误发GLM密钥。
- OpenCode兼容头已进入产品传输层，首次、格式修复和重试复用同一产品进程会话；不读取OMP/Hermes身份。此兼容不等于临床长提示或真实页面质量验收。
- 新上传资料冻结后统一建立页面质量核对任务。扫描件已有充分OCR文字时不再仅因“是图片”触发VLM；低置信、复杂版面、原文提取异常或确实缺字的页面才进入局部视觉核实。
- 局部视觉规划不再把超过首批额度的风险页标成跳过；全部风险页按`max_pages_per_call`切成短上下文批次逐一保存。局部失败保留关闭记录，其他页面仍可完成，但有任何风险页未核实时事实整理入口拒绝继续。
- 个例档案整理默认消费OCR/原生文字与来源绑定的选择性视觉观察，不再要求新资料先完成整页双模型对账。没有风险页也必须有已完成的覆盖任务；历史资料若没有新任务，只能使用同作用域、完整对账的旧双读覆盖，不借用或伪造A/B记录。
- 来源政策合同收紧：`self_consistent`只表示文字内部自洽，不能单独冒充已核实来源；单来源采信只接受交叉核实、定向核实或人工确认。
- 本包按集中验证纪律仅执行受影响Python编译、关键模块导入及`git diff --check`，均通过；风险页分批、旧读兼容、任务恢复、事实发布/Profile来源闭包留到W3–W5后的Q2一次验证。

## W4 实施证据（2026-09-22）
- 正式入排工作台左栏不再同时堆叠两套重复列表，改为“待处理/全部条款”单一视图；待处理仅按问题类型归类，不宣称同组问题可一并关闭。组内条款按风险、官方编号和组件身份稳定排序。
- 当前范围、判定筛选、单次显示数量和条款选择写入地址状态；显式条款深链优先，无深链时从当前可见列表选择最高优先项。切换范围或筛选时同步选择有效条款，不保留不可见选择。
- 中栏保留短依据、动作和关联事实，方案原文改为独立展开区；不调用模型改写事实。右栏扩大为主要桌面区域，明确标出“引用原文”与“浏览原件”；无关联事实时第一页只作浏览，不再误称引用页。
- 页码定位、资料文档和红框仍从同一完整处理修订读取；未核实精确坐标不画框。页脚分开显示规则修订、节点修订和资料快照，避免把节点修订号称作资料版本。
- 本包只执行差异检查；前端正式构建、组件状态与宽屏实操按集中纪律进入Q2/Q3。

## W5 实施证据（2026-09-22）
- 复核既有事实修订链：它已具备原文定位约束、影响预览、幂等持久任务、追加新事实/事件/用药暴露、冲突与资料期望重投影、新个例档案生成、旧版本保留、停止/失败重试和修订历史，因此不另建第二套更正服务。
- 新增统一的“当前临床材料指纹”，覆盖活动事实、事实与规则关联、事件、用药暴露、资料期望和冲突组。人工更正改变任何活动链头后，更正前冻结的审核上下文会与当前材料不一致。
- 该指纹同时进入新审核入口、持久审核续跑和工作台工作稿读取：旧任务不能继续，旧工作稿不能冒充当前结论，新审核必须重新准备当前病史。历史报告及旧上下文保持只读不变。
- 补充新资料会产生新的资料快照/完整处理修订，既有authority隔离继续使旧检索与旧工作稿失效；同一处理修订内的事实更正则由新增临床材料指纹补足失效边界。
- 受影响Python模块编译、关键导入与`git diff --check`通过；功能反例统一进入Q2。

## Q2 集中验证证据（2026-09-22）
- 前端正式构建已通过；工作台与原件组件集中测试`21 passed`。旧测试仍按左右两套列表、默认全量条款和“按根因聚合”查找界面，已按当前单列表产品合同修订；用户文案改为“按问题类型归为”，避免暗示同类问题可以一次性解决。
- 选择性视觉、观察保存、任务恢复、用户停止/重试、事实整理、更正链、工作台投影等后端集中测试`224 passed`。首次运行的7项失败均为旧合同夹具：1项要求超过单批上限即丢页，6项把已有充分OCR文字的页面伪装成必须视觉核实。修订后全部通过，产品逻辑未回退。
- 新增病史变更反例`2 passed`：更正后，旧准备记录不能再发起后续核对，旧持久审核也不能续跑；必须基于当前病史重新准备。历史记录不改写。
- `git diff --check`通过。仅保留第三方SWIG弃用警告和前端既有大包体提醒；本窗口未触发真实模型、未启动临床作业、未修改原始资料、数据库或环境凭据。
