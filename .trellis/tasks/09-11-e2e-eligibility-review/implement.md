# 当前实施记录

## 2026-09-26｜W6 第30批单批诊断后无损暂停

用户要求完成手头核查后无损暂停并递交 GitHub；本轮不再启动整方案作业。详细复盘、现行状态和接手步骤见 `HANDOFF_20260926_W6_SOURCE_COVERAGE_PAUSED.md`。现行隔离库无活动方案控制作业；`claims_complete=false`。

接上段记录：v189 作业 `54f21f593b144732b2fa710f02579527` 在第2批结构失败；v190 `a9f51d41f97841b395f800a755c81847` 入队前取消、未运行；v191 `9c651df26b6c4f5496c95294dde846c8` 预检29批可复用，运行到第30批后 `failed_final`（115/196步骤），未水合、未发布。v191的单项观察补答已起作用，但第30批又因基线值选取原则缺少准确关联而被门禁拒绝。

只读、无入库的产品 Agent 诊断使用同一冻结第30批来源、现行产品线路和现行合同，仅将单批结构修订预算由2增至3。6次尝试后得到 `已解析`，真实响应与原批门禁通过；忽略目录回执 `artifacts/0926v1-w6-deep0030-budget3-readonly.json`，SHA256 `6d5a8531c8d7a9f226266bd8292404f88f504474bfc8f40462ca14533e560529`。进一步逐条核原文发现重要**未通过的内容质量检查**：`body.p279` 中两个基线评分取值时间点在来源解释中存在，但候选只表达缺失评分和均值处理；两条取值定义被标为 `candidate_linked`，由于来源语气为 `descriptive`，没有进入现行逐项目标核对。故此回执不能作正式输入，第30批不能视为临床合格。曾试探性扩展核对决策 Schema，因跨旧检查点复用和纯背景条目处置尚未验证，已用补丁撤回；未留下半套新版合同。

本轮按功能包集中复查：`.venv/bin/python -m pytest tests/v2/protocols/test_slice58c_control_deconstructor.py tests/v2/protocols/test_protocol_control_agent_transport.py tests/v2/services/test_protocol_control_execution.py -q`，**292 passed**、退出0，仅第三方 SWIG 弃用警告；`git diff --check` 退出0。该检查不覆盖上述新发现的临床遗漏，也不代表 W6 全方案、W7 个例和 Q3 浏览器验收通过。暂停后的第一安全动作是从原文陈述到账本中的正式规则逐条建立可验证覆盖，而不是仅把修订预算调成3或重启108批长作业。

## 2026-09-26｜W6 第30批时间误判与单项补答结构

执行选择：同一隔离库、冻结方案及长作业由主线程单一持有；“随机入组资格”是否本身构成时间条件具有实质解释风险，已按现行机制完成一次独立只读会商（`runs/conference/enrollment-w6-temporal-gate-20260926/evidence_single_object.md`），所有者再以源码和正反例核定。会商不代替模型产品运行或临床批准。

v186 作业 `2abbac0af9e34ad49a1c762b6bbeb784` 停在第11批；v187 作业 `11e7bf7962e94bdbb90435fba7f5a985` 合法复用前10批，推进至第30批后 `failed_final`，共29个深审批次完成，未水合或发布。第30批原文要求选择基线值，模型未声明数值时间窗；旧门禁仅凭适用人群中的“随机入组资格”要求时间锚点，属于与原子来源脱节的假阳性。门禁 v31→v32 取消这一条对标题/人群的粗粒度判断，仍保留逐原子原文日期、时长、边界与锚点校验；“随机前X天”等真实窗口继续拒绝无锚点。仅在来源、分包、模型、提示、Schema和编译身份一致且现行来源校验和门禁均通过时，旧批次可因门禁版本单独变化而逐批重验复用，旧检查点不改；新检查点记原门禁版本。

v188 作业 `ecaab9f27ce0429ab0cc328967553edf` 从冻结解构入口合法新建；预检29批可复用、第30批仅续用经核的有源解释、余78批待执行。真实产品运行在第30批再度 `failed_final`（115/196步骤），但已越过时间误判；新问题是单项缺少观察采用规则时被送到整原子补答，且文字响应模式未附该局部Schema，模型遂写出 `adoption_note` 等合同外字段。v189 把仅此一类单项错误送入既有观察规则局部通道，文本模式附真实局部Schema，其他原子/来源保持冻结；不自动填写医学判断。方案Agent、传输与执行相关功能包集中检查 **292 passed**，源码编译和 `git diff --check` 通过。v188 及更早失败历史不改，v189正式作业尚待合法建立和真实终态；`claims_complete=false`，W6完整同源发布、W7病例及更正、Q3浏览器验收均未完成。

## 2026-09-26｜W6 四条分包真实作业与第六批校验

执行选择：隔离数据和长作业仍由主线程单一持有；结构成本与同源修订已经过 C03 独立会商，本轮按冻结来源和失败回执作确定性修复，不把模型自述当医学裁定。产品模型由内置方案 Agent 直连现行 `ollama-cloud/deepseek-v4.1-flash:high`，不使用开发者 CLI 替代产品读取。

同一冻结方案的四条分包先后建 v181–v185 隔离作业，均在具体深审批次 `failed_final`、未发布：v181 第2批短答未附结构；v182 同源两项仅修第一项；v183 第3批漏报原文时长；v184 第4批附带已知只读上下文处置；v185 前五批通过，第6批把原文明确的共享时期误判为无来源。对应通用合同、来源逐项复核和只读上下文边界已分别修正，原失败回执不改，不能视为方案通过。

v186 将同一条来源的 `scope_quote` 或标题中的确切时间范围作为合法时间摘录；目标端仍必须逐项覆盖来源的时期和时长，缺少时长的合成反例仍报 `TARGET_TIME_INCOMPLETE`。相关深审合同测试 **207 passed**。新隔离作业 `2abbac0af9e34ad49a1c762b6bbeb784` 从合法解构入口建立，复用预检为5批 `reusable`、第6批1项 `resume_partial`、余102批 `refresh_required`；正式终态尚待真实执行回执核对。`claims_complete=false`，W6发布、W7病例与更正、Q3浏览器验收均未完成。

## 2026-09-26｜W6 可恢复来源与分包成本核验

执行选择：主线程独占隔离库与产品方案作业；复用身份和分包成本先用冻结账本确定性核对，两个不同形态的批次由产品内置 Agent 直接调用现行模型做只读试跑。结构成本的 C03 独立会商已终态成功（`runs/conference/enrollment-w6-structural-cost-20260926/`）；审阅最初把未完成批次误判为版本失效，提供原始原因直方图后已撤回该结论。未把审阅意见直接当作临床采信。

旧 v178 任务 `c19604e7c9d64546bd2e5092720a497d` 的第4批保存了完整有源解释，但没有可发布线稿。跨作业恢复现允许在原方案、分包、模型线路、提示及组成身份一致且当前来源校验通过时，仅续用该有源解释；不把失败结果或不存在的线稿当成功。相邻传输、深审、执行功能包集中检查 **284 passed**。在隔离库只读预检的 327 批中，3 批可复用、1 批可续用有源解释、323 批从未完成；原回执不改。

冻结发现结果的离线分包测量：每批上限1条为327批、主提示合计约2627万字符；上限4条为108批、约886万字符，主提示最大约8.95万字符。分包器会保留冒号总则加连续清单，故即使上限1条也有少量5至22条的不可拆组。四条分包从相同冻结来源选取表格批与表格/清单混合批，产品真实只读调用分别在205秒/429秒返回“已解析”，各有2个有源候选、5轮/4轮调用，均通过原批次门禁；这只证明两种小样可行，不能宣布全方案质量、正式发布或统计速度收益。改用四条分包会改变批次身份，旧任务那3批成功和1批有源解释不能跨新分包冒用。接下来依正式作业当前身份核实可用范围并从合法入口试跑；失败保持在具体批次，`claims_complete=false`。

## 2026-09-26｜W6 冻结第4批：短解释结构与来源时点修正

执行选择：主线程持有隔离库和产品方案作业；同源多条要求的修复边界已接受一次只读独立会商挑战，见 `runs/conference/enrollment-w6-batch4-repair-20260926/evidence_single_object.md`。审阅指出同源候选可能因回写排序而转移修订对象、方案期别错误访视化未进入局部校正；按冻结回执和代码复核后修正，不采纳无来源的时间值推断。

v176 隔离任务 `2d3290731835417883c768163efe8afb` 于深审第4批 `failed_final`，未发布。当前来源校正允许在原句/标题仍保留“Ⅲ期”证据时，从访视阶段和时间词中移除该方案期别；原文和真实访视词不改。同源两候选的时间边界错误现在按线稿位置逐条授权修复，回写不再按候选内容重排；不能唯一定位时直接停在待核对，不退回整组改写。补答材料身份和执行身份分别升至 calendar-v3、v178，旧失败回执不改写。集中相关功能包 233 passed，只有第三方 SWIG 弃用警告；`git diff --check` 通过。

冻结第4批复用已保存且经当前门禁核验的五条来源陈述，产品内置方案 Agent 直连实际 `ollama-cloud/deepseek-v4.1-flash:high` 做一次**不入库、不发布**的真实单批试跑。模型身份预检一致，8轮后返回“已解析”，两项增量候选、两条来源目标核对均通过该批原门禁。回执在忽略目录 `artifacts/0926v1-w6-deep-0004-v177-focused-readonly.json`；其中一次单项解释版本不符被拒，后来由已有来源限定补入完成。该回执生成时执行源码身份为 v177，后续同源顺序修订已升 v178；它只能证明局部产品链可运行，不是 v178 持久作业、全方案临床审核或正式规则发布。

在只读数据库连接上用现行预检对旧 v176 冻结计划复算：327批中仅3批 `reusable`、324批 `refresh_required`；未将不入库回执冒充完成批次。v178 正式隔离任务 `c19604e7c9d64546bd2e5092720a497d` 合法新建并运行，在第4批 `failed_final`（89/415 步），未发布。失败链依次出现方案期别误认访视、原子缺少时间操作数、过宽补答写入无源观察选择；冻结失败回执保留，未激活。v179 把唯一缺失时间操作数优先送窄范围修订，集中合同测试通过；只读单批返回“已解析”，但没有证明这一具体修订路径在真实回复中被使用。

再次核对短解释发现：产品 `ollama-cloud/deepseek-v4.1-flash:high` 采用纯文本响应模式，三种短解释请求却只说“按随附结构”，并未真的附其 Schema。v180 只给这三种短读道在纯文本/普通 JSON 模式附各自完整结构；严格结构化通道和整批主请求不变。真实第4批短答从此返回正确版本，但装配被“相对时间、原文动作或流程摘录不能逐项回源”拒绝。捕获本次模型输出、目标与冻结访视逐项对照后定位为通用标点缺陷：来源范围末尾冒号被拆成单独时间片段，错误要求访视目录名称也含冒号。v181 只在时间片段拆分时将冒号视为分隔符，来源原句、节点、例外和校验门槛不改。对同一冻结第4批再用**产品内置 Agent、实际模型、不入库**试跑，5轮得到“已解析”，两项增量候选经同批来源闭包与原批次门禁通过；第一次正文的流程节点错配、治疗时长误作事件窗仍由原门禁拒绝并在局部修订后重核。相关传输/深审/执行功能包集中检查 **283 passed**，仅第三方 SWIG 警告。新提示只服务单项读取，旧成功批次仍由现行来源和门禁逐项复验，不因未调用的短读道说明变化强制全量重读。

W6 官方＋跨章同源发布、W7 病例工作稿与更正、Q3 浏览器验收均未完成；`claims_complete=false`。下一步先用 v181 对旧失败任务作只读复用预检，再从合法入口建新隔离任务并核真实进展；若后续仍形成逐批长补答循环，应先改分解和可恢复边界，不继续以整方案重跑代替原因分析。

## 2026-09-26｜W6 局部修复的两种结构矛盾

产品隔离 v174 作业 `0ecaf3a206b7401898fd22c3bf5a35ab` 在第6批失败：首答缺观察采用说明；原子补答明确写 `unresolved` 却漏同原子的范围及来源；候选补答又在无独立时间约束时宣称已确定时间用途。v175 只对**显式待核**的局部原子补答，从其同一原句/来源补齐缺失字段，不覆盖不同来源或 `single` 等选择；无时间约束却宣称已确定用途继续拒绝，单原子提示明确这点。旧原答离线回放从“漏字段”前进到“时间用途矛盾”，证明前者已修、后者未被静默放过；集中相邻测试 228 passed。

v175 作业 `4ce134f22da647b2bfca7d8a0513d6fd` 从正式入口建立，预检 4 批可沿用、1 批因修订材料变化需重核、其余 322 批未完成；实际运行在第4批因**另一**矛盾停止。原校验正确认为“研究Ⅲ期”不是受试者访视/回溯时间，局部纠错却禁止从 `time_words` 删除该原句词，并要求保留其错误 `affected_stage`，形成不可能完成的修订。v176 将研究期别从访视阶段/时间字段剔除，但仅在该期别仍见于冻结原句、所属标题或有据共享范围时；原句不改，真实筛选/导入时间仍须保留。原第4批模型局部答复的离线回放现通过五条来源陈述的该步校验；不代表整批、全方案已通过。旧失败作业未改、未激活，`claims_complete=false`。下一步做新身份预检，合法续跑并继续按原件核任何新失败；避免把此两处修复宣称为 W6 收口。

## 2026-09-26｜W6 第149批外部资料归因与完整复用预检

执行选择：隔离库和产品方案作业共用状态由主线程单独持有；对“外部指南的设计说明是否形成本研究排除要求”的高后果解释，已使用批准的 C03 独立会商挑战冻结来源和决策边界，报告在 `runs/conference/enrollment-w6-attributed-rationale-20260926/evidence_single_object.md`。审阅建议的逐条归因和禁止整段降级已采纳；其关于失败局部草稿必然触发 409 的猜测与现行源码不符，未采纳。

旧 v170 作业 `86b836b7cb584f8095a24ffe1516b79d` 完成146批后，第147批把治疗持续时间误写成事件窗口；v171 作业 `48d9533c02a04402a56f633402dfd687` 完成148批，第149批将引用外部指南的研究设计理由误生成独立排除控制。两者均为 `failed_final`，未发布。现在来源解释增加逐条外部归因及同句原文校验，逐项核对不能因描述性语气、`expressed` 状态或 `non_control` 处置跳过；外部资料若仍被装成候选控制，门禁拒绝。源句同时有本研究采纳要求时仍须单独核对，不能整段降为背景。执行身份为 v173；此处不硬编码方案、疾病或药物。

产品自身的只读第149批单批调用在两轮内返回 `已解析`：5条有源陈述均带同句外部归因，5条逐项决定均为外部资料设计说明，新候选为0；首轮存在结构错误，第二轮通过，不代表整方案成功。来源解释/执行相邻回归 **226 passed**，仅证明本合同；`git diff --check` 通过。正式建作业前的只读复用预检显示旧任务148批已完成结果均因提示材料变化需重核，余179批未完成，共327批需执行；未伪造可复用范围。下一步从冻结方案入口在同一隔离库建立新版本作业，复用合法发现账本而不是旧深审结论，直到完整来源与发布门禁核对。W6、W7、Q3及 `claims_complete=true` 均未完成。

v173 隔离作业 `6deb565d0c4f46ca953982aa937a726c` 从合法入口建成，85批发现结果按身份复用，深审前5批完成；第6批在约786秒后 `failed_final`，没有水合/发布。冻结诊断显示模型已将摘要与另一章节可能复述的要求标为 `potential_same_requirement`，真实终态为 `待跨章核验`；执行步骤却只接收 `已解析`，而复用预检与最终跨章关系汇总已显式接收前者。这是状态合同前后矛盾，不是可用模型结果应被当作正式规则。v174 将深审步骤接收范围与最终汇总一致，保留目标章节必须独立通过、唯一候选、逐字动作/时期和最终关系门禁；新增该状态持久化反例与既有目标未通过反例。**尚未据此宣称跨章关系成立**，下一步预检可复用的前5批和第6批局部草稿，并从新作业身份继续。

## 2026-09-26｜W6 第219批来源链与局部恢复

执行选择：主线程独占隔离库及同一产品作业链；模型直接由产品方案 Agent 调用。第219批是临床条件分支与可选语气的高后果解释，尚未采信，需以冻结原文和独立核对挑战。原件、旧回执和正式库未改；`claims_complete=false`。

通用规划现把相邻“总则＋直接续列”作为同一 owned 批次，不能因分包将四项重新筛选条件误降为只读背景。v164 首次实际试跑在新分包的映射上从原先的结构错误进至有源增量核对；第一次补入使四条未覆盖要求缩到一条。将来源补入上限从一次改为两次，并明确越界修订立即停止；此项受原门禁与无进展保护，不能自动改临床含义。第219批仍未通过。

隔离作业 `2a2d0f0a32c642d2bf99d2e3e1144fe4`（v165）预检复用218批、刷新109批，因模型输出“次数”缺单位、局部修订遗漏观察规则而停在第219批。通用单原子补答现明确须同时给出完整单位和观察采用说明；`88e53e8e56394e9db93c1012f60b68f1`（v166）再次在第219批停住，提示单修不能代替已核草稿续跑。

v167 在新作业入口验证来源、分包、实际模型身份、有源解释、草稿结构与当前原批次门禁后，允许从旧失败作业的**未发布**局部草稿继续，不把草稿当成功批次。`0b33070638b04f83bc2cf62645b370ee` 从 `20ac29acef0b4c8788ddf1be6207b218` 合法复用218批、局部恢复第219批；但其纯文本模式新会话只说“遵守随附 Schema”，实际未附 Schema，模型三次返回字段摘要而被拒。v168 仅对该新会话在文本模式附完整候选 Schema；集中相邻检查 **282 passed**，不代表整方案。隔离 `63ae2d40afae47a7ae6fde7900a41c1e` 再次合法恢复第219批，补入输出已经进入完整结构与原门禁，但仍把“可重新筛选”的可选操作改成无条件必做，门禁 `OPTIONAL_ACTION_MODALITY_DROPPED` 拒绝；此批及整个 W6 仍失败，未发布。

上述作业均为 `failed_final` 历史，不激活或覆盖。现行最高完成深审批次仍为218/327，后109批与官方＋跨章共同发布未核；W7 病例工作稿、更正/补证和 Q3 浏览器验收均未完成。下一动作是用第219批冻结原文、已有草稿和失败候选做独立来源核对，确定应如何分别表达可选重新筛选、次数上限以及触发后必做操作，再做最小通用修订；禁止为此方案写死条款、疾病或阈值。

## 2026-09-26｜W6 当前隔离作业与跨段条件风险

执行选择：继续由主线程持有同一隔离库和正式方案作业；这里的已完成批次只作有身份的局部复用，临床采信仍须完整目录与原件复核。用户已授权本轮收费模型真实调用。原始方案、失败回执、正式项目与已发布规则均未改动。

新计划把现有流程目标要求的动作种类及准确来源目标带入深审批次；建作业前按当前规划材料逐批审核历史回执，旧批次材料变化则重算，损坏则拒绝。来源摘录核验只允许同一原文句首短主语补全、以及单元内首个冒号所界定的前置时期；不能跨第二个时期标题或借入条件。集中相邻回归 **287 passed**，仅覆盖这些合同，不等于整方案验收。

隔离库 `/Users/smkzw/tmp/enrollment-review-w6-v142-relation-isolated-data` 内，v162 作业 `272ebe6ce8c14d02a0ceebcb00226800` 因旧/新分包哈希比较位置不当在首批前失败；v163 `c37a4a8942104113b177da346a80b121` 在第4批暴露同段前置时期漏继承；v163 `fbbf2f327e2d43e488032ef9991829bd` 合法复用123批后在第128批暴露同句短主语被裁掉。三次均保留为失败历史，不激活。随后从 `fbbf…` 建新作业 `2b123779ed32437db67be9669d628aa4`：产品线路与冻结线路双阶段一致，127批可复用、255批需继续；真实执行到第219批（304/470步骤），因条件分支映射未通过而 `failed_final`。它不是发布成功。

第219批拥有 `body.p624`（重新筛选总则），`body.p625`—`body.p628` 的四项条件只作只读上下文并将在后续批次单独拥有。模型先缺观察选择说明与次数单位，经两次结构修订后仍把总则条件只放在适用人群、未按来源映射触发分支，正式门禁拒绝。**更大的风险是跨段列表没有在同一候选中完整装配**：后续单项即使各自通过，也不自动证明“满足下列条件才可重新筛选”的合取/替代关系。下一步只在同一来源链上做有界补核：先检查增加一次修订预算是否让第219批合法通过，再核第220—223批及最终目录是否真正保留总则与条件关系；若不成立，应改通用的续列分组/依赖关系与复用策略，不能仅放宽门禁、补写医学答案或把失败快照当成功。W6、W7、Q3均未验收，`claims_complete=false`。

## 2026-09-26｜W6 来源范围与局部恢复实证

执行选择：主线程沿既有方案作业、冻结来源和隔离数据副本修正同一合同；真实跨章关系仍需在全方案门禁和独立审阅后才能采信，不把局部模型输出当发布结果。产品调用由本系统内置传输直达已配置的方案控制模型，没有借外部开发 harness 读原件。

单单元隔离作业的第 4 批先后暴露了三类可定位问题：治疗持续期被误作事件窗口、行首“筛选/导入期：”因句号被错误排除在后续用药句的范围之外、来源与目标核对在普通决定上多填仅供“可能同一要求”使用的对象字段。修订只限定相应修复路径、校验边界和提示合同，不填补缺失的医学含义。特别是行首冒号标题可覆盖其后同一来源单元的句子，但中间出现另一冒号标题即停止；上一动作的时间仍不得借给下一动作。旧失败回执及原文不变。

后续第 7 批发现批次结果模型与既有消费者对“待跨章核验”不一致，已补齐同一合同，该状态仍须最终对应关系复验；第 7 批又暴露无时间上界时的无意义开闭标记，解析和局部修订仅在边界不存在时规范为默认值，有实际边界时保留开闭。第 10、11 批查出确定性时间扫描将“75周岁”及指南书名《…（2022年修订版）》误认作入排时限；修订排除年龄单位和书名内出版年，不排除真实“病史至少2年”或治疗“75周后”。这两批旧原答按当前门禁只读重放通过。没有把缺失日期或临床结论自动补给模型。

从合法入口依次建立 v149–v152 隔离任务，复用同源已完成发现与深审批次；v152 前 10 批完成，第 11 批因上述指南年份误报停住。所有失败作业与原始回执保留，不原地改成成功。现行源码为 v153；相邻方案 Agent/执行功能包 **228 passed**，这只是局部回归、冻结回执重放和前 10 批结果，不是 W6 完成。`claims_complete=false`，W7 病例工作稿与更正、Q3 仍待完成。下一步从合法入口按 v153 继续隔离任务，逐项定位新的失败；若全方案通过，再核官方与跨章共同发布，不用旧失败快照替代。

## 2026-09-26｜跨章节同一要求的待核关系

执行选择：主线程直接负责同一方案作业的合同、持久化与发布消费者；跨章语义尚有临床解释不确定性，正式发布前仍须独立审阅，不以单模型输出关闭。现行产品方案控制模型为显式配置的 `ollama-cloud/deepseek-v4.1-flash:high`；主视觉/方案解构的 OmniRouter GLM 线路已恢复并通过无病例最小连通性检查，没有临时改用 MTPLX 或修改共享网关。

来源逐项核对新增 `potential_same_requirement`，仅记录另一只读原文单元可能复述同一要求；局部比较要求两端逐字动作、另一单元同句时期、来源身份及差异。运行结果保持“待跨章核验”；目标单元必须在自己的深审批次独立解析为唯一正式候选，且候选语义中的原文摘录同时含动作和时期。水合与最终门禁重算对应关系，发布目录保存内容寻址的来源关系；旧身份的失败作业不复用。这个路径没有把后文时期补写回摘要，没有借现有访视流程冒充控制，也没有产生受试者判定。

隔离只读回放沿旧失败作业 `02b77a3a37324ff1ab0982f36325f405` 的冻结来源、摘要第 8 条和后文章节 `su-f069335ff329c457a65cabb2` 运行一次当前产品局部比较：38.4 秒、模型 `stop`，回执指出两端“每次喷鼻200 μg（单侧鼻孔100 μg），每日1次”及后文“自筛选/导入期开始”；合同验证得到待核关系。原文表格的“研究药物剂量和给药方式”只是行标题，不能据此改变正文实际药物对象；局部提示已明确这一点。第一次局部提示缺少原文单元变量，已修正并增加入口级回归。相关方案解释/执行集中检查 **219 passed**，`git diff --check` 通过；这不是 146 批完整作业、独立临床审阅或正式发布成功。W6 共同发布、W7 病例/更正、Q3 均未完成，`claims_complete=false`。

## 2026-09-26｜W6 跨章节只读线索进入正式分包与逐项核对

执行方式：本次只改同一方案作业的规划合同、只读提示及其现有消费者，由主线程直接负责；高后果的跨章采信边界已在前次独立会商核过，本次仍不做新的临床采信裁决。

沿现有发现结果增加词汇中立的高相似来源短名单，仅把仍可核对的另一章节单元放进深审批次 `context_units`，不改变 owned 归属、不把相似当等价、不允许引用 `non_control` 单元。计划合同显式记录每个 owned 单元的系统只读关系，并把实际上下文计入计划身份；执行身份 v141、来源目标核对 v13，与旧回执隔离。逐项核对现在能看到只读原文，但只能报告补充/冲突/待核，不能把后文时期借给摘要或宣称官方/流程完整覆盖。

只读复算冻结 SAR 隔离清单：1,927 原文单元、472 个仍可核对单元、382 个深审 owned；42 个 owned 找到 54 条跨章节只读线索，约 0.008 秒。卡住的摘要 `su-6792a14a616edad604cf020a` 命中后文 `su-f069335ff329c457a65cabb2`；新计划 146 批，第 2 批实际包含此后文为只读上下文，提示由 82,831 增至 83,514 字符。相邻规划、方案 Agent、执行的集中回归 **217 passed**（SWIG 弃用警告）；合同反例拒绝把发现阶段 `non_control` 混入只读关系。原 30,000 字符性能断言已不符合现行 51,544 字符的正式提示，调整为 60,000 字符上限；此断言只约束合成例提示长度，不代表真实大批输入上限。

使用当前产品 `ollama-cloud/deepseek-v4.1-flash:high` 和真实正式来源核对 Schema 对旧第二批做一次**只读隔离调用**。模型身份核验通过，26,227 字符、13 条核对项；返回原答 SHA256 `7fef523d1fbb7cb04a69de41a825c6e5c971c9ab0009df5bb616a70f09bc57c7`，本地忽略回执 `artifacts/0926v1-w6-source-target-v13-raw.json`。它仍将摘要第 8 条频次错误归入仅导入期流程的完整覆盖；现有 `FREQUENCY_ONLY_COVERAGE` 正确拒绝。**扩大上下文没有解决语义对账**，不能继续把提示微调当完成。下一结构性工作是有源的原文单元配对、被引用后文深审成功依赖和发布前复验；未实现前不得把第二批改为 completed。旧作业仍 `failed_final`，W6/W7/Q3 与 `claims_complete=true` 均未验收。

## 2026-09-26｜31001 检验报告手写符号的原图核对

在隔离资料库只读对照 31001 的一张过敏原检验报告原图与已保存的页级观察。九项印刷结果、采样/接收/出报告时间均被逐项摘录；黄色便签的文字可辨为“与研究疾病相关”，但开头笔迹不能据原图可靠判定为勾选、缩写或医学分级。既有观察却将其写成确定的“√”，属于局部过度识别。观察已正确保留便签书写者与所指对象未明，故不能作为研究者对某一异常项目的书面判断。仅在通用视觉观察提示中补充符号/缩写不清时如实标注的要求，提示版本 v3→v4；旧观察保留原身份，不自动改写或采信。现行事实整理提示原已禁止将书写者不明的批注当作研究者结论。相邻观察、编排和事实消费功能包合并检查 **77 passed**、退出0；`git diff --check` 与相关源码编译通过。

对同一冻结页经产品 `run_selective_vision_review`、工作树 `cms-router/glm-5.3-flash:high` 做一次隔离无入库调用，原始回执 `artifacts/0926v1-b03-glm-handwriting-v4.json`：`stop`，输入 3399、输出 5065 tokens。新版保留九项印刷检验结果及采样/接收/报告日期，便签起始处改为笔迹不清、**不确认为勾选**，书写者及指向对象未明。另一处签名也保持辨认不清。该单页对照仅支持此提示修订在这一页有效，不代表不同资料形态或 24 页全覆盖；未写正式事实、规则或报告。`claims_complete=false`。

## 2026-09-26｜B03 PDF 原件的非文字标记进入页级核查

从已冻结处理修订读取 PDF 原始副本，只读计算每页位图、矩形、曲线、线段及可见批注数量；纯超链接批注排除。按原件 SHA 核对并在同一批内缓存，不重写页图、OCR 或旧观察。格式探针证实 Ink 电子墨迹只在批注层，故补入批注计数与超链接正反例。原生文字页即使文字足够长，只要原件含非文字标记，现有选择性视觉规划可将其列为需要对照原图；PDF 解析失败将该页标为需要原图核实，原件缺失或校验值不符则停止该范围，不把未知标记伪造为零。31001 五份原件共 24 页均无原生文字且含图像，原有扫描路径已覆盖；本补项针对可复制文字旁另有批注的资料。规划身份 v4→v5，旧视觉任务不能按新规则冒充已完成。方案与资料页五组合并检查曾 **258 passed**；随后补充批注与原件故障反例，相邻三组集中复查 **63 passed**、退出0，只有第三方弃用警告。此举只使已有风险判断收到真实输入，**不证明标记一定是手写，也不证明视觉模型已核实**；扫描页原有入口不变，DOCX 转换所得非文字标记尚无同等持久来源。W6 完整规则、病例事实及 Q3 仍未验收，`claims_complete=false`。

同一隔离作业 `02b77a3a37324ff1ab0982f36325f405` 的失败回执只读复核：摘要中“每日1次”第8条仅关联到“不得调整背景治疗”的候选，`action_candidate_indexes=[]`，并未生成可独立消费的剂量/频次控制。该条的 `scope_quote` 实为“研究药物剂量和给药方式”，不是访视范围；第8条的来源目标回复明确拒绝把频次相同当作时间范围相同。冻结发现账本中摘要、另一摘要重复段及后续合并用药段均为 candidate，但各自 `required_context_structure_unit_ids=[]`；现有发现提示只看本批及邻近上下文，因此后续段落并未进入前批来源核对。后续完整合并用药章节是有源核对线索，但尚无已通过深审的跨章对应关系；不能据此把摘要判为已覆盖或直接发布。应在后续规范段落通过门禁后，逐字与摘要做语义对账并由最终门禁验证来源依赖，不能靠名称/数值相似自动合并。仅加一个“延后确认”状态、却不在最终发布处核对后续来源，会造成错误放行，故本轮撤回了未闭合的临时代码；不复用旧失败快照。

补充只读规模核验：同一冻结清单有 1,927 个结构单元；摘要 170 个规范字符，与另一摘要单元完全相同，与后续合并用药单元约 99.4% 的摘要字符对齐，超过 50% 对齐的其他单元共 6 个。这说明文本检索可缩小候选，但**相似度不能证明医学上同一要求**；一个否定词、例外或适用阶段不同都可能改变结论。下一版必须让候选关联带双侧原文与来源身份，后续单元通过深审且最终门禁重验依赖后，才可处理摘要的重复处置；否则原未决继续阻断，不按相似度自动发布。

接续源码核验：`build_source_target_review_prompt`/`validate_source_target_review` 只向模型提供官方与流程目标，`ProtocolControlAgentRunner.run` 遇 `unresolved` 会保持“需要核对”；即使整批候选结构通过，也不能激活。既有 `ControlCrossSourceRelationDraft` 不接受原文结构单元作对端，直接复用会伪造目标类型。完整跨章节关系需在来源单元层核双侧逐字原文，在最终水合/门禁确认被引用批次已通过，再投影至既有候选/控制关系；本轮未改这些合同，未重启长作业。

## 2026-09-26｜W6 两轮合法隔离试跑及跨章节来源缺口

沿同一冻结方案、同一已完成的 85 批发现结果，以现行 v139 产品执行器新建 3 单元深审作业；没有恢复旧 `failed_final`，也没有激活规则。先核对旧作业 `58635a690d2e44cb8c5421549d6de36e` 的实际版本就是 v139（独立审阅报告将其误记为 v130），其来源目标未决检查点原先没有保存已通过的 wire。本轮补上该保存边界，并把来源目标核对提示升为 v12：原文动作和相对时点均清楚、只是目标时间不齐时列为增量，而非误称原文无法读清。`1911da9e6bf1429e90200817f4c868e9` 在第二深审批次仍因流程执行节点配错、随后漏掉本节点判定而失败；检查点无可复用 wire。修正同阶段与跨阶段修订提示的混用，单独标记修订合同身份后，`02b77a3a37324ff1ab0982f36325f405` 的同批候选和来源时间修订已通过，且保存 partial wire，但来源目标复核仍对第 8 项“每日一次”保留未决；作业终态仍为 `failed_final`（87/234），**不是完整 W6 通过**。

只读检查完整冻结深审目录发现同一剂量在后续“研究治疗 / 既往及合并用药/治疗 / 合并用药和治疗”独立结构单元 `su-f069335ff329c457a65cabb2` 中，与“所有受试者自筛选/导入期开始接受稳定剂量的背景药物治疗”同段出现。摘要批次的 `su-6792a14a616edad604cf020a` 则单写剂量和每日频次，前两个单元分别说明导入与双盲期。当前逐项核对只能引用本条原文/标题和官方或流程目标，不能引用后续有源单元，也不能保证后续单元最终被发布；所以不得把摘要的频次直接判为仅筛选期、全治疗期或已完整覆盖。下一实现方向是**有来源身份、明确语义关系和最终发布依赖的跨章节同一要求对账**，先用两种不同结构的方案验证，不以药名或本方案编号编码。不能仅放宽时间门禁或把后续章节文本复制进摘要单元。两轮产品作业回执均留在隔离库 `/Users/smkzw/tmp/enrollment-review-w6-20260924-v102-isolated-data/enrollment-review-v2.sqlite3`，源文件未改。相邻方案 Agent/执行/传输集中回归 **245 passed**，`claims_complete=false`。W6 同源目录发布、W7 一例完整审核及更正、Q3 真实浏览器验收仍未完成。

补充核验：使用现行产品 `ProtocolControlAgentRunner` 及 `_validate_deep_batch_output` 对同一冻结深审计划的第 97 批作**独立只读**调用，三轮内得到 `已解析`，来源解释确有“自筛选/导入期开始”与同句剂量；这不是持久作业通过，也不自动消除第二批未决。C01 独立只读审阅 `runs/conference/enrollment-w6-cross-chapter-scope-20260926/general_single_object.md` 一轮成功，所有者裁决见同任务 review/metrics：采纳跨章节有源依赖、发布时重验的方向，不采纳药名加数值序列自动认证医学等价、也不采纳变更语义却维持旧版本复用。原文频次即使模型漏填 `time_words`，短路径现仍从逐字原文识别并拒绝省略时间范围；相应执行/组件身份已升为 v140。此轮追加反例后集中回归 **246 passed**，未做完整发布或病例验收。

## 2026-09-26｜W6 同源第二批的有界时间修订与来源门禁

在隔离副本中连续诊断冻结批次 `pcb-63a0d80b79911f6246a7af27`，未续跑旧 `failed_final` 作业、未修改原件或数据库。真实产品模型回复先暴露流程受影响节点错配；给模型显式列出冻结流程的实际执行节点后，下一次读出又暴露相邻动作时长被借给治疗义务。来源解释层的原句核对同时拒绝了把“资格合格后”写入动作引文，以及把随机访视日期借给给药动作；这些都是有价值的拒绝，不是已发布规则。原始回执分别保存在 `artifacts/0926v1-w6-deep-0002-stage-relation-diagnostic.json`、`...target-stage-index-readonly.json`、`...validated-source-candidate-readonly.json`。

产品现有单项时间修订通道增加 `TREATMENT_DURATION_USED_AS_EVENT_WINDOW` 的精确来源定位；只准修改唯一已定位原子的时间约束，其他候选、陈述和来源冻结。真实模型的局部回复 `artifacts/0926v1-w6-treatment-duration-local-correction-readonly.json` 把“开始接受治疗”核为筛选日期同日，没有再将七天疗程写成事件窗口。该回复把无数值边界的 inclusive 标志写成 null；修订合并仅在对应边界本身不存在时规范为默认 true，有实际边界时仍拒绝 null。将两份此前保存的候选局部回复与这份时间回复，在**同一冻结批次**只读回放，`hydrate_protocol_control_agent_output` 成功、`check_protocol_control_batch_candidates` 返回零项；这不是整批来源目标复核、完整 W6 发布或临床结论。

C01 独立只读审阅 `runs/conference/enrollment-w6-cross-stage-prohibition-20260926/general_single_object.md` 核实禁止类当前观察与未来持续义务已有表示；不采纳把禁止类扩进跨阶段补充关系的建议。采纳对最终判定节点不可伪装“后续复核”的修订引导与禁止类排除反例；未扩大发布门禁，也未扩大通用修复轮次。方案 Agent/执行/传输/跨阶段合同的集中回归 **244 passed**；另一个历史发布门禁测试文件仍有 **29 项旧夹具失败**（无求值规格及来源政策），不当作本轮通过，也不削弱生产门禁。`claims_complete=false`。下一安全动作是以本批冻结来源和可复用身份进入正式隔离作业的合法恢复/新建入口，核完整来源目标复核；若仍失败，按结构化错误逐项定位，不再凭状态反复重跑全文。W7、Q3 仍待完成。

## 2026-09-26｜W6 冻结目录精简与逐条时间归属复核

执行方式为所有者直接改动 + 一次 C01 独立只读设计审阅：本轮同一冻结批次、同一代码/来源和临床语义共享可变，另派执行者收益不足；同段混合当前/后续义务存在实质解释不确定，故审阅由现行 `hermes_workflow_guard` 生成包，`conference_session_runner` 实际以 zcode/GLM-5.3-Flash:max 运行并同会话追问一次。审阅产物为 `runs/conference/enrollment-w6-source-stage-design-20260926/general_single_object.md` 与 `_round2.md`；它们是建议，不是发布许可。最初 C03 路线的主适配器在本地 runner 不受支持，未发该路线的模型；改用适合代码设计的 C01 现行路线，未偷偷用产品 VLM 代替会商。

对旧隔离库 `06817f8e947f4050adacb6e4303d2ec4` 的第二批做现行产品 harness 只读调用，原提示约 101,809 字，249.58 秒前一轮对照为 243.86 秒，旧版结构/发布错误多轮后未形成可用结果。冻结目标目录含 23 条官方目标、41 条流程目标；其给程序使用的位置信息、内部摘要和来源 ID 不是模型解释所需的原文。`build_protocol_control_agent_prompt` 只精简**发送给模型的目录视图**，仍保留每个官方编号、目标身份、访视及全部原文摘录；内存中冻结完整批次、目标逐条复核和发布门禁不变。提示版本 v2.77→v2.78，旧回执不按新提示身份复用。第二批首轮提示实测 80,089 字，比原完整目标视图少 18,613 字；一次产品只读实跑在 `artifacts/0926v1-w6-deep-0002-compact-target-readonly.json`，249.58 秒，已形成可校验 wire，但后续逐项来源复核仍拒绝陈述 6/7/8 的时间归属，**未发布**。这不是速度改进的实证，也不是完整临床验收。

独立审阅初次建议把治疗期义务送至“未来访视”；所有者立即用冻结 `deterministic_closure` 核实目标表只有导入期 D-7～D-1 与基线 W0/D1，且该跨期单元只 owned 一次，没有覆盖 W0～W4 的未来节点。同会话追问后，审阅者撤回该建议。当前死角是：同一来源单元把本次动作、随机/给药动作和治疗期持续义务混在一起；逐项原文复核能发现缺口，但原合同仅允许禁止类的未到期持续义务，不能把 required 类未来义务塞入本次“已完成”。陈述 8 只有每日频次，没有独立时期，不能从邻句自动继承。两轮建议均未直接采纳为代码，因为扩展持续义务前还须同时修改消费者与发布门禁，不能靠模型分流词汇或时间名称相似就声称闭合。

另一冻结批次（摘要表三行）在当前提示下只读实跑 109.71 秒、一次解析成功，旧回执亦一次成功且均为 0 候选；新旧两个非候选处置名称/官方链接不同，属于待核语义差异，不能拿成功数证明新提示与旧提示质量相等。原始只读回执 `artifacts/0926v1-w6-deep-0001-compact-target-readonly.json`。轻量产品端点诊断另证实 Ollama Cloud 虽接受 `json_schema` 参数，但返回 `{}` 违背完整 Schema；保持该线路 `text` 模式和原 Pydantic/发布门禁，不以看似“严格输出”删除 Schema 文字。相关方案 Agent/执行/传输集中回归 **228 passed**，5 项第三方 SWIG 警告；只是局部工程检查。W6 同源正式发布、W7 病例报告和更正、Q3 浏览器及留出仍未完成，`claims_complete=false`。

## 2026-09-26｜旧病史来源关联前置核验

隔离病例 24 页整理的模型步骤全部完成后，才在最终档案生成时发现旧事件引用 23 条非现行链头事实；已有事实虽有同值新版，但来源候选与定位不同、没有继承关系，不得自动合并。本轮在正式建整理作业入口复用现有 Profile 引用闭包校验：若当前已发布病史、用药或审核记录的来源关系本来就断开，在新作业及模型调用前明确拒绝，并提示先核对原有资料关联。没有放宽最终发布门禁，也没有按同值臆造更正关系；干净病例仍可正常建作业。来源、任务、档案与发布相邻功能包集中检查 98 passed，`git diff --check` 通过。此项只节省已知断链旧库的无效调用，**不修复**旧库的 23 条关系，也不证明 31001 临床验收。`claims_complete=false`。

W6 同源隔离作业 `06817f8e947f4050adacb6e4303d2ec4` 仍为 `failed_final`，首批成功、第二批失败。只读核其四轮错误依次是持续治疗误用普通操作完成、后续持续义务挂到非禁止动作、时间数值缺乏原文支持、时间局部回复结构错误。最后的平铺时间回复已被现行源码的精确结构兼容及测试覆盖；不能把旧回执原地改成成功。跨阶段禁止的单条短读有独立来源证据，治疗持续期与完整同源发布仍未完成。下一步只对旧库走有据更正；完整病例验收应使用新建干净项目/受试者的原件链。

另对历史隔离回执 `artifacts/0925v1-w6-source-scope-diagnostic/deep-0001-focused-target-current-runner.json` 按其**原来源作业** `bf9f839ea5fa467990fc3f7aa6bcfe22` 的冻结批次重放当前 `_validate_deep_batch_output`：批次身份相符，但现行门禁返回 `TREATMENT_DURATION_USED_AS_EVENT_WINDOW`。历史“已解析”只是当时合同结果，不是当前可发布依据；不得拿它复用或把当前新门禁改松。旧 v138 作业的批次 ID 与该回执不同，也不得混淆两者。

同源第二批的当前全文线稿提示经只读测量为 **101,809 字符**：输出 Schema 约 30,104，已知流程目标约 30,901，官方目标约 12,269，系统合同约 19,368；真正本批 3 个 owned 单元与 3 个上下文单元合计约 4,790。该比例解释了高耗时与“局部错误反复重答”的一部分，但不能直接删目标/Schema，因为目标覆盖和跨章关联必须可核。后续应先用已冻结目标目录做有源短匹配，仅给单项语义解释提供需要的完整目标，并让最终完整门禁校验；未证明召回前不改生产输入。此项是测量与下一假设，不是已完成优化或缩减来源。

## 2026-09-26｜资料整理输入预检与既有来源断链定位

用户确认 OmniRouter 已恢复。本工作树产品主模型本已配置 `cms-router/glm-5.3-flash:high`；不更改共享网关或凭据。产品自带入口的无病例最小请求以正常输出预算返回 `glm-5.3-flash`、`stop`、“已连接”；64 总额度的探针只得到 `length`/空正文，是探针额度不足，不记作模型不可用。第二路线及方案控制路线仍按现行显式配置，未暗换。

隔离副本 `/Users/smkzw/tmp/enrollment-review-0925-fact-qc-isolated` 的旧整理任务 `e4866f1eb4b84f88942b8b6a2e91545a` 在第 3 组两页执行时才发现提示为 103,150 字符，超过 100,000 字符上限；前两组已花费调用。逐页重建 24 页，单页均低于上限（最大约 90,514 字符）。在现有建作业入口加入最终提示预检，超过则缩小连续页组，单页仍超限则在建作业前报出具体文档与页码；分组大小随原有任务身份冻结，不删 OCR、定位、视觉观察或临床原文。相关规范化/来源接线集中回归 165 passed，发布相邻组 76 passed，`git diff --check` 通过。真实隔离入口新建任务 `85b03ca44cc34103a693c5319cc44d71`，自动由两页组改为 24 个单页组，视觉范围哈希不变；旧任务未重试或激活。

该新任务的 24 个模型整理步骤均完成，不再出现输入超限；最终发布仍为 `failed_final`，没有新事实或 Profile 提交。发布事件的具体错误是旧病史事件引用已折叠链头之外的事实。只读检查证实这条断链在新任务前已存在：一个旧事件 34 条事实引用中有 23 条不在现行事实链头。旧事实与同语义的新版事实虽然数值相同，但新版有不同来源候选/定位，且未声明继承旧事实；本次试图按继承链自动补引用，隔离演算证明没有可用的继承依据，因此**相关补链代码已完整撤回**。不得按相同数值或稳定身份强行合并来源、关闭门禁或宣称病史已完成。24 页回执留在隔离副本，失败作业不可直接作为当前有效发布；后续以新建干净项目/受试者的原件链验证，并另行设计对旧来源断链的有据更正。`claims_complete=false`。

## 2026-09-25｜真实跨阶段禁止要求的隔离短读

只读从隔离 W6 旧任务 `06817f8e947f4050adacb6e4303d2ec4` 的确定性闭包与失败检查点，还原 `deep_0002` 冻结批次、来源陈述第9条及已有目标核对；旧任务与原始研究方案未修改。该条属于同一句原文覆盖筛选/导入期及后续治疗期的禁止要求。新单条解释输入约 1,093 字；产品自带传输显式使用已恢复的 `cms-router/glm-5.3-flash:high`、48,000 总输出预算作一次真实调用，34.27 秒返回。原始回复和请求摘要在被忽略的 `artifacts/0925v1-w6-shared-prohibition-glm-isolated.json`。模型把本次与后续陈述分开，代码逐字核相同禁止动作、时期、来源定位和本次观察范围；候选装配通过。再将它单独置入同一冻结批次的未发布 wire，其余单元保留待确认，既有 `check_protocol_control_batch_candidates` 返回 0 项问题。未把待确认单元当已覆盖，更未激活旧失败快照或正式发布。

为避免观察范围漏掉当前时期的原文名称，短读提示补充该逐字要求；另加“基线期禁止不能绑筛选期节点”的反例。方案 Agent、传输与执行三组集中回归 **227 passed**，`git diff --check` 通过。旧发布门禁工厂仍有先前已知的 29 项求值规格夹具失败，不拿这次聚焦成功覆盖它。整批另有持续治疗、明确疗程及导入后复核等时界条目未在本短读中处理；W6 共同发布、病例事实临床 QC、W7 工作稿/更正与 Q3 均未完成，`claims_complete=false`。

## 2026-09-25｜OmniRouter 恢复后的产品路线核验

用户确认共享 OmniRouter 已由其他 Agent 修复，先前临时改用 MTPLX 的指令随之撤销。本工作树 `.env` 的方案解构及主视觉入口仍为 `cms-router/glm-5.3-flash:high`，无需切换或改密钥；第二页读模型配置保持原有 `ollama-cloud/deepseek-v4.1-flash`，不借共享网关冒充。20128 端口已监听，产品自身 `independent_vlm_chat` 的无病例最小请求返回模型 `glm-5.3-flash`、`finish_reason=stop` 和正文“已连接”。未修改共享网关或真实资料。

隔离病例副本中的原 24 页视觉核验作业 `2ad7baa04f474c818cbc3e8859002d20` 已由合法重试入口在相同作业身份下补齐，现为 `completed`，24 个不同页来源均有成功观察；此前 13 次线路错误与 1 次来源校验失败仍保留为历史回执。这只证明页级技术覆盖，不证明事实、病史或临床判断完整。观察服务现使汇总错误优先显示线路级中断，同时保留每页原错误；相邻作业检查 26 passed。

跨阶段共用一句原文的禁止要求新增有源短解释与装配草案；合成条款已走通逐字拆分、候选装配、来源覆盖和现有批次发布校验，并拒绝改写禁止动作及把未来期资料纳入本次观察。聚焦 4 项检查通过。扩大到方案 Agent、发布门禁、传输和执行整组时为 343 passed、29 failed；失败集中在旧发布门禁夹具缺少当前必需的原子求值规格，既有记录已指出 HEAD 同样缺该测试字段，不能视为整组回归通过。新路径仍需另一结构反例及真实冻结来源核对，之后才可建立新版本 W6 作业。未激活旧失败快照、未发布官方加跨章规则；病例事实整理、工作稿、更正及 Q3 未完成，`claims_complete=false`。

## 2026-09-25｜隔离病例页核验与线路中断处置

在 `/Users/smkzw/tmp/enrollment-review-0925-visual-facts-isolated` 的原库备份副本上，从产品服务合法建立当前 v4 页面核验作业 `2ad7baa04f474c818cbc3e8859002d20`；原库、原件和旧回执未改。筛选节点 24 页按当前页风险规划全部需要原图核对，旧 v1 作业仅核过 1 页，不能沿用为覆盖。首轮进程中断后，核实原进程消失、租约过期，再由同一 Job 的启动恢复续跑，未建第二作业。隔离库现留 11 条 v4 成功观察、1 条来源校验关闭、13 条线路错误关闭；作业终态 `failed_final`，**未完成页覆盖，未进入正式事实发布**。其中一个模型未准确返回来源标识的具体原答未持久保存，不能推定只是漏写还是误写。

恢复时外部模型线路短时不可连接，原循环仍逐页重复请求。现于 `SelectiveVisionObservationService.run_postprocess` 在 `auth`、额度/限流、配置、连接等线路级失败后保存当前页失败并停止本轮剩余调用；来源标识错误继续视为单页问题。相邻观察服务及持久作业检查 **26 passed**。之后产品最小无病例请求仍报连接错误；网关日志显示共享 OmniRouter 于 21:03 CST 接收 SIGTERM 并正常关闭，21:04 无监听。本任务不拥有该共享服务，未重启、未改凭据、未切换模型。此前产品凭据与 OMP 同节点值一致，不能把这次连接错误再归为 401。

待共享网关由其所有者恢复且产品无病例连通预检通过后，只对该隔离副本中的失败作业使用既有重试入口；成功页应按不可变身份复用，失败页和未运行页分别核实。即使 24 页技术覆盖完成，仍须做原始证据临床 QC、事实规范化、完整官方及跨章规则发布、工作稿和更正闭环；`claims_complete=false`。

## 2026-09-25｜W6 来源时间完整性与旧回执隔离

已核本工作树 `.env` 的 `CMS_ROUTER_API_KEY` 与 OMP 当前同节点值一致；前次 401 的凭据更新及产品最小请求已有回执，本轮没有重复写密钥、改变 OMP 或新增收费诊断。逐项目标核对现在拒绝把来源动作中直接写明、却从模型 `time_words` 漏掉的阶段、区间或频次当作完整覆盖；只在模型声称“已有官方/流程要求完整覆盖”时拦截，不替模型推断当前/后续节点。此类漏报与“频次相同但时期未证”共用仅复查原声称目标的短输入路径，不重新发送整份冻结目标目录，也不准改引另一目标。显式作业身份升至 v138，并把该校验纳入组成身份，避免旧回执被新版准入规则复用。对“只报每日”“完全不报时间词”及两类局部复查的反例，与相邻方案 Agent、传输、执行功能包集中运行：**226 passed**，5 项第三方弃用警告；`git diff --check` 通过。

这仍只是防止错误采纳的边界：跨阶段持续义务的有源解释、装配及正式发布未完成；旧失败作业未续跑，病例事实、工作稿、更正及 Q3 均未验收，`claims_complete=false`。

## 2026-09-25｜B03 视觉补读进入病史前的待核呈现

复核当前正式入口后确认：选择性视觉观察并非通过旧双模型 `page_review_coverage_id` 混入病史，而是由事实整理作业冻结观察范围、执行时回读原页/观察身份，并作为补充材料单独交给整理 Agent；原 OCR/有效文本及其定位未被改写。先前怀疑 `include_visual_sources=False` 会丢观察，经读实际消费者证伪，未据此修改来源适配器。真正缺口是视觉读到但 OCR 没有的手写、便签或图表临床内容，旧提示仅禁止直接采信，没有要求逐页报告待核风险，可能被静默略过。现要求整理 Agent 在对应页 `unresolved_items` 中说明可辨/不可辨内容与待核问题，仍不得补造事实、来源定位或研究者判断。该视觉说明已计入现有提示内容身份，旧作业不能以旧说明回执冒充新版。相邻整理 Agent、视觉观察来源与恢复功能包集中检查 **143 passed**，5 项第三方弃用警告；未作真实 24 页重新整理，尚不能证明便签实际出现在工作稿或事实发布，`claims_complete=false`。

## 2026-09-25｜W6 单条目标误覆盖的有源复核

冻结第二深审批次的第7条只逐字写出每次剂量与每日频次；同单元另有筛选/导入期、双盲治疗期不得调整的要求。首轮模型把它与仅属筛选/导入期的流程目标判为完整覆盖，原 `FREQUENCY_ONLY_COVERAGE` 校验正确拒绝。产品 GLM 在单条、同单元上下文提示下改为 `additional_requirement` 并指出阶段范围未证明（约207秒）。正式纠错路径现仅在这类错误时提供原被误认目标及完整同单元原文，不重发另外63个目标；输出仍须是该原目标或无目标，原来源/目标校验和后续发布门禁不变。来源核对 v9、执行 v137 隔离旧回执。相同冻结单条的短输入诊断约59秒，仍判增量并保留阶段差额；单次随机返回不能证明统计稳定提速，也不能证明该增量已合法装配。两次原答在忽略目录 `artifacts/0925v1-w6-source-target-focused-statement7-receipt.json` 与 `artifacts/0925v1-w6-source-target-focused-v137-receipt.json`；旧作业/正式库未写。

方案 Agent、执行、传输相邻功能包集中验证 **223 passed**、5项第三方弃用警告；`git diff --check` 退出0。复查冻结候选：第7条仍为 `candidate_linked` 而非义务表达，且仅凭频次无法证明整个治疗时期；第6条后续持续治疗、第4条导入结束后复核也需分别按源阶段核定，不能把本次目标复核当全批成功。下一步处理有源的跨阶段要求装配或明确未支持处置，再从合法入口新建版本化作业；W6/W7/Q3、病例工作稿和更正均未验收，`claims_complete=false`。

## 2026-09-25｜W6 同批两个时间误读的原件对照

只读核隔离旧作业 `06817f8e947f4050adacb6e4303d2ec4` 的第二深审批次：初答只有一条候选结构错，故本轮增加的多候选逐项修复是通用防整批重写措施，**不是这次失败的直接解法**。首项把“自筛选开始接受治疗7天”误写为零天数值窗；另一项把“筛选/导入期 D-7～D-1”的访视阶段名称借作禁止调整背景治疗的独立日历窗。原门禁两次均正确拒绝。原子级求值错误现可走已有单原子短答，但本次首项还将后续持续义务挂在非禁止原子，必须维持候选级改写，不能用冻结字段的单原子修订删掉未来要求。

限界修订：多个无关候选各有结构错时最多三项逐个短修；时间短答若漏唯一包装，仅在内部合同完整有效时补包装。不同来源原子的日历问题可依次修、最多三处，同一位置不能循环；删除时间窗时模型须同时明确原子求值用途，程序只移除与已删除时间窗配对的日期操作字段，不改其他临床内容。时间短答加入**冻结访视名称只读参照**，不把阶段标签中的相对日标记当动作本身的窗口。执行及组成身份 v136，旧回执不自动沿用。

产品内置 `cms-router/glm-5.3-flash:high` 对旧冻结候选做无入库短答诊断：第一时间字段在可留存原答中改为筛选当日；第二字段在缺访视参照时先答 `null`，旧合同无法同步核用途；改合同后仍错误借用 1～7 天；加入冻结访视参照后，约83.56秒返回 `time_constraint=null, time_purpose=not_applicable`，两处合并经**该批候选发布门禁**通过。诊断原答/摘要存于忽略目录 `artifacts/0925v1-w6-calendar-focused-v133-receipt.json`、`...v135-second-receipt.json`、`...v136-second-receipt.json`；首个未保存原答的调用只有哈希，不能用于回放。以上复用旧失败候选仅作受控诊断，不是新版本正式作业的成功回执。

继续以同一冻结来源做逐项目标核对（约264.42秒），模型把“每日1次”与仅列在筛选/导入期的流程目标判为完全覆盖；来源原文同时涉及后续双盲治疗期，原核对校验正确拒绝，终态仍为待核。还有数条增量含导入结束后复核、基线随机及全治疗期持续要求，不能从一个已过的候选门禁推断全批、全方案或当前节点完整。原答在 `artifacts/0925v1-w6-source-target-v136-receipt.json`，未改旧作业/正式库。模型时间误读与目标范围误读是**两类问题**：继续反复补格式提示没有完成价值，下一步需把阶段范围与义务时间逐项有源解构，逐项消费当前/后续范围后再新建合法作业。

相邻方案 Agent、执行、传输集中复核 **222 passed**、5项第三方弃用警告；另扩到旧发布门禁组时 **29 failed**，均由旧测试工厂缺现行求值规格、继而缺最低证据来源政策遮蔽预期错误；未放松门禁或改这些旧测试样本来假称全绿。`git diff --check` 通过。W6/W7/Q3 未完成，`claims_complete=false`。

## 2026-09-25｜W6 同批逐项装配与待核局部保存

检查现有单项路径时发现：多条已核增量原先每取得一条短答就立即调用整批发布校验，可能因同批其他条目尚未补齐而误判该条失败，进而回退到昂贵的整候选补写。现将同批短答先合并，再统一经过原来源闭包、目标核对和发布门禁；混合批次中仅有部分短答成功时，仍保存已通过门禁的局部 wire，未支持项保持待核，不伪装完成。明确持续期/跨节点要求不再使同批简单项在读取前全部丢失；简单项核后保存，复杂项仍以 `TEMPORAL_SCOPE_UNRESOLVED` 拒绝，不进入整候选补入。若初始整批 wire 已通过原发布校验但只因时界待核而停下，也保存它供同身份恢复时重新验证，不激活失败快照；对应恢复反例不再重发整批 wire。执行身份 v131 及深审组件身份已隔离旧作业与回执，不改旧成果。

冻结第二深审批次旧回执只读复核：12 条来源陈述中，保守时间识别命中第 3、5、6、7、9 条；旧 GLM 对照没有可复用的局部候选，不能据此声称真实新路径已跑通。相邻方案 Agent、执行、传输集中回归 **219 passed**、5项第三方弃用警告，`git diff --check` 通过；新增两条/整批门禁与混合时间/局部保存反例。未续跑旧 `failed_final` 作业、未调用模型、未发布规则、未完成病例或 Q3，`claims_complete=false`。下一步需为复杂时间义务建立有原文和冻结节点支持的逐项解释与现有规则消费，并在另一结构留出验证，才可考虑新版本整方案作业。

## 2026-09-25｜W6 复杂时界回退收紧与产品模型对照

冻结作业 `06817f8e947f4050adacb6e4303d2ec4` 仍为 `failed_final`，未续跑、未激活快照。前置目标核对的两份原始回执再核：第一批因完整 `W0~W4` 时界在目标中缺失而正确拒绝；第二批虽通过结构校验，仍把后续持续治疗与跨期禁止列为当前增量，故**不能**据此发布。只读会议审阅 `runs/conference/enrollment-w6-source-first-stage-boundary-20260925/evidence_single_object.md` 建议拒绝此前置探针作为临床时界；主线程采纳有源、机械可识别的保守拒绝，不新增“当前/以后”猜测字段。

在既有单项装配入口及整批补入回退前，对明确疗程长度、跨访视区间、持续/继续、多个时期和仅有给药频次的陈述保持待核；同一门禁也阻止其从短装配失败后落回整候选补写。频次相同**单独**不能证明已有流程完整覆盖；如来源原文与冻结目标明确同一访视范围，仍可通过原覆盖核对。简单单访视必做和有原文支持的相对节点仍走既有路径。来源目标核对 v8、短装配 v7 隔离旧回执；这是一条**防误发布边界**，不是对复杂义务的正式解构能力。冻结第二批历史陈述的保守识别序位为 3、5、6、7、9；其余序位仍须逐项核时界，不能把阴性筛查当作安全证明。相邻方案 Agent、执行与传输集中回归 `218 passed`、5项第三方弃用警告，`git diff --check` 退出0；未做全库、浏览器或受试者验收。

CMS 对照的首次 401 经查是两项配置叠加：shell 继承旧 CMS 密钥，显式改选 CMS 的手写诊断构造器又沿用默认 Ollama 的角色地址。产品已有 `protocol_control_transport_from_model_config` 会为替代供应商隔离端点与密钥，故本轮不新增重复修复代码，后续诊断必须经该产品工厂并清除继承的过期变量。按用户已批准的范围，只将本工作树被 Git 忽略的 `.env` 中 `CMS_ROUTER_API_KEY` 改为 OMP 当前同节点环境中的有效密钥；未改 OMP、网关或其他密钥，也未输出密钥。最小无病例请求和产品模型身份预检通过。冻结 `deep_0002` 用产品方案 Agent 的 `cms-router/glm-5.3-flash:high`、48,000 输出额度实际对照耗时 1273.31 秒；12 条有源陈述已读，规则 wire 首答 `WIRE_SCHEMA_INVALID`，一次定向修订又因持续期被错用为操作完成而 `CANDIDATE_REPAIR_INVALID`，终态“需要核对”。完整回执保存在被忽略的 `artifacts/0925v1-w6-frozen-batch-glm-contrast-after-credential.json`，隔离库只读、正式库和原件未改。这表明换模型与较高输出额度未解决该失败家族；不重开 146 批。

下一安全动作：沿已保存的逐条来源陈述，先为复杂时间义务建立有源的逐项解释与现有规则构造器消费，保留未来义务和当前审核的区分；用冻结失败批次及另一结构留出验证，再考虑新版本的合法整方案作业。W6 官方+跨章规则未正式发布，病例工作稿、更正/补证及 Q3 均未完成，`claims_complete=false`。

第二视觉读道仍不通过产品准入：Ollama [模型页](https://ollama.com/library/deepseek-v4.1-flash/tags)标注图像输入，[官方视觉接口](https://docs.ollama.com/capabilities/vision)说明原生接口使用 `images` 数组，但本项目此前用标准 OpenAI 图像消息和原生 base64 图像做合成图对照时，该模型均未读到图；同路由另一视觉模型的阳性对照可读。公开能力标记与本账号/通道实际响应不一致，当前只说明**配置未达可用视觉证据**，不推断模型本体一定没有视觉能力，不把文字可用冒充原图复核完成。

## 2026-09-25｜W6 有源深审分包对照与失败恢复

在既有隔离库及同一冻结方案来源上新建受控作业，不激活失败快照、不发布规则。`a28a1f7c8a8d49d99190e2436385ef1b` 的 85 个发现步骤与收口完成，首个 12 单元深审批次失败；随后同源任务 `88054e37ba2848e381ab4a2f1d171ed2` 复用发现结果，首批仍因时间字段和列表引言来源归属失败。来源解释提示明确纯列表引言不构成独立要求、时间属性补答使用合同字段；`129cd728c4004f77a59c1b16f255fa88` 再复用同一发现，首批仍有完成操作、持续义务、受影响节点的不同错误。缩为每批最多 3 单元的 `06817f8e947f4050adacb6e4303d2ec4` 从实际冻结收口回执核为 **146** 个深审批次，首批通过，第二批失败：12 条来源陈述中出现“操作完成”用于含持续期/结果条件的义务、把后续持续义务挂到非禁止原子、时间上界缺少原文支持，最终单项时间补答返回不合合同的额外字段。任务终态 `failed_final`，无在途作业；缩包并非已证实的通用修复，不改正式默认批次大小。所有完成与失败分母以各自任务的冻结计划计，不能混用成功批次数宣称方案完成。

针对失败恢复而非医学宽松化：同一作业的失败诊断若保存了已核原文陈述、但没有可用候选草稿，人工重试时先核冻结批次、来源、提示、模型线路和修订合同身份，再复验陈述并复用；候选仍须重新生成及通过原发布门禁。此项**只覆盖同作业人工重试**，不表示跨作业自动搬用失败回执。单项日历时间补答明确只收 `time_constraint` 顶层字段，允许字段从正式时间合同读取，不能复制无原文支持的界限；补答合同身份已更新，旧成功批次只有实际用过旧补答才因材料变化需重算，未补答的成功批次可继续独立核验复用。相邻方案 Agent 与执行器测试集中运行 `172 passed`、退出 0；这是离线合同与恢复检查，**不是**真实方案深审通过。下一步先按 0925V1 从来源陈述到单条语义候选/现有目标建立短路径，冻结可用中间结果，明确复杂条目未支持的错误；在该路径及消费者检查齐备前不再重复开同类长作业。`claims_complete=false`。

同一失败批次曾准备以产品方案 Agent 的另一显式模型作一次隔离对照，`artifacts/0925v1-w6-frozen-batch-glm-contrast.json` 仅记录不含原文的诊断摘要；当前工作树的 `cms-router/glm-5.3-flash:high` 在来源陈述调用前即收到 HTTP 401，未进入医学判读、未写正式库、未发布，也不构成两模型质量比较。不得借用 OMP/Hermes 凭据或改路由来掩饰本次授权失败；本轮继续离线来源/装配工作。

在同一冻结失败批次上，采用本产品已有 `start_source_target_review`，将已核的 12 条来源陈述与冻结的 23 条官方目标、41 条流程目标作**独立于候选草稿**的隔离小样；`ollama-cloud/deepseek-v4.1-flash:high` 约 63.83 秒返回，8 条必做/禁止陈述的结构与现有逐项来源核验通过，3 条指向已有流程、5 条列出增量，摘要在 `artifacts/0925v1-w6-prewire-target-review-probe.json`。这是“前置核对”路线可行性的单批探索，**不是**源于已形成草稿的正式覆盖结论：临时待核处分无法判定给药后语句是否属于当前审核，来源动作与目标语义等价仍需复核；本次探针只保存响应哈希/分类，未保存完整原始返回，不足以作为发布或改动现行流程的证据。下一轮须先明确阶段处置和保存原始回执，再用至少不同结构的留出来源验证；不因这一个通过的小样恢复整方案长跑。

## 2026-09-25｜Ollama Cloud 填写入口与视觉能力核查

工作树 `.env` 的 `OLLAMA_API_KEY` 已有用户指定的 OMP 同一供应商密钥，权限 `0600`，且 `.gitignore` 排除 `.env`；`.env.example` 现于文头标出填写位置。产品只从本项目环境文件读取，不依赖 OMP。相同独立路线下的合成图像请求：OpenAI 兼容接口使用标准 `image_url` 对象及字符串形式分别返回未见图像；Ollama 原生 `/api/chat` 使用官方 `images` base64 形式亦返回“请上传图片”，HTTP 200/stop。模型列表、文本请求可用不能证明视觉输入可用；当前不得将 Ollama 第二读作为已验证图像复核，也未发送临床原件或更改其他模型路线。下一步核对实际供应商视觉能力/请求接入，采用合成图像先验证，再进行产品单页试跑。`claims_complete=false`。

## 2026-09-25｜A13 本节点操作完成的有源核对

用户确认“只要求完成操作”可按本次资料中的明确完成记录判断操作本身；结果是否正常仍由其他条款判，缺记录保持资料不足，明确未做与缺记录分开。复用现有方案控制、双读原文核对、资格选择及控制计算，不新建事实表或旁路。短语义装配新增 `result_requirement` 来源分类，只在 `complete_or_verify` 且模型明确 `action_only` 时写 `action_completion`；其他情况仍 `unresolved`。完整控制 Agent 提示同步区分操作与结果。命题核对新增逐字 `action_witness`（完成/明确未做/未证实），必须属于同一 locator，关系方向须一致；双读比较把见证状态纳入一致键，消费者再次检查两侧来源、范围与冻结当前节点。计算仅在来源范围可核、无缺口与冲突时给出操作真/假；混合记录和缺记录为未知。中文缺口映射已接入；提示、消费者、计算、短装配版本已提升，旧结果不借新语义采信。

相连功能包集中测试：新增领域/提示合同、当前节点消费及方案短装配四组 `160 passed`，退出0；正式前端构建退出0（已有大块提示）。此前合并旧发布门禁组为 `275 passed/29 failed`，29 项均先报旧测试候选“控制原子尚未提供求值规格”。对照 HEAD 可见该门禁原已要求求值规格，而 HEAD 中的 `_obligation` 测试工厂未提供它；属于既有测试样本与当前门禁不匹配，不归因于本轮操作见证，也不能算总通过。未以测试绿灯代替真实病例或正式方法评测；模型把“有结果”误写成“操作已完成”的风险仍须用独立留出资料核验。仍未从当前 DOCX 发布完整规则或完成受试者工作稿、补证与 Q3。`claims_complete=false`。

## 2026-09-25｜Ollama Cloud 凭据入口纠正

用户指出 OMP 已配置同一供应商凭据，而本产品没有给出可直接填写的位置。只读核对后确认：产品已有工作树 `.env` 加载合同及 `.env.example` 中的 `OLLAMA_API_KEY=`，但实际 `.env` 缺此项；OMP 的 `ollama-cloud` 凭据存于其独立凭据库，产品不会自动读取。按用户明确指示，仅将该供应商的一条有效密钥写入本工作树被 Git 忽略的 `.env`；未更改 OMP、其他供应商或产品模型路由，未在文档、日志、命令输出中记录密钥。核对两处密钥一致、`.env` 权限为 `0600`；产品密钥直接访问 `https://ollama.com/v1/models` 返回 HTTP 200 且列出 `deepseek-v4.1-flash`。产品连接配置的最小真实请求（high、无临床资料）返回 `stop`，模型身份仍为 `deepseek-v4.1-flash`。预检脚本须先导入 `app.config` 才会按产品启动顺序加载 `.env`；单独导入 `provider_profiles` 会误报缺凭据，不代表正常产品启动失败。这只证明凭据与最小文本请求可访问，不证明服务实际采用 high、视觉输入、结构化质量或真实病例闭环；后者仍须通过产品自己的 harness 做受控试跑。`claims_complete=false`。

## 2026-09-25｜B03 扫描页手写核实的单页诊断

本轮采用所有者直接实施与核对：提示、观察身份和两份真实页诊断共享同一调用/来源边界，拆给独立执行者会增加可变输入；正式临床采信仍需双模型、来源消费和独立验收，不以本轮单读替代。

同源提示优化后身份 v3，系统仍给模型完整输出额度，只改变观察重点：逐字保留临床病史、检查数值/单位、用药，明确日期与行为关系；重复行政页脚简述而不省略疑难临床原文。使用相同两份原件、相同渲染尺寸和产品 GLM 路线各重新调用一次，原始回执 `artifacts/0925v1-b03-glm-page-diagnostic-v3.json` 与 `artifacts/0925v1-b03-glm-chart-page-diagnostic-v3.json` 均为 `stop`。病历页输出由 4,715 降至 3,391 tokens，保留三药及剂量，并明确外院就诊叙述中的“给予”不等于已证实际用药起止；检验页输出由 5,107 增至 5,799 tokens，仍保留九项结果与单位、黄色便签、其指向及书写者未明。两页合计输出 9,822→9,190 tokens，单次随机返回不能证明一般性速度收益，尤其检验页更长；尚未做第二模型或跨资料形态盲留出，不能声称精度不损。提示 v2 诊断与 v3 正式身份不得混用。v3 后将选择性视觉规划、观察保存及冻结后消费三组合并复核 `58 passed`、退出0，`git diff --check` 通过；临时渲染图已清除。`claims_complete=false`。

针对原件中旧 OCR 完全漏掉便签的实际反例，选择性视觉核实提示明确要求逐页区分手写、圈注、便签与印刷文字，保留可辨原文、位置和所指对象；无法辨认或无法证明书写者时明确未决，不以手写直接冒充研究者判断。提示中的程序风险代号改为中文说明，避免被模型照抄进医学观察。提示身份由 `selective_vision_review/v1` 升至 v2，旧观察不能借新版提示身份使用，页级风险选择仍为 v4。该改动不写临床资料，也不改变已存观察。

使用产品自己的 `run_selective_vision_review`、现有 `cms-router/glm-5.3-flash:high` 路线，对隔离输入的 31001 筛选期检验报告第1页做**单页、单模型、无入库**真实诊断；原始返回在 `artifacts/0925v1-b03-glm-page-diagnostic.json`，`finish_reason=stop`，提示/输出分别约 3,369/5,107 tokens。模型指出黄色便签可辨“与研究疾病相关”，起首字迹不清，并明确不能确定便签所指项目及书写者；与原图只读观察相符，未把这条批注当作已确认的研究者书面判断。旧 OCR 未含便签，本次证明手写提示能在这一页发现它，**不证明**整个24页覆盖、第二模型交叉核实、字段归属、事实发布或入排结论。该诊断发生在风险理由改成中文说明之前；虽同属 v2，正式请求的内容哈希不同，不能将诊断回执作为正式提示身份的验收。回答还重复了大量已清晰印刷的表格/页脚文字，后续应在不损失印刷错误核对能力的前提下评估缩短视觉补读，而非直接缩小输出额度。`claims_complete=false`。

风险理由中文化之后，选择性视觉规划、观察保存及冻结后消费三组合并检查 `58 passed`、退出0；首次运行因新增测试断言用词不一致得到 `57 passed/1 failed`，调整提示表达后整组通过，`git diff --check` 通过。仅为离线合同检查，未再调用模型以“追绿”。

产品当前第二模型仍配置为 `ollama-cloud/deepseek-v4.1-flash:high`，但工作树环境没有该供应商专用 `OLLAMA_API_KEY`；未把 CMS 密钥送往 Ollama，也未把单模型结果伪装成双读。
再次只读核对本机没有 `ollama` 命令、Applications 中无 Ollama 应用且 11434 未监听；因此不存在可直接改接的已运行本地 Ollama 服务。保留用户指定供应商与模型，不擅自换其他端点或凭据。A13 普通操作的采用语义另向用户确认；答复前保持观察选择未核实，不把“做过核查”写成入排已符合。

视觉观察进入事实整理的连接、观察仓储及迁移三组相邻检查在前一次工具中断后先核无残留进程，再整组重新运行，`25 passed`、退出0。此前中断的调用没有验收结果，不与本次通过相加宣称临床完成。原件的手写便签只作为有页来源的观察和待确认归属；来源强度取冻结文档元数据，命题层仍要求明确研究者书面判断归属，不能因诊断文字出现“相关”就当作已判定。

同一产品视觉路径再只读诊断 31001 病历第1页，回执 `artifacts/0925v1-b03-glm-chart-page-diagnostic.json`，`finish_reason=stop`，提示/输出约 3,369/4,715 tokens。模型保留病历上的 2025-04-05 就诊、三项治疗及剂量、2025-08-08 本次就诊，与原图主要文字一致；但未建立处方/给予记录与实际开始、持续使用之间的严格区分，不可据此给用药暴露补起止日。其将遮盖身份字段列为“需人工核对”也并非本次临床问题。两页观察合计不能证明每页、每项事实完整；下一阶段应核正事实整理是否保留药名、剂量、用法及相应日期的不同证据关系，并排除遮盖身份的无效待办。仅靠再加病种/药名提示不可解决此类关系问题；通用提示需要引导时间与行为的逐句对应，并在真实来源消费侧复核。

## 2026-09-25｜命题资料的当前审核节点复核

工作稿与正式结果共用的原文含义采信路径，现将受试者当前审核节点与已发布控制要求的 `decide_at_node` 冻结映射再次比较；节点不符或缺少映射时只保留待核实，不因两个模型均写“节点对应”就采用。`qualified-proposition-evidence` 消费版本升至 v8，既有旧方法授权不能自动借给新算法。新增当前节点、错误节点、仅提前关注节点及缺少节点四种反例；与相邻模型传输配置合并检查 84 passed、退出0，与工作稿及资格评测相邻服务合并检查 37 passed、退出0；前端中文说明及缺口归类接通，正式模式构建退出0。该检查不证明普通复查动作已完成或整个入排条件成立；A13 的完成见证/明确未完成语义仍待来源化实现。Ollama Cloud 专用凭据尚未出现在产品环境，本轮未发临床原件或收费请求；正式方案和病例验收均未完成，`claims_complete=false`。

同轮原件只读核对：31001 隔离输入为 5 份 PDF、24 页；`extract_native_pages` 返回全部页面原生文字长度 0。病历、检验大多为整页位图（位图覆盖约 0.91–0.98）；邮件 5 页位图覆盖低但每页有 1,627–2,914 个非文字绘制元素，亦无可读文字层。现有 `page_processor` 对空原生文字走 `VISION_OCR`，本轮只证明进入识别路线，不证明 OCR 文字、手写归属、事实或报告已核实。后续病例验收以这 24 页逐页处理回执和原件定位为分母，不以 PDF 文件数替代。

历史隔离库 `sar31001-fresh` 只读查询有两组同名 5 份原件的 OCR 页记录，各组 24 页均有 `succeeded` 行且识别文字非空；这是旧处理痕迹，不是当前快照的有效采用授权。病历首页原图与一份旧 OCR 文字抽查，就诊时间和所见三项药物主要文字相符；旧事实记录中药物被拆为不同粒度，另有日期形态异常的候选。未完成逐项原件归属、时间与用法核对，不能将这些旧候选直接写成当前病例药史或资格结论。正式复用须先按源图、处理版本、原件位置及当前页覆盖回执核身份，再以病例全页质量核对确认。

B03 实际反例：`31001筛选期检查报告单.pdf` 第1页原图的黄色便签有手写文字，历史 `ocr_pages.normalized_text` 完全没有这段内容；表格印刷数字/单位在抽查中大体保留。故旧 OCR `succeeded` 只证明印刷主读，不可证明手写判断、对象指向或研究者归属，也不得由异常箭头代替书面判断。当前 `VISION_OCR` 路线按现有选择性视觉计划仍有原图核实资格；须用本轮产品作业回执证明该页确实被视觉读到、手写原文及指向经过核对，且不把便签人工猜字回填为金标或正式事实。临时渲染图已清除，原 PDF 和历史库未改。

同一历史隔离库仅有 2 条 `selective_vision_observations` 成功记录，均对应两组病历第1页；未见上述检验报告第1页的历史视觉观察。这是旧库实际覆盖不足的证据，不代表当前改进后的选择性视觉计划已失败或该手写内容在其他历史库均不存在；正式复跑须逐页核计划、成功/失败、观察来源和事实消费，不能只看 24 页 OCR 成功数。

## 2026-09-25｜第二模型改走 Ollama Cloud（待真实预检）

云端 API 模型名复核：Ollama [云端说明](https://docs.ollama.com/cloud)要求直连 API 使用 `/api/tags` 返回的名称；[当前模型列表](https://ollama.com/api/tags)列出 `deepseek-v4.1-flash`，而模型页面的 `:cloud` 示例针对本机应用/CLI。因此四个产品角色、工作树环境合同和回归期望均改为不带后缀的直连名称；未改旧作业回执。此前 7 项本地路线测试失败，是夹具把旧共享 `INDEPENDENT_VLM_API_KEY` 当作显式改选智谱时也可借用的主读凭据；现改成该角色自己的测试凭据，未放宽生产跨供应商隔离。新增假端点请求测试，核原图、准确模型名、high、输出额度和无强制结构格式；另一旧夹具缺读道身份，按现行合同补齐。逐页、传输选项、方案控制、事实整理四组合并 93 passed、退出0，`git diff --check` 退出0；这不是 Ollama 实际调用或 `high` 档位证明，仍需专用凭据和隔离预检。A13 普通完成行为仍保守为待核实，未把已复核动作自动判作资格合格。

按用户最新要求，当前工作树中方案控制发现/深审、逐页主读 B 与事实整理统一配置为 `ollama-cloud/deepseek-v4.1-flash`，请求思考档位 `high`；GLM 主读 A 和官方方案解构不变。产品 OpenAI 兼容连接新增 Ollama 独立端点 `https://ollama.com/v1` 与 `OLLAMA_API_KEY`；按官方云端 API 模型列表使用不带 `:cloud` 的标识，后缀仅供本机应用/CLI 使用。旧角色地址/密钥即使仍在环境文件中，也不得继承到 Ollama。旧隔离作业 `69fc195d0a414f6eaf217e2fb460b028` 已为失败终态，不跨模型续跑；8911 专用服务已停止。原有原件、临床库、历史模型回执均未改写，`claims_complete=false`。

当前 `.env` 的非密钥路由已切换，四个角色均解析为目标模型与 high；但本产品环境尚无 `OLLAMA_API_KEY`，因此**没有**声称新模型可用，也没有调用 Ollama 或发送临床资料。离线定向回归 8 passed（新路线隔离及旧 CMS 路线）；更大旧测试组曾出现 13 failed/87 passed，主要涉及旧固定路由夹具、MTPLX 本机装卸状态及本次新增允许列表（允许列表已补齐），不等同于新路线临床失败，后续按受影响合同集中复核。Ollama 文档说明 OpenAI 兼容端点接受 `reasoning_effort`，但不支持的档位可能回退到模型默认；取得专用凭据后须先核真实模型身份、high 的实际支持、结构化返回和视觉输入，再建合法隔离作业。不能把发送 high 当作已证实按 high 执行。

进一步查[Ollama 官方结构化输出说明](https://docs.ollama.com/capabilities/structured-outputs)：云端当前不支持强制结构化输出。因此方案发现/深审向新端点发送普通文本请求，原有提示携带输出 Schema，产品 Pydantic/来源/发布门禁仍严格校验；事实整理也不向云端附加 `response_format`，逐页主读原本即靠提示与本地合同校验。只对新供应商选择此传输模式，不改变其他模型。官方入排条款解构继续 GLM，未把尚未验证的 Ollama 官方解构入口当作已交付。新的离线回归须复核这一路线；没有实际端点返回前，不声称其结构质量或 high 档位已验证。

## 2026-09-25｜0925V1 持续期语义与局部复核

冻结首批 `deep_0001` 的模型直接诊断已返回可解析结构（4 次尝试、295.944 秒），回执 `artifacts/0925v1-w6-source-scope-diagnostic/deep-0001-focused-target-current-runner.json`。但这不是临床通过：其中“自筛选起持续治疗7天”被写成筛选后7天内的日期上界；现有日期计算只取观察日期范围的下界，不能由此证明连续7天完成。导入期/基线期决定节点亦需与冻结访视和方案原文复核。该诊断没有写库、没有发布、没有证明全部来源覆盖，`claims_complete=false`。

同一来源的背景用药剂量陈述，整批目标核对曾误报未决；用产品方案 Agent 对其中一条单项核对，在 11.792 秒内找到冻结目标及原文剂量依据，回执 `artifacts/0925v1-w6-source-scope-diagnostic/statement-14-target-review.json`。现为至多两条 `candidate_linked` 且整批误报未决的陈述增加一次单项有源复核；只在既有来源校验通过时采用，仍未决则保留失败，不扩展为全批重读。首批直接诊断的整批核对这次已成功，因此尚未在真实作业中触发该局部复核；其真实恢复效果仍待验证。此前两个隔离 W6 作业仍为失败终态，不复用为成功。

为防止持续期被日期窗口直接否定，语义 `interval_condition` 在日期计算不符时现返回待核实，不能单凭窗口得出“未完成”；版本升为 `control-calculation-experiment/v14`，旧计算产物保留原身份。方案 Agent 提示同步区分“何时开始”和“持续多久”：持续长度留在完整命题及逐期观察范围，不写成事件发生上界。此举尚不能自动证明逐日资料已完整，正式工作稿仍必须通过来源和范围核对。方案深审、原图规划/执行、时间计算相邻包集中检查 **196 passed**、退出0，5项第三方弃用警告；未开展完整真实方案或病例验收。下一步先核持续期/决定节点的原文与冻结访视是否一致，再做有界真实批次和来源覆盖对账，不能因 Schema 通过启动发布。

## 2026-09-25｜0925V1 恢复实施

隔离真实诊断：对旧失败深审批次 `deep_0018` 使用冻结输入和当前产品方案 Agent 作单批重放，实际经 `cms-router/deepseek-latest-cloud` 返回同名模型身份，耗时 218.768 秒、3 次尝试，最终状态“已解析”。首轮的 `SOURCE_SCOPE_UNGROUNDED` 明确落在第 5 条 `scope_quote`，第二、三轮无来源错误。回执在 `artifacts/0925v1-w6-source-scope-diagnostic/deep-0018-current-runner.json`，旧隔离库和失败任务未回写。这证明有界定向恢复在该批可行，不证明全部 94 批完成；其中 4 条访视操作只有目标关联、另外 2 条尚未定位到正式条款，仍需逐项来源核对。禁止把本诊断作为正式批次回执或发布依据，`claims_complete=false`。

病例原图核实补齐扫描 PDF 边界：`VISION_OCR` 页即使印刷 OCR 文字足够且置信度高，仍保留原图核实资格，防止手写或圈注因印刷层看起来完整而漏掉；并不恢复普通原生 PDF 全页强制双模型。选择性视觉计划身份提升至 v4，旧 v2/v3 回执在产品执行器调用模型前拒绝。方案解构、证据规划和冻结后消费五组集中验证 **252 passed**、退出0，5项第三方弃用警告。这只证明风险页不会被错误跳过；手写对象归属、真实病例覆盖与临床判断仍未验收，`claims_complete=false`。

合法隔离新作业 `38796d1511344c2d9595e78bb12c2c34` 沿原冻结来源与已核发现建成，86 个发现/闭包步骤完成；`deep_0001` 六次尝试后因 `AFFECTED_STAGE_DECISION_MISSING` 失败，依赖步骤失败，63 个深审尚未运行。真实原因是单个持续期候选的筛选执行访视补充关系被指向筛选节点，但完整持续期只安排在后续节点判定，门禁正确拒绝；旧作业未改。给通用来源限定补答补充跨节点约束后新建 `bf9f839ea5fa467990fc3f7aa6bcfe22`，同样仅发现/闭包完成，首批另因两条剂量/频次陈述的目标核对报 `SOURCE_TARGET_REVIEW_UNRESOLVED`，未发布。冻结目录确有对应两节点的背景治疗流程目标，是否确能覆盖本条的全部动作、数值及时间尚需单条原文核对；不能只因目标存在就自动采信，也不应反复整批重跑。两轮隔离服务均已正常关闭，`claims_complete=false`。

用户已明确要求从暂停恢复至完整交付。当前选择所有者内联处理方案解释、持久恢复和消费者的共享合同：这些文件有顺序依赖，分写收益低且易冲突；复杂临床语义的最终采信仍需独立会商与真实来源核对。首个有限动作是修正同批超过3条时逐条短读被整体跳过的边界，同时保持最多3次短读、剩余条目未决且不发布。相关检查与相邻消费者将按功能包集中执行，不逐处全库重跑。`claims_complete=false`。

本轮实作：同批五条独立动作中，一条含例外不可短装配，另外四条仍按最多三次的本轮预算逐条核验；首次保存三条有源增量，重启后仅补读剩下一条，含例外条目仍待核，不把局部结果发布。单项装配不再允许模型将原文复核动作改写为资格已符合；固定的命题与义务正文采用同一逐字来源。上传图片即使印刷 OCR 置信度高也保留视觉核实资格，避免手写/圈注仅因印刷文字读得清而消失；PDF 非文字标记数同时含表格线，不能未经分型直接接线导致普通表格被大面积重复核读。

只读旧隔离库显示，先前 `deep_0018` 两轮失败分别是时间措辞不属于对应陈述、共享范围不属于对应来源；旧恢复分支靠正则匹配中文错误并依赖单元内仅一条陈述。现将来源核对失败定位成 `code/statement_id/json_path/source_refs/retry_class/affected_dependents`，短答按条目而非相同单元 ID 定位，历史中文只作显示，不作为控制信号。同一来源单元含两条陈述的反例证明仅校正第二条；未知来源仍拒绝，原文与旧库未改。局限是“位于前文”不等于语义上确实作用于后句，最终仍需来源/目标核对及发布门禁，不把结构校正当作医学认可。

上述方案解构、来源错误恢复、选择性视觉和相邻消费者五组集中命令 `pytest tests/v2/protocols/test_slice58c_control_deconstructor.py tests/v2/protocols/test_protocol_control_agent_transport.py tests/v2/services/test_protocol_control_execution.py tests/v2/evidence/test_selective_vision_review.py tests/v2/services/test_selective_vision_postfreeze_orchestration.py -q`（使用 `.venv/bin/python -m`）最终 **248 passed**、退出0，5项第三方SWIG弃用警告。期间两次新夹具预期错误分别是把合法补读误作不应发生、把本来位于动作之前的范围误作无源；修正夹具后整组通过。仅证明该功能包的离线行为，不证明真实模型、病例或正式发布；`claims_complete=false`。

选择性视觉风险判断的变化必须与旧回执隔离：计划身份从 `selective_vision_review/v2` 提升为 `v3`。旧 v2 “图片印刷字读得清即可略过原图”结果不能凭相同页数被新规则采信；现有服务在读图/调用模型前拒绝旧规划，成功页/原件保持不变。与用户控制任务合并的六组功能检查 **278 passed**、退出0，包含 v2 回执被拒的反例（5项第三方弃用警告）。这是离线门禁，不表示扫描或混合 PDF 的手写内容已被真实核清；后者仍须按原件做病例侧验收。

同段范围还存在语义位置风险：前一动作的“筛选时”不能仅因出现较早就扩到分号后的另一动作；但“筛选期：”明确总领的多句要求不能误拒。来源结构核对现按分号/句界与段首冒号区分，两类正反例均保留。新规则保守拒绝重复同文动作的模糊定位，交由有源核对，不凭位置自动选一处。相关方案深审、执行和视觉恢复四组集中复核 **211 passed**、退出0（第三方弃用警告）；这仍不是模型对跨句范围的医学理解验收。

A13 已经 C03 一位独立审阅，受控主路 `grok-build/grok-4.7:high` 一轮完成、无 fallback；报告 `runs/conference/enrollment-a13-observation-semantics-20260925/evidence_single_object.md`，主线程评议及计量在同 ID 的 `reviews/`、`metrics/`，review-gate 通过。裁决为 revise：不得把 `unresolved` 直接改 `any/single`，尤其不能把“已做资格复核”误作“资格合格”；需来源绑定的动作主张、当前节点核对、明确反证与未提供区分、冲突/部分范围保留，且工作稿/正式消费者一致。当前仍未实现，保持未知结果。读者身份 `episode.workflow_stage_id` 与控制的冻结节点绑定已在候选材料中，但命题提示是否强制逐项比较尚未证实。

## 2026-09-25｜0925V1 无损暂停与复盘

按用户要求结束当前离线功能包后暂停，不恢复旧临床作业。五个相关测试文件合并运行 `238 passed`、退出0（5项第三方SWIG弃用警告），只证明已修改工程路径的离线合同，不证明原文全覆盖、模型识别、整方案发布或真实病例判断。`claims_complete=false`。本轮改动和“多轮检查仍有错漏”的分层根因、严格核对机制的必要性与局限、A10/A13/B/W6/W7/Q3状态、恢复条件和唯一接手入口详见 `HANDOFF_20260925_0925V1_PAUSED.md`。本轮未调用收费模型、未恢复临床任务、未改原件或临床库；仅按用户最新明确要求准备将限定代码/测试/任务文档递交 GitHub，原有未跟踪会商文件不纳入提交。

## 2026-09-25｜0925V1 资料核实回执逐页对账（离线工程验证）

复核发现失败关闭→任务失败→人工重试→只重读未成功页面的路径已有产品级实现和回归，不另造作业框架。新增最后一层来源对账：任务完成检查点所列每一条已采用观察，必须在现有追加写仓储中确实存在、状态为成功、身份对应当前规划/模型线路/文字层，而且其实际页产物与完成回执逐页一一对应。先前仅比较两组页号和数量，可能无法发现“回执引用了另一页记录”的错误。旧已完成记录没有被改写；不匹配时病史整理仍拒绝采用该范围。

观察服务/作业控制/冻结后执行三组集中检查最终 `52 passed`、退出0；新反例模拟一条成功回执指向另一页，正常回执仍通过。首次运行3项失败：两项揭示观察计划字段为含模型与OCR层的复合身份，不能比裸版本；新夹具漏原有 `source_ref=` 来源声明，故没有有效成功记录。修订核对和夹具后再运行通过。无收费模型、临床库或正式发布；这只证明既有服务的离线闭包，不证明真实病例的印刷/手写/圈注完整识别。`claims_complete=false`。

## 2026-09-25｜0925V1 首次提示与补答提示分账（离线工程验证）

复查 A09：旧提示摘要把未曾调用的补答合同也算进每个成功批次，文字修订会无差别触发重算。现将首次解构提示身份与补答合同摘要分开；成功检查点记录是否实际调用过补答。未调用补答的成功批次在补答文字变化后仍可经来源、模型线路、组成及现行发布门禁逐项核验并复用；调用过补答的批次在该合同变化后列为受控重算。旧检查点没有显式使用记录或新版组成身份时保持重算，不猜测历史。新版补答摘要损坏直接拒绝创建复用作业，非普通缓存未命中。人工局部恢复的失败检查点必须使用相同补答合同；执行身份 v130，组成身份 v2。

集中执行服务、深审和传输三组检查 `200 passed`、退出0（5项第三方弃用警告）；包含真实使用补答的服务回执、未使用补答时可复用的服务回执、旧字段缺失和摘要损坏反例。无模型、临床作业或正式发布。本项只消除无关提示变化带来的重复工作；A10 跨来源变化的批次依赖闭包未解决，A13 完成行为消费和 B/W6/W7/Q3 仍未验收，`claims_complete=false`。

## 2026-09-25｜0925V1 同批局部成果恢复（离线工程验证）

当前 HEAD 仍为 `6bd6ce95ebf08001b698ca25f38e1d3b28766c9b`，工作树未提交；继续保留所有既有 dirty/未跟踪会商文件。A11 的同批混合条目路径已调整：可装配要求逐条通过原来源与批次门禁后保存局部 wire；剩余要求失败时，现有深审失败检查点保存局部 wire、来源解释及版本/线路身份，仍为 `failed_final`，不激活草稿。人工重试重新验证来源和门禁，跳过已核的整批初稿；因传输历史只在进程内，恢复补入走新会话并限制为来源授权的候选，不借旧会话编号。执行身份 v129，组成身份加入局部恢复版本，旧回执不能无证据复用。

测试采用相连功能包集中执行：`pytest` 的目录/深审/传输/执行四组 **219 passed**、5项第三方SWIG弃用警告、退出0；另有同批3条中1条不支持的正反例、检查点人工重试及新会话结构测试。最初一次命令给错测试文件路径而未运行，纠正后才得到219项结果。此处只证局部工程路径，不代表条件/例外已被装配，不代表真实模型重启恢复质量、正式规则发布或整例临床验收。A11的真实来源回放和未支持条目的最终处置仍待受控试跑；A09/A10/A13及病例B01–B10、W6/W7/Q3仍未整体闭合。未调用收费端点，未恢复临床作业，`claims_complete=false`。

## 2026-09-25｜W6 无损暂停（本轮最新）

用户要求完成手头工作后暂停。以 `HANDOFF_20260925_W6_REVIEW_AND_PAUSE.md` 为接手入口；下方 v114 等段落是历史。当前代码身份为方案提示 v2.76、执行 v127，`claims_complete=false`。源 DOCX 在隔离库重新冻结为 `f05217cc62924d4d9d2ce73968fdc8d5`，41 个目录项；它纠正了访视表脚注遗漏，不能与旧目录批次混用。后续 v123–v127 均未完成方案深审、共同发布或病例审核。最后 v127 作业 `1ba2fec2bc3f43c9b6ccd42de9424335` 于第一个深审批次因既有批次提示/模型回执身份不一致而 `failed_final`，并非医学判读完成。五个相关作业均为失败终态，旧回执保留；未启动新的作业。

集中相关检查 `209 passed`、5 项第三方弃用警告，非全库/临床验收。进展与停滞复盘：逐字来源、阶段/时间和覆盖门禁阻止了多次有害误判，是必要的；但 1,927 来源单元、85 个发现批次及约 94 个深审批次的广覆盖结构，叠加模型整批改写、逐个错误暴露和每次修复版本化重跑，令验证成本与遗漏面同时扩大。下一步先核 v127 复用身份差异，不跨版本强行采信；再重新评估方案 Agent 的候选范围和局部有源解构职责，保留官方及跨章完整覆盖。W6、W7、Q3 均未完成，正式规则与工作稿不得宣称已发布。

## 2026-09-25｜W6 有源单项装配的受控验证

隔离库冻结首个深审批次的模型原答仍保持不变。新增产品方案 Agent 的短答路径：对已由来源解释与目标核对认定为额外要求的单条动作，分别读取访视范围或先后节点语义；程序只装配可由冻结目录确定的节点角色、来源和结构字段，再经原候选合并、水合、整批发布校验及来源覆盖核对。不能解释的条件、例外、时间、节点或资料选择保持失败，不以程序猜测补临床规则。模型不再输出由目标节点机械决定的审核角色；若输出摘录比先前核对的动作更完整，必须仍逐字包含先前摘录且属于同一原文。通用合同为提示 v2.70、执行 v114；没有添加 SAR 疾病/药物/阈值特例。

只读冻结输入加真实产品线路的 Runner 诊断得到 3 个候选，两条增量来源均为 `expressed`、原批次校验通过；产物 `artifacts/0924v1-w6-stage-bound-diagnostic/runner-result-v2.json`，仍非正式发布或临床验收。隔离正式 v114 作业 `bd886673f6414475a92c84900b04eb9c` 完成 91/167 后在第 6 深审批次 `failed_final`：来源解释两轮均未给出冻结单元内的连续原文摘录。v115 新作业 `0b609af3415641229c337ea929060be4` 完成 87/167 后在第 2 深审批次 `failed_final`：首轮候选有三个原子的求值项漏写内层来源；整候选恢复继而引出日期属性及流程节点关系错误。两次均无发布，旧作业保留。

因此，内置方案 Agent 增加一次“单条逐字摘录”回源短答，只能修同一单元的近似误抄；数值变化、无法定位及整条意义差距过大均拒绝。wire 解析仅在原子已带成对来源 ID/原文而其内部求值项漏写时复制同一字段，显式不同的原文仍由原 Schema 拒绝。这是格式成形，不改变临床判断或来源强度。当前身份 v2.72 / v116；受影响深审/执行/传输集中检查 178 项通过、`git diff --check` 通过。下一步从合法入口以 v116 重新建立隔离作业核真实进展；W6/W7/Q3、病例和医学核对未完成，`claims_complete=false`。

## 2026-09-25｜W6 来源闭包与当前节点范围的局部恢复

在未改原件和正式规则库的隔离来源上，先证明同一原文的“当前禁止调整”与后续持续义务可以分别保存：针对当前节点观察范围的产品短答约 8.05 秒，仅缩为“截至当前审核节点可核对的筛选/导入期记录”，采用方式及两处原文来源不变；冻结第二深审批次批次校验通过，**未作临床采信**。提示身份 v2.67、发布校验 v29、执行身份 v110；有源字段以外的修改仍拒绝。

合法新建的 v110 隔离作业 `7ef3fab0dbd3493e8ff85e6ce36ff7fa` 复用同身份发现结果，首个深审批次 `failed_final`、进度 86/167，没有发布。原始回执显示模型已提出“导入治疗结束后再次依据入排标准核查”的独立要求，但新增候选最外层多写了义务内已逐字保存的同一来源摘录，严格 Schema 拒绝。现仅在多出的外层摘录与义务摘录逐字相同时忽略这个冗余字段，其他外层未知来源照旧拒绝；冻结失败答复经原来源装配和完整批次校验回放通过，两项要求未被合并或遗漏。执行身份升 v111；受影响深审/执行/传输集中检查 170 项通过，未来禁止和来源强度定向 15 项通过。旧宽门禁夹具另有 27 项缺现行求值规格，不能据此称全库绿色。下一步从合法入口新建 v111 作业继续全批深审；旧失败作业、原始答复与正式库保持原样。W6/W7/Q3 和正式医学核对仍未完成，`claims_complete=false`。

## 2026-09-25｜W6 正式隔离试跑与输出成形纠偏

追加核实（同一冻结第二深审批次，非正式发布）：候选局部回复仅缺两个 `required_source_types`；产品 DeepSeek 独立线路在约31.49秒内只返回这两个资料类型，原候选其余字段不动，Schema 接受，但这不是临床采信。校验器曾因原子摘录为整段而误报“禁止要求遗漏”；现仅在原子确实写出同一句禁止行为，或当前/未到期两句以同一来源和相同禁止动作严格还原原文时认作覆盖，引用整段但未表达禁令的反例仍拒绝。现有后续义务已正确保存，模型第二次短答约30.84秒仅纠正当前句，不覆盖既有后续记录。整段中剂量必须执行与建议上午使用的强度原先被错误混用；现优先逐短句核对，无法定位仍保守拒绝。两份原始诊断回执分别在 `artifacts/0924v1-w6-source-types-diagnostic/result.json` 与 `artifacts/0924v1-w6-future-scope-diagnostic/result.json`。回放至此仍被真实 `EARLY_DECISION_FOR_FUTURE_ANCHOR` 拦住：同一候选在导入与基线均作最终判断，却将禁止调整固定为基线日回溯。不能靠改校验或手填日期通过，应由内置方案 Agent 将本节点给药核对与后续节点完整禁止要求重新分区，保留同源与未到期义务。新提示/执行/发布校验身份为 v2.65/v108/v28。受影响深审/执行/传输 167 项、禁止句和强度 15 项通过；旧全发布夹具仍有先前已知的 29 项求值规格缺失失败，不把本轮全套运行写成绿色。W6/W7/Q3 未完成，`claims_complete=false`。

沿 0924V1 专家包核对现行方案 Agent；未修改正式临床库、原件或已发布规则。来源补入现支持一轮多个有源要求仅返回新增候选，由程序保留原候选及范围外处置；定向候选修订请求附原稿和来源身份；复合时间摘录只有逐字属于陈述且包含全部已识别时间要素才可通过；无效共享范围允许一次带明确错误的来源重查，仍无依据就失败。这些均为通用来源/结构合同，不是 SAR 药物或阈值特例。相关后端 165 项检查通过，第三方弃用警告 5 项。

隔离库 `/Users/smkzw/tmp/enrollment-review-w6-20260924-v102-isolated-data` 的新建正式产品作业均保留失败终态：v103 `8a77163dd158421fb5c732030440a594` 第一深审批次因全稿修订越界失败；v104 `7a6c2289638d4740a4f4dcd3d76903d7` 第一批先后出现未来义务提前判定、无源审核动作及串条修订；v105 `b01938e888134324ab4cc64c5da05684` 第一批通过、第二批因有效复合时间摘录被旧校验误拒；v106 `99371a875092457a850a957ec3dbc398` 第一批来源编号错字并伴结构错误；v106 六单元且冻结模型线路 `59119ea360c44ead93ce93b07cfa783f` 前两批通过、第三批共享范围无源；v107 六单元、合法复用同身份发现结果的 DeepSeek 作业 `27f97fea8031477a85d962eb7ed59b89` 第二批遗漏禁止性要求且候选修订缺字段；同源 GLM 对照 `a02f975b83fb4da0b8ad4113e0e1fe48` 第一批在时间目标定位及日期属性处失败。另一次六单元发现复用在建任务前因旧作业未冻结模型线路而被正确拒绝，未建作业。上述原始步骤/诊断在隔离库 `job_events`、`job_checkpoints` 与本地 `artifacts/0924v1-w6-v10*-isolated-full-run.json`；没有任何一轮完成 hydrate/gate 或正式共同发布。`claims_complete=false`。

阶段结论：仅缩包、改模型或追加笼统提示均不足以稳定通过约 80 个深审批次；严守原文门禁，不再以重复同法收费试跑替代架构修复。按 0924V1 P2 先做一条通用纵向切片：模型只回答一个局部语义缺口，代码定点装配且旧字段不变，随后来源双向核查与既有门禁；同源小样比旧整候选修订的质量和总耗时，再决定扩展。W6/W7/Q3 未完成，正式工作稿与签发均不可宣称完成。

更新2026-09-24；唯一当前进度。旧记录完整保留history/20260922-pre-review/implement.md，不在历史日志继续堆“最新”。

## 2026-09-24 续作：W6 混合来源纵切片

续作实质进展（隔离诊断，非正式发布）：v98 的唯一未来禁止补答将原本误借的 7 天时限暴露出来；v99 的时间界限局部校正去掉无源数值，但仍暴露“当前义务与未来义务混判”。v100 从方案 Agent 的通用初稿提示分开两种要求，冻结首批首次取得已通过结构及批次校验的初稿；来源逐项核对仍发现同一流程单元两项未被目录标题覆盖的要求。v101 的目标核对改为逐个核实全部原文时间措辞，原“已覆盖”误判被纠正。v102 在一次受限补入中汇总两项差额并保持旧候选和原访视关联；首轮补入缺时间日期属性而拒绝。v103 模型修正后通过结构，但有无源审核指引而被发布校验拒绝；v104 允许对已合并草稿作单候选局部修订，原候选冻结；v105 在同一冻结来源回放已核内容后，实时局部修订和变更后的逐项来源复核均通过，首批状态“已解析”（约16秒为本轮回放与新核对耗时，不是从原件重跑总时长）。原始回执依次在 `artifacts/0924v1-w6-v100-stage-separated-first-batch/`、`v101-source-target-review/`、`v102-multi-addition/`、`v103-multi-addition/`、`v104-local-candidate-repair/`、`v105-target-after-insert/`；诊断均未写入临床作业或发布。此链跨多个隔离回执，正式作业必须以现版本从合法入口新建并重做全批，不能把拼接诊断当成发布。提示版本 v2.61、执行身份 v102；首批通过不代表其余批次、官方与跨章目录、病例及界面验收。`claims_complete=false`。

首次合法隔离全作业 `9101f19619c74399860acbf4826ec4c5` 在克隆库 `/Users/smkzw/tmp/enrollment-review-w6-20260924-v102-isolated-data/` 新建；产品实际发现与深审均做模型身份预检，原失败作业未续用。发现和确定性分批完成，进度86/148，`deep_0001` 为 `PROTOCOL_CONTROL_DEEP_OUTPUT_INVALID`，`hydrate/gate` 因依赖失败；无发布。模型来源解释把同单元原文开头的阶段范围同时放进逐字陈述与 `scope_quote`，旧校验只接受范围严格位于陈述之前，误拒了有效前缀。另一次不写作业的产品来源重读保存原答 `artifacts/0924v1-w6-v102-isolated-source-replay/result.json`，28条陈述；在只允许“范围位于陈述开头或之前”、仍拒动作之后范围的通用边界修正下，原答离线验证通过。执行身份升 v103，旧失败任务保持原样；准备新版本合法作业。受影响四组集中检查164通过，第三方弃用警告5项。`claims_complete=false`。

用户已要求恢复连续实施。按受控任务由当前所有者内联处理共享方案合同；在需要作出高后果临床解释或发布前，再按现行机制独立会商，不以模型自信代替来源核验。冻结首批先证明一个混合原文单元可局部退出某候选而仍为另一个候选提供来源，不自动改变该单元全批处置。短修订合同现分 `released_shared_units` 与无人再引用时的 `removed_unit_dispositions`，两者互斥、逐字来源核对。v97同源隔离调用约34秒，原来源归属错误不再出现，转被未来禁止提前判定门禁拦下。另以隔离诊断预算允许现有完整候选修订一轮，约64秒；模型补出了禁止类后续义务，但重写其他原子而丢失已核日期属性，并错误地把持续义务附在完成操作上。现只允许为唯一有源禁止原子补充后续义务，其余字段冻结；执行身份 v98，不与旧作业混续。前一组受影响合同159项通过；新补充尚待集中验证及真实调用，所有诊断均未发布。W6/W7/Q3未完成，`claims_complete=false`。

## 2026-09-24 无损暂停交接

本轮至执行身份 v96，交接见同目录 `HANDOFF_20260924_0924V1_W6_PAUSED.md`。v95 独立产品诊断的短答虽仅用约30秒，却试图将一个同时被另一候选使用的原文单元整段归为给药后事项，来源闭包拒绝（`CANDIDATE_DISPOSITION_MISMATCH`）；v96 禁止此类共享单元进入单候选短修订。没有借此放宽发布条件或启动正式作业。最终集中相关检查 159 passed，5 项第三方弃用警告，`git diff --check` 通过。原失败作业未续用，W6正式发布、W7病例与Q3验收均未完成，`claims_complete=false`。下次从有源混合单元的逐动作分解与确定性装配验证入手，不重复同法模型试跑。

## 0924V1 当前实施（进行中，非验收）

- 2026-09-24 W6 双模型同源对照与 v94 校正：不改变正式 `.env`，用内置产品传输和同一冻结首批单独调用 `cms-router/glm-5.3-flash:high`；模型身份回报正确，约1426秒，来源解释13条，后续结构/局部修订三次均未通过：持续义务期间类型、日期属性、非禁止类原子附持续义务。DeepSeek 此前同批来源为29条且失败类型不同，单小样不能据条数或时长宣称模型排名；但“只切换模型即可解决”已被否定。原始对照 `artifacts/0924v1-w6-v93-glm-full-frozen-batch/result.json` 未写回作业。后续共享时间字段继承条件补齐旧原子的未来持续义务、值比较、观察选择、复查等语义字段；任何差异都不自动继承，执行身份v94。下一步按0924V1专家包在真实失败小样证明轻量有源语义解释与确定性装配的最小切片，不能直接造全量中间规则库；先比较语义完整性，再比较调用总耗时。W6/病例/Q3仍未完成，`claims_complete=false`。
- 2026-09-24 W6 v93 有界来源重分区：发布校验指出混合候选含给药后执行事项时，现仅授权同一候选来源闭包内重分；只有模型明确给移出单元 `post_treatment_execution` 有据处置时，候选来源并集才可缩小，范围外候选和处置保持冻结。没有新规则真相表。合成正反例与受影响套件集中157项通过。v93 受控对照复用同一冻结来源解释及已核的初始结构/时间修订，下一份完整重分回复经产品独立模型实时生成约200秒；模型把混合单元拆成治疗期/筛选期两候选，却仍将基线随机分组视作入排候选，且将两个旧原子的义务类型、时间约束和摘录一并改变，再次缺失日期属性。因原子语义已变，不能继承旧字段，结构校验拒绝；未到来源核对或发布。这是产品质量/合同失败，不是服务不可用，也不是模型可用性证明。原始回执 `artifacts/0924v1-w6-v93-replay-source-wire/result.json`；不得靠增加同法随机轮数或手填医学字段宣布通过。W6正式作业、病例和Q3未完成，`claims_complete=false`。
- 2026-09-24 W6 v92 时间字段与来源分区边界：单候选修订仅在原子来源、时间约束、义务类型及求值方式保持一致且唯一匹配时，保留上一轮已核的日期属性；源或时间改变即拒绝，相关合成反例纳入集中回归。v91 冻结真实批次原始回执离线复核证实两个旧日期属性可保留，已删的给药后原子不复活。v92 受控诊断复用同一冻结来源解释和前三份回执，约0.06秒、未新调模型，随即被来源分区保护拦下：模型删除给药后原子时把一个原文单元从混合候选移走，却未提供这个单元新的有源处置。该拦截正确，不是时间字段失败；不能自动把该单元或整个段落判为治疗后事项。下一步需按0924V1有界来源重分区合同，让内置方案Agent明确给出剩余候选及移出单元的有源处置，并校验完整来源闭包；在此以前 W6 不发布。受影响深审/传输/执行集中检查155项通过，5项第三方弃用警告；诊断输出 `artifacts/0924v1-w6-v92-replay-source-wire/result.json` 仅用于定位，不是正式作业或临床验收；`claims_complete=false`。
- 2026-09-24 W6 v91 表格来源纠偏：v90 同源隔离对照复用v88已核来源和结构回执，实时完成逐项目标核对及一次来源限定补入，约63.3秒、两次实时模型调用，结构状态“已解析”；新增候选有逐字来源，原已核三项目标未被重复问答。**这不是新作业/临床发布。**新鲜v89来源解释曾因“共享范围”被拒；再查冻结DOCX表格结构发现所报14个“无来源”范围全部是同单元真实行标题，原校验器漏看`table_context`。现共享范围接受本单元真实行/列标题，仍拒相邻或臆造标题；提示说明这一边界，执行身份v91、来源提示v9。曾尝试一次局部模型纠正并得到空范围，但它会删掉真实表格标题，故该多余产品路径已撤除；原始隔离回执保留供追溯，不计通过。旧v89整份来源解释在修正校验下离线通过（25条陈述），不需要重读；两次解释的25/29条差异为部分相邻动作合并，尚需按动作核完整性。v91不复用回执的完整冻结批诊断约280.2秒，因候选修订把已有时间属性改坏而停“需要核对”，未发布。另v90新增候选将“导入结束后”绑定基线节点，同时承认二者对应未核实；该临床时点问题不能由“已解析”消除，发布前须解决。集中回归156项通过；`claims_complete=false`。
- 2026-09-24 W6 v90 同源已核结论复用：v89 隔离诊断复用 v88 同冻结来源与已通过结构的产品回执，只实时调用逐项目标核对和来源补入；约118.3秒后新增候选确实形成、缺失陈述变为已表达，但模型再次核对未改变的另外两条时反转先前有源结论，故仍停“需要核对”。现仅在同一次执行内对原文、目标、处置、覆盖状态及目标链接等核对输入保持一致的已验证覆盖结论复用；同段新增候选造成的关联索引变化不触发重新问模型。任何目标/覆盖状态变化仍实时重核，新增要求本身必须在最终草稿表达。v89 全新来源小样当时被共享范围校验拒绝；后查属旧校验器漏看同单元表格标题，并非模型凭空补造，纠正见文头v91。两份回执均为隔离诊断，非正式作业；`claims_complete=false`。
- 2026-09-24 W6 v89：逐项来源核对报告 `additional_requirement` 时，现可在同一已解析草稿上进入来源限定补入；官方/流程混合来源仍要保留原链接，越界或未补齐即停。冻结首批 v88 产品隔离对照耗时326.3秒：两次结构修订后得到已解析草稿，来源核对检出1条增量要求，但原共享修订预算耗尽，状态保持“需要核对”、未发布。修订为结构修订仍最多2次，已解析后允许至多一次独立来源限定补入；补入失败不得再转普通结构修订。集中测试117项通过（含耗尽预算后补入、官方混合来源和越界拒绝）；下一步对同一冻结首批做v89产品诊断，检查新增要求是否真的进入原文有据的候选，而非仅Schema通过。原始v88回执 `artifacts/0924v1-w6-v88-first-batch/result.json`；`claims_complete=false`。
- 2026-09-24 W6/W7 相连纠偏：产品深审有待核对的有源陈述而传输缺少逐项核对能力时，原路径可能跳过并返回“已解析”；现明确停为“需要核对”、保留草稿与失败原因，执行身份升至 v87，旧 v86 作业不能混续。正式桌面入口 `start_v2_desktop.command` 原已存在；另一个旧名称双击入口 `start_enrollment_review.command` 仍启动 `app.main:app` 且只认旧 `/api/health`，容易误入旧界面。现让该旧名称入口复用既有 `create_desktop_app` 与前端构建校验，统一打开同源 V2；端口只复用带本工作树身份的 V2 实例。缺正式构建时在启动本地模型之前拒绝打开旧页；`.env.example` 与路由断言同步当前 CMS GLM/DeepSeek 配置，不改真实密钥。受影响方案/传输/API/启动合同集中检查 156 passed、5项第三方警告；脚本语法与差异检查通过。另用 `/tmp/enrollment-v2-launch-check-0924` 隔离空库真实启动正式服务，迁移至0027后 `/api/v2/application-status` 返回当前工作树标识、首页200、未知API 404，随后已关闭服务；该检查不代表正式浏览器或临床资料验收。尚未真实双击、备份恢复或临床库 Q3，W6 正式跨章目录仍未发布，`claims_complete=false`。
- 2026-09-24 W6 来源逐项核对纵切片：产品深审先保留有源陈述，主结构通过后才核未完整表达的入排要求与冻结官方/流程目标；只允许有逐字动作和时点依据的完整覆盖。核对范围已从“同单元有候选”扩至官方/流程链接及本期补充/待确认来源，排除明确其他研究期和给药后执行；草稿已有链接不能用另一条相似目标替代。部分目标对应可保留为核对线索，仍记未闭合；失败回执增加有源陈述、覆盖账和逐项核对结构，不改失败态。执行身份 v86、来源解释 v6/提示 v8、目标核对 v5。只读冻结首批 25 条来源陈述中有 4 条需逐项核对；v4 提示的一次独立产品小样约24.4秒完成，3条有原文对应，1条时间关系仍不能由原文证明，**不发布**。旧三条回执不能冒充新版四条核对；v4 模型回执仅做了替换版本字段后的 v5 校验器离线兼容复核，不是 v5 产品生成。受影响深审/传输/执行集中检查 148 passed、5项第三方警告，`git diff --check` 通过；未运行完整W6、病例、浏览器或Q3。下一步先据原方案和用户的具体临床解释处理剩余时点歧义；如需新增控制要求，只允许内置方案Agent从有源原文产生，不能手工硬编码或让旧失败作业继续。`claims_complete=false`。
- 2026-09-24 本轮收尾：v79 原始回执再核为 208.70 秒、两轮输出、22 条来源陈述；覆盖账为完整表达 2、仅同单元候选关联 5、仅链接目标 1、未定位 14。逐字匹配只是诊断，不足以宣称已有目标在动作、时间及例外方面覆盖同段混合要求；正式 W6 不据此发布。正式双击启动脚本的端口探测已修正，未显式配置时的模型默认值与 `.env.example`/现行产品配置对齐，未触碰本地密钥。集中定向回归 157 passed，脚本语法、`git diff --check` 通过；未做全库、真实病例或浏览器验收。原始诊断留本地 `artifacts/0924v1-w6-v79-first-batch/result.json`，不提交临床摘录。当前要求无损暂停；接续时先核同单元多要求的逐项来源闭包，再新建合法隔离作业，不续用失败/取消的旧任务。`claims_complete=false`。
- 2026-09-24 v79 冻结首批对照：只读校验 v67 失败作业 `fba86f5c8e65480a8009802ccd949220` 的确定性收口快照，直接通过产品控制 Agent 与 `cms-router/deepseek-latest-cloud:high` 身份预检，未启动旧/新临床 Job，也未发布。首轮仅一项日期属性缺失，第二轮69字符的定向补答后通过**当前**结构及批次候选门禁，总耗时约208.7秒；回执含所有原始响应及哈希 `artifacts/0924v1-w6-v79-first-batch/result.json`。来源清单有22条、12个 owned 单元；逐条字面账只有2条在候选义务中完整表达、5条同单元关联但未完整表达、1条仅链接已知目标、14条未定位（多数为本期排除或给药后单元）。特别是同一候选来源中的知情同意和导入后再核资格两项仍仅同单元关联；冻结目录虽有 IN-07、签署知情同意和两节点审核入排的流程项，当前候选未为这两项建立逐要求覆盖关系。结构通过不能证明该关系，也不能擅自另造重复要求。**不能据 v79 诊断发布。**下一步以来源陈述为单位核已有目标的动作/时间/例外覆盖和剩余增量，不凭标题相似或候选存在标绿；然后再从合法入口运行全批。`claims_complete=false`。
- 2026-09-24 v73/v74/v76/v77 同一冻结首批的隔离诊断：v73 约425秒，初答结构错误，后续分别为未来禁止提前判定、给药后事项错放当前阶段、无源审核动作；v74 有源陈述单独调用约66秒，但把两个不连续时间短语拼成一个假引文；改为逐段连续原文数组后，v76 约48秒得到29条有源陈述，仍把“每日一次”等用法误当跨期时间范围，因此撤销粗粒度阶段归属字段。v77 完整调用约338秒，首答把后续持续义务挂到非禁止类给药原子，还用“不可调整背景治疗”的摘录支持继续给药；第一轮被送入冻结后续义务字段的单原子修订，必然失败；第二轮整候选修订仍未纠正。原始回执分别在 `artifacts/0924v1-w6-v73-first-batch/`、`...v74-source-only/`、`...v76-source-only/`、`...v77-first-batch/`；均未发布。现有源陈述只保留逐字段落原文，不充当阶段判定，时间措辞必须出现在该陈述逐字摘录或所属标题，防同段另一动作的期限被借用；单原子修订仅处理其准许变化的求值/时间结构，后续义务错误转至候选级并明确要求原文支持，不自动删改临床含义。提示 v2.59、来源提示 v6、执行 v79；深审/传输/执行/作业与启动脚本聚焦回归157项通过。把 v77 的25条历史来源陈述仅替换版本字段后，在只读冻结批上用新校验复核通过；这**不是**v79模型响应或临床正确性证明。此修订只排除一条必败重试路径，**未证明**模型能正确解构复杂时间义务；W6、W7、Q3仍未完成，`claims_complete=false`。下一步先做有源语义到现有结构的有限装配与双向核对，再进行合法新作业；不得以同源诊断通过代替发布。
- W7 启动准备发现并修复正式双击入口的端口探测使用不存在函数，现直接检查监听占用；脚本语法和相邻启动入口定向测试通过。尚未真正启动、停止或备份恢复正式服务，不能算W7/Q3验收。旧路由断言大套件11通过/1失败，其失败为测试仍要求早期智谱默认，而现场为CMS线路；未因本修订改生产配置或追绿。
- 2026-09-24 冻结首批 v72 失败的两项义务缺独立核对日期属性。现仅对“同候选所有结构错误都恰为缺该属性”的结果启用短修复：独立产品请求只见需修原子及其直接摘录，返回每个位置的 `date_range`、`record_time` 或 `unresolved`；程序按授权位置回填，重验整个原输出，不允许改其他字段。原文不足则终止并保留未核实，不转全候选猜答。提示 v2.56、执行身份 v73，相关深审/传输/执行/作业集中回归 155 项通过；尚未进行 v73 真实模型调用。v72 后续七天被误借给另一条禁令的语义缺陷不由此修复，W6/W7/Q3 和 `claims_complete=false` 不变。下一步按专家包把逐条来源语义解释、确定性装配与双向核对接成可验证纵切片，同冻结原文对照，勿重复同类整批补答。
- 2026-09-24 v71 冻结首批受控诊断及失败证据保全：预检产品直连为 `cms-router/deepseek-latest-cloud:high`、预算 65536，身份探测回报同一模型；用 v67 失败作业中不可变 `deep_plan.batches[0]` 经产品 `ProtocolControlAgentRunner`、现有完整输出校验做一次隔离小样，未启动/续用临床作业。约 189.8 秒后仍为“需要核对”：初答三个义务原子缺观察采用说明；局部补答后暴露首原子时间窗同时填两套上界；整候选补答又缺观察采用说明。回执位于 `artifacts/0924v1-w6-v71-first-batch/result.json`，标记非临床验收、不发布。该次诊断脚本退出前只存已有 attempts/hash，不能还原原始无效响应，因此不能断言最初时间字段何时产生。为修这个真实取证缺口，现有 JobRunner 失败提交可附非成功诊断检查点；W6 深审失败只保存该批每轮原始模型输出、hash、错误类、模型/提示身份，不携密钥；成功回执格式不变，失败态/依赖阻断不变。执行身份 v72，工作流/深审聚焦回归 175 项通过（其中一条旧测试曾把未注册任务误期望为可领取失败，已按现行注册范围修正）；未再调用模型验证 v72。W6 仍未发布，`claims_complete=false`。下一步使用保全的原始输出，在同一冻结复杂小样上核清时间与观察语义的具体缺口，设计有源语义解释→有限确定性装配，而非继续追加全候选随机补答。
- v72 冻结首批第二次隔离诊断约260.6秒，仍未通过，原始三轮响应及 SHA 留在 `artifacts/0924v1-w6-v72-first-batch/result.json`（不是正式 Job 检查点、不是临床验收）。初答两项有时间约束的义务缺独立日期属性；第一次整候选补答将后续持续义务与当前原子直接来源拆散；第二次补答虽补结构，却给“筛选/导入期不允许调整背景治疗”的义务填7天上界，而该原子自身摘录不含7天时长，触发 `TIME_CALENDAR_BOUND_UNSUPPORTED`。三轮原文哈希已复核。这里不能由程序从相邻义务偷取7天，也不能把未来双盲期禁调提前判为当前满足。需要在有源语义解释中分别说明当前审核截止范围、命名期间与跨陈述时长依赖，程序只装配已明确且有对应出处的时间语义；相同冻结批不再无据重复整候选试跑。
- 2026-09-24 资料来源字段局部修订与覆盖诊断纠偏：首轮完整输出若仅同一候选的一项 `minimum_evidence.source_policy` 结构不合规，产品在原会话身份下发独立短请求，只允许返回该字段；程序验证每对来源位置/逐字摘录属于该候选授权原文，再合并回原输出并执行完整结构、来源、发布门禁。其他证据、义务、节点及候选均不可由该短请求改写。缺来源仍失败，不猜测。另将有源陈述“已表达”限定为完整陈述出现在相应义务/持续义务摘录，局部摘录重合只记候选有关联；空白陈述拒绝。诊断另列已知官方/流程目标是否完整包含该陈述的逐字摘录，但不以此自动补关系或宣告语义覆盖。冻结首批实证中签署同意与导入后复核只有备注声称由流程目标覆盖，流程目标摘录仅为项目名称，仍需逐陈述核对。提示身份 v2.55、执行身份 v71；聚焦深审/传输/执行 140 项通过。此为合同与诊断纠偏，**未证明** v67 的节点错配已解决，也未进行 v71 真实完整 W6 作业；既有失败批次和 `claims_complete=false` 不变。下一步仍以冻结来源核对多义务、时间及目标关系，形成有源语义解释到结构成形的最小闭环，再做一次受控真实对照。
- 2026-09-24 来源双向核对纵向切片：复核独立会商所指三项合同死结，当前代码已将基线通用/特例混合、访视安排/结果有效期混合列入有界候选重分区，并对原文未来期间却被当前确定性判定的情况拒绝；不重复补丁。现将有源陈述与最终候选的直接原子摘录/同单元来源片段作字面对应，成功回执逐条保存“原子已表达/候选有关联但原子未定位/仅链接已知目标/尚未定位”的诊断账，并保留处置类别；它不推断医学等价、不补临床事实、不替代既有来源/发布门禁。因成功回执结构新增该账，执行身份升为 v64；已运行的 v63 隔离服务须重启才会生效，旧失败结果不能越版续用。合成反例及深审模块集中检查 `90 passed`。只读回看同一冻结首批历史 v54：17 条有源陈述中 8 条标为必做，4 条所属单元有候选；其中 2 条虽属候选单元，却未在候选原子摘录中逐字对应。它们不能凭“候选存在”宣称已覆盖，也不能仅凭字面未命中断定医学遗漏。下一步对这两条及同段时序、例外作来源级临床核查，再决定发布阻断条件；不能把只链接官方/流程目标视为已完整表达额外约束。
- 2026-09-24 局部补答纠偏：观察采用说明的提示现在只提供需修原子及其成对直接来源，不再同时附上整段可误引的周边文字；直接来源不足时仍用未核实而不造选择策略。提示身份 v2.52、执行身份 v65；v63 隔离服务已在审计库无活动作业后正常关闭。深审模块 `90 passed`，相关局部补答 4 passed；传输广套件有 `9 failed`，多数是旧MTPLX装卸环境/默认路由断言，另有旧GLM凭据预期，不计为本轮通过，也未为了追绿切换产品模型。真实受控批次结果见下一条，不能宣称 v63 的三个错误或 W6 已经全部解决。
- 2026-09-24 v65 隔离真实反证：由产品正式 API 新建 `6313960d59ce4d7e9152ccb072a9eab1`，同一冻结来源、同一隔离库、无旧结果混接；约3分半后 `deep_0001=failed_final`，未发布目录。末轮诊断不再是观察摘录错配，而是 `RECOMMENDED_MODALITY_DROPPED`、`FUTURE_PROHIBITION_DECIDED_EARLY` 与 `PROCEDURE_AFFECTED_STAGE_MISMATCH`。这证明局部来源提示不足以稳定解决完整语义/节点装配；继续同法整批重跑无新信息。产品门禁正确拒绝，`claims_complete=false`。下一步应以此原始回执定位三个错误对应的候选/原文/节点，设计仅修该候选语义且不改非目标来源的局部成形合同；旧作业保留失败态。
- 随后尝试为上述三类错误增加短指引，集中检查立即发现与现有更精确的指引重复，且遮蔽了同阶段/跨阶段差异；这部分新增已完整撤回，提示/执行身份仍为 v2.52/v65，深审模块 `90 passed`、`git diff --check` 通过。问题不在“缺一句提示”，应把有源陈述、候选义务与目标节点分开核对后再定修订范围。
- 对已通过完整结构/来源解析、仅一个候选被发布门禁指出的问题，产品传输新增短上下文候选修订：只发送该候选原稿、授权原文、冻结目标及具体错误，返回仍限单候选 Schema；原会话历史保留供其他受限回退，系统回填后照常验证全批与发布门禁。初次结构无效、来源新增、多候选重分区不启用此捷径。提示 v2.53、执行 v66；深审模块 `91 passed`、相关传输 `3 passed`，尚未证明真实模型节时或质量改善。下一步同冻结首批只做一次 v66 受控对照。
- v66 隔离作业 `7fcc3e583e334d21ac5b696be0cfa364` 从合法入口完成前6个深审批次，在 `deep_0007` 终止（92/148 步，未发布）：当前阶段禁止要求被遗漏，来源限定补入虽启动，但新增原子的 time_constraint 与 evaluation.time_purpose 不一致，故 `SOURCE_INSERT_INVALID`，没有采信该候选。不能仅凭前6批通过归因于短上下文，亦不能用同法重复整批生成。现将已授权来源补入也改为独立短请求，保留原批历史与失败范围，明确时间约束和核对用途一致；提示 v2.54、执行 v67。深审模块 `91 passed`、相关传输 `3 passed`、差异检查通过；待一次受控真实对照。
- v67 真实对照 `fba86f5c8e65480a8009802ccd949220` 在同一隔离库从合法入口新建，约5分半后首批失败：`PROCEDURE_AFFECTED_STAGE_MISMATCH`、单候选补答新增非法 `source_excerpts` 字段，以及来源补入缺少原文定位。没有目录发布。短请求两种变体均未在真实作业证明稳定或节时，已撤回本轮新加的独立单候选传输、原稿拼接及其专属测试；保留已验证的有源陈述诊断和观察摘录局部修订，当前提示/执行身份回到 v2.52/v65。深审模块与传输合跑 `114 passed / 10 failed`，10 项为旧默认路由、旧凭据预期和未启动 MTPLX 造成的传输测试前提失败，不计通过；`git diff --check` 通过。下一步以已冻结原文和失败回执核对“每条有源陈述→候选义务→节点/时间/例外”的对应，不再重复整批随机试跑；未取得完整来源闭包前保持 W6 未发布、`claims_complete=false`。
- 来源覆盖诊断进一步区分“原文只出现在适用/触发/例外”与“作为义务或后续持续要求表达”：必做、禁止、建议陈述只有直接义务/持续义务摘录对应才标为已表达，同时保存命中层位；单元有关联但未表达仍待核，不因候选标题或触发文字出现而假装覆盖。它仍是诊断而非医学等价裁决/自动发布门禁。回执字段变化将执行身份升为 v68；深审模块 `90 passed`，没有重跑完整真实方案。下一步在同一冻结小样核查这类待核陈述是否已由官方原文直接覆盖，建立可验证的有源局部补入/结构装配后才尝试 W6 新作业。
- 对首批冻结来源只读交叉核查：旧成功小样中同一混合来源单元的“签署ICF”和“导入后重新核查”没有直接义务原子；当前冻结目录确有 IN-07 及签署知情同意流程项，也有筛选/导入期和基线期各自的“审核入选/排除标准”流程项。旧处置仅在 notes 中宣称这两条已覆盖，结构化关系为空，因此不能以候选存在或目录同名证明来源要求的动作、时点均已表达；也不能把它们未经医学核对直接判为新候选遗漏。该问题作为逐陈述“来源→现有目标/增量候选→覆盖维度”的下一项合同工作；原件、数据库和旧回执均未改写。
- 传输回归维护与实质分类修复：原测试仍把旧智谱直连当默认、要求假 MTPLX 客户端先启动本地模型，并使用早于现行求值/证据合同的夹具；这些前提已按当前独立路由和纯假客户端边界修正，未改生产模型选择。产品来源解读在**未收到响应**时现记传输失败，收到不合规响应才记结构失败，避免将服务故障误导为方案资料问题。深审+传输聚焦回归 `124 passed`；这不是 W6 真实发布或 Q3 临床验收。
- 工作台来源缺失显式呈现：正式条款或跨章要求没有原文摘录时，详情写“方案原文定位待补齐”，不把系统解构文字伪装为方案原文，也不在前端改医学判定。工作台组件 `16 passed`、正式前端构建通过；仅界面合同回归，尚未做 Q3 真实浏览器验收。
- 2026-09-24 W6 v62/v63 隔离反证：`e986aaa2367e4280b42234df2bbff547` 和 `67bacf43d4504855ad88b977e1e3664e` 均从合法入口新建、复用同一冻结发现来源，均在 `deep_0001` 终止，没有发布目录。v62 末两轮为无来源审核动作与同候选四个义务均缺观察采用说明；v63 末两轮转为求值来源错配、后续禁令提前判定及观察政策摘录不属于原子。二者分别约5分钟，不证明模型全部不可用，也不支持再用相同完整请求重试。隔离库、来源、旧失败作业均不变，`claims_complete=false`。
- 本轮工程修订：仅在单一受损义务原子时从 Pydantic 结构化错误定位，不解析中文报错分隔符；单原子补答冻结其原句、强度、时间与来源，独立短上下文发送，原会话保留供有界回退。多原子仅缺观察采用说明时允许只返回各原子的观察政策，代码只替换这些字段并重验完整模型；无授权位置或来源不符仍失败。当前提示 v2.51、执行 v63、门禁 v26。另修复“已有其他候选却未覆盖禁止句”被跳过的问题；官方/流程同文覆盖必须有明确链接及同源片段，不能只碰巧文字相同。相关深审测试 89 项通过，传输/深审定向集合 113 通过、10 个旧环境断言排除；门禁新反例 8 通过。广门禁套件另有27个旧夹具缺现行求值规格的失败，尚未整体清偿。格式与通过数均不代表医学或全文验收。
- 下一步停止同法新建整方案作业。以首批冻结来源和 v54 曾通过的历史结果作只读对照，不把旧结果跨版本发布；确定“来源解释→结构装配→双向来源核对”最小可行产物及消费点，先核同段多义务、时间分期和观察政策，再从合法新作业运行。随后仍需同源官方+跨章目录、5份24页病例、工作稿/报告、更正补证及 Q3。
- 2026-09-24 新一轮反证：v58 `c8640ed233a3444b907aaf633b6f6415` 第8批访视表失败；v59 `bb11cde1c7a84567ac4731f0cc94cdb9` 首批遇502/流中断。OmniRouter本次断流回执为499、`request_signal_aborted`、无最终用量；同段有另一条超长DeepSeek会话，拥堵只是可能因素，非已证实根因。空闲时从合法入口创建同版作业 `f38b5d026d3d495cae6d7384774a5102`，前7批通过，第8批仍失败，具体是把“在基线访视完成随机分组”误作持续期间和回溯时间锚点。现区分无相对时窗的访视操作与真正前后/持续/洗脱约束，门禁v25、执行v60；冻结原句和“基线访视前4天内”反例的聚焦检查通过。新合法作业 `b5f0c4d1b4094fc485bb6d6354068026` 又在首批失败：无来源动作、流程受影响节点错配、时间约束用途矛盾。两次不同失败证明访视修正不足以稳定整方案，已停止同法重复新建作业。所有失败终态和成功批次保持原样，无目录发布，`claims_complete=false`。
- 下一实质步骤按0924V1专家包，在现有内置方案Agent中试一个有源语义解释→确定性装配→双向来源核对→原门禁的纵向切片；先用首批冻结输入与同结构对照证明语义/时间/节点不丢，再决定是否替换既有深审输出成形。禁止硬编本方案阈值、药物或访视，禁止把格式错误转成研究者裁量。此工作不改变W0–W5/Q1/Q2既有结果，也不代表W6、W7/Q3完成。
- 2026-09-24 受控 W6 续试：v54 `b101652ca2804c519902421866d78873` 首批失败；v55 `dfe0313f268e4b758dc139c36bc66422` 完成前9批，在访视表第10批失败。冻结表格项目行仅有压缩的勾选标记，旧只读上下文缺同表前置访视行；程序现把同表前五行作为只读语境，不把它们变成候选。v56 同一批直读约270秒，首轮输出完整但某候选访视关系错，随后的整批修复丢11项/1项处置。现将访视关系错误定位到具体候选，仅允许候选级修订且系统保留其他条目；聚焦深审回归85项通过。
- v57 `3eba2fe444a44f219595dbe1facc321f` 在调用模型前被深审上下文合同拒绝：旧合同不接受同表前置行。已限定合同只接收完整来源清单内、同一表格、行序0–4的只读语境，其余额外上下文仍拒绝；新 v58 `c8640ed233a3444b907aaf633b6f6415` 从合法入口创建，在隔离库复用已核发现，开始深审，观察时前4批完成。不能以此宣布全文、医学或正式发布合格；旧失败作业原样保留。相关聚焦测试101项通过、1项既有提示长度<30000断言失败（实际约50773字符）；前端生产构建通过；`claims_complete=false`。
- 接下来观察 v58 全部深审批次与水合/发布门槛；失败以具体来源和回执重新定因，不通过随机重试、删候选、手工补临床答案或放松门禁求绿。通过后仍须同源官方+跨章正式发布、5份24页病例事实/Profile/工作稿/报告、更正补证及Q3真实界面验收。
- 2026-09-24 新证据：v52 作业 `8fc45d1c9bb9476e9e21eeea4cd6da7d`、v53 作业 `b22a19f1dde04343ad818241989a3449` 均在 `deep_0001` 失败，已停止专用服务；前者同时出现未来禁令提前判定、节点错位及时间字段冲突，后者出现建议语气无来源、求值字段冲突与观察选择缺失。不能继续声称 v52 正在运行，也不能用规范化一处字段解释全部失败。
- 同一冻结首批（12 单元）的 v54 隔离小样：产品方案 Agent 先生成有源陈述清单（单次约 57 秒、27 条、逐条摘录定位通过），再按现有完整结构与批次门槛执行，约 245 秒、17 条陈述、12 条处置、1 个候选，首轮 `已解析`，无结构/批次门槛错误；原始产品回执在 `/Users/smkzw/tmp/enrollment-review-w6-20260923-v35-deep4-01/v54-first-batch-diagnostic.json`。两次陈述数量不同，显示模型输出有随机性；首批通过不代表临床完整性或整方案可发布。清单不产生独立规则身份，原文和最终发布门槛仍是权威。执行身份 v54、提示 v2.47；合同聚焦回归 145 passed。
- v54 小样之后的正式运行及 v55-v58 现况见上方；不得跨版本复用旧失败深审，不得把单批成功或 `publishable=true` 宣称为正式发布。完整方案、病例、更正补证、Q3 均未验收，`claims_complete=false`。

- 工作方式：本轮由任务所有者内联完成相互依赖的 W6 修复与来源展示，避免在共享工作树、同一隔离库并发写；实质临床解释仍以原件和产品回执为准，若最终主观验收存在重大歧义再按批准机制独立会商。0924V1 专家包是审阅证据，不是新的临床事实或发布批准。
- W6 合同：无候选但原文有当前禁令时，可按冻结单元和来源范围新增候选；支持的产品传输只补写单候选，旧候选与范围外处置保持原样，补入后仍跑完整水合和发布门禁。标题/表头承载阶段、正文承载禁止，以及同段已有逐字绑定的一条候选却漏另一条，增加保守拦截；正则只作明确漏项提示，不代表全部语义覆盖。身份升为执行 v52、提示 v2.46、门禁 v23。新修复不从代码制造医学结论。
- 路由与界面：产品逐页双读道若角色显式地址/凭据与 CMS 路由冲突，现在拒绝启动而不暗改实际去向；审核工作区的控制要求显示为“系统解构的审核要求”，真实原文摘录经正式控制原子→审核投影→API→页面独立展示，无真实摘录时不显示伪原文。现有列表标题、分页与问题队列保留。
- 一次合并检查：W6/执行/候选分区及不含旧失配夹具的门禁测试 245 passed、30 deselected；新增来源哨兵 7 passed；审核投影/API 26 passed；工作区 16 passed；前端正式构建通过。完整旧门禁套件仍有 29 项因夹具缺求值规格失败；旧页级套件仍有 4 项流式模拟或过时默认路由断言失败。上述通过不代表临床发布或 Q3。
- 隔离真实作业：专用 V2 服务 `127.0.0.1:8911` 指向 `/Users/smkzw/tmp/enrollment-review-w6-20260923-v35-deep4-01/data`；此条形成时曾停止，现按上方 v58 状态运行。原三个失败作业只读核对均为 `failed_final`；v52/v53 也已失败。`claims_complete=false`。

## 2026-09-24 W6 深审合同与隔离试跑

- 暂停前核实：三份隔离深审作业 `b55a6c4b11ba4daeada43c5352eefb29`、`7914ce8306304f39b69837e6fe854754`、`1dad32380f22478ca6d657d4331c19b0` 在原隔离库均为 `failed_final`，没有新的正式目录。后续单单元产品诊断也经过两次定向修订仍未得到可发布输出；不能继续把缩批次、增加一般提示或多重试当作已证实的解决办法。需在既有内置方案 Agent 中降低一次输出同时承载的医学解释与结构装配负担，并保留来源闭包和失败状态，不建立平行规则真相表。
- 当前病例读取路线已用产品 `direct_completion` 对**非临床合成红色图片**做最小能力预检：A 请求 `cms-router/glm-5.3-flash:high`，B 请求 `cms-router/deepseek-latest-cloud:high`，两者均返回 `stop` 且回答颜色正确。B 服务回报模型名为 `deepseek-flash`，与请求别名不同；这只证明当前路由能处理图片，不证明上游权重版本或临床质量。未运行真实病例双读、事实规范化或 Profile 临床核对。本机 `.env` 的非密钥角色配置已对齐 CMS 路由；该文件不入库，接手时须从现行显式配置核实，不得把示例配置当生产设置。
- 本次集中定向检查：路由/凭据优先级 6 passed、深审与执行 86 passed；旧页级套件仍含与当前流式读取不符的模拟客户端，停止于 25 passed/5 failed，旧门禁宽套件停止于 91 passed/8 failed（旧夹具缺当前必填求值规格）。这些旧套件不能记为全绿，也不能据此否定上述最小能力预检。`git diff --check` 通过；Q3 和真实临床验收仍未做，`claims_complete=false`。

- 本轮核对两份合法新作业：v48 `7914ce8306304f39b69837e6fe854754` 复用已核发现后完成23批深审，在第24批访视表单元 `failed_final`；v49 `1dad32380f22478ca6d657d4331c19b0` 仅完成首批，在第2批 `failed_final`。后者四段来源混合了给药后剂量安排与“筛选/导入期不允许调整背景用药”的当前限制；失败回执依次为未来义务过早判定、流程访视关系、观察选择和持续义务来源错误。二者都没有共同发布正式目录。用产品 `cms-router/glm-5.3-flash:high` 对同一冻结第2批只做一次隔离直读，约460秒、结构通过但零候选；这会漏掉原文明确的限制，不可用作正式结果或模型替换依据。
- 对明确写出入排阶段与禁止行为、却既无候选也无逐字同源既有目标的结构单元，新增保守的来源缺漏拦截；只报原文待核，不自动生成临床规则，也不把给药后安排算入当前审核。支持“阶段在禁止词前/后”两种句式，空候选和已由同源目标覆盖有反例；门禁/提示/执行身份现为 v22/v2.44/v50。6项定向反例通过，深审器和作业执行 86 项通过；旧发布门禁宽套件仍因多项夹具缺现行必填求值规格而先行失败（本轮止于 8 failed / 91 passed），不得计作本门禁全套通过。`claims_complete=false`。
- 下一步不盲目从头重复随机全方案：先在合法冻结来源上证明复杂批次能同时保留当前禁止行为、后续持续义务与来源/时间/访视闭包，再以新版本作业验证完整发布。旧 v48/v49 失败作业和成功回执保留，不跨身份续跑；正式病例、补证和 Q3 均未启动验收。
- 同一冻结第2批仅取有明确背景用药限制的一个来源单元，由产品 `cms-router/deepseek-latest-cloud:high` 及原门禁隔离判读，约145秒、首轮加两次修订仍失败：先缺数值单位/观察选择，后两次分别把未来期间混入当前判断、再将建议语气误写为确定要求。故“四段混读是唯一原因”的假设不成立；不能继续靠缩批次或增加修订轮数宣称可交付。单段诊断未写回作业、未发布。
- 后续病例路线配置复核发现 `.env` 中第二逐页读取和事实整理仍指向旧 OpenCode 模型，而方案深审已切到 CMS。现将这两角色的非密钥配置改为 `cms-router/deepseek-latest-cloud:high`，并让 CMS 自身凭据/地址优先于遗留角色配置；显式其它供应商路线不改。假凭据路由及缺漏拦截合并检查 8 passed，深审/执行 86 passed；真实病例读取与医学验收尚未运行。`claims_complete=false`。
- 后续同一工作树的集中修订将深审提示升至 v2.42、执行身份升至 v48；旧 v41 检查点不可跨版本继续。修复了局部候选提示同时要求整批输出的矛盾，候选局部修订仍保留其他条目；多候选结构错误可按冻结位置局部修订，身份/来源不一致立即拒绝。产品传输在非严格 Schema 的截断续读不再因索引不存在的 Schema 包装而报错；任务异常恢复路径补齐 `ProtocolControlGateError` 导入。无配置默认模型与现行产品路由统一为 `cms-router/deepseek-latest-cloud`，显式 `.env` 仍优先。基线取值范围及时间约束/求值一致性的定向提示增加了通用约束，未写入本研究评分或药物常量。
- 对冻结第31批（基线评分取值、人口学、病史和治疗史四单元）用本系统产品传输与原发布校验器做一次只读隔离诊断：实际路由 `cms-router/deepseek-latest-cloud:high`，约269秒、三次输出，依次出现基线值关联缺失、条件分支/时间锚点/日历窗问题、时间约束与求值用途冲突，最终 `需要核对`；未写回旧任务，也未发布。相同来源、相同随机路线的重复试跑不再视为进展。冻结目录中确有相应筛选/基线评分检查项，失败应继续从最小来源摘录和候选结构查因，而非代填关系或放松来源校验。
- 聚焦合同/执行套件曾为 `134 passed, 5 warnings`；其后新增提示断言首轮暴露了旧的宽泛基线修订分支抢先匹配，已合并到既有分支，不另增重复分支。包含旧传输夹具的扩展套件为 `158 passed, 9 failed`，9项涉及旧默认 GLM 断言、未配置 MTPLX 装卸所有权或旧凭据报错文案，不能计作当前产品路由通过；新改动仍需一轮最终集中复核。`claims_complete=false`。

- 当前工作树的深审执行身份升至 v44，原因仅为修订/诊断路径变化，旧 v41 作业不可跨版本当成成功任务续跑。相同真实冻结第31批以产品自带 Agent 直接调用 `cms-router/deepseek-latest-cloud:high` 两次隔离诊断，均未写回旧任务、未生成正式目录：v43 路径约175秒、3次回答后报基线关联、条件分支及日历窗问题；v44 路径约196秒、3次回答后仍报基线关联，首次还有多个候选的求值结构错误。两个不同随机回答不能相互当成同一轮修订或速度排名；两次均为“需要核对”，不作为医学结论。`claims_complete=false`。
- 已在既有校验器中一次返回各独立候选的首个问题，结构性候选拆分仍先单独处理；单个候选的初始结构错误只要求模型重写该候选，原处置和其余候选由系统保留。产品传输的 `json_object`/文本通道输出截断时不再误取仅严格结构通道才有的 Schema 字段。新反例在合并的定向检查中 7 项通过；旧全库测试、正式整方案及临床核对尚未进行。下一步不能盲目加修复轮数：要针对同批多候选结构错误降低重写范围，保持原件、候选来源与最终门禁完整，再从合法版本化作业取得正式结果。
- 本轮对冻结来源做只读合同复核，并完成一轮独立只读会商 `enrollment-w6-schema-rescue-20260924`（报告在 `runs/conference/.../evidence_single_object.md`；仅工程建议，未读隔离库原件、未作临床批准）。门禁原先对混合“访视安排+结果有效期”候选无任何合法关系形状，且要求拆开的 `BASELINE_VALUE_SCOPE_MIXED` 不在候选重分区授权中。现以 `MIXED_OBLIGATION_KIND_SCOPE` 明确要求分别表达，两码均授权来源全集守恒的候选拆分；修复提示仅指示保留各自来源，不代写医学事实。门禁 v20、深审提示 v2.38、执行 v41。会商还建议研究期间持有者、字段路径与Schema简化；这些尚未采纳或证明，不能写成已解决。
- 新隔离作业 `b55a6c4b11ba4daeada43c5352eefb29` 从同一 DOCX 冻结来源和已核85批发现合法新建，保留原 v39/v40 作业；首轮至深审第24批失败，原始错误包括日期属性、时间锚点和求值结构；一次合法人工范围重试让第24批通过并继续至第30批。第31批首次因模型给出与冻结单元编号不一致的修订范围而抛技术异常；已改为明确的“需要核对”并保留诊断，不猜测编号、不改写原件，成功检查点保持可重放。该错误处理不改变任何成功输出合同，因此保持 v41 身份。聚焦的修订范围/执行/新门禁回归 62 passed。
- 第31批在修订范围保护后再次合法试跑仍失败，最近诊断为基线值关系缺失、观察选择原文不属于该原子、多个观察选择规则缺失等，任务 `failed_final`；这与旧 v39 同类复杂批次的重复失败形成反证，不再无依据重试。广泛旧门禁套件有29项既有夹具缺当前求值规格而失败（本轮定向 62 项通过，不能等同全库通过）。当前仅30批深审已通过，后续水合/共同发布、病例/Profile/工作稿/报告、更正补证及 Q3均未验收；`claims_complete=false`。下一步须改变深审输出成形与局部恢复方法，先确保来源和审核节点不丢，再从合法新作业或可验证的同版本失败范围继续；不得把失败目录发布。
- 从原始 DOCX 哈希 `075c93b4…` 由正式入口重新登记、确认 III 期并冻结新来源 `03f900e8e8e04cc3b17023a2e2ac218d`；仅在隔离库运行，未生成或发布草稿。此前官方目录冻结未传结构块，EX-06 的部分摘录在页内断开；目录构建现优先使用同一来源的完整 DOCX 段落。新目录 EX-06 对应段落由 62 字恢复到 97 字，原件段落逐字一致；旧目录和失败作业不改。冻结来源任务在 `generate_draft` 按受控准备入口停止，不能称作官方规则已发布。
- 新来源的 v39 发现/单段作业 `6cd88bddb6f74ad8a574f4d6585fa0e0`：85 批发现全部通过，1,927 单元为 398 candidate、7 uncertain、111 context_only、1,411 non_control；首条深审 `failed_final`。同批只读产品模型诊断一次返回治疗期执行、无候选，证明答案有波动，不得把诊断结果补进作业。
- 同来源 v39 四段作业 `08ad1a140f95453cb7540efdaf55c20e` 合法采用以上85批发现；首次深审 19 批完成，第20批因持续期、日期属性和输出形状失败。按持久任务的失败范围人工重试一次后，第20批一次通过，继续完成至第30批；第31批在基线评分取值和病史采集的组合上因条件分支映射、基线值范围及输出结构 `failed_final`。85批发现和30批已完成深审仍保留；水合/发布门禁依赖失败，没有目录发布。
- 曾隔离试行 v40 粗筛提示，作业 `49fe2b5deeb4481b9285080e5deda64a` 完成85批发现、首批深审，第二批失败。候选由398降到280，但逐单元对照发现它错误排除了筛选病史收集、背景用药剂量、给药前检查操作等真正所需的资料；提示和版本已撤回，v40 结果不能用于发布或速度/质量排名。这个反例说明不能以压低候选数量代替临床完整性。
- 此处原记录形成时代码为 v39；候选定向修复及完整来源段落修订当时已在工作树，目标回归曾有113项通过，但全套旧夹具/真实全链尚未通过。第31批在后续 v41 中仍重复失败，现以本节新增记录为准，不再按旧建议继续同法重试；`claims_complete=false`。
- 本日追加核对：旧单段第4批的六份真实回执，按当前 v38 逐份只读重放，最后两份通过完整批次门禁；不是重新生成临床结论。v38 单段新作业 `6a2fc4c94b534685a4170f6643ba6e72` 从合法入口复用85批已核发现，前6批通过，第7批因流程目标、过早最终判定及时间字段错误 `failed_final`。v38 四段作业 `1f2f41612ab14745b9974b29d6432ee5` 同样复用发现，前6批通过，第7批条件分支及结构错误 `failed_final`。两个作业均未水合、发布或进入个例审核。
- 校验器已去掉把“所有受试者均……”误识为条件分支的过宽模式；候选来源 ID 只做系统确定性排序去重，不改临床原文。聚焦解构/执行回归75项通过；旧门禁宽套件中缺 evaluation 的夹具仍失败，未算通过。一次针对条件许可的提示增补曾以早期 v39 隔离作业 `9d2e16c1a7ba42188274c9cdee80b86d` 实跑，首批通过、第二批因流程和字段结构失败，已撤回该未奏效提示，保留失败回执。该段记录形成时的代码为 v38/v2.36/v19；现行版本以本节首段为准，未将失败作业伪装为可续成功。
- 结论：缩批次与追加一般性提示均未稳定消除复杂条款错误；下一步在既有深审步骤内减轻单次结构化成形负担，并保持来源闭包、条件/例外与节点时间门禁。不能只因某次模型回答通过结构检查就宣布医学正确；目前正式目录、病例工作稿、更正补证与 Q3 未完成，`claims_complete=false`。
- 继续使用同一隔离库 `/Users/smkzw/tmp/enrollment-review-w6-20260923-v35-deep4-01/data`、冻结来源任务 `94ea6ac2d1454db0b0d73af91532d26b` 和已完成85批发现的作业 `59bdec6c80ef4cfab58a2a78b44c78dd`。新增任务均逐批核验发现来源后写入自己的检查点，旧失败、取消和正式库未改。
- 深审批次减到单段的 v35 作业 `069ea1ea2d414da88020e31e937be81a`：前三段通过，第四段（筛选起始的具体治疗及持续期）仍失败，说明原因不只是批次大小。原合同把带时间的操作限制为访视安排或结果有效期，不能合法表示具体治疗持续期。现增补通用的 `complete_or_verify + semantic + interval_condition + 命名锚点` 路径；单次药名值比较不能证明全疗程完成。提示、修复指引和执行版本升为 v36，保留来源和时间门禁，不写项目专属药物规则。
- v36 四段作业 `80548606d4664a33beb2cf350b25f6dd`：首批经一次定向修复通过，保留具体用药、稳定剂量、筛选起点及7天持续要求；这只证明该批结构/来源校验，并非临床全方案通过。继续运行至第7批，前6批通过；第7批四段不同用药限制的条件分支、时间用途及具体操作模式在有限修复中未收敛，任务 `failed_final`，后续 hydrate/gate 未完成。
- v36 单段作业 `0803d67f714c42d78375b56116bb1f3f`：前三段通过，第四段又在条件分支映射、来源 ID 顺序及观察摘录归属上失败。与四段第7批构成不同冻结输入的失败证据；不得再凭缩批次或增加修复轮次反复试跑。下一改动应在既有深审步骤内降低模型每次需同时完成的独立判断，并保留原批次闭包、逐字来源、合法修复和发布门禁，不手写临床 JSON。
- 定向执行/解构检查 `74 passed`；整套模型身份测试仍有13项因当前 MTPLX 装卸夹具未就绪，未算通过。正式跨章控制目录未生成、官方草稿尚未与其共同发布、病例完整审核及 Q3 均未完成；`claims_complete=false`。本轮没有可合法激活的正式规则快照。

## 2026-09-23 W6 本轮收尾与暂停

- 用户已授权隔离真实方案试跑，并将发现、深审模型改为 OmniRouter `cms-router/deepseek-latest-cloud`；仅授权为现有产品密钥新增该模型权限。该密钥其他权限未撤销，未将密钥写入仓库；本机 OmniRouter SQLite 一致备份在 `~/.omniroute/backups/storage.sqlite.bak.enrollment-key-scope-20260923`。受限密钥的模型清单为空，产品现在以不含临床资料的小请求核对返回模型身份，身份不符仍拒绝发送方案。进程继承的同名密钥可能覆盖工作树 `.env`，隔离试跑已显式装载目标配置。
- 原方案来源保持不变。v26 隔离作业 `44501a44e0df4f39b7872f9a18dc2632` 在深审第8批失败；原始回执依次显示计算语义、时间锚点和访视流程关系错误。修订通用提示后，同一冻结批次的产品读取与原校验器一次通过，约80秒，零候选；这只是结构核查，不是临床验收。
- v27 隔离作业 `293815d0aa0249b99f2e3157de368d60` 在发现第52批 `failed_final`：同一批候选引用的上下文被写作 `non_control`。复用已有跨批确定性规则，把被明确引用的同批单元仅提升为 `context_only`，不产生新控制；冻结原输出的离线回放通过。
- v28 隔离作业 `f6ad8965200c4089bb28f5e20a4cfb01` 在发现第53批 `failed_final`（55/86）：模型先回显整份 `$defs` 输出结构再给实际分类，修订请求重复发送结构后仍失败。现仅在发现修订提示中不再重发结构，首次提示和传输 Schema 仍保留；发现提示 v3、执行合同 v27。对同一冻结批次经产品传输与原校验器单批真实核查：24个单元一次返回，约37秒，状态“已解析”。不把该诊断写回失败作业、不改旧回执、不发布目录。
- 聚焦提示/解构反例合并 77 passed；身份测试仅 cms-router 三项通过，整套旧 MTPLX 夹具中13项因缺产品装卸清单失败，属另项环境/夹具缺口，不能写成全套通过。完整 W6、官方+跨章同源发布、五份病例事实/Profile/审核/报告、更正补证及 Q3 均未完成；`claims_complete=false`。下一安全动作见 `HANDOFF_20260923_W6_PAUSED.md`。

## W6 授权隔离试跑与当前观察（2026-09-23）

- 用户已单独授权**隔离真实试跑**，不等于授权修改原件、恢复旧取消作业、正式发布、提交或推送。旧 `0da5ef099b3e4972a012d14a3d5d75fe` 保持取消。试跑均用同一冻结方案来源 `94ea6ac2d1454db0b0d73af91532d26b` 的独立 SQLite 一致副本与 blobs；正式工作库未改。
- 第一份隔离副本 `~/tmp/enrollment-review-w6-20260923-v15/data` 的新任务 `5355193a66134ed08a7f0d73333fdaa8`：发现结果完整，1,927个结构单元中 423 candidate、3 uncertain、100 context_only、1,401 non_control；426个单元进入66个深审批次。首批因来源片段闭包、时间用途和未来窗结构错误而 `failed_final`，依赖步骤随之失败；没有生成发布目录。曾对发现批次63作有界真实调用并按合法任务重试，保留成功批次，不修改旧任务状态。当前代码版本已从 v15 演进，不得拿该失败任务混版本续跑。
- 产品传输的替代供应商路径曾暴露跨供应商端点/凭据继承问题：代码已修正并以定向回归验证。一次早期模型清单探测可能把 CMS 凭据送向 OpenCode 地址，未发送临床原件；应轮换该凭据。之后分别核实 `opencode-go/deepseek-v4.1-flash` 与 `cms-router/cms-glm-flash` 的模型标识、地址和响应模式。后者针对冻结首批在产品深审及原校验器下取得**结构通过**，耗时约763秒；这既不是临床内容QC，也不足以证明全方案可行或模型优劣。较短的初始 Schema 提示实验未获速度/质量收益，已撤回。
- 只读重分包模拟：当前 12 单元上限为66批、总提示约579万字符；上限4单元为130批、约1112万字符。固定合同的重复成本远大于本轮缩小批次节约的单批原文，故不以小批次直接替代。冻结期别**仅属于另一研究期**的28个单元仍被发现模型列为候选；新通用路由在深审前将它们保留为可追溯的非深审处置，若被本期单元引用则仅作只读上下文。混合、共用及未知期别不跳过。v17只读重算为398个深审单元、63批、总提示约553万字符；首批仍约9.4万字符，不能宣称根治耗时。
- 新隔离副本 `~/tmp/enrollment-review-w6-20260923-v17/data` 已经原库一致备份，新任务 `41f5ed409172484bbffcb0d7183d6482` 从合法入口创建。发现读道为产品直连 OpenCode DeepSeek，深审读道为产品直连 OmniRouter CMS GLM，两者通过模型清单预检。新发现闭包为1,927单元：381 candidate、7 uncertain、94 context_only、1,445 non_control；388个单元进入64个深审批次。与上一试跑的数量差异属于真实模型输出变化，不能当作期别路由的确定性收益。首批和第二批深审结构通过；首批需要2次输出修复。首批候选将“筛选/导入期及双盲治疗期禁止调整背景药物”合为一条，且在基线节点的命题要求证明整个未来双盲期未调整；虽然原结构门禁放行，但临床时间语义不可接受。系统通过合法取消入口请求停止，本任务终态 `cancelled`、88/152，未发布或激活任何控制目录，其他成功批次原样留存。不能把它说成方案深审完成。当前持久任务尚未冻结两读道的模型身份到 payload，不能在换模型配置下续跑。
- 针对此例只追加通用“当前节点前可核事项与节点后持续义务分离”提示，版本 `control-agent-prompt/v2.23`、执行版本 v18。用同一冻结首批、产品深审 runner 与原发布批次校验器做隔离诊断，`~/tmp/enrollment-review-w6-20260923-v18-targeted/first-batch-result.json`：约675秒，3次尝试后仍为“需要核对”，顺次为不合法 JSON、时间用途/额外字段、官方目标缺失；无合格候选。说明仅加一段提示不足以解决复杂输出的稳定性，也没有证明未来义务错误已经消失。未把失败改写为研究者医学判断，下一步须从请求/结构修复与候选时间语义两个不同问题分别查因。
- 新增通用禁止类时间越界门禁 `FUTURE_PROHIBITION_DECIDED_EARLY`，覆盖来源和求值命题均包含研究/治疗期间持续义务且绑定当前决定节点的情况；“治疗期”无需必须写作“治疗期间”。门禁版本 `control-publication-gate/v17`。对 v17 隔离库中已通过旧结构校验的原始首批候选作只读回放，现确实被该错误码阻断；未改原回执或数据库，未发布。定向反例 4 passed。整份旧 `test_slice58c_protocol_control_gate.py` 运行是 93 passed / 29 failed，失败集中于旧夹具缺少现行必填求值规格，不将其说成当前门禁回归；须按受影响消费者更新夹具并再跑。此门禁不证明所有未来时间表达都已覆盖，v18 隔离诊断的 JSON/合同失败也尚未解决。
- 同一 v17 冻结来源的首批，产品深审运行器+正式校验器隔离诊断：12单元约258秒、6单元约216秒和756秒，均未产出合格结果；6单元先遇输出结构/访视关系错误，后又出现未来持续义务误判。产品定向修复已保留失败回执，不能用诊断结果替换作业步骤。将首批拆为单单元后，描述研究对象的一条约21秒通过且无新增候选，不足以代表真实控制；真实背景治疗限制单元在提示 v2.24 下约2分半、三次输出仍因未来持续义务误判被拒。由此不能把减少批量当作根治。隔离原始回执在 `~/tmp/enrollment-review-w6-20260923-v19-targeted/`，无正式库写入。
- 正式任务建立时现冻结发现和深审两条模型线路的非密钥摘要；执行模型调用前重新核对，不同则以 `PROTOCOL_CONTROL_ROUTE_CHANGED` 停止，禁止换配置混跑。产品 API 已接入，旧执行版本不能混算。提示只移除 JSON Schema 中自动生成的 `title` 字段，保留全部临床 `description`、约束和正式响应 Schema；每次深审提示结构段由34921减至29419字符。提示版本 `control-agent-prompt/v2.25`、执行版本 `protocol-control-execution/v22`。相同限制单元在 v2.25 下约5分多、三次输出仍未通过（先为结构/未来窗错误，最后仍为未来持续义务错误）；单次随机试跑不证明质量退化或改善。接口及服务/提示聚焦回归 `67 passed`，涵盖发现/深审两读道在模型变更时均于调用前拒绝；旧大套件29项夹具失败尚未处理。
- 独立会商 `w6-temporal-scope-review-20260923` 只读完成，指出同一原子上的 `prospective_period` 会令节点前计算也变为未知。主线程核实后采用同来源的 `continuing_obligation`：固定“本次未到期”，不进入当前节点真假；不新增治疗期审核节点。正式/草稿合同、模型输出专用结构、来源闭包、时间门禁、候选水合及方案预览已接通；当前提示/门禁/执行分别为 v2.28/v18/v25。模型输出结构不再带系统内部 `schema_version`，且要求显式给出未到期状态；失败回执新增输出字符数，便于区分短残缺响应与长结构错误。聚焦合同及执行检查 67 passed；时间门禁 3 passed；前端正式构建通过。旧门禁大套件仍有28项失败，集中为夹具缺现行求值规格，不能当作已修复或新合同通过。
- 授权隔离诊断仍使用已取消 v17 作业的**只读冻结输入**，没有恢复/发布旧作业：v2.26 的一单元真实限制三次输出依次为 `PROVIDER_ID_FORBIDDEN`、`WIRE_SCHEMA_INVALID`、`WIRE_SCHEMA_INVALID`；修正模型输出结构后，v2.27 依次为时间用途 `WIRE_SCHEMA_INVALID`、`INVALID_JSON`、访视关联 `PROCEDURE_AFFECTED_STAGE_MISMATCH`。两次均为“需要核对”、无合格候选，不代表合同成功或模型质量排名。回执分别在 `~/tmp/enrollment-review-w6-20260923-v19-targeted/deepseek-batch-5-1-product-validator-v2.26.json` 和 `...v2.27.json`；试跑调用本产品 runner、OpenCode DeepSeek 独立端点，未调用外部 harness 执行临床识别。
- 下一安全动作：先用现行 wire 对同一来源做有限的模型输出结构诊断，区分输出格式失败、求值原文丢失与访视关联错误；不得扩大自动修复预算或用人工重写临床答案。只有真实候选同时通过来源、时间和访视门禁，才从**合法新作业**进入 W6 整体深审，旧取消作业不混跑。正式目录、受试者事实与报告仍未产出，`claims_complete=false`。
- 另一条已配置的 CMS GLM 读道用相同隔离单元试探，身份预检即拒绝：本机 `20128/v1/models` 返回 HTTP 200 但 `data=[]`；没有发送临床原文给 GLM，更不能把端口监听当作模型可用。隔离回执 `~/tmp/enrollment-review-w6-20260923-v19-targeted/cms-glm-batch-5-1-product-validator-v2.27.json`。未改共享服务或切换密钥。DeepSeek 的连续真实输出失败与 CMS 模型清单空属于不同问题，不能合成“所有模型不可用”。
- 受试者只读投影、API 合同、前端解码和宽屏审核详情新增独立的后续持续要求说明；当前控制义务的状态、事实选择和授权规则不变。定向后台说明测试 1 passed，前端工作台 16 passed，正式构建通过；未做真实浏览器/Q3 验收。待方案目录有合格发布实例后，仍须核对真实当前节点结果与后续说明同屏、报告/打印一致性。
- 当前修订的针对性测试：期别路由及执行合同 `9 passed`；此前提示/传输相关聚焦回归 `63 passed`。另一次宽测试的22项失败涉及旧默认模型与当前环境不一致、本地模型所有权未就绪，未作为本次通过证据。代码与诊断只解决工程边界；规则发布、真实病例事实/工作稿、补证及Q3验收均未完成，`claims_complete=false`。

## 0923V1 审阅纠偏（当前）

- 本轮获授权修订代码与定向验证，但未获授权恢复临床作业、收费调用、提交推送或改写原件。当前工作树保持脏状态；基线 HEAD `bc5d1cb815bf512c964784cda92914689ef1ce11`。
- RV1-01–05 已在选择性视觉规划、持久任务、事实整理入口及正式工作台作针对性修订：8字不再单独证明扫描页可靠，已知表格/标记和未知质量进入核对范围；失败不再提交为完成；旧v1覆盖不被v2计数借用；可读页先保存，缺图页单列，换模型不复用旧观察；跨章要求与官方条款共用完整列表、计数、筛选和分页，保留不同来源身份。任务身份现绑定不含密钥的模型路由指纹；事实整理只接收当前核对回执列明的观察ID，旧观察不混入新病史。此为合同修复，非真实病例验收。
- RV1-06/07 只读现场复核：跨章作业 `0da5ef099b3e4972a012d14a3d5d75fe` 仍为 `cancelled`、92/172，`deep_0007=failed_final` 且 `hydrate` 仍依赖它。它不是可忽略的历史失败；不得盲目 `resume_cancelled`。冻结收口有 1,927 单元：candidate 488、uncertain 50、context_only 281、non_control 1,108；538 个深审拥有单元分成 84 批，16 批仅有 1 个单元，59 种标题路径；上下文出现 182 次。实际 token 计数未知。修订史中的候选控制和仅盲法标题也进入深审，应核实分类/分包是否过宽；不能直接删候选或按特定方案硬编码。
- W6 补充只读测量：用生产 `build_protocol_control_agent_prompt` 对冻结的 84 个批次重建实际提示，共 7,381,055 字符；单批最小 83,015、中位 87,739.5、最大 101,301 字符；16 个单单元批次合计 1,359,918 字符。批次序列化中重复携带已知官方目标约 1,020,684 字符、流程目标约 1,435,980 字符；这些是字符量而非供应商实际 token/费用。538 个深审拥有单元中，目录指路性条目 20 个（7 candidate、13 uncertain），修订史 8 个 candidate。冻结的前6个已完成深审批次中，5批无候选、1批有1个候选；只说明值得继续核查粗筛，不能外推余下批次或直接删选。抽样显示部分仅为页码标题或变更说明，另有导入、给药前等真实控制；不能按章节一刀切。下一设计应区分“导航/历史文字无独立规范内容”与“当前有效的受试者要求”，并在保持每单元来源/上下文身份的条件下评估跨标题有界合批。发现提示或分包合同变更必须建立新版本作业，旧回执只能逐项证明可复用，不能混接。
- RV1-08 仍未验收真实更正/补证后新结果。正式规则尚未发布，5份24页资料尚未走事实/Profile/报告链；`claims_complete=false`。
- 聚焦回归：后端相关套件本轮扩大到 `137 passed, 2 skipped`（两项需显式启用真实端点），工作台组件 `16 passed`；前端正式 build 曾在本轮通过。当前任务回执的观察 ID 到病史输入的隔离已覆盖定向反例；尚未做产品真实连接/病例验证。Q1/Q2旧通过结果未重跑；Q3及真实病例、宽屏浏览器、第二结构留出均未执行。详细处置及剩余风险见 `HANDOFF_0923V1_RETURN.md`。
- W6 新发现提示已按上述反例修订为 `control-discovery-prompt/v2`，执行版本升至 `protocol-control-execution/v15`；旧取消作业不与新提示混算。隔离 W6 数据库只读核对：v14 跨章作业仅 1 个，状态 `cancelled`，无该版本活动作业；不能据此推断其他数据目录无活动作业。新提示的模型质量尚未试跑，不能把旧发现结果改写成新版本已通过。只读模拟按原顺序、每批最多12单元跨标题装箱，并为各单元保留自身来源/标题/必需上下文：45批、提示合计4,184,393字符，最长106,200字符，其中3批超过当前实测最长101,301字符。它只证明重复输入有缩减空间，不证明输出质量或服务上下文足够；正式分包尚未改变，需先以实际模型容量、输出预算及跨标题质量验证设定界限。
- W6 合同集中回归 `41 passed, 1 failed`：新增旧版本检查点拒绝重放与通用提示断言通过；唯一失败是既有 `test_protocol_control_generalization.py::test_discovery_gate_closes_results_and_routes_only_candidate_uncertain_deep` 的深审提示 `<30,000` 字符预算断言，当前实测 53,906 字符。此测试所测深审提示未被本轮修改；固定内容中系统合同 17,259 字符、完整输出 Schema 34,921 字符，是实质效率债务，不以放宽断言掩盖。须在不损失结构接受和临床完整性的前提下验证可否瘦身，不能直接删结构或宣称模型质量不受影响。
- 下一安全动作：先以来源保真的方式完成 W6 有界分包及版本身份，再从合法入口新建受控作业；原作业继续保持取消态，`deep_0007` 不能伪装为历史成功。不得把草稿 `publishable=true` 当正式发布。真实服务仍需验证模型别名、配置切换和逐页观察到事实/Profile的完整来源闭包。无需反复重跑已通过的 Q1/Q2。

## 本轮合同
用户已授权在唯一工作树按W0→W7和Q1→Q3连续实施，不再等待另开fork。当前不启动临床Job、不改env；按相连功能集中验证，避免逐处改动逐次测试。
方式：当前所有者负责单写者集成；共享生产路径的连续修改以内联实施为主，实质解释风险和最终临床验收按现行执行/会商机制独立挑战。

## 当前依据与环境
- 入口delivery_20260922/00_START_HERE.md；prd/design/03_PLAN/04_WORK_PACKAGES/05_ACCEPTANCE当前，旧Phase范围保留。
- 唯一worktree：/Users/smkzw/Documents/康哲项目资料/AI/入排/enrollment-review-app/.worktrees/phase5-clinical-facts-profile。
- 起点本地/GitHub HEAD一致e7f34d0508c05481164cf13569f66ddf64e2e74f；起点clean、无open PR。后续里程碑提交以当前`git log`为准。
- 现有方案、原图/OCR、持久任务、事实/Profile、求值/工作台/报告/更正骨架，不从零重写。
- C01–C10问题和专家12条裁决见01_REVIEW；模型诊断补充见10_MODEL_AVAILABILITY（生成后）。claims_complete=false。
- AST扫描592文件无语法失败；前端真实build失败6项；一批相关pytest为14失败/20通过/2xfailed。未逐项划定产品/fixture原因。
- 无应用/临床库写入、无浏览器或完整病例验收。模型诊断不等于质量验收。
- 运行时get_goal为paused，正文V3；本轮未修改/恢复/新建Goal。07_FORK_PROMPT为下一会话目标候选，执行者依据工具实际能力更新，不伪报已改。

## 0925V1 增量实施（2026-09-25，未启动临床作业）

- 执行方式：来源覆盖与复用预检共用同一冻结方案链，先由本线程按最小改动处理；不为这两项另派执行节点。审阅基线 `6bd6ce95ebf08001b698ca25f38e1d3b28766c9b`；起始未跟踪会商文件保持原状，原方案、病例、隔离库和旧回执只读。未获本包对收费调用、正式发布和推送的授权。
- A 来源：`procedure_catalog.py::_flow_footnote_refs` 已修正同一编号列表末尾的嵌套注释在 EOF 或下一节前丢失的问题，同时保留新表/新节/编号重启边界；版本提升为 `required-procedures/v4`。原模块增加末尾嵌套、下一节与编号重启正反例。真实冻结结构 `protocol_blocks/25c713e2…7903.json` 有 3,280 个结构块；SAR 访视表 `body.t3` 后的编号注释为 28 条、40 个段落来源。新冻结目录 `f05217cc62924d4d9d2ce73968fdc8d5` 有 41 项，实际引用其中 37 个注释段落；余下3处均在 v123 全方案来源清单：`body.p279`（单元 `su-f653…`）为基线入组评分条件，发现批次26标 `candidate`，待深审；`body.p315` 和 `body.p316`（`su-e1bd…`、`su-41fc…`）为后续访视评分安排，发现批次27标 `non_control`。这只是当前目录分流/发现回执，尚非正式规则发布或临床验收；不把后续事项强塞入入排目录，也不按模型输出清单充当来源分母。结构来源与语义引用仍为两个不同层次，不改旧发布目录。
- B 复用：取消根据历史版本号推测旧提示摘要的白名单。新任务建立前逐批检查冻结来源、分包、线路、旧检查点、实际保存的提示摘要及当前门禁，产出内容寻址 `protocol_control_reuse_plan`；正常提示/结构/装配变化列 `refresh_required`，组成身份缺字段、摘要格式损坏或线路/范围错误在建任务前拒绝。新批次分别保存来源、发现计划、覆盖清单、提示正文/组成、输出Schema、装配版本、校验版本和请求线路摘要；旧记录缺分项时标 `legacy_component_identity_unproven`，不反算。执行前再核计划工件及可复用批次的当前组成身份；模型实际返回名称不在现有传输回执中，明确仍未证，不伪填。不调用新模型作预检。执行版本为 `phase5/protocol-control-execution/v128`。真实 v126→v127 首批旧摘要 `327fed01…513771e`，当前组成摘要 `a084f09d…b8960276`；冻结来源和线路相同，确切变更子组件因旧版本未分别保存，不能反推或宣称兼容。当前会提前判定需重算，不再运行86步后才撞上身份错误。
- C 局部错误：`validate_source_target_review` 现在给来源/目标不一致提供 `code`、`statement_index`、`json_path`、`source_refs` 与修复范围；Runner 不再从中文报错正则猜条目，中文仍用于显示。尚未解决同批多条要求必须全部支持才走单项快路，亦未完成条件/例外的有源装配，不能称C已完成。
- 验证：初次A/B集中套件 `39 passed`；补组件身份后的同组新增反例首跑 `39 passed / 1 failed`，失败是测试误要求所有批次重算，产品已逐批区分；修订后定向 `5 passed`、C条目核对与既有装配定向 `9 passed`、脚注样式邻近反例 `4 passed`。合成样式反例曾漏测真实不同编号子列表，真实结构回放一度由28条/40段降至21条/28段；补分支后恢复28/40。本包统一复核命令 `.venv/bin/python -m pytest tests/v2/protocols/test_procedure_catalog_slice3.py tests/v2/services/test_protocol_control_execution.py tests/v2/protocols/test_slice58c_control_deconstructor.py -q`：最新 `180 passed`、退出0，5项第三方SWIG弃用警告。仅证明相关代码边界，不是临床验收。未执行真实模型、整方案发布、病例审核、前端Q3；`claims_complete=false`。
- 尚未闭合：B 的真实供应商返回别名尚未随传输响应保存，旧回执的组成身份不足以证明安全复用；C 单项成功的持久中间结果、条件/例外/多条要求装配，D 病例来源候选，E 可选小模型实验，F 正式发布和更正验收尚未完成。下一安全动作是完成本组回归，再在隔离输入上验证C的混合条目局部闭环；收费试跑/正式发布另需用户授权。

## 状态
| 包 | 状态 | 下一动作 |
|---|---|---|
| Review与资料包 | 已完成且文件检查通过 | 09为证据/裁决，10为模型诊断；可交给fork从W0执行 |
| W0 | 已实施并通过Q1 | 前端正式构建通过；资料页数按正式上传+真实解码核算，未知阻断；唯一源码树外目录+完整hash；旧watch拒绝执行；跨供应商密钥回退已关闭 |
| W1 | 已实施并通过Q1合同回归 | 当前范围最新作业优先；未完成核对不直接用已发布事实判定；回执核实的条件、原文含义、复查关系、频次和书面判断进入工作稿；正式签发仍要求独立授权；官方条款与跨章要求同修订展示 |
| W2 | 已实施并通过Q1合同回归 | 坏结构保留为技术失败并进入既有限次修复；不得改写期别、裁剪来源或把解析失败降级为医学待确认；工作台发布强制携带全文控制点覆盖结果及完整谱系 |
| Q1 | 已通过 | 后端W0–W2集中套件127项通过；正式方案发布19项通过；前端`npm run build`通过；仅有既有大包体提醒和SWIG弃用警告 |
| W3 | 已实施并通过Q2 | 新资料采用文字主读和风险页局部视觉核实；风险页逐页分批、不漏页，未核完不进入正式病史；旧双读仅作历史兼容 |
| W4 | 已实施并通过Q2 | 左栏待处理/全部条款单视图；筛选、显示数和条款深链可恢复；原件明确引用/浏览状态并扩大桌面占比 |
| W5 | 已实施并通过Q2 | 追加式更正沿用既有闭环；当前事实变化后旧准备任务、旧工作稿和旧审核入口均拒绝继续使用 |
| Q2 | 已通过 | 前端正式构建通过；工作台/原件21项、后端跨层224项、更正失效2项通过；无真实模型或临床资料写入 |
| W6 | 0925V1离线纠偏中，尚未发布 | 新DOCX官方规则草稿修订21未正式发布；旧真实深审失败，无活动新作业。来源脚注、前置复用和混合条目局部保存/人工恢复有离线证据；条件/例外仍未装配，须获授权后整方案核对 |
| W7/Q3 | 待执行 | 完成真实整例、更正补证、第二结构留出、大屏、部署与最终验收 |

## 下一动作和纪律
用户已授权隔离真实试跑；原取消作业、正式临床资料及已发布规则保持不动。不要重跑Q1/Q2；按当前冻结结果与产品实际身份，从合法入口新建版本化作业。`HANDOFF_0922V2_RETURN.md`中“84/85个单元”“failed_final只是历史步骤”两句已被现场反证，不得作为恢复依据。
实质变化时合并更新本表和证据、实际运行身份、下一动作；不每改一行写日志。原始回执在artifacts；无密钥；不得覆盖本轮未提交文档。
交接08_RETURN_TEMPLATE。仅在用户明确授权时提交/推送；不清原件/历史/别人的dirty，不把部分完成伪报全交付。

## W6 隔离试跑与通用合同纠偏（2026-09-23）

- 从未改动的 v17 来源库备份到 `/Users/smkzw/tmp/enrollment-review-w6-20260923-v29-cms-deepseek-01/data`，由产品 API 创建作业 `caba64aac19a474d8c4386d6b986ffa2`；产品经 OmniRouter `cms-router/deepseek-latest-cloud` 直接推理。85个发现批次完成，深审计划56批；首批经两轮修复仍失败，`deep_0001=failed_final`，后续依赖未运行，未发布目录。旧库与原始方案未修改。
- 根因不是单一模型故障：首批12个单元请求约6.7万输入 token，提示里重复了已通过 `response_format=json_schema` 随请求送出的完整结构；原有同源不同摘录在控制原子可表达、在求值/观察/资料政策中却被按来源位置禁止，核验字典还会丢掉前一摘录；“给药方案”的普通用语被误判为研究方案权威引用。对应合同改为逐对来源与摘录核验，完全相同的对仍拒绝，真正附录引用仍严格要求原文支持。当前临床动作混合、后续义务提前判定仍由原门禁拒绝，不自动拆分或补造。
- 通用提示区分当前节点事实与未到期持续义务；`json_schema` 通道不再在文本提示重复整份结构，非严格结构通道仍保留。请求身份版本已更新。相同冻结两单元复杂小样用 DeepSeek：旧路径3轮失败；新路径提示约8.7万降至5.7万字符，2轮在产品结构/发布门禁通过，耗时约185秒。完整首批新路径提示约6.4万字符，1轮通过、约166秒、1条候选。一次隔离GLM同小样约949秒仍失败，不能据此宣布其整体劣于DeepSeek或切换正式配置。
- 定向合同/提示/门禁检查31项通过；先前宽套件29项失败是既有缺失 evaluation 的旧夹具，未把其计作通过，也未改宽门禁追绿。以上仅是深审首批诊断，不是官方+跨章完整发布，更不是个例临床验收；`claims_complete=false`。下一步在新隔离库由合法入口跑完整版本化W6，核对每批最终状态、来源覆盖和审阅结果后才允许同源发布。
- 随后从同一v17来源另建 `/Users/smkzw/tmp/enrollment-review-w6-20260923-v34-compact-schema-01/data`，产品作业 `062afa810d0b4c53adc22a92e6e4e57b` 约21分钟后 `deep_0001=failed_final`；发现阶段重新抽出60批深审，首批相对前次多出给药后内容，因此先前通过的诊断批并非同一冻结输入。未发布。
- 从真实v34冻结批中隔离取四单元用同一产品 harness 诊断，约158秒、3轮完成并通过门禁。由此再从v17新建 `/Users/smkzw/tmp/enrollment-review-w6-20260923-v35-deep4-01/data`，经现有服务批量参数冻结每批4单元，作业 `59bdec6c80ef4cfab58a2a78b44c78dd` 约20分钟后仍在 `deep_0001=failed_final`；新发现阶段本次生成104个深审批，首批包含“自筛选开始稳定背景治疗7天”，模型未能给时间原子保留命名锚点。两次均在来源/结构校验处停止，成功发现批次保留在各自隔离库，旧库与原件不动。
- 结论：缩短提示和单批量改善了某些同源小样，尚未解决发现阶段分类波动、复杂时间/后续义务一次性成形与失败后重复全部发现的恢复成本。不得用单批绿灯或再次随机重跑宣布W6完成；先核合法的分段恢复/发现复用和深审合同拆负担，同时推进不依赖临床发布的W7准备。`claims_complete=false`。

## W6 实跑与 2026-09-23 无损暂停

- 真实方案解构作业`94ea6ac2d1454db0b0d73af91532d26b`保留在`waiting_user` 8/10；同源草稿`draft:candidate-94ea6ac2d145-in01-all`已到修订21，23条官方规则（7条入选、16条排除）通过完整性检查，`publishable=true`。
- IN03、EX01、EX12的来源、分支和逻辑结构已逐项修正；“可发布”仅是草稿门禁结果。跨章节控制点作业未完成，因此未正式发布规则。
- OpenCode Go DeepSeek端点不接受`response_format=json_schema`。现按供应商能力选择`json_schema/json_object/text`，DeepSeek走`json_object`，内部严格Schema校验不变；完整Markdown JSON围栏可剥离，不猜测或改写临床内容。
- 跨批发现把被其他候选引用的`non_control`单元同时当成不可用上下文。现确定性提升为`context_only`，只供上下文，不生成独立控制点；修复后收口成功。
- 跨章节作业`0da5ef099b3e4972a012d14a3d5d75fe`已按用户要求取消，终态`cancelled`，92/172；85个发现批次完成，深审扩展为84批，仅完成少量。84/85的比例可能提示发现合同、切分或不确定路由过宽，恢复后必须先抽查，不得盲目续跑。
- 专用`57060`服务已关闭，无活动租约；本轮未关闭不归本任务所有的共享服务。5份受试者PDF共24页尚未进入事实、事件、用药暴露、Profile和报告流程，`claims_complete=false`。
- 完整恢复状态、文件路径、验证边界与下一安全动作见`HANDOFF_0922V2_RETURN.md`。

## W1 实施证据（2026-09-22）
- `qualified_binding_selection.py` 增加与正式授权类型隔离的回执核实工作稿入口；复用既有来源拒绝、观察选择、命题、复查和频次消费者，不生成授权对象，不允许发布边界接收工作稿。
- 已完成的书面判断、原文含义、复查关系和频次作业须从原始回执重建，并与同一候选作业、审核上下文、冻结输入、比较指纹和来源配对逐项相符后才进入预览计算。
- `frozen_review_calculation.py` 对工作稿和正式选择使用同一命题、频次、复查与控制计算；二者差异保留在授权和发布边界。缺研究者书面判断仍是正常的“无法判定”缺口，不触发额外确认。
- `eligibility_review_projection.py` 只读取当前完整authority下最新作业；若最新作业尚未完成、失败或取消，显示当前未核状态，不借更旧完成结果冒充当前。官方条款和跨章节要求来自同一已发布修订。
- 活接口与前端解码已增加独立`controls`结构；方案确有补充要求时不塞入官方条款，官方与补充要求总数为零则拒绝生成审核结果。
- 本包只执行了约定允许的静态校验：相关Python文件编译、模块导入及`git diff --check`通过；功能/反例/前端构建留到W2后Q1一次集中执行。

## W2 实施证据（2026-09-22）
- 删除`protocol_control_deconstructor.py`中把缺失或不合格`evaluation`改写为`investigator_judgment`的预清洗。合同错误现在保留为`WIRE_SCHEMA_INVALID`，由既有同任务、有限次数结构修复处理；只有方案原文明确要求研究者裁量时才允许形成专业判断条款。
- 恢复`protocol_control_catalog_publication.py`中曾被临时`pass`绕过的正式发布绑定：当前方案输入、完整原文定位、草稿/规则版本/期别、全文覆盖清单、发布门禁版本、处置计划、批次来源范围和候选集合必须逐项一致。
- 正式工作台的首次发布与重新发布均要求已完成的全文控制点任务及其最终保存记录；即使没有额外控制点，也必须有来源化的全方案覆盖结果，不能用空参数绕过跨章节核对。
- 官方规则、流程节点与跨章控制仍在同一发布事务内写入；控制目录保存失败会回滚整次发布。移除了临时标准错误打印，持久化内容损坏也统一转为可理解的发布谱系拒绝。
- 本包按约定仅执行Python编译、关键模块导入及`git diff --check`，均通过；反例、服务路径和前端构建纳入Q1一次集中验证。

## Q1 集中验证证据（2026-09-22）
- 修复集中检查发现的历史临时旁路：未知/混合期别不再自动改成当前期别；跨章只读上下文不再因发现阶段标记为非控制而被裁剪；流程目录多出的访视不再静默删除；对侧期别必须有明确不适用处置。
- 深析模型在有限修复后仍无法产生合规结构时，任务以技术失败结束并保留诊断，不再伪装成`PENDING_CONFIRMATION`医学待确认项。
- 当前节点没有完整核对作业时，审核投影明确显示资料尚未核实，不采用未经过本次资格选择的已发布事实；缺少节点映射不会再令整个页面异常退出。
- 后端集中命令覆盖W1/W2、API、页级读取与资料处理，共`127 passed`；方案正式发布服务另`19 passed`；只有第三方SWIG弃用警告。
- `frontend/npm run build`正式构建通过；仅报告既有入口包体超过500kB的性能提醒，不属于本轮合同失败。`git diff --check`通过。

## W3 实施证据（2026-09-22）
- 复核“模型不可用”原始回执并作当日最小调用：OpenCode旧400由缺少稳定会话标识造成；补齐产品自有会话身份后MiMo与DeepSeek可用，Muse仍由上游返回503。当前`.env`的Ollama main-A缺专用凭据，产品按供应商隔离规则在本机拒绝，不再误发GLM密钥。
- OpenCode兼容头已进入产品传输层，首次、格式修复和重试复用同一产品进程会话；不读取OMP/Hermes身份。此兼容不等于临床长提示或真实页面质量验收。
- 新上传资料冻结后统一建立页面质量核对任务。扫描件已有充分OCR文字时不再仅因“是图片”触发VLM；低置信、复杂版面、原文提取异常或确实缺字的页面才进入局部视觉核实。
- 局部视觉规划不再把超过首批额度的风险页标成跳过；全部风险页按`max_pages_per_call`切成短上下文批次逐一保存。局部失败保留关闭记录，其他页面仍可完成，但有任何风险页未核实时事实整理入口拒绝继续。
- 个例档案整理默认消费OCR/原生文字与来源绑定的选择性视觉观察，不再要求新资料先完成整页双模型对账。没有风险页也必须有已完成的覆盖任务；历史资料若没有新任务，只能使用同作用域、完整对账的旧双读覆盖，不借用或伪造A/B记录。
- 来源政策合同收紧：`self_consistent`只表示文字内部自洽，不能单独冒充已核实来源；单来源采信只接受交叉核实、定向核实或人工确认。
- 本包按集中验证纪律仅执行受影响Python编译、关键模块导入及`git diff --check`，均通过；风险页分批、旧读兼容、任务恢复、事实发布/Profile来源闭包留到W3–W5后的Q2一次验证。

## W4 实施证据（2026-09-22）
- 正式入排工作台左栏不再同时堆叠两套重复列表，改为“待处理/全部条款”单一视图；待处理仅按问题类型归类，不宣称同组问题可一并关闭。组内条款按风险、官方编号和组件身份稳定排序。
- 当前范围、判定筛选、单次显示数量和条款选择写入地址状态；显式条款深链优先，无深链时从当前可见列表选择最高优先项。切换范围或筛选时同步选择有效条款，不保留不可见选择。
- 中栏保留短依据、动作和关联事实，方案原文改为独立展开区；不调用模型改写事实。右栏扩大为主要桌面区域，明确标出“引用原文”与“浏览原件”；无关联事实时第一页只作浏览，不再误称引用页。
- 页码定位、资料文档和红框仍从同一完整处理修订读取；未核实精确坐标不画框。页脚分开显示规则修订、节点修订和资料快照，避免把节点修订号称作资料版本。
- 本包只执行差异检查；前端正式构建、组件状态与宽屏实操按集中纪律进入Q2/Q3。

## W5 实施证据（2026-09-22）
- 复核既有事实修订链：它已具备原文定位约束、影响预览、幂等持久任务、追加新事实/事件/用药暴露、冲突与资料期望重投影、新个例档案生成、旧版本保留、停止/失败重试和修订历史，因此不另建第二套更正服务。
- 新增统一的“当前临床材料指纹”，覆盖活动事实、事实与规则关联、事件、用药暴露、资料期望和冲突组。人工更正改变任何活动链头后，更正前冻结的审核上下文会与当前材料不一致。
- 该指纹同时进入新审核入口、持久审核续跑和工作台工作稿读取：旧任务不能继续，旧工作稿不能冒充当前结论，新审核必须重新准备当前病史。历史报告及旧上下文保持只读不变。
- 补充新资料会产生新的资料快照/完整处理修订，既有authority隔离继续使旧检索与旧工作稿失效；同一处理修订内的事实更正则由新增临床材料指纹补足失效边界。
- 受影响Python模块编译、关键导入与`git diff --check`通过；功能反例统一进入Q2。

## Q2 集中验证证据（2026-09-22）
- 前端正式构建已通过；工作台与原件组件集中测试`21 passed`。旧测试仍按左右两套列表、默认全量条款和“按根因聚合”查找界面，已按当前单列表产品合同修订；用户文案改为“按问题类型归为”，避免暗示同类问题可以一次性解决。
- 选择性视觉、观察保存、任务恢复、用户停止/重试、事实整理、更正链、工作台投影等后端集中测试`224 passed`。首次运行的7项失败均为旧合同夹具：1项要求超过单批上限即丢页，6项把已有充分OCR文字的页面伪装成必须视觉核实。修订后全部通过，产品逻辑未回退。
- 新增病史变更反例`2 passed`：更正后，旧准备记录不能再发起后续核对，旧持久审核也不能续跑；必须基于当前病史重新准备。历史记录不改写。
- `git diff --check`通过。仅保留第三方SWIG弃用警告和前端既有大包体提醒；本窗口未触发真实模型、未启动临床作业、未修改原始资料、数据库或环境凭据。
