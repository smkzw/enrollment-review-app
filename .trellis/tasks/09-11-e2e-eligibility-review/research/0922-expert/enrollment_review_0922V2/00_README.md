# 入排审核子系统增量复审与纠偏包 · 0922V2

仓库：smkzw/enrollment-review-app。分支：codex/phase5-clinical-facts-profile。
本轮固定提交：`e7f34d0508c05481164cf13569f66ddf64e2e74f`；前轮基线：`00c46cbc05eda5ac5ce63909b47de34ee72e18ff`。
本包只属于入排审核子系统，不混入医学监查、写作或竞品情报项目。

## 核心结论
有真实修复，不应整库回退；但“F01—F10全部完成”不能替代调用链、构建和实际闭环验收。
最重要的新发现是完整条件资格选择缺失、拒绝结果折叠成None后可回落旧类别路径、
前端 unused-local 构建错误、规模集默认路径 NameError 和图片页数不计入分母。
HTTP 400 的实际根因仍待同一产品适配器的单变量诊断，不应直接要求用户再换供应商。

## 使用顺序
1. 01_REVIEW_FINDINGS.md：分清已修复、部分修复、新缺陷和未证实事项。
2. 02_AGENT_MASTER.md：开发 Agent 主指令；沿用已有任务，不另造平台或Phase。
3. 03_HARNESS_AND_UI_SPEC.md：请求诊断、完整资格消费、例外工作台和更正闭环。
4. 04_ACCEPTANCE.md：必须执行的测试与证据口径。
5. work_packages.json：执行依赖；probes/：离线控制流复现；sources.json：固定提交索引。

## 证据边界
本轮读取的是GitHub固定版本源码/交接和提交差异。容器到GitHub直接下载因DNS失败，
没有完整克隆仓库，没有运行完整pytest、npm build、真实产品/模型、临床数据库或浏览器E2E。
本包执行18个离线观察用例（包含修复对照和正例），不是“18项产品验收通过”。
Python/TypeScript函数片段人工转录自已读取源码，支撑类型与依赖使用合成对象。
独立TS检查使用本容器5.8.3，仓库声明7.0.2；必须在本机锁定依赖下再执行真实构建。
未使用或验证任何泄露凭据，没有运行原始watcher、没有修改远端仓库。

用户已决定：云端使用无限制；几十至一百余页复杂扫描/批注/翻拍；例外驱动，随时可见
全部条款和原件；不固定双完整VLM；方案Agent内置前置；不猜遮盖、不独立判波形/影像。
新模型能力与端点可用性不从模型名称推定。密钥控制台轮换不以源码删除冒充完成。
