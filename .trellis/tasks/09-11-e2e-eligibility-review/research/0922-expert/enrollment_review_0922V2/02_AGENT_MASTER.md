# 交给开发 Agent 的实施主指令 · 0922V2

你继续修订 smkzw/enrollment-review-app。外部审阅基线e7f34d0508c05481164cf13569f66ddf64e2e74f。
轮次固定0922V2，所有RETURN、测试、截图、运行回执使用该标记。沿用原任务及平台，
不另建产品Phase，不整库回退、不清理原件/数据库，不把历史文件的暂停或模型默认覆盖最新用户决定。

## 目标
交付可用、来源可靠的医学经理工作稿及连续例外审核操作。先解决实际请求合同与完整资格
消费，停止用25页全量重复请求调试同一400。前端与离线安全修复不等待供应商恢复。

## 0. 现场保全与差异判定
读取现有AGENTS、当前任务、HANDOFF_20260922_FINAL及本包。记录pwd/branch/HEAD/dirty范围；
逐条标R2-01至R2-12为仍成立、已解决、部分成立或不成立，并给当前函数、测试和反证。
不得reset/clean/rebase用户dirty工作；提交推送遵循用户现有授权。保留真实运行与源码修改边界。
不要使用旧泄露key进行试探。若轮换未确认，只暂停需该凭据的网络请求，其他离线工作继续。

## 1. 立即可并行修复
A. 修正severityOf未使用，保持TS严格检查；运行锁定依赖下npm build。
B. 修复规模集os作用域、回退/符号链接/源码树路径检查；支持真实媒体解码、PDF页数及TIFF帧数。
C. 将错误HTTP、应用错误、范围错误、0条款或缺少预期要求等分别拒绝；不静默复用他人受试者。
D. 守望配置路径必须先解析应用根；健康判定/终态错误不能正常退出；禁止硬编码凭据。

## 2. 资格消费：先修语义，再追“更多确定结果”
- 保留pair_direct_selection_rejection_reasons，但它只是单配对检查，不是整条件选择。
- 复用或抽取_select_facts_for_identity/_select_with_ordering等完整语义，按value/date_range等
  操作数区分，执行时间资格、多观察/排序/复查/命题/频次与书面判断所需边界。
- 工作稿与正式报告共享证据语义；正式发布授权仍由原gate执行，不伪造签字或授权。
- qualification结果用显式状态返回。全部拒绝、不完整、失败、陈旧不能折成None后走旧类别匹配。
- 当前rule/episode/evidence/processing/fact/metadata/locator集合必须与输入scope一致；
  用完整审核上下文定位关联任务，不从全库最新任务猜测。将全库fetchall改为先scope过滤再排序；
  对同scope新未完成与历史可用结果的选择政策明确版本化，不静默回退。
- 删除已返回后的旧选择死代码；不要为通过老测试恢复空候选FALSE或删除资料缺口。

## 3. 请求合同：最小真实路径诊断
先复用产品实际HTTP客户端构造一次净化的请求合同，核对是否真的发送HANDOFF所述参数。
headers只记录存在性，image只保留hash、尺寸、格式、payload字节数，密钥/临床原文不进仓库。
在已批准配置中做单页控制变量；记录HTTP状态、error.code/message/param、request_id、
流式finish、response模型声明、解析失败阶段。不要把HTTP400、流截断、JSON/schema错误混为一类。
同一确定性请求错误不允许25页反复付费；一页完整读取/结构/来源验证通过，再放大3–5页。
诊断低预算不等于生产默认低预算；生产预算应按角色、输入、输出任务量与服务能力显式规划，
不得截断后当完成，也不得强制所有角色统一65K下限。

## 4. 模型身份与来源迁移
requested/reported/canonical分开存，reported缺失不得or回填；别名显式登记并版本化。
新增字段贯通回执、记录、恢复、覆盖选择、历史兼容与执行合同；不只改一个model赋值。
保留用户当前指定路由；有证据再决定是否更换，不把未验证的猜测转交用户选择新key。
正式单主读/局部核实策略必须接通计划器、记录、消费者、发布和UI；旧双完整读取只在
显式选择的策略中运行。不得伪造第二读、强制采信单读或关闭来源门禁冒充迁移。

## 5. 正式前端，不做另一套展示壳
修改真实正式入口。左栏改“待处理问题/全部条款”同区切换，不上下重复。
对组和组内成员使用同一严重性排序；默认最高优先实际issue，尊重显式deep link。
依据/影响/动作使用短bullet，原文可展开且保留来源，不能只把散文装进三张卡片。
列表宽度有上限，原件区域吃掉宽屏新增空间；14–16px可读正文，禁止靠缩根字号/负字距/隐藏风险减密度。
受试者代号、issue标题、证据按钮分别直达正确对象；返回保持filter/search/sort/page/selection/scroll。
区分已采用引用、待核实候选和全部原件浏览；首页浏览不是引用来源。资料版本必须来自真实快照。

## 6. 更正与补证
共享页定位仅是相关候选，不是同一观察证明；不要据siblings自动批量改值。
以来源观察身份和显式派生链完成一次更正，原子保存必要变更及失效，重新资格核对后形成新工作稿。
检索范围“未发现”的依赖包含整个证据快照，新资料即使不引用旧fact也须失效相应否定检索。
应提供受影响工作清单与重算状态，不能仅让用户逐条修内部fact，不能以全UNKNOWN宣告闭环完成。

## 7. 退出条件
至少：真实npm build、后端目标回归、反例/正例同测、同一产品适配器单页成功合同、
一次完整规则下个例工作稿、一次更正/补证后重新资格及新结果、旧历史不变，正式前端连续操作验证。
真实资料或服务未就绪时交回准确blocked_external记录；不得假称已验收，也不得停止所有独立开发。
先不强制用户为查看工作稿做逐条gold标注或方法签字；正式方法验证/采用保持独立流程。

## 回交
生成HANDOFF_0922V2_RETURN.md：当前HEAD与dirty、逐项findings disposition、真实命令与退出码、
请求合同和脱敏错误回执、前后接口身份/版本、真实截图与视口/缩放、已完成链与未完成链。
进度表不用95%等无分母百分比；区分技术completed、证据可用、工作稿可用、正式发布。
所有运行失败也存档；不要仅交“已加函数”“已推送”“测试全绿”的口头总结。
