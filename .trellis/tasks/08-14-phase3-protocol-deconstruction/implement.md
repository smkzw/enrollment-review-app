# Phase 3 实施计划

## 开工门槛

- [x] 用户审阅并明确批准本 `prd.md`、`design.md` 和 `implement.md`。
- [x] 运行 `task.py start`，确认当前任务为 `08-14-phase3-protocol-deconstruction`。
- [x] 记录两份真实方案哈希、LibreOffice 版本和现有全量测试基线。
- [x] 确认 Phase 2 的 V2 领域表、发布完整性和持久 Job 基座均已通过；不得为 Phase 3 再造平行存储。
- [x] 确认仅处理 V2 隔离数据目录，不改源方案、旧项目或人工资料。

## 切片 1：文档结构与开源候选 spike

- [x] 用 MG-K10-SAR、D001 对 python-docx/OOXML、Docling-slim 候选和 LibreOffice 渲染通道做限定对比。
- [x] 记录页眉、第一页、官方编号、嵌套条目、流程表、渲染页定位、耗时、依赖和许可。
- [x] 冻结主路线；未采用候选写明原因并清理隔离缓存。
- [x] 新增原始方案、渲染派生物、来源片段和提取快照契约及迁移。
- [x] 单测文档哈希、部件顺序、嵌套表格、编号、修订标记、来源定位和渲染 manifest。

验证：针对两个真实 DOCX 的只读提取测试；迁移升级/回滚；契约 schema 测试。

## 切片 2：元信息、研究期别与权威材料

切片 1 验收约束：页眉/页脚可作为方案身份候选来源，但不得因其规则级定位状态为
`DEGRADED` 而被误判为低可信身份来源；“身份字段权威性”和“规则正文定位精度”是
两套独立语义。

- [x] 实现元信息候选及页眉/页脚优先级，区分模板编号/版本与方案编号/版本。
- [x] 实现 II、III、共享条款和真正无缝候选的章节适用图。
- [x] 适用范围细化到段落、表格行和访视列；“操作无缝”或新受试者继续入组不得触发合并候选。
- [x] 实现需要用户确认的期别状态；选择单一期别后只输出所选期别和共享块。
- [x] 新增 InterpretationSource/Conflict 及“只能解释、不能改写”的确定性约束。
- [x] 添加 MG-K10-SAR、D001 身份与期别回归；文件名冲突和通用标题反例必须失败。

切片 2 返修（2026-08-14 主会场阻断后）：

- [x] 将表格单元作为最小段落来源块；表格行/访视列只在子项期别一致时继承，混合聚合标记 `mixed` 并从单期投影排除。
- [x] 单期投影只纳入精确选定期别或明确共享块；投影只生成 `projection_text`，不覆盖 graph 的源 `text`，聚合块保留子块 ID 和全部 source span。
- [x] 元信息冲突只比较正式/非 fallback 的最高 priority/identity-authority 层；D001 文件名 `CMS-D001` 作为备选证据，不再制造正式 protocol_code 冲突。
- [x] 增加合成大表反例、真实 MG/D001 投影实际文本/来源归属强断言，并复核真实源文件哈希、大小、mtime 和目录未改变。
- [x] 修复正文和同一表格单元格的共同标准上下文；裸“共同排除标准”可共享，单期+“共同适用”仍属单期，两期混合但无明确配对共享语义时必须为 `mixed`。
- [x] 已确认身份不得携带未解决冲突；确认值必须精确对应冲突候选，并保留历史已解决冲突、显式项目代号和日期精度。
- [x] 文件名兜底分离编号/版本/日期，不将编号尾数误当版本；项目代号、方案日期和确认时间均加入列/payload 镜像篡改拒绝。

验证：元信息优先级/确认单测、期别投影属性式检查、解释冲突测试、0005 升降级与持久化 round-trip；
最终 `tests/v2/protocols` 63 项通过，全仓 `530 passed, 1 skipped, 18 subtests passed`，两个真实 DOCX 的来源哈希/目录只读回归通过。MG/D001 II/III 投影均无对侧专属或 mixed/unknown 聚合块，派生显示文本对侧期别污染计数为 0。新鲜独立 `gpt-5.6-luna:max` 审查会话经三轮拒绝与系统性修正后，第四轮给出无 P1/P2 的 `ACCEPT`。

## 切片 3：官方目录、Protocol Deconstructor 与确定性检查

开工前先做页定位恢复 spike：仅对前后唯一权威页范围足够窄、无页序逆转的块生成
`DEGRADED` 候选页，量化恢复率与误差带。全局唯一 `TEXT_RANGE` 和唯一表格
`PAGE_ONLY` 才是权威页；重复、碰撞、插值和其他降级页只作提示，不得单独满足
`source_coverage` 发布门槛。

- [x] 从结构通道冻结官方父规则目录，以及按研究期别和访视实例拆分的基线及以前必做项目录；两份目录均含原文范围和来源片段。
- [x] 扩展 ProtocolDeconstructionInput/Draft 和 AgentCall 契约。
- [x] 实现 Agent 适配器、PromptVersion、Schema 校验和同会话有限修复。
- [x] 实现 12 类 ProtocolDeconstructionGate 检查及自然中文问题投影。
- [x] 用变异测试验证 AND 改 OR、阈值/单位/指标错配、例外缺项、随机前锚点丢失必定被拒绝。
- [x] 确保零规则、数量/编号不一致、流程表漏检/空结果/占位结果、访视实例误合并、来源缺失绝不进入可发布状态。

验证：Agent 契约、Gate 单元/属性/变异测试；模拟失败和恢复；切片结束即完成两份真实方案的服务级草稿，并逐项对账冻结目录，不能把空流程问题留到切片 7。

## 切片 4：流程节点、证据要求与发布事务

- [x] 从流程表、流程图和正文建立基线及以前 WorkflowStage。
- [x] 生成规则事实要求和流程必做要求；去重键包含研究期别和访视实例，筛选与基线的同名工作分别绑定 due_stage。
- [x] 实现无受试者的 EvidenceExpectation 模板投影测试，为 Phase 4/5 保留接口。
- [x] 实现草稿 revision、手工编辑、反馈分类、保存、取消/恢复和结构化 diff；未保存编辑取消时提示，已保存历史不物理删除。
- [x] 反馈或手工编辑必须保持与当前方案来源一致；不得增删冻结目录、改写流程节点或用解释材料改阈值/逻辑。
- [x] 实现发布事务、幂等、ProtocolAuthorityRecord/Manifest/Confirmation 和新项目/新 RuleSet revision。
- [x] 验证取消、失败和 revision 冲突不覆盖正式版本。

验证：逐个流程访视实例都有块级来源；占位 requirement、摘录不匹配和跨节点误去重均被拒绝；再验证 SQLite 事务/回滚、Job 检查点、幂等、两标签页旧 revision 冲突、发布完整性哈希。

### 2026-08-17 切片 4 完成裁决

- 流程节点按研究期别、审核阶段和访视实例保留稳定身份；同一阶段重复访视不会被名称去重。
- 规则组件来源和流程必做项目来源分别闭合，EvidenceExpectation 模板只能引用同一
  RuleSet revision 中真实存在的资料要求和审核节点；数据库同时强制 RuleSet、资料要求、
  审核节点三组外键及非空节点。
- 草稿 revision 保存完整快照、确定性结构化差异和反馈分类；状态转换只能改变状态，
  创建时间、身份、差异、反馈说明和正文均不可夹带改写。规范化列与正文镜像在直接读取、
  列表、计数和链头读取前统一校验，漂移不能通过筛选条件被隐藏。
- 澄清材料只能补充解释说明，不能换绑方案来源、改阈值、布尔逻辑或流程节点；发布前重新
  计算完整差异并验证全量来源闭包，过期草稿、不同输入重放和部分写入均原子拒绝。
- 同一独立 Luna 审查会话经过多轮可复现反例审查后最终 `ACCEPT`；终局复测包含时间篡改、
  revision `get/list/get_head`、模板直接读取/list，以及通用快照和门禁记录的创建时间漂移。
- 隔离分支最终 V2 为 `667 passed, 2 subtests passed`；合并主分支后全仓为
  `797 passed, 1 skipped, 18 subtests passed`。唯一跳过仍是遗留 06003 OCR 缓存夹具不存在。

下一安全动作：切片 5 的 V2 API 与首次解构工作台。不得提前进入受试者审核，也不得让前端
重新推断规则、差异或来源关系；界面只消费本切片已冻结的类型化合同和自然中文问题投影。

## 切片 5：V2 API 与首次解构工作台

- [ ] 新增类型化 V2 API；错误使用自然中文问题/影响/下一步信封。
- [ ] 建立方案工作台首页，区分“首次解构新方案”和“重新解构已有项目”。
- [ ] 实现上传、身份核对、期别确认、进度恢复和草稿首屏。
- [ ] 实现父子规则树、逻辑标签、完整性问题、编辑区和原文定位联动。
- [ ] 建立用户可见术语表和 DOM 文案审计，使用“草稿第 N 稿”“完整性检查”“来源定位”等自然中文。
- [ ] 在窄窗口改为共享选择状态的标签页，页面不出现整页横向滚动。

验证：组件测试、类型/构建、键盘/焦点、错误/空/部分失败/恢复状态；选中规则后在宽屏/窄屏切换仍保持同一来源；提前执行窄屏、150%/200% 缩放和 axe 冒烟。

## 切片 6：重新解构、差异与发布交互

- [ ] 显示当前项目、正式方案和规则摘要；支持上传新方案或基于反馈修订。
- [ ] 当前版本和新草稿并列，逐条显示八类结构化差异。
- [ ] 实现继续编辑、继续说明、再次解构、保存草稿、取消和发布。
- [ ] 实现同方案编号+同研究期别项目碰撞检查，首次入口不得再建平行正式项目；重新解构错传另一方案/期别时拒绝发布。
- [ ] 解释冲突、完整性失败和发布确认均给出可执行中文提示。
- [ ] 帮助页补充从上传到发布的逐步说明、常见问题和恢复方式。

验证：Playwright 覆盖全部分支；取消/发布前后数据库断言；可见文案审计。

## 切片 7：真实方案全流程验收

- [ ] 使用清洁 V2 数据目录从 MG-K10-SAR 原方案创建 III 期项目。
- [ ] 验证 IN 7、EX 16、官方编号、父子逻辑、流程节点、来源和方案 V2.1。
- [ ] 使用 D001 原方案走 II 期解构并验证 IN 6、EX 30；另一期别保持独立项目边界。
- [ ] 验证两份方案的冻结必做项目录逐项有来源，筛选和基线重复工作未被去重；MG-K10-SAR 不因“操作无缝”合并项目。
- [ ] 执行错传另一方案、错选期别、同项目重复首次创建、摘录不匹配和一条占位流程 requirement 的反向测试。
- [ ] 人工逐条抽核复杂规则、共享规则、随机/基线前条件和流程必做项，不只比较总数。
- [ ] 模拟服务中断、浏览器关闭、重复提交、草稿取消和发布冲突。

验证：真实数据库、真实任务运行、真实浏览器和原始方案来源回跳。

## 切片 8：独立医学监查员视角试用与修正循环

- [ ] 按用户指定的 CodeBuddy/kimi-k2.6、Pi/cms-router/minimax-m3、Grok Build/grok-4.6 分别做连通性测试，记录实际模型/会话；不同 harness 不随意 fallback。
- [ ] 使用用户指定的可用路由，让测试者启用视觉、从清洁系统自由创建/删除测试项目并完整走两份方案。
- [ ] 测试者从医学逻辑、证据完整、来源可追溯、流程覆盖、中文自然度、视觉和交互效率独立审评。
- [ ] 每个异常先复现并定位共享机制，禁止按具体项目/条目写死补丁。
- [ ] 修复后由新上下文复测；Codex 用真实浏览器和确定性锚点做最终验收。

验证：100 分钟硬等待策略；保留精简审评报告、截图和测试输出，不保留重复缓存。

## 切片 9：全量回归、规范同步与归档

- [ ] 后端全量 pytest、契约/迁移/恢复/临床回归通过。
- [ ] 前端全量 Vitest、TypeScript build、Playwright、axe 和截图矩阵通过。
- [ ] 更新 backend/frontend Trellis specs、`docs/PROJECT_CONTEXT.md` 和阶段记录。
- [ ] 删除临时数据库、派生方案副本、试验依赖缓存、重复截图和旧轮次报告；保留最小回归夹具。
- [ ] 独立 `trellis-check` 验证；审阅差异后按逻辑切片提交并归档任务。

## 主要验证命令

```bash
uv run pytest tests/v2 -q
uv run pytest tests -q
npm --prefix frontend test
npm --prefix frontend run build
npm --prefix frontend run e2e
```

真实方案测试必须读取原文件或明确的只读测试挂载，不能复制后篡改为更易通过的版本。

## 回滚点

1. 每个迁移前用 SQLite backup API 备份并校验；失败恢复旧库。
2. 每个切片独立提交；发布事务失败不改变当前 RuleSet revision。
3. Agent/解析器候选依赖在隔离环境 spike，通过后才锁定；拒绝后完整清理。
4. 前端新工作台使用 V2 路由，不替换 legacy 只读参照页，直至 Phase 3 验收完成。

## 2026-08-14 无损暂停点

- 切片 3 仍为 `in_progress`，未进入切片 4。
- 确定性方案测试集已通过；本轮最后的聚焦回归为 `86 passed`，真实目录/输入组装回归为 `16 passed`。整个方案测试集曾运行为 `171 passed, 1 failed`，唯一失败是 MG 旧期望数 `42`；已改为临床语义正确的 `41`并用聚焦回归验证。暂停前尚未重跑整个方案测试集。
- 真实 MG-K10-SAR III 期运行已通过：IN 7、EX 16、41 个必做项，23 条父规则、73 个组件、187 条证据要求、2 个工作流节点；6 次同会话尝试后最终门禁问题为 0。
- 恢复证据：`.trellis/tasks/08-14-phase3-protocol-deconstruction/metrics/real-agent-acceptance/mg-iii/acceptance-summary.json`、`final-draft.json`、`run-result.json`；会话 `protocol-chat-57a31af30d0046b08e12e1432e309bd1`。
- 下一安全动作：先重跑 `tests/v2/protocols` 全集确认零失败，再全新运行 D001 II 期 Agent；遇到任何目录数、官方编号、来源定位或语义门禁异常时停止并查根因，不盲目修复或继续。

## 2026-08-15 D001 真实验收根因修复检查点

- 方案模块全量回归已恢复为 `180 passed`；契约生成物、领域逻辑和日期评估聚焦回归为 `165 passed`。新增首次给药日锚点已进入领域枚举、JSON Schema 和日期评估器。
- 修复前 D001 II 期真实运行正确冻结 IN 6 / EX 30 / 必做项目 50，但 9 次尝试后仍有 18 个问题。问题不是单条项目补丁：系统把“筛选或基线时”误当相对日期锚点；缺少“首次给药日”专用锚点；修复调度反复占用前序规则；局部修复可覆盖已经更好的规则；不连续原文被模型拼成不存在的连续句。
- 共享机制现改为：筛选/基线审核节点只用多个 `due_stage` 资料要求表达，同一临床条件不复制；首次给药前使用 `first_dose_date`；修复父规则按历史尝试次数公平轮转；逐父规则比较阻断问题并拒绝回退；组件和谓词均支持多个逐字连续片段，禁止拼造句子。
- 修复前完整 D001 证据已归档到 `metrics/real-agent-acceptance/archive/20260815-before-temporal-fair-repair/d001-ii/`，保留问题轨迹，不作为新结果。
- 修复后首次重跑在初稿分批收集阶段遇到 DeepSeek 空正文，未进入语义门禁。根因是传输层只读正文且立即抛错，导致已有批次不能落盘。现增加一次相同请求重试、结束原因/推理长度诊断（不记录推理内容），连续失败则以“需要核对”正常收口并保存会话，不再崩溃。
- 下一安全动作：归档空响应产生的部分输入摘要，重新运行 D001 II；若仍空响应，读取新诊断而不是重复调用。通过后逐项核对官方编号、阶段资料要求、时间锚点、例外、逐字来源和问题收敛轨迹。

## 2026-08-16 切片 3 完成裁决与无损暂停点

- [x] MG-K10-SAR III：IN 7、EX 16、必做项目 41；EX-04“1周≥4天”已形成
  4 天阈值与 1 周发生周期。
- [x] D001 II：IN 6、EX 30、必做项目 50；EX-04 复发性带状疱疹为独立分支并
  保留 2 次/2 年，全部 12 类发布门禁通过。
- [x] 组件/流程要求双向恰好覆盖、父级括号例外、嵌套 ALL+ANY、研究/治疗期间、
  多片段时间限定、唯一两片段来源恢复、空正文重试和修复预算均有确定性回归。
- [x] 增加持久方案会话恢复和已水合草稿到语义候选的无损还原；仅允许在完整用户/模型
  交替历史后续接，且不能覆盖运行中会话。
- [x] 全仓 `687 passed, 1 skipped, 18 subtests passed`；`git diff --check` 通过。
  唯一跳过为 `tests/test_phase_workflow.py:388` 的遗留 06003 OCR 缓存缺失。
- [x] 新鲜独立 Luna 终局审查 `ACCEPT`，确认 EX-04 以外父规则与修复前逐字段一致。
- [x] 原始 MG、D001 方案文件哈希、大小和 mtime 未变；两张并行修改的前端截图未触碰。

切片 3 已完成，但 Phase 3 父任务继续保持 `in_progress`。MG 的 EX-07w 原文只写
“6个月内存在或疑似蠕虫感染”，未命名从筛选日还是随机日回溯；系统正确保留为唯一
阻断发布的方案解释问题，禁止猜测。下一安全动作是切片 4 的首次方案解构持久用例/API，
本次未启动切片 4、前端工作台或受试者审核。
