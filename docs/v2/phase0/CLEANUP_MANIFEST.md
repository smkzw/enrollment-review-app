# Phase 0 清理清单

**清点日期：** 2026-08-12

**原则：** 先清点、再删除；临床源文件、旧项目、报告、会商证据和回归夹具不因“体积大”被删除。

## 本阶段允许清理

| 类型 | 数量/大小（清点时） | 原因 | 恢复方式 |
|---|---:|---|---|
| `.DS_Store` | 11 个 / 92,204 B | macOS 可再生目录元数据 | Finder 自动重建 |
| `*.pyc` 与 `__pycache__` | 22 个 / 407,915 B；5 个目录 | Python 可再生字节码缓存 | 再次运行 Python 自动重建 |
| `.playwright-cli/` | 69 个文件 / 1.4 MB | 旧 CLI 临时截图、YAML 与控制台缓存；正式视觉证据另存于 `output/visual_checks/` | 重新执行浏览器测试 |

删除后必须执行 Git 状态、legacy 测试和健康检查，确认未触及受保护资产。

## 本阶段保留

| 路径 | 清点时大小 | 保留理由 |
|---|---:|---|
| `projects/` | 3.5 GB | 旧项目、原始资料与临床回归锚点；强制只读 |
| `output/` | 3.3 GB | 历史报告、模型对比、视觉和临床验证证据 |
| `output/d001_phase2_upload_staging/` | 2.5 GB | D001 全流程原始/阶段性回归资产，未完成去重核验 |
| `output/deleted_project_backups/` | 734 MB | 旧项目恢复资产，需迁移/校验后再决定 |
| `logs/` | 18 MB | 当前服务与历史故障证据；待建立轮转后按保留期清理 |
| `context/ prompts/ runs/ reviews/ metrics/` | 约 1 MB | 任务决策、会商、执行与接受证据；Phase 9 再归档 |

## 后续清理条件

- 大型 staging/backup 只有在文件哈希去重、项目可恢复性和临床回归替代证据均通过后才可处理。
- 活动日志先实现轮转、关联 ID 和审计分离，再按明确保留期清理。
- 新生成的 `frontend/dist/`、`.venv/`、`node_modules/` 和测试缓存继续由 `.gitignore` 管理，可随时再生。
