# Phase 5 Slice 5.8: 方案解构严格结构化输出运行时修订

## 目标

在不退回普通 `json_object`、不放松领域门禁、不写死 D001/MG-K10 项目规则的前提下，修复 oMLX `Qwen3.8-27B-oQ8e-fp16-mtp` 对整份复杂 Pydantic JSON Schema 进行约束解码时单次调用超过 50 分钟的问题，使真实方案解构可在合理时间内完成并产出可验证草稿。

## 当前可复现实证

- 隔离数据目录：`artifacts/phase5-acceptance/20260823/data-schema-rerun-20260824`。
- D001-II 任务：`7c63f8ffc5614db1bba547106997a12b`。
- `generate_draft` 自 2026-08-23 17:53:43 UTC 起持续 `running`，租约和 revision 持续推进，约 50 分钟无模型完成响应。
- oMLX 进程持续高负载；此前普通 JSON 调用均在约 0.5-6 分钟完成，严格 Schema 调用没有完成日志。终止隔离后端后 oMLX 负载立即下降。
- 旧问题 `SEMANTIC_DRAFT_MISSING` 的根因是普通 JSON 没有领域结构；因此禁止简单撤销严格输出约束。

## 边界

- 只修改方案解构 transport/contract/runner 及聚焦测试；必要时可加入紧邻配置。
- 不修改源方案、原始临床资料、隔离历史数据库、Phase 5 事实/Profile 语义。
- 保留候选草稿与局部规则修订两类显式输出类型。
- 必须保持 IN/EX 官方编号、父子逻辑、来源定位、阶段与时间锚点门禁。
- 失败必须有有界中文诊断，不保存模型原始输出或隐藏推理。
- 工作树包含大量本任务既有改动，不得回滚或覆盖无关内容。

## 完成证据

1. 明确说明严格 Schema 极慢的具体机制及为何修订不会重新引入结构漂移。
2. 代码采用更小、可组合、可验证的结构化响应合同或等价方案，并限制单次输出规模。
3. 聚焦单元/集成测试证明 candidate/repair 路由、批次闭包、失败诊断和 DeepSeek 兼容性。
4. 父 Codex 将在全新隔离数据目录重新跑真实 D001-II/MG-K10-SAR-III；worker 不声明最终验收。

## 分工

- worker_01：只读分析当前 Schema、批次算法、Prompt 体积和 oMLX 约束解码风险，提出最小可靠修订。
- worker_02：实现运行时修订并运行聚焦测试。
- worker_03：独立审查结构闭包、回归风险与测试缺口，不修改代码。
