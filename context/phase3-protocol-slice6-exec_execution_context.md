# Phase 3 Slice 6 执行合同

## 唯一写入范围

仅可修改当前隔离工作树 `.worktrees/phase3-slice6-exec`。主工作树只读。不得修改任何
`frontend/e2e/screenshots/` 文件，不读取仓库外临床原始资料，不安装新依赖。

## 权威来源

- `AGENTS.md`
- `.trellis/tasks/08-14-phase3-protocol-deconstruction/{prd.md,design.md,implement.md}`
- `docs/REARCHITECTURE_FINAL_DESIGN_20260812.md`
- `plans/REARCHITECTURE_IMPLEMENTATION_PLAN_20260812.md`
- Slice 4/5 既有领域合同、草稿 revision、完整性门禁、发布事务、V2 API 与前端仓储。
- 康哲站点设计合同：`/Users/smkzw/.cc-switch/skills/kangzhe-design/design_specs/{ROUTER.md,core.md,track_site.md}`，仅前端执行者使用。

## 产品与技术边界

- 复用现有不可变草稿历史、确定性门禁、原子发布事务和项目谱系检查，不建立平行持久化模型。
- 重新解构任务持久保存目标项目；上传的新版本只允许改变版本、日期和哈希，方案编号谱系及研究期别必须与目标项目一致，否则阻止发布并给出中文下一步。
- 差异按官方父规则编号对齐，确定性输出八类：新增、删除、原文、逻辑、时间窗、例外、证据要求、应完成阶段。保留必要的稳定组件/资料要求/节点定位及前后结构值。
- 前端只消费类型化差异，不解析自由文本推导临床语义或发布状态。
- 支持选择项目、上传新版或基于反馈修订、手工编辑、继续说明、保存、取消、发布；保存/取消不改变正式版本，成功重新发布在同一项目生成新的不可变规则版本。
- 用户可见文案必须是中文临床工作语言，不出现 Agent、schema、pipeline、provider、gate、log 等内部词。
- 仅面向 1920x1080 至 3840x2160 最大化宽屏桌面，不新增移动端或窄屏分支。

## 验证

- 后端：八类差异精度、项目正式版本投影、目标项目持久化、跨方案/跨期拒绝、取消/保存不改正式版本、重新发布及幂等。
- 前端：项目选择、上传/反馈两条路径、并列差异、编辑反馈、取消确认、保存、发布及错误恢复。
- 可运行聚焦 pytest、Vitest、build 和聚焦 Playwright；Codex 负责全量、真实浏览器和最终验收。

## 完成结果（2026-08-18）

- 已完成重新解构两条路径、正式版本投影、不可变草稿谱系、八类确定性差异、手工修订/反馈/保存/取消/发布及中文帮助闭环。
- 关键共享修复：任务初始化事务竞态；稳定来源优先的子项对齐；例外时间语义；手工来源冻结；选中父规则局部修订；规则树、子项来源映射和资料要求全局身份闭包；前端分类差异严格解码。
- 独立审评会话 `01a011b5-64b6-7243-98fd-d13d3c6687fd` 经三轮阻断修复后 `ACCEPT`。审评者只读，未修改文件。
- 最终锚点：后端 V2 `746 passed, 2 subtests passed`；前端 `258 passed`；生产构建通过；常规浏览器 `256 passed, 41 skipped`；真实 HTTP 三宽度 `3 passed`。
- 测试编排注意：常规 `build:e2e` 与生产/真实 HTTP 构建均写 `frontend/dist`，必须串行；若 4173 复用旧 preview，会错误连接 API。先终止旧 preview，再由目标 Playwright 配置自行重建。
- 用户要求本步骤完成后无损暂停。下一动作仅为切片 7 真实方案全流程验收；未启动、未预创建项目、未读取临床源文件。
