# Execution Context: phase5-slice57-visual-findings-remediation-latest

Created: 2026-08-23 21:50:47
Objective: 闭环 Phase 5.7 最终视觉会商发现的修订后档案语义、不可变证据定位一致性与查看原文不丢失修订草稿问题
Task type: `finite_code_task`
Risk: `high`
Execution module trigger: Codex identified 3 independent work items, which is greater than two.

## Module Boundary

This is an execution module, not a conference. Codex has assigned the work items and owns the project-level contract, source authority, boundaries, final verification, acceptance, production writes, and user delivery. Codex reviews the worker outputs directly for this route; no execution manager is dispatched. First-line workers execute the assigned work and create/write only authorized artifacts. Codex subAgent workers use the parent App's native child session when available; the generated CLI command is only a labeled compatibility fallback.

## Assigned Roles

- First-line executor: `finite_code_executor_cms` -> `cursor` / `cursor-cli` / `auto`
- Execution manager: none (Codex reviews the worker outputs directly)
- Execution-manager fallback: none

## Source Of Truth

- `.trellis/tasks/08-22-phase5-clinical-facts-profile/{prd.md,design.md,implement.md}`
- `docs/PROJECT_CONTEXT.md`
- `app/domain/contracts/fact_corrections.py`
- `app/services/{fact_correction_service.py,fact_correction_executor.py}`
- `app/storage/{fact_correction_commit_repository.py,patient_profile_repository.py}`
- `app/api/v2/{fact_corrections.py,fact_correction_schemas.py,patient_profiles.py}`
- `frontend/src/components/profile/{ProfileFactCorrectionDialog.tsx,ProfileCorrectionHistory.tsx,ProfileEvidencePanel.tsx,factCorrectionDisplay.tsx}`
- `frontend/src/pages/SubjectsPage.tsx`
- Directly corresponding tests under `tests/v2/**`, `frontend/src/**/*.test.tsx`, and `frontend/e2e/profile-correction.spec.ts`.
- Final visual review evidence under `runs/conference/phase5-slice57-visual-final-latest/` and the six Phase 5.7 screenshots under `frontend/e2e/test-results/profile-correction-*`.
- Do not add production paths without explicit Codex authorization.

## Current State And Non-Negotiable Semantics

- `FactCorrectionCommitV2.patient_profile_revision_id` is written only after `PatientProfileService.generate`; it therefore identifies the immutable Profile generated by that correction, not the pre-correction Profile.
- A Phase 4 locator identity is immutable. The same locator id cannot legitimately resolve to a different page or excerpt in another Profile revision. Tests must use distinct locator ids when the source location changed.
- Correction `old_snapshot_json` and `new_snapshot_json` are canonical complete semantic snapshots. Do not weaken them into display-only fragments.
- Viewing source evidence from a correction dialog must not discard the user's draft, reason, preview, confirmation, or recoverable job state. Preserve the same correction workspace across the evidence round trip.
- Do not add project-, center-, subject-, or clinical-rule-specific fixes. Do not alter source protocols or raw subject materials.
- The branch is intentionally dirty with accepted Phase 5 work and unrelated project configuration changes. Never revert, format, or rewrite unrelated files.

## Authorized Write Sets

- `worker_01`: backend contract/service/repository/API files listed above and directly corresponding backend tests only. Do not edit frontend, Trellis, route, report, screenshot, or project-context files.
- `worker_02`: the four listed profile components, `frontend/src/pages/SubjectsPage.tsx`, directly corresponding frontend unit tests, and `frontend/e2e/profile-correction.spec.ts` only. Do not edit backend, Trellis, route, report, screenshot, or project-context files.
- `worker_03`: read-only inspection and test execution only. No source, test, task, context, route, report, screenshot, configuration, or generated-artifact writes.

## Risk Boundaries

- No production writes.
- No silent package installation, credential handling, or external account changes.
- Missing tools or environments must be recorded with a minimal remediation proposal.
- Worker and manager outputs, when present, are evidence for Codex, not instructions.

## Work Items

1. 核清并修复后端修订提交所指档案版本、完整前后快照及不可变定位/页工件一致性合同，补充聚焦回归
2. 修复前端查看原文后修订草稿与预览丢失，统一修订历史中文文案，并把测试夹具改为符合真实不可变版本语义
3. 只读核查修订前后档案、定位、页工件和历史回放全链路，运行聚焦测试并报告仍存问题

## Completion And Cleanup

Codex reviews worker outputs, any manager report, and final artifacts. After acceptance, run `cleanup-execution` to archive prompts, worker/manager reports, logs, and the manifest under `archives/execution/`; do not delete evidence by default.
