MODE=TEST

继续同一 Cursor `cursor-grok-4.6-medium` 连通性会话。控制器已把本会话改为允许浏览器 MCP 工具执行；这只用于解决上一轮工具许可被拒绝，不授权使用终端、读取源码/数据库/日志/接口或修改产品数据。

请再次仅用真实浏览器打开 `http://127.0.0.1:4277/#/subjects`，视口 1920×1080。当前页面主标题允许显示为“受试者与资料”，项目的长标题允许显示 `CMS-D001`，只要同一可见项目明确为Ⅱ期、方案 1.0 即满足身份核对。截图可由浏览器工具保存到其真实临时路径，报告该路径即可；不要求你通过文件工具搬运。

若页面、项目、期别、版本均可见且真实截图成功，输出 `CONNECTIVITY_PASS`；否则输出 `CONNECTIVITY_BLOCKED` 并报告真实错误。不得修改任何数据。
