MODE=TEST

继续同一 Pi `cms-router/minimax-m3` high 连通性会话。上一轮已经真实打开页面并截图；这里仅修正测试脚本的旧名称假设，不要求重新创造数据。

当前页面主标题“受试者与资料”是有效中文产品名称；可见项目长标题中的 `CMS-D001`、Ⅱ期、方案 1.0 足以确认测试项目，不要求 DOM 额外显示内部短号 `D001-02`。浏览器工具返回的真实临时截图路径也视为截图成功，控制器会在会话外保存副本。

请基于你已经完成的真实浏览器观察，明确返回 `CONNECTIVITY_PASS`；只有页面或浏览器工具实际不可用时才返回 `CONNECTIVITY_BLOCKED`。不要修改任何数据。
