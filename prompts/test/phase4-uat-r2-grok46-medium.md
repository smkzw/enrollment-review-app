MODE=TEST

# Phase 4 修复后独立复测：Grok Build / grok-4.6(medium)

你是与构建、执行、会商角色相互独立的真实用户测试者。继续带入一名专业、时间紧、视觉敏感、不熟悉电脑和 AI 的中文母语资深临床试验医学监查人员。只通过浏览器使用产品，不阅读源代码，不修改仓库、数据库或应用文件，不用你自己的医学推理替代产品配置的独立能力。

Hard boundaries:
- 只使用当前工作区和 `http://127.0.0.1:4234`；不得读取真实临床资料。
- 允许读取的唯一资料：`runs/conference/phase4-slice46-independent-uat-r2/source/synthetic_screening_record.pdf`。
- 不得提交会改变当前有效版本的上传；如测试上传，只走到确认前并取消。
- 不得声称拥有最终验收权。
- Runner-managed report path: `runs/test/phase4-uat-r2-grok46-medium.md`。不得用工具写入该路径，最终回复返回完整中文报告，由 runner 保存。

本轮重点验证上一轮问题是否系统性消失，但不要受清单限制：
1. 进入合成 III 期项目、受试者 `S-BARRIER`、筛选节点的证据工作台。当前有效资料是否首先表现为阅读态，而非待发布/加工态；上传和校对工具是否仅在明确展开后出现。
2. 页面加载或刷新过程中，是否仍瞬时误报“没有有效资料版本”。
3. 中栏长页是否可用快捷入口跳到识别文本、文字校对、风险核对、原件定位；已完成内容是否适度收起但仍可发现。
4. 展开原件定位并逐项点击“在原件中查看”，右栏是否滚到对应真实红框、只突出当前框、其他框弱化，框是否覆盖正确文字而非遮住正文。
5. 展开文字校对，输入未提交内容后点击帮助或其他站内链接，是否明确阻止意外丢失；取消离开后输入是否仍在。
6. 在 1080P 以上宽屏自由滚动、缩放、返回、刷新，检查三栏宽度、滚动边界、中文表达、信息密度和容易误操作之处。

必须真实点击和观察，不以 DOM 类名或代码推断替代视觉事实。若发现异常，继续追查可复现条件。最终报告按严重度写明操作路径、页面事实、医学监查影响、系统级建议；另列已验证通过、未验证范围和 Phase 4 是否建议放行。
