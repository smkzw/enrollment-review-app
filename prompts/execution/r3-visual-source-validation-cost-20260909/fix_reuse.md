Continue same bounded execution, not accepted yet. Owner found material correctness and scope issues.

1. VisualLocatorBatchContext does NOT bind transaction identity at all, and session.get may return stale identity-map rows. Root/manifest/correction fingerprints omit metadata, risk and other children verified by full revision closure. Your docstring claims all mutated rows cannot pass, but report admits otherwise. This violates contract. Replace partial fingerprint scheme with a minimal conservative invalidation mechanism that actually covers all database changes: consider transaction identity, connection identity, SQLite data_version and total_changes, plus pending ORM new/dirty/deleted state and real snapshot semantics. Do not treat this suggested mechanism as automatically sufficient; test same-session SQL UPDATE/commit, nested rollback, external commit and unchanged root hash/changed metadata child. Unsupported backend must fall back to full verification. Do not retain a failed cache entry. Avoid >200 lines of speculative fingerprint framework.
2. A bounded helper unused by finalize dominant loops won't solve 56-minute delay. Allowed scope now additionally app/storage/fact_authority.py and app/domain/gates/fact_evidence_closure.py ONLY for routing existing locator verification through safe batch reuse; do not alter clinical logic or skip authority checks. Inspect callers before changing. If transaction-wide reuse unsafe, use explicit batch at existing multi-item calls and report residual limits.
3. Preserve all current source-fidelity checks, exact locator equality, invalid source rejection. No process-global cache. No app config/model/prompt edits. Tests under tests/v2/storage and tests/v2/domain only as needed. Existing tests plus new unchanged-root metadata drift, same-session transaction invalidation, SQL mutation, external commit and invocation-count checks. Do not write/read clinical artifact DB or run model calls.
Return compact source changes, actual tests, remaining performance limits. Same report schema. Do not claim final acceptance.
