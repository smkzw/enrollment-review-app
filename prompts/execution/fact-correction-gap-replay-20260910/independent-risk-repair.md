Continue same implementation session, same allowed files and prohibitions as provenance-repair.md. Owner reran 123 tests successfully. Independent review confirmed core fixes but found two precise residual defects. Repair minimally with regression tests; no real sources, network, installs, model calls or delegation.

1. _prior_unreconfirmed_signals currently returns early for any global concrete signal and skips any template with any concrete signal. This can erase a prior unreconstructible uncertainty when an unrelated DESCRIPTION_INSUFFICIENT appears alongside complete facts. Replace template-existence suppression with actual input provenance coverage. Do NOT merely compare kinds: two independent same-kind concerns must not erase each other. For known prior concrete inputs, skip padding only if every applicable concrete prior signal is actually reproduced by canonical structured equality in current applicable signals. Global applicable IDs and template IDs must remain correctly scoped; do not match by loose detail substring. Legacy None unresolved remains conservative regardless of unrelated new concrete signals. Existing observed/not_due and genuine source-strength weak exclusions remain as justified. Do not interpret absence of reproduced signals as resolution. Pad remains non-fallback unverified, references prior expectation, no clinical fact copying.

2. accepted_requirements from current facts must not let SourceStrength.UNVERIFIABLE facts suppress rejected-candidate risk. Verify enum identifier and actual coverage rules before implementing; no invented coverage semantics. Preserve finalize None behavior.

Add synthetic integration tests for prior concrete/pad + unrelated new concrete + complete coverage remains weak; independent same-kind different detail not erased; exact reproduced input does not create unnecessary pad; unverifiable current fact cannot erase rejected-source specific risk; prior other-authority exclusion if practical. Tests should call actual helper/repository chain, not monkeypatched behavior. Run correction-gap, correction-job, normalization-persistence and projection suites. Return exact evidence and limits. Do not expand scope or claim clinical acceptance.
