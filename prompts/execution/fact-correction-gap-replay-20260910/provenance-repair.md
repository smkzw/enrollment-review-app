Continue same implementation session. Owner reviewed current code and independent reviewer confirmed three remaining flaws. Do not accept current implementation merely from 73 tests.

Writable paths now: existing four from repair.md, plus app/domain/contracts/evidence_expectations_v2.py, app/projections/evidence_expectations.py, app/storage/evidence_expectation_repository.py, tests/v2/projections/test_evidence_expectations.py and tests/v2/storage/test_evidence_expectation_repository.py (verify actual existing filename before editing; if absent ask in report). No other files, real data, models, network, dependency acquisition, uvx, or delegation. Earlier uvx use violated no-network/tool-install scope even if project env unchanged; do not repeat. Use apply_patch if provided; report actual tool limitation, do not imply substitution meets the literal instruction.

Required minimal complete repairs:
1. Source selection: trace correction ancestors for EVERY currently active non-superseded fact/event/exposure as well as this target, not only the current target. Do not include all correction records' discarded branches or all historical runs. Keep authority/cycle/missing-record checks. Add cross-chain test (correct X, then unrelated Y; original X risk remains specific).
2. Reprojection's rejected candidate suppression must use current non-superseded published facts' supported requirements, not historical accepted candidates that may now be superseded or unbound. Give shared derivation an optional explicit accepted-requirements override; None preserves original finalize behavior exactly. Add same-requirement accepted+rejected then remove accepted fact binding test: rejected-source risk must remain, not generic no-record fallback.
3. Remove heuristics that infer fallback versus concrete solely from stored gap_type/source_coverage. Add a backward-compatible optional input_gap_signals list to EvidenceExpectationV2: None means legacy input provenance unknown, [] means known no signals, otherwise exact applicable structured input signals including fallback_only. Freeze deterministically, reject unrelated template IDs and duplicates (or canonicalize deterministically), preserve all old rows/hashes. Populate in ALL pure project_expectation return paths, including not_due, using only signals applicable to that template. Include in repository idempotent semantic comparison so changed provenance appends a revision even if visible status same. Existing payload JSON can store the field; avoid a new database table/migration unless demonstrably required. Do not rewrite old records.
4. Prior risk recovery: when source replay cannot reproduce prior concrete input, carry a non-default observation_unverified linked in its detail to that prior same-authority expectation (not copied clinical gap). Read exact input_gap_signals to decide concrete vs fallback, so weak/absent uncertainty cannot evaporate on the next correction. Explicit prior fallback-only []/lists may clear when complete evidence arrives. Legacy None is genuinely unknown: unresolved prior states conservatively remain unverified rather than guessing they were fallback; do not downgrade prior observed/not_due or purely source-strength-based weak coverage unless a concrete uncertainty basis exists. Explain exact policy in docstring and tests. No new clinical resolution mechanism.
5. Tests: concrete-unverified weak->complete and absent->complete must remain unverified without explicit disposition; fallback-only absence->complete must clear; old payload omitting new field decodes without rewriting; same-visible-status/different-input-provenance creates a revision; orphan exclusion stays. Use actual seeded integration where practical. Preserve original D1 tests and source-run seeding. Do not test wrong behavior as expected.

Run affected new tests, original correction_job/normalization_persistence/projection/storage suites available. If exported JSON schema snapshots need regeneration outside allowed paths, report exact command/paths to owner; do not silently leave claim all contracts synchronized. No clinical acceptance. Return exact tests, compatibility issues and still-unverified cases.
