# Worker 01 同会话根因修订

父 Codex 已审阅你的第一轮实现。请继续在同一会话、同一授权文件边界内修复以下共享合同缺口，不要读取 D001 或扩大范围：

1. `stable_protocol_control_matrix_row_id` 和 `stable_protocol_control_matrix_anchor_id` 目前只能生成身份，但合同/校验器没有强制实际 `matrix_row_id` 和 `source_anchor_id` 必须与系统派生值一致。这会允许调用方伪造身份。请强制稳定身份闭包，并增加行 ID/来源锚点 ID 篡改反例。来源锚点应以行身份与稳定序位派生，且行内顺序不得被调换后静默接受。
2. `render_protocol_control_matrix_markdown` 是面向中文临床试验医学监查人员的人工审阅工件，不得显示 `official_eligibility`、`selected_phase_applicable` 等英文枚举。隐藏的机器身份注释可保留稳定英文值，可见表格一律映射为中文原生用语。
3. 当前 Markdown 仅有身份/来源/标题/期别六列，无法作为 5.8d 人工审阅矩阵。请在不制造巨型超宽表的前提下，每个控制用中文分组区块展示：官方父/子编号（如适用）、做什么、达标条件、禁止事项、审核节点及“提前关注/本节点判定/后续复核”、方案命名时间锚点/窗口、六类义务及 DNF 逻辑、最低证据、专业判断、跨章节关系、源标题路径/结构引用/逐字摘录。首页可有简洁索引，详情必须可逐条审阅。
4. 扩展 `validate_protocol_control_matrix_serializations` 和测试，证明人工 Markdown 中的行顺序/身份与 JSON 一致，且英文枚举不会出现在可见表格/标题中。
5. 增加对“且”复合义务不能被弱化为单原子或任意替代组的回归，以及只包含某些义务类型时不得用虚假占位义务补齐六类的回归。

修复后运行聚焦矩阵测试和 5.8a-c 相关协议回归，返回完整更新报告。
