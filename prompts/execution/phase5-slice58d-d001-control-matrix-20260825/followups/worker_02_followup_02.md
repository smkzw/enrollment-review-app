# Worker 02 同会话返修：迁移 v2 合同与临床可扫读标题

Worker 01 已将矩阵合同升级到 `phase5/control-matrix/v2`。请在同一会话内迁移你拥有的两个 D001 工件，不得修改应用合同/测试或其他文件。

## 必须完成

1. 加载最新 `ProtocolControlMatrix` v2。流程行当前尚无正式 `required_procedure_catalog`，必须把上一轮 `structural-procedure-*` 从正式目录字段迁移为符合合同的稳定 `source_candidate_id`；`required_procedure_catalog_item_id` 置空。候选身份必须从冻结来源确定性派生，不能伪装为正式目录。
2. 对候选流程来源，冻结 manifest disposition 必须按 v2 校验器要求闭合：明确处置为流程必做、正式流程链接为空、不得借用候选控制字段。矩阵仍为 `claims_complete=false`。
3. 将 36 个官方条目的 `title_zh` 从“官方入选 IN-01 / 官方排除 EX-01”改为能让医学监查人员一眼理解核心控制含义的简洁中文临床标题。标题必须来源于条款实质，不得机械复述编号，不得引入原文没有的判断。例如 IN-01 可概括为“知情同意、沟通与遵从能力”，IN-02 可概括为“签署知情同意时年龄范围”；其余逐条自行从原文提炼并核对。
4. 用最新 `render_protocol_control_matrix_markdown()` 重建 Markdown。可见正文应只显示“本项目适用”和“本项目适用性”，不得出现“跨期共享”“选定期别适用”“期别处置”；JSON 内部 disposition 必须保留真实值。
5. 保持上一轮经临床边界筛选后的 36 官方 + 22 流程行，不得把已排除的建议性皮损照片、DLQI、随机动作、PK/IL-17A 研究采样、给药/治疗支持项目重新放回。若迁移过程中发现任一 22 条仍不影响入排节点、随机或首次给药资格，必须再次追查并可减少，不以 22 为硬目标。
6. 使用 v2 构建、渲染和序列化校验；重建冻结覆盖 manifest 后执行严格来源闭包验证。若候选流程 disposition 无法按现有合同无损闭合，停止并报告具体单元，不得把候选 ID 填入正式链接绕过。
7. 可见 Markdown 继续不得泄露内部字段、枚举、来源 ref、候选身份或稳定 ID。D001 DOCX 保持只读，复核指纹。

## 验收证据

- `ProtocolControlMatrix.model_validate_json()` 成功且 schema 为 v2。
- 36 个官方标题全部通过机械标题拒绝，且人工抽查可从标题理解控制主题。
- 所有流程行均为候选身份，无伪正式目录 ID。
- 冻结来源/处置闭包、序列化一致性、可见中文扫描通过。
- 聚焦测试通过；报告实际行数和原文件指纹。
