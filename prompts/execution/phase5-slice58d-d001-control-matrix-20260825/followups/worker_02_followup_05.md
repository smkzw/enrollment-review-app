# Worker 02 同会话恢复：完成中断的 D001 II v4 临床语义重构

你正在恢复同一执行会话 `01a03518-8ba7-7413-a3f9-ed0389229798`。上次运行被用户要求立即无损暂停，磁盘上的 JSON/Markdown 是中间产物，不是已接受结果。继续只修改原授权的两个 D001 工件；不得修改合同、测试、应用代码或原始 DOCX。

## 父级现状验收

现有中间产物可以由 `ProtocolControlMatrix` v4 加载，包含 36 条官方父规则和 22 条流程候选，JSON/Markdown 序列化一致，现有合同回归 152 项通过；但临床语义不合格，不能发布。已确认的共享失败机制如下：

1. 官方入选条件仍被错误放入 `applicability_expression`；官方排除条件也被错误放入 applicability，`trigger_expression` 为空。适用性只能表达真正的人群限定；入选达标进入原子化义务表达，排除触发进入 `trigger_expression`，例外进入 `exception_expression`。
2. 自动按“和/与/或”切词造成非谓词碎片，例如“研究者进行良好的沟通”“理解”“限制条件”“筛选”“银屑病面积”“参/与者”。禁止继续使用机械字符串切分；每个原子必须由你阅读完整原文后写成独立、完整、可判定的医学谓词。
3. 研究流程表中的 `X`、`（X）`、脚注上标和竖线只是访视执行标记，不是医学条件。流程行可以引用该来源证明节点，但不得把 `BSA评分 | X | ...`、`入排标准审核 | X | ...` 等放入适用人群、条件原子、义务或可见行动。
4. 最低证据仍有“来源记录及结果核对”式空泛模板。必须写清事实对象、资料类型、到期节点、同期客观来源要求及研究者评估记录要求。
5. 流程项目中的解释性定义、记录动作、随机动作、治疗后事项被混入资格控制。只保留预筛选、筛选、导入、基线、随机/首次给药前确实影响资格判定的必做检查、检验、评分、资料记录和复核；可选/建议、随机动作本身、给药后治疗支持、PK/探索性采样不得纳入。

## 必须完成的重构

### 36 条官方父规则

- 保留且仅保留 `IN-01` 至 `IN-06`、`EX-01` 至 `EX-30`，不新增伪官方子编号。
- IN 的达标要求使用完整的 `obligations + obligation_expression`；只有生育能力等真正人群限定才使用 applicability。
- EX 的排除触发使用 `condition_atoms + trigger_expression`；例外单独进入 `exception_expression`。不得用 applicability 表达排除条件。
- 每个原子只表达一个完整谓词；DNF 无损表达 `(A且B)或C`。列表标题、引导句、`注：`、复测说明、允许措施、禁用预防药物说明均不得成为并列触发分支。
- 审核节点只按原文和同矩阵“入排标准审核”流程行确定；原文无节点时使用 `workflow_basis_row_id`，不得把流程锚点复制进官方行。

逐条特别核对：

- `IN-01`：签署 ICF、具备良好沟通能力、理解要求、能够遵守要求是四项完整 AND 义务。
- `IN-02`：年龄 `>=18` 且 `<=75`；“性别不限”不作为阻断条件。
- `IN-03`：研究者确认斑块状银屑病、病史 `>=6个月`、稳定期三项 AND。
- `IN-04`：PASI `>=12`、PGA `>=3`、BSA `>=10%` 三项 AND；筛选和基线分别核查，不把“筛选/基线”拆为条件原子。
- `IN-06`：仅生育相关人群适用；怀孕/捐精计划、避孕要求以及 ICF 至末次给药后 3 个月的边界分别准确表达。
- `EX-02/08/11/16/20h/21`：疾病或异常存在与研究者判断之间的 AND 不能弱化。
- `EX-09`：活动性结核、潜伏感染等分支无损；复测说明、预防治疗允许路径及不允许利福平/利福喷丁不是并列排除触发。
- `EX-18`：各治疗类别为 OR；类别内部完整；所有固定窗口用首次给药 `BEFORE`；“3个月或5个半衰期取较长者”同时保留；来氟米特清除洗脱是例外。
- `EX-20`：各阈值为 OR；ALT/AST/TBil 为同一 OR 组；其他实验室异常必须同时满足“异常 + 有临床意义 + 研究者判断参与研究构成不可接受风险”。不得产生“参/与者”碎片。
- `EX-22`：HBV/HCV/HIV/梅毒为 OR；HBV、HCV 内部逻辑保真；梅毒例外必须是“非特异性抗体阴性 + 研究者判断既往感染已治愈”两项 AND，不能混淆特异性抗体。

### 流程必做控制

- 重新逐条判断当前 22 行是否确属资格控制；数量可因临床复核减少，但不得为保持 22 条而保留噪声，也不得漏掉真实筛选/基线必做项。
- 流程表 `X` 只支持执行节点；控制语义来自该行名称、脚注/访视说明和相关检查章节的完整原文。
- 同期客观检查/检验/评分需要客观报告或原始评分记录；病史、治疗史需区分筛选病历记录与既往原始资料。长期阳性病史只有筛选转述时可作为当前证据，但应保留溯源要求，不能擅自把方案未要求的既往原件变成硬门槛。
- “入排标准审核”必须同时表达筛选初审与基线/首次给药前复核；流程行本身不得包含随机动作、给药后 AE/合并用药记录等非资格内容。

## 完成与报告要求

1. 从真实 D001 DOCX 与既有冻结结构来源重构，不得以当前错误原子为模板继续修补。
2. JSON 必须由 v4 合同加载；重新生成 Markdown，并通过 `validate_protocol_control_matrix_serializations()`。
3. 必须重建/复用同源冻结 `ProtocolSectionCoverageManifest`，运行严格 `validate_protocol_control_matrix()`，验证摘录、Span、处置、期别、流程依据与来源候选闭包。若无法恢复该清单，停止并明确报告，不能只报告 shape/serialization 通过。
4. 报告逐条列出 36 条官方规则的：完整原子、DNF 形态、例外、时间锚点、审核节点、最低证据；另列每条最终保留流程控制的节点、资格作用和最低证据。
5. 单独列出从官方触发中剥离、待跨章节控制承接的复测、操作、有效期和预防治疗说明，保留来源定位。
6. 运行 v4 聚焦测试并复核原 DOCX SHA-256 仍为 `362443131f0d384c82c80f6a37396084f7d3301b51162201749c0488b0f2dd98`。
7. 任一条无法无损表达时，停止并报告具体编号、逐字原文和阻断原因；不得以长段文本原子、统一证据模板、统一筛选/基线节点或隐藏未表达逻辑绕过。

