Same-session bounded repair. Original task boundaries remain: write only app/domain/page_review_evidence_sources.py and tests/v2/domain/test_page_review_evidence_sources.py, no clinical calls, DB, credentials, network or other files. Return report in final; runner stores it.

Owner review found material gaps. Repair before integration:
1. ContractModel is not frozen. Current mutable lists/nested observations invalidate the immutable claim. Either implement true immutable content representation or explicitly use validated serialization with honest terminology; all materializer inputs must be revalidated via dumps because model_copy/update and mutation can bypass validators. Do not alter global ContractModel.
2. VisualFactSource checks readers agree with each other but never checks its own normalized_value/unit against them. HandwritingSource likewise own kind/text. Add rejection tests with recomputed IDs to prove hashing is not semantic validation.
3. Existing source_aligned_fact_keys can accept DIFFERENT normalization keys across lanes. Current materializer incorrectly requires each accepted key in both lanes. Inspect and reuse existing PageAssociationSource/source_aligned_fact_keys: accept optional explicit validated association source, bind its actual hash to reconciliation.association_text_sha256, and only derive exact unambiguous pairs using the existing algorithm semantics. Do not invent fuzzy matching. If no compatible reusable pairing primitive exists, clearly report this boundary instead of implementing an incompatible algorithm; exact same-key mode must still work.
4. Strip bbox only in derived locator precision, not the original raw observation provenance. Original model observation should remain available without implying its coordinates are authenticated. Correct docstrings accordingly. No text hash may be derived from image hash; additionally preserve exact excerpt hash with actual sha256(excerpt.encode()). The current unequal-hashes check alone does not prove provenance.
5. Check contradictions between conflicts and accepted targets where existing contract permits them; revalidate binding against actual record IDs. Add regression tests for relevant mutated input, wrong exported value, and source-associated different-key cases.

Keep minimal coherent implementation, avoid bloated duplicated classes/helpers and decorative commentary. Run assigned tests and relevant existing page source association/reconciliation tests only. Report exact tests and unresolved integration limitations. This is repair execution, not independent clinical acceptance.
