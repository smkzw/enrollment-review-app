Active task: .trellis/tasks/09-11-e2e-eligibility-review
Same-session targeted repair. Keep original boundaries. Do not call pi-worker/subagents again; your initial attempt violated the no-recursion instruction. No further delegation, tests, model calls, DBs or new process files. Return complete execution report as before.

Owner source review found blocking correctness defects. Fix these in the three new modules before claiming this unit implemented:
1. _source_policy_for_control loops every control with only layer/group/atom position. Filter exact protocol_control_id FIRST. Preserve all applicable policies and validity fields; do not throw away every policy when >1 or hide policies as None. For official predicates multiple component requirements have no explicit per-predicate attribution: preserve the complete candidate policy set as unverified association, do not guess by fact_type. Do not use string tokens investigator_assessment/professional_judgment as proof. Preserve original full policy material for recheck, including time windows.
2. Existing candidate _comparison_artifact already proves final raw request/reply matches candidates. Reuse it (or factor a shared pure receipt verifier within allowed source files), loading actual completed read checkpoints, validating current candidate contract/prompt/batch shape and exact comparison artifact equality. The new loader presently only validates arbitrary candidate JSON and then trusts comparison pairs; it does NOT prove receipts or that pairs came from replies. Reject empty/missing batches or coverage mismatch, not silently empty-success. Bind candidate job/type/contract/frozen hash/comparison/receipt identities throughout. Verify existing method signatures and contracts, don't invent API fields.
3. Qualification _verify_current must reconstruct frozen qualification material from source job and compare the pair/policy payload, not just current input hash. validate_binding_qualification_structure must compare supplied pair fact/locator/document/condition/policy with expected frozen material; currently a mutated body with same IDs passes. Check actual enums/date fields against definitions.
4. public_agreement_key MUST NOT include free-text unresolved_reasons. Preserve each lane reasons independently, compare defined structured dimensions; disagreement in explanations is not automatic contradictory evidence. Strongly validate exactly main-A/main-B, matching pair IDs in structural/judgments and parent scope across records/summary. Pair identity must be scoped by input/job or record validator must enforce parent fullscope closure.
5. Prompt input currently omits episode/stage/anchors and complete parent source context, so cannot evaluate temporal applicability/source qualifications. Include relevant frozen episode and parent/control source context. Remove peer lane_declarations entirely from model-visible prompts (persist as provenance only). Do not prime both models with peer verdict labels. Do not say source admissible from file names alone.
6. Current follow-on sends all pairs in one call, duplicating full fact/locator per pair. Add bounded deterministic pair batching (existing JobRunner each lane serial; remote/local across lanes only as authorized). Deduplicate referenced facts/locators/conditions within batch with IDs, preserve all required pair IDs; freeze batching in payload, verify on execution. Avoid hardcoded disease/model and silent input truncation. No new scheduler.
7. Structural pending semantic checks are always copied forever, so output cannot ever prove the intended distinctions. Keep structural facts/shape distinct from semantic candidate review; explicitly record what remains unverified versus what both lanes rechecked, without clinical adoption. Avoid claiming this produces clinically verified bindings. Still no API registration or autoacceptance. Preserve full genuine unknown and no-candidate accounting.

Keep modules focused; the 945-line service is needlessly verbose/copied. Refactor within new binding_qualification-named modules if needed, reuse existing recorder/validation where appropriate; do not grow pre-existing giant files. Do the complete repair with only compilation/source checks. No staged tests. Report actual remaining integration gaps; optional test suggestion is for final unified testing only.
