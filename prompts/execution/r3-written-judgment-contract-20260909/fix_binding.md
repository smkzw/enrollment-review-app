Continue same execution session and same two-file write scope only. No production wiring or new files beyond original two. Consolidated owner findings need fixes before acceptance:

1. PositiveSource validates equal excerpt but NOT same document/page/image/page number across two locators; evaluator also never checks source page belongs to expected_page_scope. Add deterministic checks and counterexamples. Different pages with same text must not become a dual reading of one source.
2. ReadScope is unbound to authority/request/requirement/object/stage or reading ID. provider/model fields are unused. Bind scope to the same absence reading and request, including actual per-page immutable identity evidence rather than free page strings alone; keep minimum coherent structure and don't duplicate all of FactAuthority unnecessarily. Test replaying successful scope of another subject, model, read, request or page version cannot verify absence. Full multi-page scope cannot honestly be backed by one unrelated page_review_id. Consider an explicit scope reading receipt identity and per-page identity references, with storage authentication explicitly pending.
3. VERIFIED currently ignores one conflicting explicit absence if not both; ABSENCE_VERIFIED currently ignores a positive source with one known positive and other unknown. Any explicit contradictory known evidence must prevent verified absence; positive vs absence must remain unverified even if one opposing read. Add tests.
4. Duplicated/invalid lanes when absence_readings length !=2 aren't validated; >2 may silently become unverified. Validate duplicate/unsupported lane inputs regardless of count. Known context defaults True are unsafe; make required or default false. Empty strings/whitespace in scope IDs must reject.
5. `supporting_ids` list violates deep immutable claim. Use tuple. Add exact workflow stage identity or explain authority episode binding and preserve same-ReviewStage distinct-node rejection in tests. Do not claim stage enum alone proves node.
6. Keep source text original but avoid requiring different readers to emit byte-identical excerpt where harmless whitespace differs; either deliberately preserve strictness as unverified (not crash), or reuse existing normalized source primitives. No numeric/semantic rewriting. At minimum report this limitation honestly, no clinical acceptance.

Please simplify duplicated bindings/boilerplate where feasible; 477 lines is large for a pure check. Do not paper over missing source authentication with booleans. Existing models/contracts unchanged. Run focused tests, return scope and decisive evidence; integration still owned by Codex.
