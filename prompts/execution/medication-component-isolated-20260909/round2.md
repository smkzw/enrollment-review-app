Continue the same bounded execution session. Only modify scripts/medication_component_experiment.py and tests/v2/scripts/test_medication_component_experiment.py; run only that test file. No model/network/clinical/config access. Owner's runner is separate; do not modify it. Owner added batch omission rejection and adjacent-drug-not-accepted test; preserve them. v1 source has been frozen in artifacts, do not edit archives.

Expanded real observations expose design flaws. Implement v2 of the ISOLATED experiment, not production, with these precise generic revisions:
1. MedicationObservation requires lane main-A/main-B and page_review_id, carried into BoundComponentExtraction. compare must reject same lane or same record; do not infer independence from ids. Owner will update runner.
2. drug_name extends component with mandatory completeness complete/fragment/uncertain/absent; null value requires absent. Same textual fragment on both sides must be status unresolved, never agree. Complete is a model assertion requiring source_qc; document this limitation. No drug aliases or project mapping.
3. Replace single time component with required times:list[MedicationTimeComponent]. Preserve multiple explicit time roles including prescription_date,use_start,use_end,administration_date,unclear. Each item uses raw substring value/quote and mandatory precision day/month/year/partial/unclear. Explicit interval must be two items use_start/use_end, never entire interval marked start. Unknown day placeholders stay exact; partial dates not silently dropped. Add required ongoing component (same value/quote nullable) and administration_count component, separate from frequency (single injection once is count, not daily frequency). Model is not allowed to infer use_start just because dated use is mentioned; unclear remains valid when genuinely ambiguous.
4. Compare times by role preserving multiplicity, compare source-normalized text and precision. Matching unclear role yields unresolved, not agree. Different precision, missing side, differing values must remain explicit. Stable order must not create agreement from different date roles. Existing other component comparisons retain units and source binding. Overall still product_acceptance=false and source_qc_required=true.
5. Prompt compact native Chinese, include JSON INSTANCE SHAPE separately from schema, explicitly forbid emitting $defs/properties/schema. Avoid giant repeated schema; either compact shape + contract instructions or schema unambiguous. Faithful source naming including formulation, no truncating dosage form prefixes; no guessing a complete drug from page fragments. No clinical gold/examples tied to real data.
6. Tests update for v2 and above defects, plus existing source/units/missing/extra/preservation tests. Genuine fragment vs complete test; partial dates; start/end order; same lane rejection; count vs frequency. Keep small pure module, no abstraction framework.

Return API changes, actual test result and limitations. Do not claim product or clinical acceptance. No additional files.
