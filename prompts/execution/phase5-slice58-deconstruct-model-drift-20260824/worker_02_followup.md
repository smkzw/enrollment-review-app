Delegated mode. Continue the same bounded execution session for `worker_02`; do not start a new audit or conference.

Read the updated `context/phase5-slice58-deconstruct-model-drift-20260824_execution_context.md`, especially `Real Rerun Finding After The First Repair`. The parent Codex has verified the real oMLX route and isolated failure.

Task:

1. Diagnose and implement the smallest coherent shared fix so oMLX protocol deconstruction receives the exact strict JSON Schema appropriate to each call: semantic candidate versus semantic rule repair. The output kind must be passed explicitly through the transport contract and caller; do not infer it from prompt text. Preserve DeepSeek's existing `json_object` behavior unless strict-schema support is already proven by current code/tests.
2. When the runner ends without a `final_draft`, preserve a bounded Chinese summary of its actionable schema/gate issues in the durable StepFailure detail. Do not persist raw model output or expose hidden reasoning.
3. Update all affected fake transports and focused tests inside the authorized files. Run focused protocol tests, compile checks, and `git diff --check`.

Do not relax domain schemas, official numbering, phase logic, source locators, or clinical gates. Do not touch frontend, runtime data, source protocols, or unrelated dirty files. Return the same complete execution report schema and clearly distinguish implemented changes from remaining real-run verification.
