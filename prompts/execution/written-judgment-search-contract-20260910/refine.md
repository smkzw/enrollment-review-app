Continue same session and same three-file write scope. Owner source review found limitations in the initial contract (including owner's original too-strict ambiguity wording); correct before integration. No network/models/install/delegation. Actual initial implementation remains candidate-only and unwired.

1. A page/channel may contain multiple independent judgment excerpts. Replace singular excerpt/bbox with a tuple of small source candidate excerpt objects (exact text+bbox optional), no arbitrary model IDs needed. found requires nonempty candidates; ambiguous MAY preserve zero or multiple tentative excerpts, which remain ambiguous and must not be promoted to found. not_found and unreadable must not fabricate excerpts. Coverage summary must retain tentative excerpts in a clearly ambiguous collection alongside gaps, and all found candidates. Never silently retain only first excerpt. Add multiple same-page candidate and ambiguous snippet preservation tests.
2. Scope/result uniqueness: reject same document version+page number represented by two page_artifact_ids and reuse of one artifact ID for different pages/documents. These are one-source-page identity collisions, not two pages. Existing exact hash checks remain. Add counterexamples.
3. provider/model nonblank strings: reject whitespace-only and normalize surrounding whitespace for comparison; same model differing only case/whitespace must not constitute independence. Do not pretend this proves aliases across providers; document real-route verification remains repository/runtime responsibility. Include reasoning_effort metadata as a required nonblank field on lane results for audit but NEVER in independent-model identity comparison; no timestamp fabrication. Update tests.
4. Report actual Write/Edit limitation honestly; remove claim effect equals literal apply_patch compliance in future report. No existing product paths modified, no clinical proof or source-scope completeness claim.

Run new suite plus previous 73 scope. Return exact edits/tests and remaining required builder/receipt checks. Do not dispatch reviewer.
