Continue same session. Prior refined contract owner reran 104 tests successfully. Extend allowed writes ONLY with new app/services/judgment_search_source.py and tests/v2/services/test_judgment_search_source.py; existing three contract files read-only unless a concrete indispensable bug is reported before editing. No other existing files, network, models, dependencies, credentials, real clinical DB, API wiring or delegation. No fallback/local platform. Apply_patch if available, else disclose actual limitation.

Implement read-only build_judgment_search_scope(session, authority: FactAuthority, requirement_id: str) -> JudgmentSearchScope using EXISTING repositories and validators. Validate active FactAuthority, requirement belongs to exactly one frozen current rule-set expectation template (list_expectation_templates is existing validated boundary; inspect before use), retrieve CompleteEvidenceProcessingRevisionRepository and ALL revision.manifest pages, PageArtifactRepository validates artifact. Exact document-version/page-number/artifact identity and nonempty image SHA required. No page status/date/file-category/reconciliation acceptance filtering. Failed OCR with valid rendered image remains in supplied scope. Missing image or malformed/foreign identity produces specific bounded source error; do not fabricate hash or silently omit page. Current revision repository checks source closure; verify its guarantee from code, do not duplicate large validator or assume completeness from name. Document if page-count completeness cannot be proved; do not overclaim.

Use stable ordering/hash functions from new contract. No writes/commits/model calls, no route configuration needed. Scope is entire active supplied revision, not a conclusion all clinically required external files were supplied; summary source_scope_verified remains False. No clinical professional_judgment producer.

Tests real temporary SQLite/repositories (reuse existing synthetic seed helpers from correction-gap/projection/normalization tests as appropriate): complete nonempty authoritative scope, stable repeated hash, stale authority, missing/foreign requirement, missing page image, artifact/manifest identity mismatch, read-only no DB mutation. Add multiple-page and failed-OCR-with-image inclusion if existing fixture supports it; if not, explicitly report gap rather than pretend one-page proves all-file coverage. Do not manually inject clinical answers. Run new tests and page-review job planning plus contract coverage suites. Return exact tests, guarantees from actual repositories, limitations and no product clinical acceptance.
