# E03 same-session repair: published target semantic consistency

Same primary zcode/GLM-5.3-Flash:max, no fallback. Only modify app/services/judgment_search_source.py and tests/v2/services/test_judgment_search_source.py. No clinical files/DBs/model calls/network/credentials/local operations/installation/subdelegation. Preserve all owner changes. Tools disclosure as before. This is repair of current unit, not a new framework or permission to modify repositories.

Owner inspection found current checks insufficient:
1. Template fact_type/due_stage alone do not establish semantic equality. Read the existing save_expectation_templates boundary in repositories.py around2708: description, required_source_types(set semantics), requires_contemporaneous_objective_source and allows_screening_record_transcription must ALSO agree with published requirement. Reuse its exact semantic contract without writing. Reject mismatch; do not hide by just omitting divergent template fields from target.
2. workflow_stage_id startswith is only namespace, not actual stage. Use existing get_workflow_stages_by_ids/native repo to verify stage exists, exact ID, protocol/study phase, stage==due_stage, and requirement in due_requirement_ids, as existing write boundary requires. Do not invent stage or silently ignore missing stage.
3. Membership by component ID alone is inadequate for normalized row versus enclosing payload drift: parent rule in loaded RuleSet must exactly equal get_rule result; selected component in that rule must equal get_rule_component result; selected requirement in component must equal get_evidence_requirement result. Reject self-consistent but semantically divergent normalized payloads. Exact structured equality is appropriate here, not fuzzy text. This only validates selected path, not unrelated whole-tree closure; do not overstate it.

Add real synthetic SQLite adversarial tests with self-consistent payload/hash/mirror mutation (not merely broken checksum) proving same-fact-type description drift, absent workflow stage or missing due requirement, and selected child/parent payload drift reject. Preserve valid fixture behavior and read-only fingerprint. Existing procedure unsupported boundary remains explicit. No clinical acceptance claims. Run source+reader+coverage+relevant template repo tests; return actual count and failure causes.
