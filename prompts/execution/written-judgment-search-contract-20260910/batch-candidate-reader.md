# Bounded multi-target candidate reader, one page call per lane

Same primary frozen E03 zcode/GLM-5.3-Flash:max session, no fallback. Implement a bounded explicit multi-target variant of existing candidate reader. Owner reviewed storage:114 tests pass. Existing product main reader/default routes remain unchanged. Allowed modifications ONLY app/llm/judgment_search_reader.py, app/services/judgment_search_results.py, tests/v2/llm/test_judgment_search_reader.py, tests/v2/services/test_judgment_search_results.py; new tests/v2/llm/test_judgment_search_batch_reader.py permitted. No other edits/DB/source material/network/API/model/local services/installs/delegation. Do not invoke formatter on unrelated code. Disclose unavailable apply_patch.

Reason: avoid requirements*pages*2 repeated image reads. Current v4 single-target reader remains byte-for-byte prompt-compatible and its API/version supported. Add read_judgment_search_page_batch with explicit repository-prepared list of (scope,target_text), single page input, one route, optional injected completion. It makes exactly ONE direct completion call for ALL requested targets, no retries, no selection/omission, no clinical judgments, no acceptance. Caller controls a small explicit batch; do not impose arbitrary medical count limits, auto-truncate targets, or cache guessed absence. This is candidate reading only, not job integration or rollout approval.

Minimal coherent contract extension in existing reader module:
- Separate BATCH prompt version (v1), keep current SINGLE v4 unchanged. Add frozen batch target identity metadata to receipt (default empty for single): each target includes requirement_id, scope_sha256,target_sha256; distinct IDs exact. Add only what deterministic raw binding needs, no new certificate framework. Full target prose need not be duplicated in receipts, hashes identify frozen input. Same scope authority/pages must be verified for every input target BEFORE model call, target required nonblank, page membership/hash current; different requirement_id allowed, cross-authority/different-page-set rejected. Reuse existing image freeze and candidate source prompt, do not hand-roll a weaker send path.
- Model output batch: one complete JSON object with results list; each item has requirement_id plus the unchanged handwritten/printed_analysis payload. Exact requested ID set, no duplicate/missing/foreign IDs. JSON duplicates and nonterminal completion rejected same as single. No guessing IDs, deleting unknowns, or filling missing not_found. Preserve all excerpts/uncertainty_note/unverified coordinates.
- One raw completion must be preserved identically in every per-target receipt, same request messages hash and requested route. Each receipt binds selected target scope/hash and target identity group. Return a tuple of ordinary extended receipts in requested order. If any batch target/output invalid, raise existing reader error with completion retained; don't emit partial success as complete. Legitimate same text related to different targets MUST remain separate source-linked candidates, NOT automatically downgraded/rejected just for text equality. No automatic clinical acceptance.
- Share a small response-selection helper between reader and results binder instead of duplicating parsing: single version requires empty batch metadata and existing payload; batch version requires nonempty valid group metadata, exact raw results ID set, selected target metadata matching receipt.scope_sha256,target_sha256,requirement_id(current scope). Binder remains strict for obsolete/unknown versions and raw channel mismatch; cannot admit a batch response by relabeling it single or vice versa. Current target hash still checked against repository-prepared text; do not trust stored scope alone. Review model_copy bypass and cross-target swaps.
- Existing receipt save/load helpers should work without modification because they validate extended receipt + call binder. Run their tests too. Do not rewrite v4 historical artifacts or claim all group source content can be reconstructed from hashes. Full requests already saved by caller, receipts aren't external attestation.

Tests: one request for two explicit same-source targets with distinct responses; exactly requested ordered receipts; different targets can have same quote; missing/extra/duplicate IDs fail with raw response retained; non-stop/fenced complete JSON behavior; missing page/image drift/cross-authority/page-set mismatch fails before request; altered selected target/group/response channels/version rejected by binder; same direct route max budget unchanged; source prompt v4 single unchanged and its existing tests pass. No real calls. Run reader/batch/results/artifacts/source/coverage tests. Return findings/limitations without claiming product orchestration/clinical acceptance.
