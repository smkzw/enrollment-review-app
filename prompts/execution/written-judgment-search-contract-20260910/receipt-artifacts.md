# E03 bounded receipt artifact storage

Same frozen primary zcode/GLM-5.3-Flash:max session, no fallback. Read latest v4 reader, results binder and ArtifactStore. Allowed NEW files only app/services/judgment_search_artifacts.py and tests/v2/services/test_judgment_search_artifacts.py. No other edits, no clinical/source DBs, network/model calls, credentials, local services, installs, or delegation. Report unavailable apply_patch truthfully.

Implement the smallest reusable candidate receipt save/load helpers using EXISTING ArtifactStore raw_response kind (do not invent a persistence framework/table/new artifact kind). Store the complete validated JudgmentSearchReaderReceipt as canonical JSON bytes, retaining embedded raw PageCompletion, quotes, unverified coordinates, metadata. Return existing StoredArtifact. Loading takes an existing storage_ref, uses ArtifactStore integrity verification, strictly parses one complete JSON object with duplicate keys rejected, validates receipt, and leaves clinical acceptance false. Reject non-raw_response references. Before save and after load, reuse assemble_judgment_search_coverage with repository-prepared current scope+target and the one receipt to validate actual raw-response binding, current page/target/model/prompt identity; incomplete single receipt is valid storage, not coverage completion. Caller prepares current scope/target; do not silently build trust from saved scope. Do not relabel or migrate old receipts. No automatic acceptance, gap producer, retry, or job wiring. Corrupt/foreign/obsolete receipts must not silently disappear or turn into not_found. Propagate a clear consistent ValueError/domain error retaining cause. Reuse existing errors if coherent rather than inventing more exception taxonomies.

Tests with tmp_path real ArtifactStore + synthetic reader receipts. Roundtrip including ambiguous uncertainty note/raw completion; same bytes give same identity; changed reply gives different identity preserving old; stale target/scope/prompt; raw response vs page-result mismatch rejected before save; wrong kind; missing artifact; corrupted bytes; duplicate JSON keys rejected (store crafted bytes via existing put then load). Reuse existing test helpers only if genuinely importable; don't over-refactor unrelated tests. Run new tests plus results/reader/source/coverage. This is engineering storage only, not durable orchestration or clinical validation. Return exact checks and remaining integration work.
