# E03 same-session: receipt-to-current-target binding only

Primary zcode/GLM-5.3-Flash:max same session, no fallback. Allowed NEW files only app/services/judgment_search_results.py and tests/v2/services/test_judgment_search_results.py. No shared changes, raw clinical material/DBs, API/model calls/network/install/credentials/local platforms or subdelegation. Read current owner v4 reader and latest source builder; preserve all. Disclose missing apply_patch rather than claiming compliance.

Implement minimal pure assembly function consuming a CURRENT repository-prepared JudgmentSearchScope + target_text and a sequence of JudgmentSearchReaderReceipt, returning existing JudgmentSearchCoverageSummary via existing summarize_judgment_search_coverage. This is candidate supplied-page coverage only, not source-universe proof, clinical acceptance or professional_judgment producer. Do not add a new certification DTO or storage framework. Read app/domain/judgment_search_coverage.py for native missing-lane/page handling. Caller is responsible for prepare_judgment_search_target from current DB; source/target references alone are not a claim that real calls occurred. Record this boundary.

Required deterministic consistency BEFORE assembly:
- Revalidate scope and receipts through dumps to catch model_copy bypass. target_text must be exact nonblank source string, target hash equals each receipt, scope hash identical.
- Every receipt page identity and page_result identity/hash exactly agree with each other and scope membership. Reject foreign/extra page, duplicate same lane/page (do not silently last-wins or count repeat as second model).
- Requested model/provider/effort nonblank, main-A/B only, positive budget. Each lane has consistent provider/model/effort across pages. Response model must be nonblank and case-insensitive stripped equal to requested model for this narrow binder; do not invent aliases or ignore unknown model. Across lanes same provider/model is not independent; reuse existing coverage checks. Disclose aliases unsupported, no automatic fallback.
- PageCompletion finish_reason stop, receipt mirrored response_model/id/finish_reason/completion_text_sha256 consistent with raw completion. Reparse completion using current candidate payload parser and require BOTH channels exactly equal bound page_result channels; do not trust a forged result beside contradictory raw response. Preserve uncertainty_note and coordinate_convention=unverified; no coordinate rescale or inline quote repair. Existing complete single fence parser allowed, no model retry.
- Require current supported prompt version to prevent mixing older candidate search meanings unnoticed; historical receipts kept but not silently relabeled or admitted. Do not rewrite artifacts. Same current version within all lanes.
- Build native JudgmentSearchLaneResult from consistent lane identities + page results. Missing page/lane remains incomplete, never fill not_found. Empty list likewise returns native incomplete rather than creating absence. Allsupplied not_found preserves native three false invariants, not clinical proof.

Tests synthetic receipts produced using injected completion/read_judgment_search_page with in-memory page bytes and real contract scopes. Valid pair/found/ambiguous and full-not_found summary; missing one page/lane/none; mismatched current target/scope, foreign page, duplicate receipt, changed page_result beside raw none, hash/responseID/responsemodel drift, unknown response model, same model two lanes, mixed effort in one lane, outdated version. Reject invalid receipt even if altered by model_copy. Legitimate identical text candidates in separate contexts must NOT be unconditionally treated as contamination: no such heuristic. Run new tests + reader/source/coverage focused suite. Return exact numbers and remaining integration/persistence/retry/clinical-applicability work, no clinical approval.
