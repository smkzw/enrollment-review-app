# Same-session E03: authoritative candidate target context

Primary zcode/GLM-5.3-Flash:max only, existing packet/same session, no fallback. Allowed changes only app/services/judgment_search_source.py and tests/v2/services/test_judgment_search_source.py. No raw clinical databases/files, credentials, models, network, installation, local services, subdelegation or other writes. Preserve owner changes (FactAuthorityValidator.validate_and_get_revision, latest source tests); use apply_patch if available and disclose unavailable tool. No design/plan/checkpoint edits.

Current candidate reader app/llm/judgment_search_reader.py is unwired and takes free target_text. Next requirement is a narrow readonly preparation function that returns existing validated JudgmentSearchScope plus a canonical JSON target string from CURRENT PUBLISHED sources, for later reader use. No new framework or model calls. Read source builder, contracts/rules.py & evidence.py, repositories.py get_rule/get_rule_component/get_evidence_requirement/get_rule_set/list_expectation_templates and related workflow procedure repository before choosing minimal implementation. Reuse existing builders and repository semantic checks. Source scope must still be revalidated for each preparation; no cached trusted flag, no original DB writes.

Why description alone is insufficient: a real published description says "研究者评估上述疾病是否可能影响受试者的疗效和安全性判断". That is an example of a pointer, NOT permission to hardcode its study, disease or ID. Preserve its published parent rule source_text and selected component title/expression/exception_expression plus the exact EvidenceRequirement and template stage metadata. Do not summarize, rewrite, infer omitted thresholds/dates, or use current model output as authority. Include source IDs/revisions/protocol/template projection hash in the canonical target payload; canonical JSON/hash must deterministically change with actual target content. Avoid full RuleSet/ClausePack: only the exact parent source and selected child/requirement context. Candidate target is not proof that all cross-references have been semantically resolved.

Require exact membership and consistency: template requirement belongs to requested authority ruleset/revision and published requirement, selected component belongs to parent, protocol and study phase match. Follow explicit IDs, no title matching. If procedure-catalog requirement has no rule_component_id, inspect existing authoritative workflow path: either support the precise native source with all existing checks, or explicitly fail with bounded unsupported-source error; NEVER fabricate a parent component. Record supported boundary. If published source is missing/contradictory or template differs, reject; no description-only fallback. Avoid adding another whole revision reread beyond existing scope builder. Snapshot page scope remains supplied scope, not full external clinical proof.

Return a small named tuple/dataclass or tuple(scope,target_text), whichever matches existing style; frozen metadata in canonical string is enough, no parallel certificate DTO. New tests real synthetic SQLite fixtures must prove parent original/source context included verbatim, scope/target stable, source mismatch rejected, no whole ruleset included, unknown requirement/stale authority still rejected, procedure boundary explicit, and DB remains unchanged. Tests must not assume title text implies investigator judgment or infer date roles. Keep no clinical acceptance fields that can be true. Run updated source tests plus adjacent reader/coverage and repository tests. Report exact changes/tests and known limitations; target preparation is not D2 absence producer or clinical approval.
