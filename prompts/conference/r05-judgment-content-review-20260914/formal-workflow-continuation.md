Read-only adjacent source review in the same approved session. No edits, runtime/imports/tests, database, clinical raw materials, model calls, network, delegation. Return concise concrete file/definition-based recommendations, not general cautions. This is engineering advice, not clinical approval.

Current source is authoritative: prepared_review_intake.py/progress.py and qualified_review.py now create/read four explicit-owner prepared-review-runtime/v1 task types; PageReviewRuntime registers existing executors with shared admission. JobRunner claim/recovery filters ownership. Historical isolated jobs remain unowned. Qualified formal publication still requires independently evaluated method approvals; never fabricate approval. The front end must offer one user action and not require users to start internal steps. Closing browser must not prevent continuation. No staged tests authorized until product built, source review only.

Question: what is the smallest complete persistent continuation that sequences predicate/control candidates -> qualification and required judgment-content -> ready for formal publication using existing JobRunner/JobService/receipts, without second queue, recursive runner, treating dependency wait as failure or waiting_user, launching old isolated jobs, or auto-publishing unapproved semantics? Read complete affected workflow runner/jobstore/jobservice and current enqueues/checkpoints to find existing primitives. If no persistent dependency hook exists, compare minimal new workflow-intent storage + reconciler in the existing maintenance loop against an explicit deferred dependency outcome in the existing JobRunner. We need durable stop/retry boundaries, idempotent crash recovery, source/route identity pinning, stale context behavior, correct parentage, zero-candidate handling, and no cancellation of shared historical jobs. Don't recommend cascading arbitrary current model config into an already pinned run. Avoid whole-framework migration.

Identify actual code reuse and minimum changes; say what needs a new contract versus UI only. Also inspect prepared_review_progress for significant current integrity or pagination faults. Return no more than 100 lines. Sources: app/services/prepared_review_*.py, page_review_runtime.py, review_runtime_ownership.py, predicate_binding_job.py, control_binding_job.py, binding_qualification.py, judgment_content_job.py, qualified_review_command.py, app/workflow/*.py, app/api/v2/app.py; broaden only if decisive dependency requires it.
