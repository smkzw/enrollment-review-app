Same C03 source-only advisory review. No edits, imports, tests, app/database/model/network calls or delegation. User defers tests until the complete build. Review current source; no clinical acceptance.

Inspect new qualified_proposition_evidence.py, proposition_method_evaluation.py and the changed qualified_binding_selection contract/service, qualified_review_command.py, review_method_evidence.py, frozen_review_publication.py, frozen_review_calculation.py, projections/control_calculation_experiment.py, prepared_review_workflow.py/publication.py.

Current intent: separately evaluated proposition method bound to original paired source qualification; exact input, receipt, route and summary hashes. Authorization v4/selection v3/consumer v7/evaluator v9; old serialized inputs must retain their prior fields. A relation is included only if existing strict pair_direct_selection_rejection_reasons is empty. This is deliberately conservative: no waiver of professional or time/policy uncertainty. It may leave source-qualified semantic material unusable when legacy arithmetic qualification reports missing operands; report this limitation rather than calling it complete.

Calculator receives selected relations only from sealed qualification material in formal path. No time-bearing semantic truth yet. Single semantic observation remains UNKNOWN without scope verification. ANY TRUE / ALL FALSE witness is the sole new calculated direction, only under an explicit published observation policy, no source conflict, a pair-local agreed entails/contradicts and no time requirement. Reverse directions and unsupported policies stay UNKNOWN. No missing evidence => false. Deterministic comparisons still use code, not relation records. Parent workflow v3 schedules the relation child; v1/v2 retain old expected sets. Empty relation jobs don't call a model and parent publication does not demand a relation evaluation if there are no pairs.

Find source-proven bugs (especially old-version hashes, wrong job/approval scope, relation direction, contradictory sources, workflow progress), and identify minimal necessary corrections. Separate real blockers from stricter future scope coverage. <=70 lines, file/line evidence. No blanket runtime claims or old fallback narratives.
