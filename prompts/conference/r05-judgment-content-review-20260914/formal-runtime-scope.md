Read-only adjacent workflow review, same session. No edits, runtime tests/imports, DB/model/network calls, delegation. Source review only.

Goal: wire existing predicate/control candidate, binding qualification and judgment-content executors into product runtime using same PageReviewAdmission/model routes and JobRunner. Do not resume old isolated jobs automatically or introduce a second queue. Formal prepare/save HTTP routes now registered; source/evaluation/approval checks retained, no actual acceptance. Existing enqueues carry optional review_context_id/hash but job types overlap historical isolated runs. app/api/v2/app.py default_executors lacks these four tasks. JobRunner currently claims all queued types (including unknown), recovery scans all jobs. Need concrete minimum complete routing/recovery boundary before wiring.

Read app/workflow/runner.py claim/maintenance, jobstore.py claim/requeue/recovery helpers, recovery.py, app/api/v2/app.py startup, page_review_runtime.py, predicate_binding_job.py, control_binding_job.py (discover actual path), binding_qualification.py, judgment_content_job.py. Recommend precise minimal existing-queue ownership/admission extension that permits newly explicitly prepared review jobs and restart continuation, while leaves historical isolated tasks untouched, including retries/expired leases/cancel requests. Avoid arbitrary new job type aliases breaking receipt validation or in-memory-only allowlists. Identify required registration/intake changes and compatibility defaults; no broad redesign. If a read-only structural query of source suffices don't propose running tests. This is engineering review, not clinical approval.
