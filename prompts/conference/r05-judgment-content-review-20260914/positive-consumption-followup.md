Continue the same read-only review. No edits, tests, imports, DB, product model calls, web, delegation or route changes. Return report to runner only. This is engineering advice, not clinical acceptance.

Owner is integrating the existing written-content producer into the formal path, not creating another framework. Inspect current judgment_content_input.py, judgment_content_receipts.py, judgment_content_comparison.py, qualified_binding_selection.py, qualified_review_command.py, frozen_review_calculation.py, frozen_review_publication.py, review_method_evidence.py, domain/gates/assessment.py. Read narrowly relevant definitions fully.

Proposed minimal integration:
1. Optional content job + content evaluation digest + content summary digest attached to per-job QualificationAdoptionAuthorization as an all-or-none scope. Existing binding evaluation and method approval still required; separate JudgmentEvaluationManifest must also match exact source qualification method and content routes/versions. No actual approvals created.
2. Rebuild content receipts; require same candidate job, frozen input, comparison, review preparation as qualification. Only pair-level content_supported with both five fields favorable can clear professional_judgment_applicability_unverified, in addition to existing source qualification requirements. A content rejection/disagreement is not absence and cannot create a new encoded fact.
3. For an official professional predicate, allow selected existing value only when the exact pair has both source and content verification. Preserve current single-observation restriction until protocol-derived policy implemented. Existing missing/ambiguous/unlinked excerpts retained.
4. A requirement's judgment gap can be removed only when every explicitly attributed predicate has a usable qualified content-bound value AND all found excerpts for that requirement are accounted for (no dropped conflict/ambiguous/unlinked content), within same context and due node. Other gaps (missing file etc.) remain. Avoid claiming every source document requirement is fulfilled just because one predicate can be calculated. Freeze positive evidence refs in selection material so publication replays the same proof.
5. Existing calculation performs value/polarity/time math, not model-generated truth. Version consumer/evaluator/publication for changed semantics. No ordinary HTTP activation until evaluated approval.

Challenge this against actual fields and control flow: identify minimum specific corrections, especially whether no blanket requirement-gap removal is possible without explicit additional proof; avoid demanding unrelated infrastructure. Positive path must be achievable, not permanent UNKNOWN. Also inspect recent date-window pair coupling and prompt v3 for actual contradiction, not names alone. Source-qualified event_date is window operand, not numeric threshold derivation. Auth prompt default was updated to v3 after previous turn.

Provide prioritized concrete findings with file:line, precise implementation recommendation and remaining acceptance obligations. Do not mistake source review for runtime verification.
