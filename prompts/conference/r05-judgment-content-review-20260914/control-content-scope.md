Same read-only C03 engineering review. Source only; no edits, execution/imports, tests, DB, network, product model or delegation. The next missing product capability is control-family written investigator judgment, not more UI plumbing. Examine app/services/judgment_content_input.py, judgment_fact_linkage.py, judgment_content_job.py, qualified_judgment_content.py, qualified_binding_selection.py, app/domain/contracts/judgment_method_evaluation.py, control_evaluation_spec.py and existing control-source policy projection. Determine the smallest COMPLETE generalized extension, reusing current content job/receipt protocol and no new queue.

Current facts: input and consumer explicitly reject control family; input links requirement-local predicate_ids; controls already use atom identities and atom_refs; current deterministic consumer rejects semantic/investigator_judgment. Five existing written checks verify explicit writing, attribution, target, node and encoded fact value. They do not establish arbitrary proposition truth. Do not propose treating content_supported as truth or flipping prohibited/required polarity. Do not invent disease-specific examples as implementation rules. User requires missing written judgment to remain reportable unknown without blocking workflow or asking redundant confirmation. No automatic method adoption is authorized.

Return source-grounded exact extension points, missing fields/meaning and ordered cohesive implementation units; separate provenance/content reuse from non-deterministic proposition evaluation and explain which needs a genuinely new result contract. Include contradictions and risks of overengineering/extra inference. Prefer reuse with explicit version changes preserving old receipts. User deferred runtime tests until full build, so list future checks but don't run/request them. <=85 lines. Prior publication exception translation has now been fixed; no need to re-review it.
