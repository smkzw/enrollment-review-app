Same-session read-only source check. No edits, tests/imports, DB, product calls, web or delegation. Review current control_time_binding.py and adjacent ProtocolReviewControl, validate_control_evaluations, compose_control_layers plus latest source_validity changes in qualified_binding_selection/control_calculation_experiment.

Implementation deliberately avoids implicit global inheritance: optional control_time_bindings list, omitted when empty to preserve old hashes; each target atom must already contain exactly the global constraint, explicit purpose and its own supporting excerpts. Global without bindings still parses as history but fails publication/composition. Mapped atom arithmetic and identity remain unchanged; no invented target mapping or clinical approval. Current product candidate producer already uses per-atom times and never emits global time, so this is an explicit published-contract path, not a claim to have reinterpreted historical global controls.

Check source validity: single declared deterministic date_range policy only can clear the generic pending marker after six-dimensional favorable agreement; normal identity selection still requires same-fact qualified time pair. Value comparison with expired source remains UNKNOWN, time-only freshness remains its temporal truth. No runtime acceptance or staged tests. Identify concrete source defects, including hash preservation/field placement and whether pending markers still prevent valid declared date operands. Return only findings with file/lines; if no defect identify remaining integration and verification limits. Do not invent new scope or a broad redesign.
