Same read-only engineering role. No edits, imports/runtime/tests/DB/network/model calls or delegation. Review current implementation, especially concrete failure paths. Parent owns integration; not clinical approval.

Owner adopted existing-maintenance/idempotent-children approach, but not context-only auto-sequencing: added explicit prepared_review_workflow Job parent with 3 steps in existing jobs table. It is intentionally not a model executor; PreparedReviewContinuation is called by same JobRunner maintenance, briefly leases one queued parent, waits without consuming attempts, queues owned children and records same JobStore checkpoints. Uses existing runner lease_heartbeat while source/preflight/queue I/O runs; dedicated root-only recovery because registered-executor scope excludes parent. Normal child queues/admission unchanged. New workflow_job_id propagates in payload/hash, validates parent context/routes/active uncancelled state at child creation. Parent cancellation propagates to owned children immediately (standard callback or scoped API) and maintenance rechecks. No new queue/table/framework. Parent completion means checks gathered, NOT approval or formal publication. Manual child routes remain compatible.

Read app/services/prepared_review_workflow.py, review_runtime_ownership.py, prepared_review_workflow_view.py, relevant enqueues, page_review_runtime.py, app/workflow/runner.py + jobstore.py + recovery.py; app/api/v2/qualified_review.py/app.py. Check crash gaps, cancellation races, leases, starving other jobs, retry only failed work, pinning to actual routes, missing/zero/control children and source currency. Reject invented blockers, but identify genuine bugs with references and minimum fixes. Your previous proposal to ignore mismatched same-context hashes in progress SQL was rejected: context is immutable, mismatch should be visible, not hidden. Also context hash equality alone cannot validate current authority; owner uses FactAuthorityValidator.

Read-side DTO frontend next; don't demand UI already completed. No runtime validation permitted now, clearly distinguish source findings from test evidence. Return <=90 lines.
