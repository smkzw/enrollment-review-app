Continue the same bounded read-only source review. Higher-priority/global/project instructions apply. Do not edit source, write or run tests, import application code, access databases, call product models, start services/browser, or read raw clinical material outside this workspace. Return the complete report for runner persistence.

Owner decisions and issues requiring a corrected design:
1. Accepted and implemented the two REASON_GAPS mappings; evaluator now v15. These guards are incomplete functionality, not clinical acceptance.
2. Reject counting qualified fact rows as clinical occurrences: multiple records can describe one event; a fact may itself summarize a frequency. ClinicalEventV2 identity/lineage or source-qualified event/summary semantics are necessary. Also reject generic TRUE-witness/FALSE-incomplete: comparator '<='/'<'/'==' has different monotonicity. Do not invent a scope anchor absent in OccurrenceWindow; distinguish duration from review lookback.
3. Reject using mere date order to establish initial/retest relationship, even when protocol permits a repeat: events may be unrelated measurements, multiple cycles, or different specimens. Existing object_match does not prove this relation. Need source-linked explicit relation and task roles, not recoding earliest/latest.
4. We want the smallest coherent end-to-end implementation for conditional retest, not another isolated schema. Please re-inspect existing proposition/judgment content and qualifier builders to find whether already-supported per-condition semantic scope can supply role/relation certificates in the SAME two-lane job, keeping arithmetic in code. If it genuinely cannot, name the exact minimal extension and producer, verifier, consumer and report changes. Avoid pretending an empty or deliberately-unresolved consumer is completion.
5. For prospective statements, do not broaden investigator-only judgment semantics to participant statements. Assess general proposition layer reuse with an explicit official predicate mode; consult actual producer input and structured temporal scope, not only docs. Do not assume current date qualification certifies full intended period coverage.

Deliver a bounded corrected design with exact current code references and a recommended first complete implementation slice. No new project/disease-specific rules. No need for user to decide abstract implementation choices; Codex owns those. Identify decisions that truly change clinical acceptance separately.
