Same-session targeted read-only follow-up. Do not delegate, write source, run model services, read clinical data, or add/run tests.

Reopen current files. Owner has implemented option A: EvidenceProcessingRevision now freezes preparation_policy with legacy default; new upload image policy is persisted in base revision and has a separate revision-id suffix. Complete repository permits missing OCR only when the decoded base explicitly uses original-page-images/v1 and the page artifact is successful, source-bound, and has image hash. Legacy OCR-bearing pages retain success/risk/correction checks. Review this exact path through full revision creation/activation and downstream normalization.

Check your earlier inference about empty sources against app/projections/page_review_pending_normalization.py, fact_normalization_executor.py and existing visual source publication validation. Do not require every page to produce accepted facts or locators: genuinely irrelevant/blank pages and unresolved readings must remain reportable without inventing facts. Identify concrete bypasses, not merely absence of facts. Historical OCR inheritance is preserved evidence, not a new OCR inference; do not replace its identity with a weaker key merely to remove OCR names.

MTPLX owned driver is explicitly not integrated; no claim of lifecycle completion. Only flag actual code defects in it, not the already documented missing integration. It must never kill another service. Installed CLI supports app-launch-id, verified by owner via help; actual driver runtime remains unverified.

Return concise findings with file:line, distinguish fixed prior findings, new confirmed defects and unverified runtime. No broad rescan or clinical acceptance.
