Same read-only C03 adviser, no edits/imports/tests/runtime/DB/model/network or delegation. Need a narrow design decision before modifying semantic scope. Inspect proposition_evidence contract/reader, control_evaluation_spec and control_calculation_experiment source, plus goal design §17.

Problem: the reader verifies exact proposition, target and node, but pair_local alone does not prove the scope of a source-declared single observation. Adding another full model job solely for this would duplicate source/prompt cost. Candidate option: add scope_correspondence (supported/partial/unresolved) to the existing two-reader result and include the exact published observation_policy (scope + source excerpts) in the current prompt. A decisive relation must have supported scope with an exact source quote; no coverage inference from empty matches. For single, admit only a single source-qualified fact with matching dual scope results, no time constraint, no source conflict, and explicit policy single; all/any reverse directions remain UNKNOWN because pair-local evidence cannot prove exhaustive observation coverage. The main reviewer owns future clinical evaluation; no automatic approval.

Challenge whether this sufficiently distinguishes an explicitly documented whole single proposition from a partial observation, and what smallest additional structured field or scope proof is genuinely required. Do not demand a new queue or another general whole-document read. Consider multiple candidate facts, observation identity vs selected count, explicit denial vs absence, and not treating two agreeing models as clinical truth. Give a concrete implementable minimal recommendation, not generic caution. <=50 lines. Also comment on whether existing per-pair entails already asserts full proposition and how to avoid redundant contradictory labels. No need for user approval questions unless essential unresolved clinical policy.
