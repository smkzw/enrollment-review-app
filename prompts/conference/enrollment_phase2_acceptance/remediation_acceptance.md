# Phase 2 修复后独立验收

你是独立验收者，不参与实现，也不得修改文件。请在当前仓库中读取 Phase 2 的实现、测试、Trellis 任务和三份上一轮审评报告，重点核验本轮修复是否真正关闭系统级缺陷，而不是只让测试通过。

## 必查范围

- `app/storage/idempotency.py`：并发同键同内容、同键不同内容、外层事务完整性。
- `app/workflow/jobstore.py`、`runner.py`：终败事件、Checkpoint 恢复进度、依赖级联进度、取消与步骤提交边界、退避未到期是否可被错误认领。
- `app/api/v2/jobs.py`、`errors.py`、`vocabulary.py` 与前端 SSE 适配器：服务端真实末序号、错误分类、失败终态订阅。
- 版本化 JSON Schema 是否与领域模型一致。
- 上一轮 Grok 提出的 stale 同源同版本重开、ReviewRun 规则集作用域两项，判断是缺陷还是误报，并给出领域依据。

## 已知验证锚点

- 后端全量：464 passed，18 subtests passed，1 个历史 OCR 缓存夹具缺失而跳过。
- 前端：207 passed，生产构建成功。
- 新增 8 线程真实并发幂等测试。

请自行检查代码并在能力允许时运行定向测试。不要做安全测试，不要扩大到 Phase 3 功能。输出：

1. `ACCEPT` 或 `REVISE`；
2. 仍存在的真实问题，按严重度排序，带文件/行号和可复现路径；
3. 对四项争议结论逐项裁决；
4. 已关闭问题的证据；
5. 尚未验证的边界。

不要因为实现复杂而推测缺陷，也不要因为已有测试就自动接受。以本地单 Mac、单用户、SQLite 持久任务的实际产品边界作判断。
