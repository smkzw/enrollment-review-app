Same-session direct read-only C03 follow-up. Higher-priority instructions apply. No delegation, memory search, tests, product execution/imports, database, browser, model calls, or source writes. Return your own concise source report. Only the earlier listed affected source files and their directly necessary definitions may be read.

Changes since followup.md:
- HalfLifeEvidence now requires applies_to_quote at start of prefix before the half-life cue; coordinated/contrastive markers outside that target phrase cause rejection. This does not claim semantic/pharmacological proof. Existing exact formal/component provenance remains.
- mask_duration locates a unique exact raw token with digit boundaries and blanks its character span. Control calendar scans use that mask, so duration 1天 cannot erase suffix in 11天.
- Sourced half-life elapsed duration now compares date-only range (distance_min-1, distance_max+1): false if distance_max+1 <= duration, unknown with half_life_time_precision_insufficient if distance_min-1 < duration, else calendar constraints continue independently. Calendar-only and legacy dictionary paths retain prior convention. Applies to day/week/hour/minute sourced PK durations including partial-date bounds. Evaluator v18. New reason maps date_or_anchor_missing and native description/action records insufficient clock precision. Current DateValue cannot consume clock timestamps; source receipt/record alone does not close that gap. This limitation must remain explicit, not a claim that uploading clock text guarantees recomputation. Owner will address precision support separately if needed; no inferred clocks.
- No runtime artifact audit, no new published data or model calls; no assumption existing DB absent. Current source revisions invalidate new recalculation of older evaluator inputs.

Confirm actual changed control flow and remaining substantive bugs, particularly open interval comparisons and source-mask boundary correctness. Distinguish advisory improvements from must-fix errors; do not demand a fixed defect count. Source review is not clinical or final acceptance. Do not repeat unchanged whole-workflow descriptions.
