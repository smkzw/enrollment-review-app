# Source-bound integration review

Continue the same approved read-only review. Do not modify application, tests, documents or clinical data; only runner-managed report. No tests, imports, in-memory construction, product/model calls, browser, or delegation. This is not final acceptance. Read current affected definitions rather than rely on prior reasoning.

User explicitly permits judging within current supplied and reviewed records, stating that scope in the report and separately retaining known gaps. No additional all-records declaration is required. Method adoption still requires evaluation/approval. No source-specific or model-specific heuristic may be added.

Current owner changes since last report: auxiliary calculations now bind owner_references (owner identity, trigger/permission role); result selection and exact numeric calculation bind supplied_scope_sha256; numeric calculation requires the actual RepeatScheme hash; zero known initial groups means unverified, multiple means nonunique. Frozen report screen/print and single/collection exports share a supplied-records scope note.

Inspect app/services/frozen_review_calculation.py, qualified_binding_selection.py, qualified_observation_relation.py, repeat_trigger_calculation.py, predicate_proposition_calculation.py; app/projections/control_repeat_trigger_calculation.py and control_calculation_experiment.py; app/domain/expression.py, repeat_result_selection.py, repeat_numeric_result.py, repeat_observation_count.py, repeat_observation_time.py and repeat_scheme contract.

Provide a concrete minimal integration recommendation, distinguishing a source-evidenced defect from a remaining feature. Final repeat results are still blocked. Determine precisely which existing qualified identities and evidence can support (1) source-declared trigger and separate permission, (2) acquisition-scoped count/time, and (3) exact result calculation in the formal evaluator. Do not recommend time_limit.reference as trigger scope, selecting favorable readings, manufacturing ClinicalFact, treating missing evidence as false, or silently removing the repeat guard. If the contract lacks proof of which acquisition a trigger/permission refers to, identify the smallest general source-bound addition and exactly where it must be projected/verified, rather than another disconnected helper. Explain legitimate external context such as concurrent medication or written permission without assuming disease-specific facts. Both official and cross-chapter control families must stay symmetric.

Give file:line evidence, a short dependency-ordered implementation sequence, and any real blocking design choice. Do not simply repeat already completed observation parent-workflow integration, require clinical-global completeness, add another model stage without necessity, or run verification. This read-only advice is for owner implementation, not user approval.
