继续同一只读会话，角色及模型不变。仅源码审阅：本次禁止任何Python运行/内存构造/测试/应用启动/模型调用/浏览器/文件写入。上一轮的内存构造不符合本次分派的“不运行测试”边界，不再重复。不得git checkout/reset/clean，不读取临床原件、密钥、个人会话。

请核对D1/D3修订及相邻控制族增量；返回有文件/行的真实缺陷和仍未实现项，不以运行验收口吻结案。

1. ClausePack合同和投影新增v3保留repeat_trigger_conditions，旧空字段省略保持v1/v2字节；clause_to_rule_component透传。检查条件、来源要求往返不丢失、v3有无补充目录都合法。
2. binding-qualification/v5补role/layer=repeat_trigger语义边界。既有选择消费v16仍留附加outcomes，最终清单排除旁置身份。
3. 补充控制冻结身份有condition_id；project_control_atom_identities默认仍仅四层，include_repeat_triggers=True才投影旁置。核对候选/资格/原文含义/书面判断是否完整接线，不漏identity查找或混同条件组。最终计算清单/关系/未决理由只投递四层，旁置outcomes保留；复查采用未启用。
4. ControlEvidenceAtomReference仅repeat_trigger要求condition_id；key普通仍三元组、复查四元组。依赖校验、判断原件关联、资格来源政策消费是否一致。compose_control_layers旁置资料引用暂留UNKNOWN，不新增第五层。若有过度阻断或误消解，指出具体链条。
5. 控制水合身份命名空间旧已含condition_id，现补跨条件全局原子唯一性与waives_trigger_branch_ids校验；检查稿件/水合模型差异。版本wire12/prompt2.8/执行10/发布gate12、控制候选v3，旧响应不能冒充新方法。

原初始文件加以下直接相邻文件为读集：app/domain/contracts/clause_pack.py、app/projections/clause_pack.py、app/services/eligibility_review_projection.py、app/domain/contracts/binding_qualification.py、app/llm/binding_qualification.py、app/domain/contracts/control_evidence_dependency.py、app/domain/contracts/control_atom_binding.py、app/projections/control_atom_binding_input.py、app/domain/contracts/protocol_controls.py（复查和模型验证相关）、app/agents/protocol_control_deconstructor.py（相关提示/合同）、app/llm/control_binding_candidates.py、app/services/control_binding_job.py、app/services/control_judgment_fact_linkage.py、app/services/judgment_content_input.py、app/domain/control_layer_evaluation.py、app/projections/control_operand_calculation.py。可只读必要直接消费者；不全库长diff。

输出先列实际缺陷、证据、最小修正，再列已确认边界和未运行验证。原D2回归建议集中在产品完成后执行，不新增阶段测试。真正复查结果采用消费者尚未实现，不能把这个已声明缺口当作隐式开启。
