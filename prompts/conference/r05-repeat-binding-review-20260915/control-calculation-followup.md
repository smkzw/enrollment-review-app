# C03 read-only continuation

Continue the same approved read-only review session. Do not edit any file, run Python or in-memory constructions, tests, applications, browsers, models, or read raw clinical materials, credentials or private histories. Read only source via shell. Report source-derived defects and limitations; no runtime acceptance.

Owner requests an independent review of the next bounded increment:

1. `app/projections/control_calculation_experiment.py`: original evaluator body now private `_evaluate_control_selection`, returning original calculation plus atom truths. Public `evaluate_control_layers_experiment` signature and ordinary four-layer path remain unchanged. Auxiliary mode selects only projected repeat_trigger identities, composes no final layers, and gives the selection hash a distinct purpose.
2. `app/projections/control_repeat_trigger_calculation.py`: auxiliary DNF results use the shared arithmetic/proposition/source-conflict logic. `app/domain/control_layer_evaluation.py` adds `evaluate_control_condition`; inspect complete selected functions. No automatic adoption or final verdict is authorized. `app/projections/control_operand_calculation.py` and `app/projections/control_atom_binding_input.py` are adjacent source.
3. `app/domain/repeat_observation_count.py`: pure supplied-acquisition counting, no default limit. Incomplete coverage can prove exceeding a maximum but not compliance. Caller must later establish scope and distinct acquisitions; this is not connected to formal adoption yet.
4. Previous J1 not copied blindly: `app/domain/observation_relation_graph.py` permits first retest to refer to same initial/preceding group, detects contradictory agreed intermediate paths. J2 now separate origin_unresolved_fact_ids without changing legacy unclassified semantics. J3 explicit missing-identity rejection in predicate_proposition_calculation/repeat_trigger_calculation. Check source logic, not constructed data.
5. `app/domain/expression.py` adds repeat_relation_unverified to the otherwise generic unresolved deterministic predicate reason, without changing truth or professional-judgment reason. `app/services/frozen_review_calculation.py` version v23 binds changed reason behavior. Do not demand staged tests; user explicitly defers them until complete construction. State remaining runtime uncertainty.

Check regressions to existing ordinary selection semantics, accidental eligibility composition of auxiliary trees, observation/source/time/proposition completeness assumptions, conflicting-source preservation, and unsafe positive results. Distinguish a pure calculation's preconditions from verified clinical adoption. Missing final consumers and method approval are known pending work, not declared complete. Do not expand scope into implementing them. Use file:line evidence and severity, then concise confirmed boundaries and next necessary corrections.
