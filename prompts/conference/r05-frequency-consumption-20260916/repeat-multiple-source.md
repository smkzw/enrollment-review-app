# Read-only incremental engineering review

Same adviser, source inspection only. No file writes, imports, tests, services, browser, product calls or clinical files. Read complete changed definitions and decisive adjacent consumers, not earlier broad reviews. Report concrete defects with locations, minimum remedies and limitations. No clinical acceptance.

Current increment implements only explicitly source-declared per-initial-then-all/any, not governing initial date selection or across-chain numeric aggregation. RepeatScheme v4 adds nullable multi_initial_result (mode + exact source_excerpts); historical v1-v3 reject populated new field and retain serialization. Both deconstructors request v4 with no inference from existing observation policy. Consumer31/evaluator35/publication17, official wire18/controlwire23/prompt2.20/gate2026-09-16.3.

Inspect app/domain/contracts/repeat_scheme.py; agents protocol_deconstructor.py/protocol_control_deconstructor.py affected schema/prompts; services repeat_condition_selection.py (_condition_targets now covers un-repeated initial chains), repeat_multi_initial_selection.py, repeat_result_resolution.py, repeat_atom_calculation.py (fact/group and new nested selection evaluation), repeat_observation_ordering.py; contracts evaluation_result.py evidence sources; projections repeat_review_presentation.py and frontend/src/domain/reviewConditionNotes.ts. Existing graph/count source was reviewed in preceding step.

Full supplied scope, including roles and links, is required before chain selection; this is not a historical completeness claim. Each chain's invalid repeat retains unknown adoption, not clinical false or deleted record. Every chain is evaluated; any/all is then applied only when it agrees with source observation policy. Numeric means remain within each chain, no artificial ClinicalFact. Global ordering plus multi-chain policy stays unknown; dates never create links. Missing source policy remains unknown. Per-initial no-repeat conditional trigger must be calculated even if other chains have repeats. Nested absence, frequency and numeric sources retained for report.

Pay special attention to existing interface assumptions that only one policy_selection exists, identities of conditions and count scope, empty/unknown cases, old serialized artifacts, and whether UI claims adoption inconsistent with actual calculation. The new implementation has compilation only, no stage tests by user request. Distinguish bugs from explicitly unsupported modes.
