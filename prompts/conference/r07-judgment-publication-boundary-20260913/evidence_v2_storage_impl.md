# Review the implemented dual-lineage storage revision

Same approved read-only conference session. Do not edit, execute tests/migrations, access databases, raw clinical files, credentials, external models/network, browser or other agents. Return report to runner. Codex owns integration. Read complete changed definitions and adjacent necessary code, not only the worker report.

Frozen review scope: app/domain/contracts/review_evidence_scope.py; changed ReviewRun, AssessmentCandidate, FinalAssessment, ActionTransition, ActionRequest in app/domain/contracts/review.py; AgentCallContract in agents.py; changed formal-chain tables and new association definitions in app/storage/models.py; complete new app/storage/migrations/versions/0023_review_v2_evidence_lineage.py; affected AppendRepository/AssocSpec/configs/scope checks/action methods in app/storage/repositories.py; app/storage/review_reference_validation.py. Read migration manager/env/0010 only to verify actual transaction, backup, restoration and parent-FK behavior. Domain contracts now use explicit review/v2 rather than adding semantics under fixture/v1. No old database has been migrated.

Find concrete defects in this bounded implementation: old canonical payload preservation (including serializers and gate-owned fingerprints), exactly-one lineage with legitimate protocol-only AgentCall nulls, same run/episode/snapshot/complete revision/subject, actual parent FKs, historical child-row preservation, downgrade refusal, formal reference associations and empty transitions, readback mirrors and frozen references. Challenge migration commit/FK toggles against the actual manager, not generic advice. New source validates fact authority and locator membership; do not mislabel it as predicate proof. Call out whether later source corrections can invalidate historical reads incorrectly.

Known NOT implemented yet and not claims to repeat as bugs in this slice: V2 formal context assembly and original judgment receipt proof; V2 publication branch; HTTP/report/UI commands; integration/clinical acceptance. Existing legacy gates still require legacy snapshot and must not be presented as accepting V2. Current contract/repository changes are preparatory and not a new exposed formal endpoint. Identify any way these partial changes silently weaken the old path instead of rejecting. Positive semantic predicate acceptance remains disabled.

Owner direct checks only: 63 existing fixture objects preserve canonical payload hashes; compile/diff checks. Worker only constructed DDL, did not migrate. Latest owner added action trigger lineage CHECK and verifies actual FK pragma state rather than swallowing commit errors. No test suite was run per user latest sequencing; report runtime/migration verification as unavailable, not passed. Prior model report's claim that SQLite never rewrites references with foreign_keys=OFF is not accepted as a general fact.

Output severity-ordered source findings plus minimal precise corrections; avoid broad architecture restart or fresh test rounds. No user decision is currently needed. The application continues to use its own independent product harness, not this engineering conference model.
