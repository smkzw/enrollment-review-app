Same-session bounded continuation, source-only adviser. No writes, tests, imports of application code, databases, app/model/browser/network calls, delegation, personal history or other reviewers. Runner persists your answer. Prefer a few exact source reads; do not scan the repository broadly.

Current increment applied your previous concerns: relationship prompt only sends observation scope, not policy/count/conditions; both link endpoints require excerpts; scope errors use typed Chinese messages. Pair-model hook shares existing executor. Observation graph detects duplicate groups/cycles/multiple predecessors, but agreement is not qualified proof and no consumer activates it.

Resolve one specific remaining design question, not a general project review. RepeatScheme currently stores a source_condition trigger_excerpt, not an evaluable expression. Existing RuleExpression/AtomicPredicate and control evaluation already own deterministic values, semantic propositions and investigator judgment. We must support genuine conditional retests without inventing eligibility criteria or using model opinions as numeric truth. Count also needs explicitly sourced scope (per initial acquisition vs current episode); result combine needs its actual operation. We must avoid a new clinical expression engine, recursively repeating retest policies, O(n squared) jobs, or permanent blanket UNKNOWN presented as implemented support.

Read complete relevant definitions in app/domain/contracts/repeat_scheme.py, rules.py (AtomicPredicate, AtomicExpression, LogicalExpression, RuleComponent), control_evaluation_spec.py; narrowly inspect DNF wire schema in app/agents/protocol_deconstructor.py and binding input identity code only as needed. Which smallest coherent reusable source contract and consumer path can represent ancillary repeat trigger expressions for BOTH official and supplementary conditions? Consider a separate source-bound ancillary expression on the owning condition/component versus recursive expressions inside RepeatScheme. Give one recommended concrete field ownership, identity, prompt/wire, source validation and runtime binding strategy. Explain how it stays out of official IN/EX rollup, still uses existing verified qualification/value/proposition/judgment paths, and avoids cycles. If no existing safe path can simply be reused, state exact missing pieces rather than assuming they exist. Recommend a bounded implementation order, not a new framework. Do not write code.

Also inspect app/domain/repeat_observation_time.py for definite errors only. This newly added pure function has not been tested or connected to selection. It resolves no reference identity and grants no permission. Do not claim runtime or clinical acceptance.

Return concise recommendations with source references and any definite blocker; not a request to wait for owner. Prior reviewer recommendation to count repeat edges is insufficient for merged duplicate acquisition scope and has not been adopted.
