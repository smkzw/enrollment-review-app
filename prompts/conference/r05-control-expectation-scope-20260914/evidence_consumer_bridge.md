# Read-only consumer bridge continuation

Continue the existing approved C03 session, not a new independent opinion. Do not edit files, execute tests/assertions/imports, access databases, invoke product models, browse, or delegate. Read source; the runner saves your report. No raw clinical material outside this worktree.

Inspect the current complete definitions in app/projections/control_operand_calculation.py, app/services/control_binding_job.py, app/domain/contracts/control_evaluation_spec.py, app/domain/control_layer_evaluation.py, app/domain/expression.py (evaluate_bound_component_experiment and time/value functions), app/services/frozen_review_calculation.py, and design sections 17.1.1 and 17.3. Expand only for decisive dependency questions. Cite file:line.

The next dependency is a real consumer of source-backed selections, not another candidate-only wrapper. Current conditional arithmetic deliberately does not certify semantic correspondence; model candidate agreement is not proof. Automated correspondence remains subject to isolated evaluation and user approval. User defers tests until the product is constructed. We must build a coherent chain without bypassing that boundary or leaving every valid case permanently UNKNOWN.

Provide a concrete minimal implementation recommendation: what existing selection/evaluation contract can carry the source-bound atom operand into the four-layer composer, which scope checks are mechanical, and where unverified semantic/object/source eligibility must remain explicit. Distinguish an isolated evaluation entry from final acceptance. Consider value comparison with event_membership versus interval_condition versus source_validity: an out-of-window observation must not imply clinical threshold failure; what result/reason must the adapter return? Multiple observations cannot be arbitrarily first-selected, and absence cannot prove a negative condition. Semantic and investigator modes must not be disguised as deterministic arithmetic. We need a bounded next code unit, not a new framework or a list of general cautions.

Also review latest arithmetic changes: one validated frozen input/index per batch, all selected identity pairs validated before arithmetic; record_time stored UTC lacks original calendar date, so returns UNKNOWN rather than truncating UTC into a clinical day. Existing PartialDateRange reconstructs its full month/year through DateValue precision. Determine whether any immediate concrete correctness bug is present. Do not demand staged tests in this assignment or claim clinical acceptance.
