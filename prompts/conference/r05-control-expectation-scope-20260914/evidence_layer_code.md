# Same-session bounded source review

Read-only C03 adviser. No implementation, tests, assertions, imports, DB/model calls, browsing, or recursive delegation. Read only relevant workspace source; final report text saved by runner. User defers all staged tests until construction finishes. Do not repeat a proposal as an implementation fact.

Review the complete new app/domain/control_layer_evaluation.py, app/domain/contracts/control_atom_binding.py, app/projections/control_atom_binding_input.py, app/services/control_binding_input.py, app/llm/control_binding_candidates.py and affected private-to-public time arithmetic in app/domain/expression.py. Read parent DNF contracts and decisive validators as needed. Existing full-review and control-template coverage guards remain; no production acceptance or runtime validation claimed.

Priorities: branch-local waivers and alternative obligation activation; unknown exceptions/applicability; group scope; no collapsing mixed mandatory/recommended atoms into a final exclusion finding; input identity bound to the publication and current fact snapshot; candidate references remain candidate-only. The layer composer only exposes intermediate truths/activations, not eligibility or verified semantic evidence. Inspect for source-defined semantics missed by the composer, especially reciprocal links, implicit triggers, replacements and control-level time constraints. Identify exactly what cannot be treated as a final result yet.

Check source-policy changes accurately: EvidenceRequirement/template flags now permit None specifically with control origin; repository exact comparison allows matching None. Do not repeat the earlier erroneous claim that unknown policy cannot persist. Source freshness arithmetic was extracted without semantic value creation; it still needs a date-range consumer, not a fabricated point date.

Report prioritized real defects and the smallest repair, with file:line. If no defect in a boundary, say source review only and list unperformed final verification. No repair, no code output, no separate task or follow-up initiated by you.
