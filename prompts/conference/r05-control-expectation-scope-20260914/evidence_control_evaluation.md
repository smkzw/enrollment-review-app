# Same-session continuation: full control evaluation boundary

Read-only C03 adviser. Inspect current source independently, report final text only. No file edits, tests, assertions, imports, database/model/application calls, migrations, browsing, delegation, or raw clinical material outside this worktree. The user defers tests until construction finishes. This is engineering review, not clinical acceptance.

Read full affected definitions in protocol_controls.py: condition, obligation, exception DNF and ProtocolReviewControl. Inspect existing expression evaluator, frozen_review_calculation, predicate binding proof inputs, evidence expectation contracts/projection/service and control publication projection. Also inspect current EvidenceRequirement and template changes and repository integrity checks as needed. Avoid broad logs/inventories.

Current changes: each minimum evidence has explicit node IDs and source policy with nullable flags, full result validity constraint and specified/not_specified/unknown status. Independent control origin and shared v3 templates preserve those fields. Publication now saves derived requirements/templates in the same transaction without mutating old workflow due lists. Subject coverage explicitly refuses control templates until applicability and full validity are implemented. Existing full-review guard remains. No product run or clinical acceptance has occurred.

Propose the smallest generic integration for four-layer control evaluation using independently verified atom evidence, not fact-type buckets or model verdicts. Explain branch-scoped waivers and replacement obligations, unknown applicability, mixed modality per obligation, and how raw evidence coverage can remain separate from eligibility blockers. Do not collapse mixed modalities into one control-level flag. Do not generate protocol-wide templates based on one subject's applicability. Preserve recommendation/advice in review without treating it as exclusion. Do not infer application from an evidence match.

Identify existing reusable proof/evaluation interfaces with file:line references. Decide which exact missing contract must be implemented first; distinguish decisive code findings from design choices. Examine whether full TimeConstraint can reuse the current evaluator without fabricated ClinicalFact or semantic predicate. Flag source-policy unknown as distinct from absent patient information. Give a concise implementation sequence and actual prioritized risks, not a rewrite or new framework. No implementation and no tests.
