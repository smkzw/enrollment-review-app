# Same-session bounded continuation: evidence source policy and storage boundary

Role: read-only adviser, same C03 packet, not product execution or clinical acceptance. Continue the prior review but independently inspect current source. No recursive delegation, tests, assertions, imports, application/model/DB calls, migrations, installs, browsing or source edits. Read source files only within this worktree. Return your report as final text; runner saves it. No raw clinical material outside the workspace.

The owner adopted explicit workflow_stage_ids on minimum evidence and per-node derived identities; implemented wire v2/prompt v1.8/gate v2/execution v2. Historical absent new fields serialize unchanged; new publication requires explicit nodes. New app/projections/control_evidence_requirements.py derives independent origin IDs; page-review prompt v15 includes compact references, not fact acceptance. Shared expectation storage is not yet integrated. Current complete-review guard stays in place. User explicitly defers tests until whole build.

Inspect complete relevant definitions in app/domain/contracts/rules.py (EvidenceRequirement), evidence.py (EvidenceExpectationTemplate), protocol_controls.py (minimum evidence); app/projections/evidence_expectation_templates.py and evidence_expectations.py; app/storage/repositories.py requirement/template persistence; app/services/fact_expectation_gaps.py, evidence_expectation_projection_service.py; app/projections/control_evidence_requirements.py; docs engineering design section17.3. You may inspect adjacent source as required, but avoid broad inventories/logs.

Resolve a narrow integration question: existing EvidenceRequirement has exactly component or procedure origin; source-policy defaults allow transcription and do not require contemporaneous objective source. ControlMinimumEvidence currently has source types but no equivalent policy. We must not guess clinical constraints, turn a lookback into examination validity, or automatically mark conditional/recommended evidence absent as blocking. Determine the smallest coherent shared pipeline design, not a parallel whole evidence subsystem.

Consider explicit source-policy fields in new semantic output with unknown distinct from false, preserving historical bytes; a typed control-origin reference plus independent requirement ID; node closure proved through the catalog without modifying published workflow due IDs; and a conditional requirement's applicability checked separately from raw coverage. Do not propose deriving source-policy values from drug/disease strings, source type alone, or blanket calendar-lookback mapping. Evidence review due node is not original collection date. An earlier source can support a later explicit review. Distinguish source coverage from eligibility conclusion.

Return prioritized actual risks with file:line evidence, a concrete minimal field/consumer/storage sequence, and tradeoffs only where source cannot decide. Explain how unknown source-policy and unknown applicability should remain visible without falsely passing or blocking. Also inspect the current sibling-visit projection change for scope correctness. No code output, no patch, no test execution, no claim of clinical approval. One response only, no self-launched follow-up.
