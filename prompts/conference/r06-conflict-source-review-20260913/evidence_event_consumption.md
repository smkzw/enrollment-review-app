# R06 same-session follow-up: event/exposure conflict consumption

Continue the approved evidence_single_object advisory role, same model and session. Read-only tools within current worktree only. No source/DB edits, no model endpoints, no delegation, no internet, no personal configuration. Do not create report files; return the complete report for the runner. This is engineering review, not clinical acceptance. Prior source-successor work is retained; challenge this next step without repeating the whole audit.

Read complete relevant definitions in app/services/eligibility_review_projection.py (_phase3_conflict_groups and project), app/domain/contracts/facts.py (event/exposure/conflict), app/storage/active_conflicts.py, app/storage/fact_rule_link_repositories.py if present (otherwise locate the actual FactRuleLinkV2Repository), app/domain/gates/assessment.py (derive_gate_gap_types), related tests, and design section 17.5 plus recovery plan T3. Follow direct dependencies as necessary.

Observed current behavior: _phase3_conflict_groups filters out all event/exposure groups. Ordinary fact conflict with obsolete fact references now fails closed rather than silently remapping; a real isolated-copy GET returned that honest failure and now translates to a safe 409. Do not recommend ID rewrites or deleting groups to make UI pass. Do not touch artifacts clinical database. Existing event/exposure source successors have explicit parents and preserve conflicts.

Determine the smallest coherent next implementation for retained event/exposure conflicts. Is propagating each entity's fact_ids through existing fact-to-component links sufficient provenance for affected clauses, or does that overclaim which attribute is disputed? Account for a shared base fact used by two events, dispute only in an event date while an unrelated numeric clause uses its underlying fact, same fact supporting multiple conditions, corrected heads and dangling members. There is no authority to invent entity-clause semantic links or autoaccept model bindings. Distinguish a truthful unresolved association warning from a confirmed relevant clinical conflict. Keep a single evaluator path and avoid a new inference engine. If additional versioned relationship evidence is genuinely required, specify exact fields and safe interim behavior, not a broad abstraction.

Return source:file:line findings, concrete minimal patch recommendation, tests including a valid non-conflicting counterexample, and remaining uncertainty. Address how the current UI can preserve the unresolved dispute without incorrectly declaring unrelated clauses in conflict. Do not classify all future clauses as blocked by default. Also identify existing reusable code/contracts, not just new designs. Complete one bounded pass; do not wait for user feedback.

Output headings: # Conference Output: r06-conflict-source-review-20260913 - evidence_single_object ; ## Output.
