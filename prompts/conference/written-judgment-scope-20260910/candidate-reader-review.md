# Read-only independent review: candidate search source and reader

Continue C03 same-model review, primary zcode/GLM-5.3:max only, no fallback. You are a read-only reviewer, not executor or final clinical authority. Do not modify app/tests/docs/data, read credentials/raw patient materials, run real model calls, access network/install tools, or operate local platforms. Use existing .venv for bounded synthetic tests.

Review current app/llm/judgment_search_reader.py and tests/v2/llm/test_judgment_search_reader.py against app/domain/contracts/judgment_search.py and source builder. Also inspect new FactAuthorityValidator.validate_and_get_revision and source builder's reuse: original validate returns None, complete validation remains required on every invocation, no cache. Read relevant recovery plan section2 and current product page_review_harness.py/gemini_transport.py, not worker reasoning. Owner altered the initial implementation to require finish_reason=stop, strict whole JSON + duplicate key rejection, and preserve raw completion in successful receipts; independently verify, don't assume those claims true.

Criteria: complete image byte/source binding before request, no swallowed pages or false absence, both handwritten and printed channels including multiple and ambiguous candidates, no patient-specific rules, native product transport only, requested versus response identity and usage receipts honest, non-stop/invalid/truncated output always failure with raw evidence preserved. This function intentionally makes one call, with no retries/persistence/authoritative target loading; do not count these known unwired boundaries as hidden completed functionality. Determine if local contract itself has actionable defects.

Important design question for next integration: avoid an expensive duplicated all-pages-per-requirement loop. Identify the smallest integration with existing product workflow that preserves full supplied-source coverage, authoritative target/version binding, failure bookkeeping, and clinical applicability. Recommendations must not assume context.time_text equals measurement date, not label supplied scope complete external clinical materials, not turn found into accepted, and not accept metadata category as investigator judgment. Discuss whether one page search for a compact group of unresolved judgment requirements can preserve distinct per-requirement coverage without forcing new generic framework. Do not implement this proposal or give formal clinical approval.

Run narrow new reader+source tests and report exact results with severity/file/line findings. No generic wish list; separate current bugs, known integration boundaries and recommendations needing validation. Return to specified report only; no extra agents or tool installation. Same vendor/family independence is reduced; acknowledge limitation.
