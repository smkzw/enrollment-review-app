Continue same read-only conference session. D1 was reproduced with real temporary SQLite and actual correction job, then implementation changed. Read actual current app/services/fact_expectation_gaps.py, fact_correction_service.py, fact_normalization_executor.py and tests/v2/services/test_fact_correction_gap_reprojection.py; adjacent source/contracts as needed. No edits, network, uvx/tool installation, credentials, real clinical data, model calls or delegation. Allowed pytest: the new correction-gap tests and smallest existing synthetic tests as needed. Owner reran new tests plus correction_job and normalization_persistence:73 passed,48.82s. This is not acceptance.

Audit actual code, not worker report. Verify shared logic preserved, authority/run lineage source selection, node/local impact, older unresolved items and candidate/gate merging, rollback/append-only behavior and lack of circular imports. Important owner concerns requiring decisive tests or source reasoning:
1. select_reprojection_source_runs traces ancestors only for current correction target, not every other currently active already-corrected entity. Could that lose real source risk or replace known risk by generic uncertainty?
2. _prior_state_needs_unreconfirmed_pad guesses fallback vs concrete from gap kind/source_coverage. Can a concrete observation_unverified on a weak/absent expectation disappear in a second correction, silently upgrading to observed? Stored projection lacks input signal provenance. Is the heuristic safe? Do not endorse merely because conservative in the existing one sample.
3. Merging candidate gates across selected runs lets any historical accepted candidate suppress another rejected candidate with same requirement even when superseded. Can this clear risk without explicit resolution?
4. Return whether a narrowly scoped correction is sufficient or new persisted input-signal provenance is genuinely required. Prefer exact source replay; never infer clinical resolution from numeric correction, copy a prior gap as current clinical fact, or gather all historical runs. No broad architecture rewrite.

Report priority findings with code/tests, minimal recommended changes and explicit remaining tests. You remain reviewer, not executor. D2/R1/printed analysis remain pending independently; do not conflate their completion with D1 repair. Engineering executor was different variant GLM-5.3-Flash:max, same vendor independence limitation noted.
