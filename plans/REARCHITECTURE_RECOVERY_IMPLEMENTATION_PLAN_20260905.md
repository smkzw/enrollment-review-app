# 入排审核系统 R3 接回修订实施计划

> 2026-09-17 当前执行入口：用户已采用 V3 内置方案Agent + 主OCR/局部VLM/人工真实来源路线，取消全量双VLM及所有角色统一65K要求。以 `.trellis/tasks/09-11-e2e-eligibility-review/research/ENROLLMENT_REVIEW_AGENT_RECOVERY_V3_20260917.md` 与该任务 `prd.md/design.md/implement.md` 为本轮增量；分工见 `AGENT_ASSIGNMENTS_V3_20260917.md`。下文冲突的旧路线/顺序仅作历史，非当前默认。此次只完成分工文档与Goal正文同步，未运行产品、未宣布验收；原Phase剩余范围保留，claims_complete=false。细节按实际修改章节由接手者局部更新，不重建规划。


**2026-09-16当前优先项**：指定双模型为显式MTPLX 27B Optimized-Speed xhigh + Flash-Next Optimized-Speed xhigh，严格串行；两者基本视觉调用通过，正式装卸接线未完成。新摄入改为原图准备后直接双读，不以外部OCR为前置；已补版本政策、完整修订和视觉规范化源码，真实整链待验收。先闭合MTPLX资源所有权/预检/取消、重处理入口，再从隔离真实资料继续T4，不恢复旧GLM额度等待。详细证据集中在09-11任务checkpoint最新段，claims_complete=false。

**2026-09-15最新用户授权继续，当前未暂停。** 当前记录 `CHECKPOINT_20260915_RETEST_TRIGGER_PAUSED.md` 文头恢复段优先（文件名保留历史）：T3/R05已接旁置复查条件双族来源合同、冻结身份、候选和资格，附加结果不进入最终入排表达式；下一步为复查许可/次数/期限/结果采用及报告。Phase5/5.5仍未验收；历史测试数字不能代表当前结果，完整建设后集中测试。

修订：2026-09-12。当前任务 `.trellis/tasks/09-11-e2e-eligibility-review`；唯一工作树 `.worktrees/phase5-clinical-facts-profile`，审阅基线4caf392。当前文档替换此前累加的相互矛盾“下一步”；历史过程可从Git基线、本任务/父任务HANDOFF和artifacts回查，不能当作当前运行命令。

依据：`docs/REARCHITECTURE_R3_ENGINEERING_DESIGN_20260905.md`（§6.1、§17）、`docs/REARCHITECTURE_FINAL_DESIGN_20260812.md`；问题证据见当前任务 `ENGINEERING_REVIEW_20260912_CODEX.md`。旧Phase编号和临床退出条件保留；本表T0–T7只是跨阶段修复顺序，不新增Phase。

## 1. 当前边界

后续时间处理构建：当前消费/控制计算v5已接单次date_range有效期，未获新评测批准；控制级时间仍需明确、逐原子、源支持的用途映射，并在投影、资格、计算及保存中共同消费。不得把范围未知改成本次不适用，不以过期报告证明临床条件不满足；最终统一验证包括有效/过期/部分日期/缺日期及独立时间命题。

最新构建补充：补充要求消费v4/控制计算v4已支持有证据的ANY真与ALL假；反向结论在观察范围未证明完整前不得采用。报告原因已接线，未验收。剩余实施须补明确的范围完整性依据、全局时间语义及控制族判断，不得把单向支持误标为多观察全功能完成；最终集中验证覆盖异时同值、缺页、例外及范围不全。

2026-09-13最新执行调整：前端遵循kangzhe-design-3d站点轨、仅宽屏桌面，真实浏览器使用ego(lite)。不另建阶段性测试轮次；以下测试条目作为整链完成后的统一验收清单，开发中仅保留必要的编译与关键正确性检查，不因未跑完整测试声明验收。T4的集中验收后置不阻止T5–T7依赖已具备部分的实现，但不能提前标记Phase5/5.5验收通过；未批准的新语义对应仍隔离，不能为赶进度接入明确判定。每次双模型调用后根据冻结输入、遗漏/错误关联、耗时和重试证据评估通用改进，不立即重复全例或扩大模型横评。临床来源和发布边界不因验收后置而放宽。

- 接回工程审阅已转入持续实现，具体已实施范围以PROJECT_CONTEXT当前摘要为准；初始审阅“尚未修改或调用”的记载不代表现在状态。文档完成不等于产品完成。
- 当前指定部署为MTPLX 27B Optimized-Speed xhigh + MTPLX Flash-Next Optimized-Speed xhigh，充分输出额度且严格串行；基本调用通过不代表临床质量验收。临床harness须跨模型/方案/疾病通用，模型名仅在部署和传输能力层出现。不得按历史默认恢复旧模型或档位。
- 产品使用自有harness直连端点、显式env；不运行个人Hermes/OMP/Pi，不运行时搜个人凭据。工程执行/独立会商按最新全局机制，按实际需要派发、120分钟长等待，不重复催询。
- 不修改原方案、病例原件、旧临床库、手工IE或既有报告；不reset/git clean/批量清未提交资料。真实运行从合法入口在隔离数据域新建。
- 历史已成功读记录可按旧身份回放；不是新模型/提示实测。成功步骤可按兼容合同复用，不重跑整例只为换显示名。
- 分项用药对应已获准设计和隔离扩测；尚未获准自动进入正式事实。两轮针对性复核已获准，不能自动改写事实。缺研究者判断符合完整性条件后作为报告发现，不等待用户再次确认。
- 每次实质里程碑合并更新一个checkpoint。连续执行已授权计划，除真正完成、用户要求、必要决策或无法自行解决的阻塞，不人为停顿。

## 2. 阶段实况

| 范围 | 可确认进展 | 仍未通过 |
|---|---|---|
| Phase0–4 | 历史归档，领域/来源/任务和摄入可复用 | 本轮发现跨章控制未成为正式审核输入，回修Phase3，不重开整套架构 |
| Phase5 | 事实、事件、用药及Profile生成/存储/展示已实现，真实运行产物存在 | 增量Profile完整性、原件临床QC、事实与判断关系；claims_complete=false |
| Phase5.5 | 双主读持久任务、覆盖、规范化、判断检索/复核及UI已接线 | 泛化关联质量、当前模型配置/实跑、有效金标复跑、完整视觉验收 |
| Phase6 | 实时确定性投影与工作台已提前实现 | 存在R01/R02/R08风险，正式ReviewRun/FinalAssessment未有当前实例 |
| Phase7 | 打印视图和基础Action合同存在 | 冻结报告、真实Action关闭/重新审核、批量与正式摘要未闭合 |
| Phase8/9 | 历史测试/安装片段可以复用 | 新项目留出临床验证、完整打包/用户发布批准未完成 |

当前库总体2884事实/296事件/97暴露混合多个节点及运行，不能当作已核实完整率。筛选可投影的当前校正后集合516事实/46事件/22暴露；最新Profile可能只取最后一次运行。当前ReviewRun/FinalAssessment/ActionRequest均0。历史三档同hash缩图不足以证明各viewport。详见报告，不混加不同时间测试成绩。

## 3. 下一执行顺序

### T0：锁定真实起点与通用模型合同（优先，范围受控）

输入：当前代码/config/env、任务冻结路由、最新用户指定组合；只读核对服务归属。先跑配置测试，不先发长病例请求。

1. 在既有配置及PageReaderRoute里明确A/B的effort、输出预算、provider与模型；当前A high、B xhigh。针对性复核不能降成high；判断检索、选择性视觉、方案语义和整理节点检查实际配置来源。旧第三模型回退当前禁用，不删除历史解码。
2. 将模型专属要求限制在adapter：能力/档位映射/JSON/图像/流式结束原因/物理预算。逐调用点收敛，不能仅去掉模型白名单然后接受任何不兼容输入。兼容其他模型是可扩展性要求，不是本轮启动其他模型。
3. 新语义任务初始65536、length最多一次131072，共享思考+正文；检查实际物理上限。若不支持须在提交前明确说明，不隐性继承8K/16K或降档。失败类型、请求/有效身份/额度、用量和缓存有回执。
4. 预检env/端点/模型归属；最小无病例视觉+JSON探针核effort和额度。单次成功不叫病例质量验收。MTPLX已有服务先核归属，不抢占他人测试；本地平台和OCR协调串行。
5. 测试：默认组合、显式配置优先、无凭据、能力不支持、实际模型不符、xhigh贯穿复核、预算及截断一次、旧任务身份不变/不同模型不可续旧任务。配置差异与运行差异分开报告。

主要文件：`app/config.py`、`app/llm/page_review_harness.py`、`page_review_transport_options.py`、`independent_vlm.py`、`app/services/targeted_page_review_jobs.py`、现有协议/整理路由及相邻测试。新增能力小模块仅在能消除重复时使用，不把通用改造扩成新框架。
退出：当前所有实际启用的产品语义角色仅选指定二者，旧结果可读；通用临床代码无模型特判；请求证据证明指定档位/预算，而非只改.env或文档。

### T1：先阻止错误明确结论（R01/R02/R07/R08）

1. 用当前真实投影只读回放和不含病例专名的最小反例复现：大类病史匹配无关条件/例外、同值不同日期、多值不同时间、缺判断却明确通过。
2. 收敛唯一组件求值/缺口验证服务，实时投影与正式发布同用。不采用“同fact_id触发与例外即矛盾”或“需研究者判断永远不允许终态”补丁。
3. 按原子对象、属性、时点/窗口、单位、极性及允许来源绑定事实；类型alias只选候选。合法同一事实多谓词、真实例外仍可成立，未核实语义不能用exists放行。
   绑定链按设计§17.1.1执行：先冻结原子条件/既有事实/原件输入与边界测试，再沿现有normalizer/发布链形成受限候选和验证记录，最后统一实时/正式消费。已确认supported_requirement_ids仅检查编号存在，FactRuleLink仅到要求/组件，PredicateObservation仅镜像Evaluator；不能复用其名称冒充语义验证。先完成隔离正反例与独立审阅，正式自动采信仍须既定接入确认。当前两项strict xfail只是缺陷证据，不算该步骤完成。
   最新隔离进展：r5完整6批/12次中11次stop，B3在900秒超时，整轮约60分钟failed_final，37候选未采信；独立补测仅B3，564.813秒stop、8候选，旧任务不改。新入口核来源/分批/提示/路由/原请求一致，不重跑其他11次；7200秒等待为独立运行条件，不作单变量提速结论。候选对象/属性/来源允许性与时间作用仍待验证及留出评测，不得直接将同键交集或numeric_value接入正式审核。2个旧R01 strict xfail仍未关闭；r4为旧提示历史，不当作新组合临床证据。R07实时v2计算已去除伪装v1的model_construct，但正式发布证明与历史工件仍未闭合。
   2026-09-13独立审阅补充：区分资料有效期与事件/间隔条件。窗外检查不能直接变成明确不符合，洗脱不足也不能一律改为未知；复用已发布source_validity_window并补有版本的时间用途/证据选择合同，不按疾病、药名或比较符猜用途。核对旧规则哈希及历史回放；六个严格预期失败记录未完成项，不算通过。见`reviews/codex_conference_t1-temporal-scope-review-20260913_review.md`。
4. 要求选择、判断检索、资料期望、评估共用适用性规则。补determination_mode与source_types不一致反例；没有摘要表示尚未查完，不得写“未见”。
5. 保留四态：未完成、候选未核实、完整供给范围未见、已核实判断。无阈值异常无判断时报告无法判定及研究者补充要求，不暂停整个审核。
6. 修复后重放筛选/基线，逐项解释结论变化及来源；不直接修改原临床库。

主要文件：`eligibility_review_projection.py`、`domain/expression.py`、`domain/gates/assessment.py`、`judgment_search_job_service.py`、`fact_expectation_gaps.py`及相关仓储/投影测试。
退出：错误放行反例消失，合法例外和已核实判断没有被全部拒绝；缺口与结论一致，乱序不改变结果；两节点差异有源证据。

### T2：把每个问题正确带到原件（R03/R04）

1. 为组件/控制项提供稳定身份，官方编号与父关系分离；API decoder、前端key、URL、筛选、统计、打印一起改，禁止同号首条兜底。
2. 文档版本+entry/page identity+locator贯穿引用；真实逐字条款/事实与摘要分开。接上现有原件查看器locator、红框、滚动回选，不另写PDF组件。
3. 多文件同页码、一父多子、深链刷新、错误/缺坐标和校正版本测试；原件缺失给出明确中文反馈，内部ID不在临床主视图。
4. 1920/2560/3840真实浏览器逐项点击，等待数据和图像解码后截图，记录viewport、图片naturalWidth和来源身份；不能只等标题出现。

   2026-09-13增量：已分开source_text与摘要，旧接口无原文不冒充原文；投影/API17项、前端13项及构建通过。真实隔离副本因历史争议来源引用未衔接仍不能生成审核，现以409明确说明而非500笼统错误，三宽屏错误页已查看。不得把该结果称为正常条款/原图页面验收，须T3闭合后重验。

退出：点击任何子项只选中该项；标题/原因/对应原件一致；无坐标诚实降级；已有来源可可靠返回。首屏问题优先、信息密度和分栏可用，不额外改导航。

### T3：补跨章正式要求与完整档案（R05/R06）

**当前增量覆盖下方版本沿革**：频次消费v6/scopev4；RepeatScheme/v4、官方wire18/控制wire23/prompt2.20、资格v31、计算v35/发布v17。有限明确范围逐期间及辅助复查频次已接；新增来源声明的逐初查先采用、再all/any组合，逐组独立次数与数值、无复查条件和报告依据，不跨链拼数值或按日期造对应。两次只读增量会商及编译/tsc完成，非临床验收。无界/部分范围、外层组合、日期选初查组及其他聚合顺序仍显式未决；后续核R06并收齐T5–T7源码，最终集中产品模型/原件/ego验收，不重复已接链路。无新产品调用、方法批准或claims_complete。

当前T3/R05：逐次许可、采用计算与报告已接源码；条件选择v2/资格v29、观察v5/消费v8、计算v33/发布v15。最近/最早与复查政策仅在共同指向唯一检查时可用；无复查政策须有原文，条件性缺席只认完整触发FALSE。共用辅助依据须有明确范围原文；书面拒绝不等于许可成立。频次scope/v3、官方wire16、控制wire21/prompt2.18、读取v4/消费v5已接来源期间、端点可能范围、计数上下界、逐次/单日原文与汇总约束交集、两族封存和报告说明，父工作流v7复用原任务执行及方法授权。未证明异次不作确定次数，单次部分日期不扩为每日发生，至少/至多不作精确次数。最新源码只读会商与编译不等于运行验收。剩余顺序：复杂窗口→复查旁置频次→聚合排序/多初查→R06原件核对与T5–T7→集中完整运行及临床验收。claims_complete=false，不重复已有读取链，不用UNKNOWN冒充完成。以下逐日记录为沿革。

最新增量：既有观察任务的辅助来源/归属已接两族输入、提示、双路对账与来源资格，任务v3、观察消费v5/资格v20；结果记录与辅助记录分列，多检查归属保留未决，不新增模型阶段。下一项改为逐目标初查/前次的辅助证据限定及触发/许可计算，再接正式结果采用；不重复搭建已完成输入/对账。历史读取保留v19，新方法仍须集中评测，未获自动采用批准。

最新接续：次数/期限计算已由原文资格消费者调用并存入材料（观察v4/资格v19），不再是无调用纯函数。初查组计数与显式前次/初查/节点日期计算已接；当前节点范围计数仍须成员证明。下一依赖是既有观察任务增加旁置来源的检查归属：现组仅含owner事实，其他测量的触发事实不能靠日期或ID交集猜归属；辅助事实须与owner结果数值严格分列。之后完成逐目标触发/许可和最终采用，不另建模型阶段、不重复已接次数/期限模块。最终集中测试仍待完整构建。

复查取证范围的进一步实作：旁置条件已增加逐原子角色及逐字来源，官方dnf-v10、控制wire15均强制逐项解构，旧空映射不补造；来源、重编号/水合与哈希已贯通，不另增模型阶段。下一步按具体目标复查，将初查/前次角色与核实观察图对应，外部背景依命题原文核对；然后接次数/日期/精确结果与正式求值。角色标签已存在不代表目标对应已完成，原文未明不得默认为外部背景。

2026-09-15资料范围取舍已获用户确认：按本次已提供并核对的资料判断，不额外索取全部复查资料声明；已知缺失仍单列。观察消费v3/选择消费v18保存该范围及其哈希，结果选择、精确计算与方案/观察图身份相互绑定；许可命题已进入原旁置来源合同，不能再列为尚无表达途径。报告页面与打印、单份及汇集导出已加入统一范围说明，尚未浏览器验收。下一实作仍为把合格操作数、逐所有者触发/许可、次数/期限和结果计算接入正式审核，不把范围批准视为新方法采用批准；集中验证后置，claims_complete=false。

2026-09-15当前接续优先于下方沿革：观察关系已接方法/来源/父工作流和封存材料，尚非结果采用；repeat-scheme/v3明确合并结果是否包含初查，两族解构已接；结果组选择及精确数值计算已构建为纯函数，图和方案身份绑定，同次记录不重复计数。七轮C03源码审阅与所有者取舍见当前checkpoint和review，不代表运行。接下来只补正式链缺失部分：逐条件原文触发范围（不以期限参照替代）、研究者许可命题、所供资料范围与实际缺件分离、计算结果与最终求值及原件解释。不能再把已接关系任务列为未开始，也不能用新增纯函数或更多未定报告替代正式消费者。完成后统一验证和取得适用的方法采用批准。

2026-09-15最新增量：官方与控制旁置条件均已接候选/来源资格/原文含义及书面判断材料，ClausePack v3保留完整旁置条件供重组；默认最终表达式仍两树/四层。C03四轮源码审阅后修复投影丢失、版本Literal遗漏、观察次序清单和回指冲突；旁置条件共享求值和检查组计数已实现为纯计算，尚无正式采用消费者。观察关系v2逐原文保留初查/复查及回指类型，计算v23保留具体复查未决原因，不解除repeat_relation_unverified。仅编译/source/diff，无产品或集中验收。接着完成关系图的方法授权及来源消费、作用域和次数期限核实、采用结果与报告，不新增独立编排器。

2026-09-15当前复查实作（优先于下方版本沿革）：已完成来源合同与两族解构源码、未定保护、同要求整组原文关系双读及持久回执重建，显式正式入口可路由该任务但未启动。下一步贯通父审核编排、证据关系图、触发条件/次数作用范围/期限/结果采用及授权冻结报告；不能把关系一致当复查许可，不以无关系推断无复查，不以日期排序、记录条数或同血样替代临床观察身份。source-contract独立审阅已回收并作最小修订，新关系模块待续审。仅编译/tsc/diff，无运行或临床验收，claims_complete=false。执行事故恢复详情在artifacts/recovery-20260915/RECOVERY_REPORT.md；共享大文件暂由所有者直接写入，独立节点仅只读。

2026-09-15语义最近/最早已接共享日期选择器及双族资格消费，控制v3/wire9/prompt2.5/gate9、消费v14/计算v20；不跳过未核实候选内容或日期，未选记录单列。E03+两轮C03源码回看完成，编译/diff通过，无运行验收/方法批准。官方解构既有policy必填和排序字段。下一项条件复查、去重事件次数、时刻精度及R06/T6/T7；全产品构建完集中测试，claims_complete=false。

2026-09-15正式命题衔接：显式semantic_proposition进入共用双路原文核实、回执重建、版本化资格、工作流及冻结报告；命题v6/作业v3、命题消费v6、选择v5/消费v13、工作流v5、计算v19/发布v6。正式及补充命题必须有完成的核实任务（无可核对原文时保存零配对检查，不调用模型），不能绕过步骤后以UNKNOWN冒充完成。待核实原件单列且不作为已确认事实；范围政策不匹配仅隔离该配对，不改原回答。两轮C03源码意见已整合，不构成验收或新方法批准。

半衰期已补明确方案内单一时长的来源/单位/适用范围候选、发布核对、冻结计算与报告；无值时保留合法倍数要求，范围/复杂限定不自动取值。三轮C03发现并修订控制日历数值误认、数值后缀遮盖、限定对象和日期无时刻边界；组合窗明确FALSE优先于另一条件UNKNOWN。最后两项修订仅所有者源码与编译检查，非新独立验收。DateValue仍无具体时刻，后续须把来源绑定的时刻精度贯通后才能自动解除边界疑问。下一顺序：条件复查、去重后的事件次数、语义最近/最早与时间精度；全部构建完再集中验证，claims_complete=false。

2026-09-15期间相邻修订（优先于下方版本沿革）：官方计算v15补未核实期间/频次保护及正确缺口；控制命题已接声明期间双路来源核实到冻结报告，命题v5/作业比较汇总v2/内容消费v4/控制组合v10，无新批准。C03三轮源码审阅后补原子完整来源、合同限制和确定结果说明的页面/导出一致性；编译/tsc/diff通过，无产品调用或阶段测试。仍须完整构建条件复查关系、官方未来命题及频次消费，不能用保护性未定代替功能完成；频次先核临床事件身份而非记录条数，复查先核原文关系而非日期顺序，持续行为不能由意愿声明证明。此段不改变最终集中验收与自动采信授权边界。

2026-09-15当前构建状态：官方及补充要求共享观察政策、逐所供事实核对说明、唯一日期排序、确定性any/all和未选原件解释已接源码；当前计算v14/发布v5/选择消费v11，无新增采用批准。C03三轮同会话源码复核后修复额外范围拦截、错误选定文案和窗口先后歧义，控制四层报告已接审计；只做编译/tsc，全部运行验收留到完整构建后。接下来先完成条件性复查的通用来源合同与消费，核对R06来源衔接剩余项，再收齐T6/T7；不得以已提供事实枚举冒充临床资料齐全。批量重新识别及同源历史耗时参考已接线，后文相反描述仅为沿革；无样本不预估、货币费用未知仍未知。

2026-09-14来源衔接增量：正式冻结上下文已区分旧流程到期清单与独立发布控制来源，跨章资料不再要求反写历史due_requirement_ids；按同一来源身份公式核目录、具体访视、内容和完整政策。多观察政策及隔离四层计算已构建，仍未取得语义/选择范围/来源资格证明，不能以此宣布正式审核或T3完成。接续优先完成这些证明的正式消费和报告发布，避免继续只加候选工件；最终集中测试需覆盖合法跨章模板进入冻结上下文、错误来源/访视/政策拒绝和旧上下文读取。

1. 沿现有ProtocolControl候选校验/发布事务，持久化已核实控制目录，与RuleModelRevision同源版本绑定；RuleSet/ClausePack/审核计划/资料期望/报告共同消费。
   2026-09-14构建状态：已接不可变目录、0024迁移草案、显式作业/完成检查点与同事务发布；状态API可提供待发布身份。节点桥接使用原必做项目与草稿ProcedureCatalogMapping，不猜名称或直接套旧编号。ClausePack v2及双读/重建/冻结上下文共同携带目录，历史v1保持原序列化；提示v14共享摘录文本，保留四层分支关系，读取身份加入实际消息哈希。未运行迁移、模型或阶段性套件。
   前端已实现控制目录查看、四层条件与原文展示、发布身份协调和明确失败重试；仅完成TypeScript检查及生产构建，未作浏览器或临床验收。C03 r05-control-expectation-scope-20260914已完成独立审阅，采纳显式节点绑定和逐节点独立身份；不采纳回溯窗口等同检查有效期、仅允许判断节点收集资料的建议。
   2026-09-14共享接线：第三来源列及0025迁移草案、共享要求/模板v3、完整有效期状态和发布同事务派生已构建；未知来源政策可原样保存，旧字段不被重写。时间求值拆出纯日期接口，旧事实入口仍附原事实身份。仅编译/导入，迁移未执行。C03同会话续审已回收，所有者拒绝“未知不能存”和“义务不需证据绑定”的建议，详见对应review。
   接续状态：四层原子身份与候选读取、分支组合及纯日期接口已构建，但候选尚非证明。资料atom_refs已贯通解构/发布/查看，现有组合器逐引用保留条件真值或义务激活态及强度，不把多用途合为一个布尔值。控制书面判断目标和正式模板重建已接来源，尚非临床求值。剩余顺序：产生经验证的原子证明，消费控制级时间及完整来源有效期，接独立资料覆盖/未核实条件，再完成统一临床缺口与冻结报告。不修改历史已发布流程。未知适用性不得变成无条件缺口，推荐与必须不合并。不得移除当前“只算官方条款不能形成完整审核”的限制，直至这条链完整并在最终集中验证中通过；现阶段不据此标记T3完成。依用户要求，构建期间不新增或运行阶段性测试，最终集中验证以上合同及真实原件。
2. 目录与官方规则的重复、补充、例外及冲突显式保留。未材料化的控制不能计作已审核；不可按药名/时间点补丁。来源非本节点相关的管理内容可标不涉及，避免全方案机械膨胀。
3. Profile、FactRuleLink、EvidenceExpectation统一读取当前authority的校正后集合，run仅追溯；事件和暴露引用闭合。跨快照继承明确验证，不按整个subject粗合并。
   - 2026-09-13已统一Profile、FactRuleLink、EvidenceExpectation与校正后的当前事实选择：run不再缩小范围；先取修订链头再排除正式校正目标，避免更早旧值复活。累计补读事实/事件/暴露反例及相邻145项通过；最后校正消费者修订另80项通过，详见T3_CUMULATIVE_PROFILE_20260913.md。此为集合选择修复，不代表R06引用衔接或临床QC闭合。
   - 后续补齐实时审核消费者：其原先仍先排除再折叠版本，真实仓储发布两修订的反例证明撤回最新记录后会引用旧记录。现同样调用current_fact_heads；审核/API/有效事实19项及档案/校正/关联扩大76项通过。mode=ro隔离库仍可投影81组件，不将其结果视为临床正确，R01/R07保持开放。
   - 只读真实隔离库发现24项事件旧事实引用均有同语义新头，但旧定位未全部保留；不得直接改fact_ids或删事件。依工程设计§17.5细化版本化来源衔接，独立审阅后做原件证明与留出反例。还须核对旧run未解决风险的跨run继承，防止补读无该项导致风险消失。
   - R06已实现显式事实来源继承、事件/暴露/资料要求及未解决冲突的只追加来源修订，临床内容及状态不变；三版发布、更正后再追加、历史重放与第三矛盾值保留均有回归。冲突当前选择统一，不隐式换事实编号；来源未衔接仍整体撤回。C03同模型两轮独立审阅完成，209项相邻及后补24项聚焦通过；该版全后端终态4900通过、4既有合同工件失败、3跳过、8严格预期失败，970.18秒，不涵盖后续R01/R07/T2变更。接续：事件/暴露争议的条款消费；24项历史旧引用按原件核对后从合法受控入口重建，绝不追补伪造旧继承关系。只读复查仍未闭合，R06仍开放，不能用合成测试宣布临床完整。
   - 不直接复用 `active_entity_ids_after_corrections` 当完整输入：其中先过滤悬挂引用的事件/暴露/期望，可能隐藏需要处理的遗留引用。先明确等价事实合并与显式校正的引用处理及审计，再验证全量→补页→更正；不能靠过滤掉旧事件或仅取最后run形成“小而成功”的档案。
4. 连续“全量→补一页→更正一项→基线新增”回归；以前核实内容不丢、替代链不悬空、筛选历史不变；真实当前库离线重建只写隔离产物。
   事件/暴露争议已作为未确定条款影响的独立提醒接入，非按基础fact关联扩散；缺成员拒绝，合法争议不改条款，正式更正后只显示新争议头。93项相邻及33项补验、15项前端通过，两轮C03已审；实际新提示条视觉、已验证的具体条款影响及历史原件来源重建仍待完成。
5. 分项用药仅沿已批准隔离实验：分开核药名/剂量/用法/时间，处方/购买与实际使用不能混，未知日期不补。独立留出来源达标后再提正式接入决定。

退出：真实跨章限制确实进入组件评估；当前完整档案不因最后run缩减；异常与未核实仍可见，来源逐项闭合。实验成功不代替自动采信批准。

### T4：Phase5/5.5 临床与产品退出验证

1. T0–T3回归与只读回放通过后才创建指定双模型的新正式隔离任务，使用当前权威方案和节点原件；不手工插入coverage或人工事实。
2. 按页格式分层验证少量代表样本，再完整31001筛选/基线；新模型身份与旧GLM/Gemini不同，不按旧调用成绩签收新组合。
3. 逐项原件QC覆盖事实、日期/否定、手写归属、事件、用药暴露、Profile及要求对应；文件命名/主体归属不明先查证，禁止自动归人。
4. 冻结有效金标、评分和来源版本；历史宽松score与有问题金标先纠正后冻结，不把旧0.969直接转移。并集事实召回≥0.95，金标负判定静默漏判0；另报双源采信召回、未对应、错误冲突、手写及错误关联。新增采信/性能阈值提交用户批准。
5. 记录模型与非模型阶段耗时、TTFT/首正文、输入/输出/思考、重试、缓存与未知用量；本地串行，远端可与其并行。不删上下文或降档追求速度。
6. 从正式HTTP/UI完成上传/处理/读页/整理/复核/档案/原件。工程测试、真实调用、临床QC、三档交互、发布分列结论。
7. Phase5与5.5分别复盘回写主Plan。未决条款可合法报告，但数据缺口未核清不把claims_complete改true。

退出：对应阶段的原件及有效评分证据通过。未通过继续修通用原因，不开始另一轮平台横评或擅自放宽临床边界。

### T5：Phase6/7 正式审核、行动与报告

2026-09-15批量OCR接续：单例与批量重新识别源码均已贯通，批次复用维护回路，成员精确冻结、响应丢失幂等重连、停止归属核实、当前状态与本批记录分列、结果精确深链且不自动启用。E03回收及C03源码审阅后补数据库忙延后和无变化结果处理；对应review记录实际fallback及限制。下方“批量重新识别待完成”已被此段替代，但运行/临床/视觉验收尚未进行。下一实作是有证据的耗时费用展示、复杂观察选择及T6/T7，最后集中完成T4与全产品验收，不以源码完成代替Phase退出。

2026-09-15最新实作（优先于以下沿革）：正式今日工作/项目目录、批量审核v1及分页重连、单份与中心筛选跨页勾选报告合集已接线；报告逐份重读冻结来源，缺项拒绝导出，不重算临床结论。单例OCR重新识别已接独立命名空间、任务重试和页面明确核对/构建/启用；上传冻结并沿用已确认识别页，校对不跟随后来活动指针，原缓存/原报告不改。C03四轮源码复核及所有者修正已完成，详见r05-ocr-reprocessing-design-20260915 review。仅源码/编译/tsc，不是运行或临床验收。剩余：批量重新识别、耗时费用估算、复杂观察选择、T6/T7全产品验证和交付；不因任务completed声称临床完成，不越过T4最终验收。

最新增量：计算v12补当前节点补充要求的已核实判断缺失与研究者待办；正式今日工作已接真实待办和近期冻结记录预览，不含示例计数/完成率，不重算入排。首页只读查询及深链构建完成，尚未实际运行验收；项目全局摘要与批量正式操作仍待构建。现有宽屏视觉沿用工作台，最终统一ego验收，不借新页面取消T4/T6验收。

当前接续（2026-09-15，优先于以下沿革）：双族书面内容、非数值命题任务、正式入口/工作流v4及批准后的消费已接源码，未启动应用或评测。单观察范围在现有双读内核实，全部合格配对不得遗漏；语义时间复用同事实合格日期与冻结节点计算。资格提示v4/命题提示v3/求值规格v2/授权v4/消费v8/计算v11不得继承旧批准；旧时间规格不自动补写。下一顺序：核实研究者正面依据与缺失路径、补全范围及复杂观察选择的实际产品边界、收齐T5–T7剩余功能，最后统一实际双模型/原件/ego三档验收。未执行统一验收不写完成；不因局部条件可计算而宣布完整语义审核。当前没有可确认废弃的共享脏文件，清理须先核具体引用。

2026-09-15构建接续：正式后台工作流、开始/取消/重试/进度及从已完成工作流生成报告的HTTP和页面已接线，尚未运行验收；配置只引用既有方法批准，不生成批准。取消异常兄弟任务不阻止有效子任务停止；保存状态与核对状态分离。接续按C03 control-content-scope源码会商：先补控制族书面原文的来源对应（已新增独立control_judgment_fact_linkage、复用原有精确原件索引，未接正式消费），再扩展同一内容任务/回执、双族工作流及采用映射；旧谓词输入身份须保留。来源对应与书面内容准确不代表命题真假；语义/研究者命题求值需独立明确结果合同及版本化消费，不能把content_supported当TRUE。最后才统一验证并取得适用的采用批准。不得把暂保UNKNOWN当这些能力已实现，也不得用未决报告替代完整目标。

2026-09-14核心缺口复盘（C03 `r05-core-consumption-review-20260914` 已终态，无fallback，仅源码审阅）：下一实现从外围准备校验转回正面消费。先以既有要求归属、页身份、摘录及assertion_basis建立检索候选到已发布事实的对应；匹配本身不造事实、不证明判断内容。补判断内容与编码值的双路核实及单独评测范围，再由版本化消费者送入既有求值；仅来源可用或双路一致不能代替该证据。随后为官方与补充要求提供方案原文支持的观察选择政策和范围完整性证明，不默认最新/第一条/ANY，也不将多观察永久排除出目标。生产、消费、批准和正式界面须一起闭合，不能以另一份accepted=false汇总替代交付。最终统一验收再验证真实摘录匹配召回和临床正确性。

2026-09-14当前接续：计算v8/发布v4已接来源资格与书面内容的可选联合证明，官方单观察正面值可进入既有计算；同准备/配对/回执/评测/批准均需匹配。要求级检索未见仍按显式归属报告，正面证明只解除本要求判断疑问，不消除缺文件或其他条件；普通数值兄弟项不受内容核实失败连带影响。C03正面链两次续审已终态，最后状态/混合项修正仅源码与编译检查。仍需多观察选择、控制族与实时一致性、实际评测/采用批准和正式界面接线；无归属历史不猜配、不新增批准。最后集中测试须覆盖正面与拒绝内容、两判断不同状态、未来/同阶段不同节点、缺文件、OBSERVED矛盾、过期期望、不同准备和回执改变，尚未执行，T5未验收。

正式提交HTTP适配已构建但未注册：受试者/节点路径先与冻结上下文一致，再调用服务端核对作业授权及事务保存；仅身份入参，不收客户端事实/授权对象。缺工件不写结果。批准来源签发、入口启用与界面整合仍未完成，当前仅编译检查。

同链后续源码已构建：候选完整配置回执的正式核对、服务按作业编号重建逐任务授权并在保存点内调用正式发布，尚未注册或执行。接续重点为有明确用户来源的方法批准入口、评分实质与当前部署核验、完整语义/专业判断/多观察消费及正式HTTP集成；最终集中验证包括授权与结果同回退、并发重试、历史不改写。不得仍把逐任务授权列为完全未实现，也不得把源码实现写成已验收。

2026-09-14方法证据增量：类型化评测清单/明确采用回执及独立工件类别已接正式发布v2和资格消费者，重建元数据不改变历史任务哈希；C03两次源码审阅后修正路由空值类型。尚无批准签发、产品启用或临床验收。接续完成候选完整路由回执、受控明确批准入口和服务端逐任务授权，不把调用方手组对象作为最终命令合同；评分与来源需最终实测及用户采用决定，不自动制造批准。宽屏待办布局仅编译检查，最终ego验证后置。

2026-09-14本轮实现合并（非验收）：整批发布函数已构建但未启用，官方结论和补充要求共用既有行动链；未知适用条件按组一项，普通未核实不冒称来源冲突，保存与历史读取绑定完整来源。正式项目待办只读分页及页面已接入，旧审核未办结不因新审核隐藏，直接定位原报告办理；不复用trial状态，不假报全局总数。必要编译通过，最终迁移/并发/原件/三档浏览器验证未进行。方法批准与完整语义可用证明、正式命令、真实今日/项目摘要、重审和批量仍待完成，不能标Phase通过。详细回执与采纳见PROJECT_CONTEXT和本轮review。

2026-09-14控制历史接线（未验收）：新增只追加控制快照与0026迁移草案、冻结历史读取、接口和报告原件入口。C03源码审阅已终态；补独立名称/强度及不完整提示，未把时间戳当可展示报告证明。没有运行迁移/产品/阶段测试。下一步仍为同次正式发布与控制行动，必须绑定整批选择及控制子计算各自哈希；不能将两者直接等同，不能绕过采信授权，不能仅凭本次读取接线收口。

2026-09-14补充结果增量：冻结计算v6消费control_review_outcome逐义务结果，保留模态/命题/独立身份、事实定位和分支路线，不合成官方判定。C03源码审阅后修复共用时间窗口及假冲突、空选择含义；控制资格未定原因透传而非改为没记录。原件/运行验证未做。正式存储仍缺控制结果持久结构及其历史/行动消费；不能只完成官方条款后宣布整批报告完成。前述语义/观察政策/专业判断及授权签发待办不因此取消。

2026-09-14核实结果消费增量（未验收）：已完成回执重建到冻结计算的禁用路径和官方/跨章双类完整性检查；修复摘要哈希混用及手工夹带。官方未核实条件进入共享布尔计算时标为observation_unverified，不冒充未记录；详细原因随选择材料保留。源码C03意见已修订，编译/diff通过，不是运行验收。接下来按同一链完成：(1)来源完整性及语义/研究者判断的可用与不可用状态，不能以全部UNKNOWN代替；(2)对应要求级原因、研究者行动和补充控制正式结果；(3)基于已批准评测的授权签发及整批原子事务发布；(4)统一历史报告和集中验收。当前不签发批准、不启用自动采信；实际物理模型身份、原件QC和最终ego验证仍未执行。

2026-09-14官方资料归属增量：现有方案语义输出、系统编号装配、冻结输入及资格核对已传递可选逐条件引用；缺失引用不猜配，历史空字段不改变指纹。E03实际完成、C03源码审阅后修复重复编号歧义和提示冲突，删除重复校验分支。明确引用仅说明政策归属，不证明事实可用；混合未归属资料继续保留待核实，默认来源政策的历史行为未更改。新发布方案与正式消费仍待整链验证，不据此启用自动采信或收口T5。

2026-09-14来源资格构建：新增显式后续作业消费完成候选的原始请求/响应和比较，保留逐条件政策/节点/数值日期/原件及去重分批。E03与C03后已修正串来源、缺导入、说明字符串比较及回执/身份闭包，尚未注册或自动采用。下一实质缺口是官方逐谓词政策归属及经评测的语义证明生产、控制临床消费和整批正式发布；不能继续把更多accepted=false汇总当这些缺口已闭合。仅源码/编译核查，无阶段性测试/产品调用；整链验收仍待完成后统一实施。

2026-09-14闭环会商裁决：V2正式写入半链尚未接通，不能先以旧类型alias生成正式结论。按现有任务/仓储构建完整但未启用的来源资格、语义核实、版本化绑定、共同计算与整批发布链；经评测与授权后统一启用，不新增队列。已有两路候选任务summary现逐批保存来源绑定的声明比较（官方v7/控制v5），包括单路及空项，不自动采信；它是后续核实输入，不是语义证明或T5验收。正式写入与计算均要求新冻结要求全集版本，历史读取不查当前head替换原依据。

已有只读投影缺陷可在T1/T2先修，不冒称阶段已完成。正式推广须T4前置通过。

1. 正式审核命令创建ReviewRun，冻结要求全集、事实与资料版本、检索/复核记录和算法；复用T1唯一求值服务，不再新增第二套推理。
   已接共同计算的逐条件显式资料选择，冻结计算v5不再按类别补配；触发和例外逐项完整列出，无对应保留空项。整批返回包含官方及跨章四层条件计算，目录/节点/完整事实同源检查和选择哈希已接入，未核实来源仍保留。仍缺经来源资格核实的绑定生产者、跨章临床消费与正式发布命令，不能据此宣布整链完成或启用候选自动采信。
2. 持久FinalAssessment和ActionRequest，责任方/要求/证据/节点/来源明确；未决情况可完成本次审核但不表示可入组。
3. 人工办结需说明及对应回应原件，只表示办理完成，不表示临床要求已满足；补资料/确认后的临床重评产生新审核，不直接改旧结果。缺研究者判断报告继续，其后回应可追踪。
4. Reports读取冻结审核及时间，打印/页面一致；今日工作/项目摘要/批量取真实记录，不恢复trial数据。移除正式入口占位不代表删除所需功能。
5. 正式链测试包含未决报告、撤销/补充/节点切换、历史重开、来源校正受影响重算和多组件父级汇总。

2026-09-14实现进展（未验收）：新review/v2上下文/存储草案和历史GET已接入，Reports改为历史列表与固定记录，不再打印实时投影。完整ClausePack、对象登记信息、方案版本及流程名称随审核保存；不随现行投影器或登记更改重建。逐条件观察随正式结论存储；报告原件面板复用连续页控件，核对原审核资料版本和三项页面身份，真实认证坐标才标框。当前办理状态单列，不能当原审核时状态。人工办结/重新办理命令、幂等与修订检查、独立回应资料版本、追加历史和前端原件选择已构建；回应不得改变原结论。新版具体待办文案推导已补，使用冻结条款及共享责任方/期限规则，仍待发布器消费。尚待正式发布/谓词证明及真实摘要整合；旧Reports测试及新增合同统一在最终验收中更新/核对，原库未迁移，不宣称T5收口。

### T6：Phase8 留出验证与性能/视觉完善

从真实原方案新建非校准项目（至少覆盖另一II/III期场景），原资料/人工IE只作事后对照。完整临床和浏览器验证，每种历史错误类别有反例；任何产品提示修改在已见/留出同时回归。
按设计§6.1分别验收病例、方案与模型三个泛化层级：先冻结提示/Schema/算法及校准清单，再使用具有不同要求结构的未调优方案；记录接入新模型是否仅修改配置或传输适配、是否保持原有临床标准。被用于调优的留出资料移入校准清单，不能继续冒充独立留出。暂不扩大模型横评不影响先建立这些合同及确定性反例；未实测层级明确保留未验证。
仅在主要正确性通过后优化大JS块、重复投影/闭包查询、图表和动效。不重跑无需变化的模型步骤；相同来源内容寻址缓存需版本/失效校验。性能目标用分层实测和用户批准，不凭单页或并发回归期间数据定P95。

### T7：Phase9 交付与清理

最终关闭验证补充：正常关闭已改为等待当前步骤/并行波保存完成，在下一持久边界归还租约，不再领取后续步骤，也不新增失败尝试；全部步骤已结束时仍正常结算。应用await工作线程实际退出后再释放数据库，不采用五秒超时后继续释放。需在统一验收覆盖成功、失败退避、取消、并行波和再次启动；强制结束进程仍按既有过期租约恢复，不能宣称强制结束等于优雅关闭。

正式启动器指向V2、明确env/数据根/端口、无开发依赖可启动，模型不可用仍能看既有资料。隔离检查双击入口、重启/取消恢复及备份回读。

2026-09-14构建：新增显式 `create_app(browse_only=True)` / `app.api.v2.browse:create_browse_app`，仅打开已有兼容资料库（SQLite mode=ro/query_only），不迁移、不恢复任务、不调用模型、不注册运行配置。复用已有项目/受试者/原件/Profile/冻结报告读取，命令请求明确拒绝，界面显示仅查看状态；正常模式不自动降级。新增 `scripts/start_v2_desktop.command` 选择正常/仅查看，`scripts.run_v2_desktop` 显式产品env/资料根、OS分配独占环回端口、服务启动成功后才开浏览器；`app.api.v2.desktop` 同源提供V2及预构建前端。构建清单记录非演示/非stub/同源标志和每个bundle文件哈希，启动拒绝旧静态UAT或混改产物。临时目录正式构建及文件校验通过（首次generateBundle早于首页输出导致缺首页，已按post顺序修正）；Python导入/编译、tsc、zsh语法通过。仍须最终隔离验收双击、模式切换、原件及报告、数据库未变、不启动后台任务和无开发依赖安装。仅查看未提供实时计算/任务操作；不将它冒充全功能离线模式或交付完成。旧启动脚本未删除或替换，桌面最终交付需明确采用新入口。
依清单收敛旧入口/不用的缓存和过程文件，原件/历史验收/未决错误/会话数据库保留；不因未跟踪就删除。更新阶段复盘、交付文档和未完成项，正式发布由用户批准。

## 4. 原工程纠偏清单的归属

| 原项 | 当前落点 |
|---|---|
| 1 env/预检 | T0，已非唯一阻塞；凭据探针和临床质量分开 |
| 2 生产依赖 | T7，保留历史无dev安装证据，改依赖才复验相邻 |
| 3 中性传输命名 | T0/相邻修改；保留已有AR修复，不按历史类名误报调用DeepSeek |
| 4 方案docx-only | T3/T4入口回归；病例PDF/图片不受影响 |
| 5 legacy双轨 | T1/T5核正式入口和import隔离，不借旧审核器填新链 |
| 6 主仓漂移 | T0只读核身份，确认引用后才同步/删除；本轮不合并主checkout |
| 7 巨型模块 | 每项只提取共同职责，新增双读独立文件，不大重构 |
| 8 lint/端口 | 每项聚焦及T7，核PID/服务所有者，不触碰8900其他项目 |

## 5. 验收与记录纪律

每个T项完成的证据单位：原始问题及反例、改动边界、代码/提示/输入版本、明确成功或失败结果、受影响来源/浏览器检查、未验证范围。独立review挑战冻结产物，意见由所有者按来源裁定；审阅者修复后不自证独立接受。无模型真实调用不称新组合通过，无原件QC不称临床完成。

最先执行T0配置/冻结/探针与T1失败反例，随后完成T1纵向修复；无须用户重复批准普通工程细节。新采信算法、临床阈值、范围更改和最终发布需要相应批准，准备具体证据后再问。

新goal prompt见当前任务 `GOAL_PROMPT_20260912_REVIEWED.md`。现有活动目标API只提供complete/blocked而不支持改写objective/恢复暂停；不得谎报旧目标完成来替换。新文档完成不自动宣告工程目标完成。
