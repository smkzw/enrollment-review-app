# Codex Conference Review: phase5-slice58j-d001-phase-evidence-design-20260826

Date: 2026-08-26

## Verdict

**部分采纳并进入最小实现。** 会商证实“来源闭包”和“期别临床闭包”必须继续分离，且矩阵既有 `phase_disposition` 只能作为待核对主张。下一步先生成单位级正向依据视图和异质代表包清单，不全跑 137 包，不翻转完整声明。

## Boundary Compliance

- 两名参与者均为只读顾问，没有修改应用、矩阵或源方案，也没有代替 Codex 做最终医学验收。
- Cursor `auto` 单轮完成，无 fallback。
- 验证器仍把 `cursor-grok-4.6-medium` 作为 `general_grok46` 的兼容逻辑节点名；本轮实际主路由是 `cursor-cli/auto`，`grok-build/grok-4.6 medium` 仅为未触发的后备路线，兼容名不代表另行调度或模型替换。
- Qwen 主会话连续返回未执行工具调用而无报告；三次同会话补全耗尽后，主控按声明链尝试后备 Muse。runner 因夜间调度重新水合回 `alibaba/qwen3.8-max:xhigh`，新会话最终形成完整报告。该过程不是干净的 fallback，已保留原始不完整输出并在指标中记录；有效报告的实际模型仍为 Qwen3.8 Max。
- 测试者模型没有参与本轮会商，执行/会商与后续真实浏览器测试角色保持分离。

## Hermes

实时会商路由未声明 Hermes 参与者或主席，因此本轮没有 Hermes 会话，也没有用 Hermes 替代 Pi、Cursor 或 Codex。所有有效/无效尝试均由 `conference_session_runner.py` 保留模型、会话、耗时和恢复证据。

## Participant Outputs Reviewed

- 一致且采纳：单位级来源/期别证据应是主视图；矩阵行处置不是结构单元真值；多来源行须逐单元闭合后再汇总。
- 一致且采纳：“未限定某一期”、混合父章节或标题邻近不能升级为跨期共用；共同适用须证明同一义务家族对两期均成立。
- 一致且采纳：代表包按异质风险选择，至少覆盖明确 II 期流程、共同章节未知叶节点、表 5、结核/妊娠、结果有效期/复测、对侧期引用和治疗后污染，不使用随机数量抽样。
- 采纳：开放问题按唯一受影响行和单元统计；当前 136 个问题只影响 78 行，不能把问题条数当剩余控制数。
- 采纳为后续设计问题：表 5 对 EX-18 的补充关系需要明确单一评估/展示聚合语义，避免未来一项暴露被总体结论重复计数；当前关系存在本身不等于已经发生重复判定。
- 不采纳：把 `su-34232d...`“Ⅱ/Ⅲ期评估和程序一致”或盲法共享语句广播到所有入排、研究治疗和表 5 单元。桥接证据只能在同一义务家族且有可靠交叉引用时使用。
- 不采纳：因官方 IN/EX 编号或给药前时间锚点就把 `unknown` 单元确定性判为 II 期适用；这些信息证明控制性质/时间，不证明期别范围。
- 不采纳：矩阵引用面外 117 包“永远不必运行”。5.8d 可先用 152 个引用单元完成矩阵核对，但全文声明最终仍要求所有待处置单元有权威处置。

## Conference Panel Review

下一实现应只增加一个派生研究视图，不新增第二套临床语义引擎：

1. 以 152 个实际引用单元为主键，展示冻结来源、结构期别、引用它的矩阵行、矩阵待核对处置、现有 Agent/人工处置（如有）、冲突与未决原因。
2. 将 `blocked_structural` 与 `unresolved_semantic` 分开：单元内部混合 II/III 成员时先修结构边界，不能交给单值期别 Agent。
3. 行汇总按全部锚点合取；任一锚点未知、混合、对侧或与行处置冲突，行继续阻断。
4. 生成异质包覆盖清单，先验证门禁与上下文充分性；代表包通过后再决定其余矩阵相关包，全文剩余包留在后续完整处置范围。

## Main-Venue Codex Review

- 本地重算确认：82 行、155 个锚点、152 个唯一单元；116 个仅 `unknown`、35 个仅 `phase_ii`、1 个 `mixed`；60 行标为跨期共享、22 行标为选定期适用。
- 136 个期别问题由 76 个 `PHASE_UNRESOLVED` 与 60 个 `PHASE_SHARED_SOURCE_MISSING` 组成，实际影响 78 行；仅 4 行当前无此类问题。
- 全清单只有 2 个结构 `shared` 单元，分别只证明双盲设计和评估/程序一致。它们均不在 152 个引用单元中，且语义适用范围不同，不能成为全局共享通行证。
- 两行 `cross_phase_shared` 只引用明确 `phase_ii` 单元，说明人工矩阵处置确实需要与来源结构并列展示而非直接采信。
- 妊娠检测行引用一个 `mixed` 单元和一个 `unknown` 单元，且混合原文同时包含 II/III 治疗期节点；应先标记结构阻断，再决定原子化/拆行方式。

## Codex Independent Verification

- 使用当前闭包矩阵与 1,840 单元冻结清单独立重算上述计数，没有读取旧 1,689 单元汇总。
- 逐字复核两个结构共享单元、两条“II 来源却标共享”的矩阵行、妊娠混合行和表 5→EX-18 关系。
- 前序矩阵来源闭包聚焦回归 `92 passed`，本轮会商未修改产品代码，故没有伪造新的测试通过结论。
- 本轮属于后端临床设计挑战，不含 UI 变更；浏览器/视觉检查及三路测试者尚不应启动。

## Final Decision

接受“单位级期别依据视图 + 结构阻断/语义未决分层 + 异质代表包”的方向。先用确定性生成器产出可审阅视图和覆盖报告，并为共同章节错误广播、矩阵处置非权威、混合单元和行级部分闭合增加回归；之后再选择少量真实语义包。保持 `claims_complete=false`，5.8d 继续进行中。
