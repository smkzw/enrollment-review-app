# Codex Execution Review: phase5-slice59h-20260827

## Verdict

拒绝接受旧冻结计划下第 39 包的语义结果，并要求按修复后的 131 包新基线重跑合并用药相关包。接受本轮作为一个发现系统性期别边界缺陷、完成修复和建立反例的诊断轮次；不接受任何旧包结果自动迁移为新计划的临床验收。

## Worker Outputs

- `worker_01` 在旧 137 包计划中运行第 39 包，模型和门禁将 `body.p345` 至 `body.p356` 全部判为“选定Ⅱ期适用”。报告同时注意到 W52 等异常线索，但没有继续追查表题与后续说明的结构关系。该绿色结果被 Codex 拒绝。
- `worker_02` 在旧计划第 69 包运行合并用药总则，11/11 通过门禁，并正确指出表 5 逐行时间窗不在本包内。结果只保留为旧计划诊断证据。
- `worker_03` 在旧计划第 70 包运行表 5 的 `r0` 至 `r11`，12/12 通过门禁；其交接已主动指出“较长者为准”、来氟米特清除剂缩短洗脱及中成药两周例外未完整进入理由。结果只保留为旧计划诊断证据。
- 三个执行者的外层路线按实时资源合同改写为 Cursor CLI/auto；产品内实际语义调用均为 `mtplx-qwen38-27b-optimized-quality`、`medium`、16384 输出上限，未静默替换内置模型。

## Manager Assessment

本路由没有独立执行经理，Codex 直接核对三份执行报告、旧/新冻结计划、原文定位和真实回归。第 39 包失败不是“模型偶发误判”：`表 2 Ⅲ期临床研究阶段流程表` 是普通正文段，没有大纲层级；旧结构规则只把表格本体识别为Ⅲ期，没有让表题继续约束表后说明，导致 `p345` 至 `p372` 以 `unknown` 交给模型猜测。相同缺陷也让 `表 1` 后的 28 条Ⅱ期说明成为无谓语义目标。

修复将明确的分期研究流程表题视作窄范围结构上下文，作用持续到下一个真实结构标题。该规则针对文档结构语义，不包含 D001 项目特异文本或条款编号。

## Codex Independent Verification

- 新基线将 `body.p314` 至 `body.p341` 确定为Ⅱ期，将 `body.p345` 至 `body.p372` 确定为Ⅲ期；`p345` 不再进入Ⅱ期语义包。
- 稳定原文定位比较：旧目标 1301、新目标 1245，恰好移除上述 56 条；新增 0、其余共同原文的适用期/范围/摘录变化 0。全部 1245 个共同目标只因新冻结计划身份而重算内部 ID。
- 新基线为 3581 个结构块、3405 个期别图块、1848 个全文覆盖单元、1245 个语义目标、131 个包。源 DOCX SHA-256 保持 `362443131f0d384c82c80f6a37396084f7d3301b51162201749c0488b0f2dd98`。
- 稳定差异报告 `diff-qc.json` 已替换错误的派生 ID 比较；报告为 `accepted=true`，无意外删除、无遗漏预期删除。
- 新计划中：`body.p748` 位于第 63 包，`body.t10.r0` 位于第 64 包，`body.t10.r12` 位于第 65 包。旧计划第 69、70 包不能作为这些新包的验收。
- 聚焦期别修复回归：`73 passed, 5 warnings`；完整 `tests/v2/protocols`：`787 passed, 58 warnings in 215.71s`。
- 旧计划已复核包 36/57/60/61/67/78/79/110/121 均标记为不可自动接受；即使 8 个包可按相同原文集合映射到新序号，也必须在新计划下重新运行和复核。

## Boundary

本轮只修复分期流程表题对后续说明的结构作用域，并重建 D001 Ⅱ期冻结基线。没有修改原始方案、受试者资料或既有临床报告；没有运行新计划的语义包、受试者审核、浏览器、视觉或独立测试者。`claims_complete=false`。

## Hermes

实时路由没有声明 Hermes worker 或 manager，未伪造其参与。三个工作项使用 guard 生成的执行命令；外层执行路线改写与产品内 MTPLX 调用身份分别保留，二者没有混写。

## Cleanup Decision

审计和 review gate 通过后归档 runner 可重建的提示与过程输出；保留紧凑执行报告、拒绝审查、稳定差异报告、新冻结基线及临床运行工件。下一步只运行新计划第 63、64、65 包，不扩大到其余 128 包。
