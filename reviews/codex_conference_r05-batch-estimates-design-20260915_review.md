# Codex Conference Review: r05-batch-estimates-design-20260915

Date: 2026-09-15

## Verdict

Advisory received, implementation deferred by user pause. Partial recommendations accepted; no estimate feature or runtime acceptance claimed.

## Boundary Compliance

Source-only assignment; no tests, databases, application, product models or browser reported. Runner terminal 0: zcode/GLM-5.3/max, no fallback/failure, exec 71262 and wait 1873 closed.

## Participant Outputs Reviewed

runs/conference/r05-batch-estimates-design-20260915/evidence_single_object.md, fully read.

## Conference Panel Review

Useful: use actual job events and response receipts, freeze comparable route/task identities, distinguish missing usage from zero, do not assume agent_calls has production samples. No pricing evidence was established. These are source observations, not historical data verification.

## Main-Venue Codex Review

Do not accept the broad claim that existing records already suffice for reliable whole-batch prediction. Summed child wall time omits inter-child maintenance gaps; summed parallel call durations are work-seconds, not active elapsed time. Same subject/stage is insufficient comparability when pages, requirements, cache or resumed work differ. Cancelled/failed or human-waiting attempts cannot silently become successful completion samples. No automatic project pooling until size/context comparability is explicit. Do not label an OCR route free/local solely from its task type. Currency remains unknown without configured authoritative billing data. No need to ask user merely to preserve unknown values.

## Codex Independent Verification

Targeted source search confirms page-review executor writes elapsed_seconds and usage; proposition receipt/job files do not directly expose elapsed_seconds, so follow inherited execution path on resume before claiming coverage. git diff --check passed. No runtime/data density/cost or model evidence was collected. Full testing remains deferred by user.

## Final Decision

Save as next-work design input, not implemented specification. Resume only after user authorization. Complete measurement provenance and comparable-sample rules before adding read-only estimates API/UI. claims_complete=false.
