# Codex Execution Review: phase5-slice58k-d001-unit-phase-evidence-20260826

## Verdict

接受经父级修订后的本轮单位级期别证据视图与提示合同；拒绝 Worker 02 的理由文本关键词门禁，也不接受把结构相容候选解释为临床语义闭合。

- 接受：当前合并矩阵的 82 行、155 个来源锚点和 152 个唯一冻结单元形成可复现的单位主视图，并将结构阻断、语义未决、来源冲突与结构支持候选分开。
- 接受：共同章节提示明确要求同一义务范围的正向来源、禁止全局广播，并在部分来源未闭合时保持待确认。
- 拒绝：用“同一义务家族”“部分闭合”等理由字符串命中作为确定性门禁。模型不念固定术语不代表语义错误，确定性层也不能从自由文本补造语义判断。
- 边界：`claims_complete=false`，137 个语义包全部保持待运行；本轮没有形成 D001 II 全方案期别闭包或任何受试者审核结论。

## Worker Outputs

- `worker_01` 实现确定性单位级证据视图、行级全锚点汇总、异质代表包选择和真实冻结工件回归。父级保留其结构，但把 `candidate_closed` 改为 `structurally_supported_candidate`，防止结构证据被误读为临床闭合。
- `worker_02` 正确强化了 v5 系统提示、修复提示与理由合同，但错误新增理由文本词表、否定窗口和关键词拒绝测试。父级删除该词法层，保留提示强化，并增加“不使用固定术语但结构化来源与两期共同操作证据闭合”的通过反例。
- `worker_03` 只读独立复算数量、分层、计划身份和源文件哈希。其使用旧状态名 `candidate_closed` 的报告仅代表修订前观察；父级重建后数量不变，状态改为结构支持候选。

## Manager Assessment

实时路线未声明独立 execution manager。三个执行者均按 guard 生成路线使用 `codex/gpt-5.6-luna:max` 单轮完成，返回码 0、无 fallback；Codex 负责父级拒绝、修复、重建和最终验收。

父级修复遵循最小共享边界：不新增第二语义引擎，不写项目特异规则，只删除不可靠的词法推断，并把确定性状态名降到其真实证据强度。共同章节的医学语义仍由 Agent 基于冻结来源包理解，确定性层继续校验来源、目标、身份、处置和闭包。

## Boundary

- 本轮只接受单位级期别证据研究视图、行级阻断汇总、代表包清单和共享适用提示合同，不接受全方案期别闭包。
- 原始方案、冻结清单、闭包矩阵、冻结计划和临床资料均未改写；生成结果是独立研究工件。
- 没有运行任何语义包、病例审核、浏览器验收或视觉测试者；这些步骤继续受 5.8d 临床期别边界阻断。

## Hermes

实时执行路线没有声明 Hermes worker 或 manager，因此未伪造 Hermes 参与记录。三个执行者均使用 guard 生成的 Codex CLI compatibility 路线，模型与 effort 保持 `codex/gpt-5.6-luna:max`，无 fallback。

## Codex Independent Verification

- 两次独立重建的视图与报告字节一致；新 SHA-256 分别为 `57500f020c516c1636a8f32decbff283a5f2e27fc5903c388fb39dfd1d0025dd` 和 `3cf71496cd0185e473c5ee5e065e71c48f085f088692ffc5724a5374353398e5`。
- 真实视图保持 82 行、155 锚点、152 单元。单元状态为 1 个结构阻断、116 个语义未决、6 个来源冲突、29 个结构支持候选；行状态为 1、74、3、4。
- 6 个代表包覆盖 7 个声明异质分层；它们只是后续定向验证清单，不是临床抽样结论。
- 137 个语义包全部仍为 `pending`，原始输出和最终输出均为 0；共享正向证据核验数为 0，`claims_complete=false`。
- D001 源方案 SHA-256 保持 `362443131f0d384c82c80f6a37396084f7d3301b51162201749c0488b0f2dd98`。
- 聚焦研究/期别/理由/合批/控制/矩阵回归 `162 passed`；完整协议回归 `716 passed, 58 warnings`；292 个 Python 文件内存编译通过，仅保留既有 `app/models.py` 转义警告；Trellis validate 与 `git diff --check` 通过。

## Cleanup Decision

review gate 与 execution audit 通过后归档本执行任务的 prompts、runs、logs 和 manifest；保留生成器、测试、冻结输入、单位证据视图、报告及无损检查点作为可复现证据。当前切片不启动病例、浏览器或独立视觉测试者。
