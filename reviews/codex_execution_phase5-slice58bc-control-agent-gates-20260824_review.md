# Codex Execution Review: phase5-slice58bc-control-agent-gates-20260824

## Verdict

Accept with explicit scope. Phase 5.8b and 5.8c implementation are accepted as a provider-neutral full-protocol disposition chain and publication stop gate. This does not accept a D001 or MG-K10-SAR clinical control catalog.

## Worker Outputs

- `worker_01` added deterministic batches, plural unit-to-candidate disposition, frozen official/procedure/workflow targets and system hydration.
- `worker_02` added the independent Chinese-native protocol-control Agent adapter, strict reference-free wire contract, exact-source closure and bounded same-session repair.
- `worker_03` added publication authority reconstruction from complete hydrated batches, full coverage and source closure, explicit DNF/time/workflow/relation checks and mutation regressions.

## Codex Remediation And Assessment

Codex rejected model-owned future identities, singular candidate links, borrowed same-batch excerpts, keyword-driven source reordering, free-text workflow nodes and caller-supplied standalone candidates. The accepted chain is now:

`全文结构清单 -> 冻结处置批次 -> 独立 Agent 草稿 -> 系统水合 -> 完整批次结果 -> 发布门禁`.

Official IN/EX and required-procedure identities remain frozen targets. Other protocol controls use system-owned identities and do not receive pseudo-official numbering.

## Independent Verification

- Focused Phase 5.8a/b/c tests: `73 passed`.
- Complete `tests/v2/protocols`: `561 passed, 58 warnings`.
- Targeted `compileall`, public import probe and diff checks passed.
- Real read-only D001 inventory retained 1,689 units. The current extraction shows 802 `UNKNOWN/MIXED` units; table 5 retains its header and all 12 control rows.

## Hermes

Hermes was not used. The live guard route selected Codex subAgent `gpt-5.6-luna` at `max` effort. Native v2 admission was unavailable, so the declared same-model CLI compatibility path was used without model substitution or fallback; Codex independently reviewed and remediated the actual workspace changes.

## Clinical And Architectural Boundary

The D001 result proves complete input membership, not phase applicability. Publication correctly stops while `UNKNOWN/MIXED` remains. The next slice must add a generic, source-grounded phase-applicability resolution path and an independently checked D001 II manual matrix. It must not silently treat unknown content as shared, infer phase from keyword proximity, or hardcode D001 chapter rules.

The current conjunction and sibling-exception checks are defense-in-depth structural checks. They do not replace later semantic comparison against the source protocol.

## Cleanup Decision

Run execution audit, review gate and Trellis validation. Preserve compact reports, this review and the acceptance checkpoint; archive runner process material through the workflow guard after audit.
