# Codex Execution Review: phase5-slice58i-d001-matrix-source-closure-20260826

## Verdict

有条件接受本轮“矩阵来源与关系闭包”实现；明确拒绝把它解释为 D001 II 期别适用性闭包或全文控制闭包。

- 接受：25 行交叉章节清单与 82 行合并矩阵中的来源锚点均可按“来源引用完全相等 + 摘录被该冻结单元原文包含”唯一回绑；关系目标也已闭合。
- 不接受：当前冻结全文清单没有任何最终期别处置，且 `claims_full_coverage=false`；两个闭包产物均继续保持 `claims_complete=false`。
- 后续边界：先建立可审阅的人工期别依据与少量异质真实语义包，不直接运行全部 137 包，也不按标题、邻近位置或“未限定期别”猜测跨期共用。

## Worker Outputs

- `worker_01` 完成只读盘点，确认交叉章节 47 个来源锚点、合并矩阵 155 个来源锚点均能唯一命中当前冻结清单，并指出临时结构 ID、期别未决和旧汇总工件漂移。其将所有补充/进一步解释关系都要求增加独立关系解析字段，超出了当前合同；父级未采纳该要求。
- `worker_02` 实现通用确定性映射器、闭包报告和聚焦回归，生成独立的 `*-closed.json`，没有覆盖原始人工矩阵，也没有读取或修改源 DOCX。
- `worker_03` 独立复算来源、关系、时间、节点、DNF、例外、证据与工作流合同，确认来源闭包通过而期别闭包失败。其报告的“交叉清单 17 个外部目标”和报告重算为 82 的口径均不正确；父级以实际被关系引用且不在本矩阵内的唯一目标集合重算为 16，合并矩阵为 0，并补充确定性测试。

## Manager Assessment

本次实时路由声明不设置独立 manager。三条 guard 生成的 CLI compatibility 命令均按原模型 `codex/gpt-5.6-luna:max` 单轮执行，未发生模型替换或 fallback；Codex 负责最终综合与验收。

实现符合最小闭包边界：只做可确定的结构身份回绑，不改写临床处置。对 6 个与当前冻结清单并非完全相等的旧摘录，逐项确认它们仍是唯一命中单元中的连续原文子串，因此映射器没有改写摘录，也没有进行模糊择优。

## Boundary

- 本轮只接受来源身份和关系引用闭包，不接受期别适用性、全文完整性或最终临床控制闭包。
- 原始方案、原始人工矩阵和既有临床报告均未被覆盖；新结果写入独立闭包副本。
- 未启动病例审核、浏览器验收或独立视觉测试者，因为当前基础临床期别门禁仍未通过。

## Hermes

实时路由没有声明 Hermes worker 或 manager，故未伪造 Hermes 参与记录。所有执行者均使用 guard 生成的 Codex CLI compatibility 命令；其身份、返回码、耗时、工具调用和无 fallback 状态均保留在 runner 日志中。

## Codex Independent Verification

- 运行矩阵来源闭包测试及相关方案控制合同：`92 passed`。
- 交叉章节：25 行、47/47 锚点、45 个唯一冻结单元，来源闭包和关系闭包通过；实际外部关系目标 16 个。
- 合并矩阵：82 行、155/155 锚点、152 个唯一冻结单元，来源闭包和关系闭包通过；外部关系目标 0 个。
- 期别闭包按设计拒绝：交叉章节 46 个问题，合并矩阵 136 个问题；两者均不得设置完整声明。
- 源方案 SHA-256 仍为 `362443131f0d384c82c80f6a37396084f7d3301b51162201749c0488b0f2dd98`。
- 与当前 1,840 单元冻结清单不一致的旧 1,689 单元汇总文件已移入 `legacy-artifacts/slice58e-1689/`，保留反查而不再作为当前证据。

## Cleanup Decision

在 review gate、execution audit、Trellis 检查点和项目上下文均落盘后，归档本轮 guard 过程文件；保留闭包脚本、测试、输入/输出矩阵、闭包报告和无损检查点作为可复现证据。
